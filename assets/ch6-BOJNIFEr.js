var e=`---
id: ch6
title: 图
order: 6
---

## 图的基本概念
<!-- sec: ch6-s1 -->

### 图的定义与基本术语
<!-- kp: ch6-s1-k1 emphasis: key -->

## 图的定义

**图（Graph）**是由**顶点（Vertex）**集合 V 和**边（Edge）**集合 E 组成的二元组，记为 $$G = (V, E)$$。图的元素称为顶点，边是顶点对。图 G 的顶点数为 $|V|$（图阶），边数为 $|E|$。

## 基本术语

- **无向图与有向图**：无向图中边 $(v_i, v_j)$ 无方向；有向图中边以**弧**（有向边）$<v_i, v_j>$ 表示，$v_i$ 为**弧尾**，$v_j$ 为**弧头**；
- **完全图**：无向完全图任意两顶点间都有边，边数为 $\\frac{n(n-1)}{2}$；有向完全图任意两顶点间都有方向相反的两条弧，弧数为 $n(n-1)$；
- **稀疏图与稠密图**：边数很少的图称稀疏图，反之为稠密图；
- **度**：无向图顶点的度是关联该顶点的边数，所有顶点度之和 = 2|E|；有向图分**入度**与**出度**，入度之和 = 出度之和 = |E|；
- **路径与回路**：顶点序列中相邻顶点间有边相连则为路径；首尾顶点相同的路径为**回路（环）**；
- **简单路径与简单回路**：路径中顶点不重复为简单路径；除首尾外顶点不重复的回路为简单回路；
- **权与带权图**：边上带权值（如距离、代价）的图称为带权图（网）；
- **子图**：$V' \\subseteq V$ 且 $E' \\subseteq E$ 的图 $G' = (V', E')$ 是 G 的子图。

**重点：**"度之和 = 2|E|"（握手定理）与"入度和 = 出度和 = |E|"是计算类选择题的基础公式。

### 图的连通性与分类
<!-- kp: ch6-s1-k2 emphasis: key -->

## 连通性概念

- **无向图**：若两顶点之间存在路径，则称它们**连通**。若图中任意两顶点都连通，则该图是**连通图**；否则为非连通图。n 个顶点的连通图至少有 **n-1** 条边（树状结构）；
- **连通分量**：无向图的**极大连通子图**（再多加一个顶点或边就不再连通）。n 个顶点、无边的非连通图有 n 个连通分量；
- **有向图**：若任意两顶点 $v_i, v_j$ 间存在从 $v_i$ 到 $v_j$ 与从 $v_j$ 到 $v_i$ 的路径，则称为**强连通图**；**强连通分量**是极大强连通子图；
- **生成树**：连通图的生成树是包含图中**全部顶点**的极小连通子图（含 n-1 条边）。n 个顶点的非连通图，其所有连通分量的生成树构成**生成森林**。

## 重要结论

- n 个顶点的**非连通无向图**最多有 $\\frac{(n-1)(n-2)}{2}$ 条边（即除一个孤立顶点外其余顶点构成完全图）；
- n 个顶点、n-1 条边的连通图是**树**；
- 至少需要 n-1 条边才可能连通，至少需要 n 条边才可能出现回路（环）；
- 有 n 个顶点、恰好 n-1 条边的图可能是树、也可能含环（当不连通时）。

**重点：**"极大连通子图"（连通分量）与"极小连通子图"（生成树）的区别是 408 常考辨析点；非连通图的最大边数公式需牢记。

## 图的存储结构
<!-- sec: ch6-s2 -->

### 邻接矩阵法
<!-- kp: ch6-s2-k1 emphasis: key -->

## 邻接矩阵

用一维数组存储顶点信息、用二维数组（邻接矩阵）存储顶点之间的边信息。无向图 G 的邻接矩阵 A 满足：

$$A[i][j] = \\begin{cases} 1, & (v_i, v_j) \\in E \\text{ 或 } <v_i, v_j> \\in E \\\\ 0, & \\text{否则} \\end{cases}$$

带权图（网）中，$A[i][j]$ 存放权值，不存在的边存 ∞。

\`\`\`c
typedef struct {
    char Vex[MaxVertexNum];                 // 顶点表
    int Edge[MaxVertexNum][MaxVertexNum];   // 邻接矩阵
    int vexnum, arcnum;                     // 顶点数与边数
} MGraph;
\`\`\`

## 性质

- **无向图的邻接矩阵是对称阵**（$A[i][j] = A[j][i]$），因此只需存储上（下）三角即可节省一半空间；
- 无向图中，顶点 $v_i$ 的**度**等于第 i 行（或第 i 列）非零元素的个数；有向图中，**出度**等于第 i 行非零元素个数，**入度**等于第 i 列非零元素个数；
- $A^k[i][j]$ 表示从 $v_i$ 到 $v_j$ 长度为 k 的**路径条数**；
- 邻接矩阵法求某顶点的度很方便（O(n)），但**空间复杂度为 O(n²)**，与边数无关，适合稠密图。

**重点：**邻接矩阵的对称性、度与行列的关系、$A^k$ 的路径计数意义是三大考点。判断图中是否存在"长度为 k 的路径"可用矩阵乘法。

### 邻接表法、十字链表与邻接多重表
<!-- kp: ch6-s2-k2 emphasis: key -->

## 邻接表

邻接表由**顶点表**（顺序存储，含 data 与指向第一条边结点的指针）与**边表**（链式存储）构成。每个边表结点含邻接点下标 adjvex 与指向下一边表结点的指针 nextarc。

\`\`\`c
typedef struct ArcNode {        // 边表结点
    int adjvex;                 // 邻接顶点在数组中的下标
    struct ArcNode *nextarc;
    // InfoType info;           // 权值等
} ArcNode;
typedef struct VNode {          // 顶点表结点
    VertexType data;
    ArcNode *firstarc;          // 第一条边
} VNode, AdjList[MaxVertexNum];
typedef struct {
    AdjList vertices;           // 邻接表
    int vexnum, arcnum;
} ALGraph;
\`\`\`

## 邻接表的特点

- 无向图每条边存储两次，边表结点数为 **2|E|**；有向图每条弧存一次，边表结点数为 **|E|**；
- 空间复杂度：无向图 O(|V| + 2|E|)，有向图 O(|V| + |E|)，适合**稀疏图**；
- 无向图中求某顶点的度需遍历其边表（O(度)）；有向图中求**出度**很方便，但求**入度**需要遍历整个邻接表（O(|V|+|E|)）——除非建立逆邻接表；
- 邻接表便于**找某顶点的所有邻接点**，但不便于检查任意两顶点间是否有边（需遍历边表）。

## 十字链表与邻接多重表

- **十字链表**：有向图的链式存储，每个弧结点含弧尾/弧头顶点域及两条链指针（分别指向弧头相同与弧尾相同的下一条弧），可同时方便地求入度与出度；
- **邻接多重表**：无向图的链式存储，每条边只存储一次（边结点含两个顶点与两条链指针），便于**删除边**等操作。

**重点：**邻接表 vs 邻接矩阵的选择：稀疏图用邻接表（省空间），稠密图用邻接矩阵（方便判断边）；"求入度困难"是邻接表的固有缺点。

## 图的遍历
<!-- sec: ch6-s3 -->

### 广度优先搜索 BFS
<!-- kp: ch6-s3-k1 emphasis: key -->

## BFS 思想

**广度优先搜索（BFS）**类似于树的层次遍历，借助**队列**实现。从起始顶点 v 出发：访问 v 并入队；当队列非空时出队一个顶点，依次访问其所有**未被访问**的邻接点并入队；重复直至队列为空。若图非连通，需另选一个未访问顶点重新开始，保证访问到所有顶点。

\`\`\`c
bool visited[MAX_VERTEX_NUM];       // 访问标记数组

void BFS(Graph G, int v) {
    visit(v); visited[v] = true;
    InitQueue(Q); EnQueue(Q, v);
    while (!QueueEmpty(Q)) {
        DeQueue(Q, v);
        for (w = FirstNeighbor(G, v); w >= 0; w = NextNeighbor(G, v, w))
            if (!visited[w]) {
                visit(w); visited[w] = true;
                EnQueue(Q, w);
            }
    }
}
void BFSTraverse(Graph G) {
    for (int i = 0; i < G.vexnum; i++) visited[i] = false;
    for (int i = 0; i < G.vexnum; i++)
        if (!visited[i]) BFS(G, i);   // 非连通图需多次调用
}
\`\`\`

## BFS 的复杂度与性质

- 邻接表存储：每个顶点入队一次（O(|V|)），每条边（弧）被访问一次（O(|E|)），总时间 **O(|V|+|E|)**；辅助空间 O(|V|)（队列）；
- 邻接矩阵存储：查找每个顶点的邻接点需扫描整行，总时间 **O(|V|²)**；
- BFS 生成树/生成森林：BFS 过程中记录的边构成广度优先生成树（森林）；
- BFS 可用于求**无权图单源最短路径**（边数最少的路径）——首次访问到某顶点的层次即其最短距离。

**重点：**BFS 序列与具体存储结构、起始顶点相关（不唯一）；邻接表上的 BFS 序列只与邻接表有关，邻接矩阵上的 BFS 序列与矩阵有关，两者可能不同。

### 深度优先搜索 DFS
<!-- kp: ch6-s3-k2 emphasis: key -->

## DFS 思想

**深度优先搜索（DFS）**类似于树的先序遍历，借助**递归（系统栈）**实现。从顶点 v 出发，访问 v 并标记，然后依次对 v 的每个**未被访问**的邻接点递归执行 DFS；当某顶点的邻接点全部被访问过，回溯到上一顶点继续。非连通图需再次从其他未访问顶点开始。

\`\`\`c
void DFS(Graph G, int v) {
    visit(v); visited[v] = true;
    for (w = FirstNeighbor(G, v); w >= 0; w = NextNeighbor(G, v, w))
        if (!visited[w])
            DFS(G, w);
}
void DFSTraverse(Graph G) {
    for (int i = 0; i < G.vexnum; i++) visited[i] = false;
    for (int i = 0; i < G.vexnum; i++)
        if (!visited[i]) DFS(G, i);
}
\`\`\`

DFS 也可以用**显式栈**改为非递归实现（栈保存待访问顶点）。

## DFS 的复杂度与性质

- 邻接表：每个顶点访问一次、每条边检查一次，时间 **O(|V|+|E|)**；空间 O(|V|)（递归栈）；
- 邻接矩阵：**O(|V|²)**；
- DFS 生成树/生成森林：DFS 过程记录下 n-1 条边构成深度优先生成树；
- 借助 DFS，可判断图中**是否存在回路**（DFS 过程中遇到已访问且非双亲的顶点说明有环）、可计算**拓扑序列**（用 DFS 的逆序）等。

## BFS 与 DFS 对比

| 对比项 | BFS | DFS |
| --- | --- | --- |
| 辅助结构 | 队列 | 递归栈 |
| 遍历序列 | 广度优先序列 | 深度优先序列 |
| 复杂度（邻接表） | O(|V|+|E|) | O(|V|+|E|) |
| 复杂度（邻接矩阵） | O(|V|²) | O(|V|²) |
| 应用 | 无权图最短路径 | 判环、拓扑、连通性 |

**重点：**DFS 生成的树或序列取决于"起始顶点"和"邻接点的访问次序"，故序列不唯一；复杂度公式 O(|V|+|E|)（邻接表）与 O(|V|²)（邻接矩阵）必须默写无误。

## 最小生成树
<!-- sec: ch6-s4 -->

### 普里姆 Prim 算法
<!-- kp: ch6-s4-k1 emphasis: key -->

## 基本思想

**最小生成树（MST）**：在一个带权连通无向图中，权值之和最小的生成树（含 n-1 条边、连通全部 n 个顶点、无回路）。

**Prim 算法**（加点法）：初始从任一顶点开始，将顶点集 U 初始化为该顶点；然后每次选择连接 U 与 U 外顶点（V-U）的**权值最小的一条边**，把该边及其另一个顶点并入 U；重复 n-1 次，得到最小生成树。

## 算法步骤与实现要点

1. 选定起始顶点 $v_0$，令 U = {$v_0$}，TE = ∅；
2. 在所有 u ∈ U、v ∈ V-U 的边 $(u,v)$ 中找权值最小的边 $(u_0, v_0)$，将 $v_0$ 并入 U，边并入 TE；
3. 重复步骤 2，直到 U = V，此时 TE 中恰有 n-1 条边，构成最小生成树。

\`\`\`c
void Prim(MGraph G) {
    int mincost[MaxVertexNum];   // 各顶点到 U 的最小边权
    // 初始化 mincost 为顶点 0 到各顶点权值，并标记已加入顶点
    for (int k = 1; k < G.vexnum; k++) {
        // 选 mincost 最小且未加入的顶点 v
        // 将 v 加入 U，用 v 更新其他未加入顶点的 mincost
    }
}
\`\`\`

## 复杂度

Prim 算法的时间复杂度为 **O(|V|²)**，与边数 |E| 无关，因此适合**稠密图**求最小生成树。用堆优化后可达 O(|E|·log|V|)。

**重点：**Prim 是"选点"，每次新加入的顶点必须与已选集合连通；算法结果唯一当且仅当最小边唯一。408 常以"给出权矩阵，写出 Prim 选边次序"命题，需严格按"每次取连接内外的权最小边"执行。

### 克鲁斯卡尔 Kruskal 算法
<!-- kp: ch6-s4-k2 emphasis: key -->

## 基本思想

**Kruskal 算法**（加边法）：按权值**从小到大**依次选择边，若该边的两个端点**不属于同一连通分量**（不会形成回路），则加入生成树；否则舍弃。直到选出 n-1 条边为止。判断是否形成回路使用**并查集**：Find 两端点，若根相同则成环。

\`\`\`c
void Kruskal(Edge edges[], int n) {
    // edges 按权值从小到大排序
    Initial(UFSets);          // 并查集初始化
    int count = 0;
    for (int i = 0; i < E_num && count < n - 1; i++) {
        int r1 = Find(UFSets, edges[i].u);
        int r2 = Find(UFSets, edges[i].v);
        if (r1 != r2) {       // 不成环
            Union(UFSets, r1, r2);
            // 将 edges[i] 加入生成树
            count++;
        }
    }
}
\`\`\`

## 复杂度

Kruskal 算法的时间开销主要来自**对边排序**（O(|E|·log|E|)）与并查集操作，总时间复杂度为 **O(|E|·log|E|)**，与顶点数关系不大，适合**稀疏图**。

## Prim 与 Kruskal 对比

| 对比项 | Prim | Kruskal |
| --- | --- | --- |
| 策略 | 加点（选点） | 加边（选边） |
| 依赖 | 顶点间的连接 | 全局边的排序 |
| 时间复杂度 | O(|V|²) | O(|E|·log|E|) |
| 适用 | 稠密图 | 稀疏图 |

**注意**：①最小生成树**不唯一**（当存在权值相同的边时），但权值之和唯一；②若图中存在权值相同的边，Prim 与 Kruskal 都可能得到不同的 MST；③最小生成树中一定不含权值为负的回路（图是树自然无回路）。

**重点：**Kruskal 判环依赖并查集，是图与并查集结合的经典综合题；"加边法 vs 加点法"的区分、两种算法各自的时间复杂度必须记牢。

## 最短路径
<!-- sec: ch6-s5 -->

### Dijkstra算法（单源最短路径）
<!-- kp: ch6-s5-k1 emphasis: difficult -->

## 算法思想

**Dijkstra 算法**求从某源点 $v_0$ 出发到其余各顶点的**最短路径**，要求边权**非负**。算法按路径长度递增的次序逐步生成各顶点的最短路径，维护两个集合：已确定最短路径的顶点集合 S 与未确定集合 V-S，以及数组 dist[]（当前最短距离）与 path[]（前驱顶点）。

## 算法步骤

1. 初始化：S = {$v_0$}，dist[$v_0$]=0，dist[其余] = 邻接矩阵中 $v_0$ 到各顶点的权值（无直接边为 ∞），path 记录直接前驱；
2. 在 V-S 中选 dist 值**最小**的顶点 $v_k$，将 $v_k$ 加入 S；
3. 用 $v_k$ 作中间点**更新** V-S 中顶点的 dist：若 $dist[v_j] > dist[v_k] + w(v_k, v_j)$，则更新 $dist[v_j]$ 并令 path[$v_j$] = $v_k$；
4. 重复步骤 2、3 共 n-1 次，得到从源点到所有顶点的最短路径。

## 示例

顶点 A,B,C,D，边权 A→B=2，A→C=5，B→C=1，C→D=1。从 A 出发：dist = [0,2,5,∞]；选 B(2) 入 S，用 B 更新 C：dist[C] = min(5, 2+1)=3；选 C(3) 入 S，更新 D：dist[D] = min(∞, 3+1)=4；选 D(4) 入 S。最终最短路径 A→B=2，A→C=3，A→D=4。

## 复杂度与注意事项

- 时间复杂度：**O(|V|²)**（未优化），可用最小堆优化到 O(|V|·log|V| + |E|)；
- **不能处理负权边**：负权会导致"已确定最短路径"的结论失效；
- 每轮从 V-S 中选最小 dist 是核心，选顶点与更新共 n-1 轮，故 O(n²)；
- Dijkstra 只能求**单源**（一个起点到所有点）最短路径。

**难点：**正确性依赖"最短路径的子路径仍是最短路径"这一最优子结构性质；一旦某顶点入 S，其 dist 不再改变。408 中常以表格形式要求逐步填写 dist 数组。

### Floyd算法（各顶点之间最短路径）
<!-- kp: ch6-s5-k2 emphasis: difficult -->

## 算法思想

**Floyd 算法**用**动态规划**求图中所有顶点对之间的最短路径，允许**负权边**（但不能有负权回路）。核心递推：用顶点 k 作为中间顶点更新路径：

$$dist^{(k)}[i][j] = \\min\\{dist^{(k-1)}[i][j],\\; dist^{(k-1)}[i][k] + dist^{(k-1)}[k][j]\\}$$

即考虑"是否经过顶点 k"能使 i 到 j 的路径更短。对 k = 1..n 逐轮更新，最终 $dist^{(n)}[i][j]$ 即 i 到 j 的最短路径长度。

## 算法实现

\`\`\`c
void Floyd(MGraph G, int Path[][MAX], int A[][MAX]) {
    for (int i = 0; i < G.vexnum; i++)
        for (int j = 0; j < G.vexnum; j++) {
            A[i][j] = G.Edge[i][j];
            Path[i][j] = -1;
        }
    for (int k = 0; k < G.vexnum; k++)
        for (int i = 0; i < G.vexnum; i++)
            for (int j = 0; j < G.vexnum; j++)
                if (A[i][j] > A[i][k] + A[k][j]) {
                    A[i][j] = A[i][k] + A[k][j];
                    Path[i][j] = k;      // 记录中间顶点
                }
}
\`\`\`

## 复杂度与比较

- 时间复杂度：**O(|V|³)**；空间复杂度 O(|V|²)；
- 三重循环次序必须为 **k 在外、i 和 j 在内**（i、j 可互换，k 必须最外层）；
- 允许负权边、不允许负权回路；可用 \`A[i][i]\` 是否为负判断是否存在负权回路；
- 求单源最短路径用 Dijkstra（O(n²)）或 BFS（无权图）；求全源用 Floyd（O(n³)）。

| 算法 | 类型 | 时间复杂度 | 权值限制 |
| --- | --- | --- | --- |
| BFS | 无权单源 | O(|V|+|E|) | 无权图 |
| Dijkstra | 单源 | O(|V|²) | 非负权 |
| Floyd | 全源 | O(|V|³) | 无负权回路 |

**难点：**Floyd 的三层循环顺序、$Path$ 数组记录中间点的递归回溯求路径，以及"经过前 k 个顶点的最短路径"递推含义，需反复理解。

## 拓扑排序
<!-- sec: ch6-s6 -->

### 拓扑排序的定义与算法
<!-- kp: ch6-s6-k1 emphasis: key -->

## 基本概念

**有向无环图（DAG, Directed Acyclic Graph）**是指不存在环（回路）的有向图，是描述工程先后关系的工具。

**拓扑排序**：对一个有向无环图 G，将顶点排成一个线性序列，使得若图中存在弧 $<v_i, v_j>$，则在该序列中 $v_i$ 必须排在 $v_j$ 之前。这样的线性序列称为**拓扑序列**。一个有向无环图可能有**多个**拓扑序列。

## 算法步骤（基于入度）

1. 从图中选择一个**入度为 0** 的顶点并输出；
2. 从图中**删除该顶点及其所有出边**（即其邻接顶点的入度减 1）；
3. 重复步骤 1、2，直到图为空或不存在入度为 0 的顶点。

若算法结束时**还有顶点未输出**，则说明图中**存在环**，无法进行拓扑排序——这是判断有向图是否有环的方法之一。

\`\`\`c
bool TopologicalSort(Graph G) {
    InitStack(S);                        // 存放入度为 0 的顶点
    for (int i = 0; i < G.vexnum; i++)
        if (indegree[i] == 0) Push(S, i);
    int count = 0;
    while (!StackEmpty(S)) {
        Pop(S, i);
        print(i); count++;
        for (p = G.vertices[i].firstarc; p; p = p->nextarc) {
            int v = p->adjvex;
            if (--indegree[v] == 0) Push(S, v);   // 入度减为 0 则入栈
        }
    }
    if (count < G.vexnum) return false;   // 存在环
    return true;
}
\`\`\`

**重点：**拓扑排序用**栈或队列**暂存入度为 0 的顶点均正确；入度为 0 的顶点可能有多个，故拓扑序列不唯一；每个顶点入栈出栈一次，每条边检查一次，邻接表下时间复杂度为 **O(|V|+|E|)**。

### 拓扑排序的应用与逆拓扑排序
<!-- kp: ch6-s6-k2 -->

## 应用场景

拓扑排序用于解决"工程任务先后顺序"问题：顶点代表任务，弧代表先后约束，拓扑序列给出一种可行的执行顺序。典型应用包括课程安排的先修关系、编译时的依赖分析等。

## 逆拓扑排序

**逆拓扑排序**每次输出**出度为 0**的顶点并删除其入边，重复直到图为空。同样可用于判断有环。基于 DFS 的拓扑排序：对图做深度优先搜索，当某顶点的所有邻接点都被访问后将其压栈，搜索结束后依次出栈即得到一个逆拓扑序列（也可直接得到拓扑序列）。

**注意**：拓扑排序的**输出结果不唯一**，但**有向无环图的拓扑序列至少存在一个**。若题目要求"判断两个给定序列哪个是合法拓扑序列"，直接按定义模拟：序列中每个顶点的所有前驱必须已出现在它之前。

## 相关结论

- 一个 DAG 的邻接矩阵可能是三角矩阵（顶点按拓扑序排列时）；
- 有向图可拓扑排序 ⟺ 该图是 DAG；
- 若使用**邻接矩阵**存储，求每个顶点入度需扫描整列，求所有入度总时间 O(|V|²)。

**重点：**"拓扑排序能否完成"与"图是否有环"等价，是判断 DAG 的三种手段之一（另两种：DFS 回边检测、顶点计数法）。

## 关键路径
<!-- sec: ch6-s7 -->

### AOE网与关键路径的概念
<!-- kp: ch6-s7-k1 emphasis: key -->

## AOE 网

**AOE 网（Activity On Edge）**是**带权有向无环图**：顶点表示**事件**（状态），有向边表示**活动**，边上的权值表示活动持续的时间。AOE 网用于估算工程**完成的最短时间**。

与 AOV 网的区别：**AOV 网**（Activity On Vertex）顶点表示活动，边表示活动间的先后约束，用于拓扑排序；**AOE 网**边表示活动，用于计算关键路径。

## 关键路径相关概念

AOE 网中通常只有一个入度为 0 的源点和一个出度为 0 的汇点。定义：

- **事件 v 的最早发生时间 ve(v)**：从源点到 v 的**最长路径长度**（源点 ve = 0）；
- **事件 v 的最迟发生时间 vl(v)**：在不推迟整个工程完成时间的前提下，事件 v 最迟必须发生的时间（汇点 vl = 它的 ve）；
- **活动 a 的最早开始时间 e(a)**：活动 a 的**弧尾事件**的最早发生时间，即 e = ve(弧尾)；
- **活动 a 的最迟开始时间 l(a)**：活动 a 的**弧头事件**的最迟发生时间减去活动持续时间，即 l = vl(弧头) - 持续时间；
- **关键活动**：满足 $$e(a) = l(a)$$ 的活动，即最早开始时间等于最迟开始时间、一点不能延误的活动；
- **关键路径**：由关键活动组成、从源点到汇点的路径（最长路径），其长度决定整个工程的最短完成时间。

**重点：**关键路径是**最长路径**而非最短路径；关键路径可能**不止一条**；加快非关键活动不能缩短工程工期，只有加快关键路径上的关键活动才可能缩短工期。

### 关键路径的求解方法
<!-- kp: ch6-s7-k2 emphasis: difficult -->

## 计算步骤

**第一步：拓扑排序求事件最早发生时间 ve**

按拓扑次序计算：$ve[源点] = 0$；对弧 $<v_i, v_j>$，$$ve(v_j) = \\max\\{ve(v_i) + w(v_i, v_j)\\}$$（取所有前驱中最大值）。

**第二步：逆拓扑排序求事件最迟发生时间 vl**

按逆拓扑次序计算：$vl[汇点] = ve[汇点]$；对弧 $<v_i, v_j>$，$$vl(v_i) = \\min\\{vl(v_j) - w(v_i, v_j)\\}$$（取所有后继中最小值）。

**第三步：求每个活动的最早与最迟开始时间**

对活动 $a = <v_i, v_j>$（持续时间为 w）：

$$e(a) = ve(v_i), \\qquad l(a) = vl(v_j) - w$$

**第四步：确定关键活动与关键路径**

满足 $e(a) = l(a)$（即 $ve(v_i) = vl(v_j) - w$）的活动是关键活动，由关键活动连成的从源点到汇点的路径即关键路径，其总长度等于 $ve(汇点)$。

## 计算示例

AOE 网：源点 1，事件 1→2 活动 a1(2)，1→3 活动 a2(3)，2→3 活动 a3(1)，2→4 活动 a4(5)，3→4 活动 a5(2)，汇点 4。

- 拓扑序：1,2,3,4。ve：ve1=0；ve2=max(0+2)=2；ve3=max(0+3, 2+1)=3；ve4=max(2+5, 3+2)=7；
- 逆拓扑序：4,3,2,1。vl：vl4=7；vl3=min(7-2)=5；vl2=min(7-5, 5-1)=2；vl1=min(2-2, 5-3)=0；
- 各活动：e(a1)=0, l(a1)=vl2-2=0 ✓；e(a2)=0, l(a2)=vl3-3=2 ✗；e(a3)=2, l(a3)=vl3-1=4 ✗；e(a4)=2, l(a4)=vl4-5=2 ✓；e(a5)=3, l(a5)=vl4-2=5 ✗；
- 关键活动：a1、a4，关键路径 1→2→4，工程最短完成时间 7。

**难点：**ve 取前驱**最大**、vl 取后继**最小**的方向最容易出错；求 l(a) 必须用弧头事件的 vl 减持续时间。408 常以"计算并填写 ve/vl/e/l 表"命题，务必按四步规范求解。
`;export{e as default};