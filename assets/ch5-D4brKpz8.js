var e=`---
id: ch5
title: 第五章 传输层
order: 5
---

## 传输层概述
<!-- sec: ch5-s1 -->

### 传输层的功能与端口
<!-- kp: ch5-s1-kp1 emphasis: key -->

**1. 传输层的地位与功能**
传输层位于网络层之上、应用层之下，为**应用进程之间**提供端到端的逻辑通信。网络层只负责"主机到主机"的通信（IP 地址标识主机），而传输层负责"进程到进程"的通信（端口号标识进程）。传输层的主要功能：
- **复用与分用**：多个应用进程共享一条传输连接（复用），收到的数据正确交付给对应的应用进程（分用）；
- **差错检测**：对收到的报文段进行差错校验（UDP 校验和、TCP 校验和）；
- **提供可靠的/不可靠的传输服务**：TCP 提供面向连接的可靠服务，UDP 提供无连接的不可靠服务；
- **流量控制与拥塞控制**：TCP 通过滑动窗口和拥塞窗口实现。

**2. 端口号**
端口是传输层的服务访问点（TSAP），用 16 位端口号标识应用进程，范围 0~65535。端口只具有本地意义，同一主机上不同进程使用不同端口。

**3. 端口分类（熟知端口）**

| 类别 | 范围 | 说明 |
| --- | --- | --- |
| 熟知端口 | 0~1023 | 系统保留，分配给固定服务 |
| 登记端口 | 1024~49151 | 需登记供普通用户使用 |
| 短暂端口 | 49152~65535 | 客户端动态使用 |

**常用熟知端口：** FTP 21（控制）20（数据）、SSH 22、TELNET 23、SMTP 25、DNS 53、HTTP 80、POP3 110、HTTPS 443、IMAP 143、DHCP 67/68、TFTP 69、RIP 520。这些端口号是 408 常考记忆点。

**4. 套接字**
套接字（socket）是传输层寻址的完整标识，由 **IP 地址 + 端口号** 组成，即 **（IP，端口）**。一对套接字（源套接字，目的套接字）唯一确定一条 TCP 连接。例如 TCP 连接的端点表示为 (192.168.1.1:12345, 192.168.1.2:80)。

**重点：** 传输层提供"进程到进程"通信；端口 16 位、0~65535；套接字 = IP + 端口；熟知端口与协议对应关系。

**难点：** 同一个端口号在不同主机上可被不同进程使用（端口号本地意义）；TCP 连接用一对套接字唯一标识，而 UDP 无需连接标识。

### TCP 与 UDP 的选择及应用场景
<!-- kp: ch5-s1-kp2 emphasis: key -->

**1. 面向连接与无连接的本质区别**
TCP 提供**面向连接的可靠字节流**服务：通信前建立连接、通信结束释放连接，通过序号/确认/重传保证无差错、不丢失、不重复、按序到达；UDP 提供**无连接的不可靠数据报**服务：直接发送，不保证到达、不保证顺序、无连接开销。

**2. 各协议典型应用场景**

| 协议 | 特点 | 典型应用 | 原因 |
| --- | --- | --- | --- |
| TCP | 可靠、面向字节流、有拥塞控制 | HTTP/HTTPS、FTP、SMTP/POP3/IMAP、Telnet、SSH、BGP | 文件、网页、邮件要求可靠完整 |
| TCP | 可靠、有序 | DNS 区域传送（主从服务器间） | 数据量大需可靠 |
| UDP | 无连接、低时延、无拥塞控制 | 视频直播、语音通话、实时游戏 | 对实时性要求高，可容忍少量丢包 |
| UDP | 无连接、报文短 | DNS 查询（53）、DHCP（67/68）、TFTP（69）、RIP（520）、SNMP | 查询-应答短报文，UDP 效率高 |
| UDP | 广播/组播支持 | IPTV、在线广播 | UDP 支持一对多，TCP 只能点对点 |

**3. 选择依据**
- 需要**可靠交付**（文件、邮件、网页、远程登录）→ TCP；
- 需要**实时性/低时延**且可容忍少量丢包（音视频）→ UDP；
- **短报文查询-应答**（DNS、DHCP、SNMP）→ UDP（免连接开销）；
- 需要**多播/广播**（组播应用）→ 只能选 UDP（TCP 不支持组播）；
- **流水线填坑**：HTTP 曾用 TCP（0.9/1.0/1.1/2），HTTP/3 改用 UDP（QUIC）以降低队头阻塞。

**4. 考点归纳**
- "可靠传输靠 TCP，实时/广播靠 UDP"是选择应用层协议的根本依据；
- 同一协议族内部可能混用：如 SMTP 用 TCP，但电子邮件的查询 DNS 用 UDP；
- 记住各常用协议的传输层协议归属：HTTP/FTP/SMTP/POP3/Telnet→TCP；DNS/DHCP/TFTP/RIP/SNMP→UDP。

**重点：** 各典型应用层协议的 TCP/UDP 归属；选择 TCP 或 UDP 的判据（可靠性 vs 实时性/连接开销）。

**难点：** DNS 默认用 UDP（53），但**区域传送**用 TCP——查询报文短、UDP 够用，区域传送数据量大需可靠；这一细节是 408 的经典陷阱。

## UDP 协议
<!-- sec: ch5-s2 -->

### UDP 的特点与数据报格式
<!-- kp: ch5-s2-kp1 emphasis: key -->

**1. UDP 的特点**
UDP（用户数据报协议，User Datagram Protocol）提供无连接、不可靠的传输服务，特点：
- **无连接**：发送前不需要建立连接，直接发送；
- **尽最大努力交付**：不保证可靠，出错可能丢失；
- **面向报文**：UDP 对应用层交下来的报文"原样"加上首部后交付 IP 层，**不合并、不拆分**，一次交付一个完整的报文；
- **无拥塞控制**：适合实时应用（视频、语音），网络拥塞时 UDP 仍按原速率发送；
- **首部开销小**（8 字节）；支持一对一、一对多、多对一、多对多通信。

**2. UDP 数据报格式**

| 字段 | 源端口 | 目的端口 | 长度 | 校验和 |
| --- | --- | --- | --- | --- |
| 长度 | 2 B | 2 B | 2 B | 2 B |

- **源端口/目的端口**：16 位，0~65535；
- **长度**：UDP 用户数据报的总长度（首部 + 数据），最小值为 8（只有首部）；
- **校验和**：检测整个 UDP 数据报在传输中是否出错。

UDP 用户数据报的总长度为 16 位表示，最大 65535 字节，超过时由 IP 层分片。

**3. UDP 与 TCP 对比**

| 比较项 | UDP | TCP |
| --- | --- | --- |
| 连接 | 无连接 | 面向连接 |
| 可靠性 | 不可靠 | 可靠 |
| 传输单位 | 用户数据报 | 报文段 |
| 首部 | 8 字节 | 20 字节以上 |
| 拥塞控制 | 无 | 有 |
| 效率 | 高 | 较低 |
| 典型应用 | DNS、TFTP、RIP、视频 | HTTP、FTP、SMTP |

**重点：** UDP 无连接、不可靠、面向报文、无拥塞控制；首部 8 字节四字段。

**难点：** "面向报文"的含义——UDP 保留应用层报文的边界（一个报文 = 一个 UDP 数据报），而 TCP 是面向字节流的，无报文边界。

### UDP 校验和
<!-- kp: ch5-s2-kp2 -->

**1. 伪首部**
UDP 校验和计算时，除了 UDP 首部与数据外，还要临时加上 12 字节的**伪首部**。伪首部不真正传输，只参与校验计算，其字段为：源 IP 地址（4 B）、目的 IP 地址（4 B）、0（1 B）、协议字段（1 B，UDP 为 17）、UDP 长度（2 B）。伪首部的意义是**校验数据报是否被路由器错误地交付到了错误主机/错误端口**，把 IP 层的地址信息也纳入校验。

**2. 计算过程**
1. 发送方把校验和字段置 0；
2. 在 UDP 数据报前加上伪首部；
3. 若数据部分不是偶数个字节，则**补一个全 0 字节**（不传输）；
4. 按 **16 位一字**为单位，全部**二进制反码求和**；
5. 把求和结果取反码填入校验和字段。

接收方同样加伪首部、按 16 位反码求和（包括校验和字段本身）：若结果全为 1，则认为无差错，接收；否则丢弃。

**3. 为什么采用反码求和**
- 反码求和的进位循环回加（end-around carry），实现简单；
- 不依赖机器字节序（大小端），便于跨平台实现；
- 校验能力强，能发现单个比特和多比特错误。

**4. 考点归纳**
- 伪首部只在计算时存在，不随报文传输；
- UDP 校验和是**可选的**（IPv4 中不强制），而 **TCP 校验和是必须的**；
- 校验和字段为 0 表示发送方未计算校验和（接收方不应按 0 接收）——实际实现中几乎都计算。

**重点：** 伪首部包含 IP 地址、协议号、长度；反码求和计算；校验和字段置 0 参与计算。

**难点：** 反码求和的"进位回卷"——每 16 位相加产生的最高位进位要加回到最低位（end-around carry），最终结果再取反；408 曾出过手算反码求和的题目。

## TCP 概述与连接管理
<!-- sec: ch5-s3 -->

### TCP 的特点与首部格式
<!-- kp: ch5-s3-kp1 emphasis: key -->

**1. TCP 的特点**
TCP（传输控制协议，Transmission Control Protocol）的特点：
- **面向连接**：通信前必须建立连接（三次握手），通信结束释放连接（四次挥手）；
- **点对点**：一条 TCP 连接只能连接两个端点，不支持广播/组播；
- **可靠交付**：通过序号、确认、重传机制保证数据无差错、不丢失、不重复、按序到达；
- **全双工**：双方可以同时收发数据；
- **面向字节流**：TCP 把应用层交下来的数据看作无结构的字节流，按字节编号并组成报文段发送，接收方按字节流重组；
- 提供流量控制（滑动窗口）和拥塞控制（拥塞窗口）。

**2. TCP 报文段首部格式（固定 20 字节）**

| 字段 | 长度 | 含义 |
| --- | --- | --- |
| 源端口 | 2 B | 发送方进程端口 |
| 目的端口 | 2 B | 接收方进程端口 |
| 序号 seq | 4 B | 本报文段数据部分第一个字节的编号（按字节流编号） |
| 确认号 ack | 4 B | 期望收到对方下一个字节的序号（等于"已正确收到 n-1 号字节"） |
| 数据偏移 | 4 bit | 首部长度（以 4 字节为单位，即首部长度=数据偏移×4） |
| 保留 | 6 bit | 保留 |
| 标志位 | 6 bit | URG、ACK、PSH、RST、SYN、FIN |
| 窗口 | 2 B | 接收窗口 rwnd，告知对方"我能接收的字节数" |
| 校验和 | 2 B | 含伪首部的校验（伪首部协议号为 6） |
| 紧急指针 | 2 B | URG=1 时有意义 |

**3. 六个标志位**
- **URG**：紧急指针有效；
- **ACK**：确认号有效（**连接建立后的所有报文段都必须 ACK=1**）；
- **PSH**：尽快交付给应用进程；
- **RST**：复位连接（异常断开、拒绝连接）；
- **SYN**：同步标志，**连接建立**时使用（SYN=1 表示连接请求/同意）；
- **FIN**：终止标志，**连接释放**时使用。

**4. 序号与确认号**
TCP 把数据按字节编号。首字节的序号随报文段携带。确认号 ack 表示"希望收到对方的下一个字节"，因此 **ack = 已收到的最后一个字节的序号 + 1**。TCP 采用**累积确认**：ack=n 表示序号 < n 的所有字节都已正确收到。注意确认号与标志位 ACK 的区分：ack 是字段，ACK 是标志位。

**重点：** TCP 面向字节流、可靠、全双工；首部固定 20 字节；序号/确认号/数据偏移/窗口字段含义；六个标志位。

**难点：** "确认号 ack 表示期望收到的下一个字节序号"——与 GBN 的累积确认思想一致；数据偏移为何以 4 字节为单位（首部最大 60 字节，即 15×4）。

### TCP 三次握手建立连接
<!-- kp: ch5-s3-kp2 emphasis: key -->

**1. 三次握手过程**
设客户端为 A，服务器为 B：

**第一次握手**：A 向 B 发送连接请求报文段，SYN=1，seq = x（随机初始序号）。A 进入 **SYN-SENT（同步已发送）**状态。注意该报文段不携带数据，但消耗一个序号。

**第二次握手**：B 收到后，若同意则发送确认报文段：SYN=1，ACK=1，seq = y（B 的随机初始序号），ack = x+1。B 进入 **SYN-RCVD（同步已收到）**状态。

**第三次握手**：A 收到后发送确认报文段：ACK=1，seq = x+1，ack = y+1。A、B 都进入 **ESTABLISHED（已建立连接）**状态，连接建立成功，开始传输数据。

**2. 为什么需要三次握手（关键考点）**
若只有两次握手，可能出现**失效的连接请求报文段**问题：A 发送的第一个连接请求在网络上滞留很久后才到达 B。若按两次握手，B 会认为 A 想建立新连接而发送确认、建立连接，但 A 并无此意，造成 B 资源浪费（并可能建立无用连接）。**第三次握手正是为了确认"B 的确认报文段"能被 A 正确收到**，防止已失效的连接请求再次到达服务器端而产生错误连接。因此三次握手是**防止失效请求造成资源浪费的最小握手次数**。

**3. SYN 洪泛攻击**
攻击者只发 SYN=1 的连接请求而不发第三次握手确认，使服务器停留在 SYN-RCVD 状态、大量半开连接占满半连接队列，导致合法连接无法建立。缓解手段：SYN Cookie 等。

**4. 状态转移（简要）**
CLOSED →（发 SYN）→ SYN-SENT →（收 SYN+ACK，发 ACK）→ ESTABLISHED；
LISTEN →（收 SYN）→ SYN-RCVD →（收 ACK）→ ESTABLISHED。

**重点：** 三次握手中各报文段的标志位与序号/确认号；各阶段状态名；"防止失效连接请求"这一原因。

**难点：** 序号 x、y 随机初始值的作用——防止旧连接报文段与本地序号冲突；若第三次握手丢失，B 超时重发 SYN+ACK，A 需重新确认。

### TCP 四次挥手释放连接
<!-- kp: ch5-s3-kp3 emphasis: difficult -->

**1. 四次挥手过程**
设 A 主动关闭，B 被动关闭：

**第一次挥手**：A 发送释放连接报文段，FIN=1，seq = u（已发送数据最后字节序号 + 1）。A 进入 **FIN-WAIT-1（终止等待1）**状态。

**第二次挥手**：B 收到后发送确认报文段，ACK=1，ack = u+1，seq = v。B 进入 **CLOSE-WAIT（关闭等待）**状态。A 收到后进入 **FIN-WAIT-2（终止等待2）**状态。此时 TCP 连接处于**半关闭**状态：A 不再发送数据，但 B 仍可继续向 A 发送数据。

**第三次挥手**：B 的数据发送完毕后，发送释放连接报文段，FIN=1，ACK=1，seq = w，ack = u+1。B 进入 **LAST-ACK（最后确认）**状态。

**第四次挥手**：A 收到后发送确认报文段，ACK=1，ack = w+1。A 进入 **TIME-WAIT（时间等待）**状态，等待 **2MSL** 后进入 CLOSED；B 收到确认后进入 CLOSED。

**2. 为什么 A 要等待 2MSL（关键考点）**
MSL（Maximum Segment Lifetime，报文段最长寿命）。A 等待 2MSL 的原因：
- **确保最后一次 ACK 能到达 B**：若该 ACK 丢失，B 会超时重发 FIN，A 在 2MSL 内能收到并重新确认；否则 B 无法正确关闭；
- **让本连接的所有旧报文段在网络中自然消失**：2MSL 保证连接双方发送的所有报文段都已在网络中过期，避免"旧连接残留报文"干扰新连接。

**3. 为什么 TIME-WAIT 要 2MSL 而不是 1MSL**
因为 A 发出的最后 ACK 若丢失，B 重发的 FIN 最坏要在 1MSL 后到达 A，A 再次发 ACK 又需最多 1MSL 到达 B。2MSL 保证这条"最坏往返"能完成。

**4. 状态转移（简要）**
ESTABLISHED →（发 FIN）→ FIN-WAIT-1 →（收 ACK）→ FIN-WAIT-2 →（收 FIN，发 ACK）→ TIME-WAIT（2MSL）→ CLOSED；
ESTABLISHED →（收 FIN，发 ACK）→ CLOSE-WAIT →（发 FIN）→ LAST-ACK →（收 ACK）→ CLOSED。

**重点：** 四次挥手的四个报文段与状态变化；半关闭状态；TIME-WAIT 等待 2MSL 的两个原因。

**难点：** 为什么是"四次"——因为 B 收到 FIN 后可能还有数据要发，不能立即关闭，ACK 与 FIN 必须分开发（而三次握手的 SYN 与 ACK 可合并）；TIME-WAIT 不能省略的原因。

## TCP 的可靠传输
<!-- sec: ch5-s4 -->

### 序号、确认与超时重传
<!-- kp: ch5-s4-kp1 emphasis: key -->

**1. 按字节编号**
TCP 是面向字节流的协议，发送方把应用层的数据看成字节流，**每个字节都编上序号**。报文段携带"数据部分的第一个字节的序号"。接收方按序号把字节按序重组，用**累积确认**回送 ACK：ack = 已正确收到的最后一个字节序号 + 1。

**2. 确认机制**
- TCP 默认采用**累积确认**：ack=n 表示 n 之前的字节全部正确收到；
- 为提高效率，接收方可以采用**延迟确认**（隔一段时间批量确认）和**捎带确认**（在返回的数据报文段中捎带 ACK，效率更高）；
- 若收到乱序报文段，接收方可以立即对"按序到达的最后一个报文段"重复确认，提示发送方快速重传。

**3. 超时重传（RTO）**
发送方每发一个报文段就启动**重传计时器**，若超时仍未收到确认，则重传。重传时间 RTO 不能设置太长（等待浪费）也不能太短（误重传），采用自适应算法：

$$\\text{RTT}_S = (1-\\alpha)\\times\\text{RTT}_S + \\alpha\\times\\text{RTT}_\\text{样本},\\quad \\alpha=\\frac{1}{8}$$

$$RTO = \\text{RTT}_S + 4\\times\\text{RTT}_D$$

其中 RTT_S 是加权平均往返时间，RTT_D 是 RTT 的偏差的加权平均值。当 RTT 波动大时 RTO 相应增大，波动小时 RTO 减小。

**4. 冗余 ACK 与快重传**
发送方收到 **3 个重复的 ACK**（对同一序号的确认）时，可以认为该序号的下一个报文段丢失，**立即重传**该报文段，而不必等待超时——这就是**快重传**。

**5. 考点归纳**
- 序号按字节编号，ack 表示期望收到的下一字节序号；
- RTO 由 RTT 加权平均自适应计算；
- 快重传：收到 3 个重复 ACK 立即重传。

**重点：** 累积确认与 ack 的含义；RTO 自适应于 RTT；快重传的触发条件（3 个重复 ACK）。

**难点：** 计算 RTT 样本时只采样"未重传过的报文段"的 RTT（重传过的样本不可靠）；累计确认下 ack 的字段值永远是"下一期望字节号"，而非"最后收到的字节号"。

### TCP 滑动窗口与可靠传输实现
<!-- kp: ch5-s4-kp2 emphasis: difficult -->

**1. 发送窗口与接收窗口**
TCP 用滑动窗口实现可靠传输与流量控制。发送方维护**发送窗口**，其大小为 min(接收方通告的接收窗口 rwnd，发送方的拥塞窗口 cwnd)。发送窗口内的字节可以连续发送：

$$\\text{发送窗口}=\\min(\\text{rwnd},\\ \\text{cwnd})$$

接收方维护**接收窗口**，接收窗口 = rwnd，表示接收方当前还能接收的字节数。接收方通过 ACK 报文中的"窗口"字段通告 rwnd。

**2. 发送窗口的三个指针（概念）**
发送窗口将待发送数据分为三部分：已发送并确认（窗口左侧，可以右移）、已发送未确认（窗口内）、未发送（窗口右侧）。窗口内"已发送未确认"的数据若超时未确认则重传。接收窗口则把收到的字节流划分为"已确认并交付上层、按序未交付（缓存）、未到达（空缺）"。

**3. 累积确认下的滑动机制**
- 发送方发送窗口内序号连续的数据；
- 接收方按序缓存，对"最后按序到达的字节"回 ACK，窗口随确认右移；
- 发送窗口右移后，新的字节可以发出；
- 若中间有字节缺失，接收方缓存后续字节（TCP 接收缓存），窗口不右移，并对缺失字节前的序号重复确认。

**4. 窗口与流量控制的关系**
接收方通过 rwnd 限制发送方一次能发送的数据量，防止发送过快导致接收方缓存溢出——这就是 TCP 的**流量控制**（下一节详述）。若 rwnd = 0，发送方暂停发送。

**5. 考点归纳**
- 发送窗口 = min(rwnd, cwnd)，接收窗口 = rwnd；
- 滑动窗口实现"流水线发送"，使信道利用率远高于停等协议；
- 超时/3 个重复 ACK 触发重传。

**重点：** 发送窗口 = min(rwnd, cwnd)；累积确认驱动窗口右移；窗口内的未确认数据可连续发送。

**难点：** 窗口是"字节"为单位不是"报文段"为单位；ack 到达后窗口右移多少取决于 ack 值，而不是固定滑动一个报文段。

## TCP 流量控制与拥塞控制
<!-- sec: ch5-s5 -->

### TCP 流量控制（滑动窗口）
<!-- kp: ch5-s5-kp1 emphasis: key -->

**1. 流量控制的目标**
流量控制解决"**接收方来不及接收**"的问题：发送方的发送速率不能超过接收方的接收能力。TCP 通过**滑动窗口 + 接收方通告的 rwnd** 实现流量控制。接收方在 ACK 报文段的"窗口"字段中通告自己的接收窗口 rwnd，发送方据此调整发送窗口：

$$\\text{发送窗口}=\\min(\\text{rwnd},\\ \\text{cwnd})$$

当 rwnd 减小甚至为 0 时，发送窗口相应缩小甚至停止发送，从而保证不会"淹没"接收方。

**2. 零窗口问题与持续计时器**
当接收方通告 rwnd = 0 时，发送方停止发送。但若接收方的缓存恢复后要通知发送方"窗口重新打开"，而这个 rwnd 更新报文段若丢失，发送方会一直等待、接收方会一直等待，形成**死锁**。解决方法：发送方启动**持续计时器（persistence timer）**，周期性地发送**窗口探测报文**（1 字节），迫使接收方回复当前的 rwnd，从而打破死锁。

**3. 流量控制与拥塞控制的关系**
两者都限制发送速率，但**性质不同**：
- **流量控制**是端到端的、接收方能力限制（防止"快发方"淹没"慢收方"）；
- **拥塞控制**是全局的、网络能力限制（防止"注入太多数据"导致网络拥塞）。

**4. 考点归纳**
- 流量控制通过 rwnd（接收方通告）实现，属于接收方主导；
- 零窗口 + 持续计时器 + 窗口探测报文；
- 发送窗口取 rwnd 与 cwnd 的较小值——"两个窗口共同约束发送速率"。

**重点：** rwnd 是流量控制的核心；发送窗口 = min(rwnd, cwnd)；零窗口死锁与持续计时器。

**难点：** 区分流量控制（接收方能力）与拥塞控制（网络负载）——前者是"我不想收"，后者是"网络太堵"，两者都通过窗口机制共同决定发送速率。

### TCP 拥塞控制（慢开始、拥塞避免、快重传、快恢复）
<!-- kp: ch5-s5-kp2 emphasis: difficult -->

**1. 拥塞控制的概念**
拥塞控制的目标：当网络出现拥塞（丢包、时延增大）时，主动降低发送速率，避免网络崩溃。TCP 通过维护**拥塞窗口 cwnd** 实现，**发送窗口 = min(rwnd, cwnd)**。拥塞控制四种算法：

**2. 慢开始（Slow Start）**
连接建立时 cwnd 初始为 1 个最大报文段长度（MSS）。每收到一个确认，cwnd **+1 MSS**。效果：每经过一个传输轮次（RTT），cwnd **翻倍**（指数增长：1→2→4→8→...）。慢开始的"慢"是指初始 cwnd 小，不是增长慢。

**3. 拥塞避免（Congestion Avoidance）**
当 cwnd 达到**慢开始门限 ssthresh** 时，改用拥塞避免算法：每个传输轮次 cwnd **+1 MSS**（线性增长，加法增大）。目的：缓慢试探网络容量，避免很快进入拥塞。

**4. 拥塞发生后的处理**
- **超时重传**（网络拥塞严重）：令 ssthresh = cwnd/2，cwnd = 1，重新开始慢开始；
- **快重传**：收到 **3 个重复 ACK** 立即重传缺失报文段（不必等超时）；
- **快恢复（Fast Recovery）**：配合快重传使用——收到 3 个重复 ACK 时，令 ssthresh = cwnd/2，**cwnd = ssthresh**（不降到 1），然后进入拥塞避免（线性增长）。快恢复避免了慢开始的剧烈重启，提高吞吐量。

**5. 总结对比**

| 算法 | 增长方式 | 触发条件 |
| --- | --- | --- |
| 慢开始 | cwnd 每轮翻倍（指数） | 连接开始或超时重传后 |
| 拥塞避免 | cwnd 每轮 +1 MSS（线性） | cwnd 达到 ssthresh |
| 快重传 | 立即重传（不增窗口） | 收到 3 个重复 ACK |
| 快恢复 | cwnd = ssthresh，再线性增长 | 收到 3 个重复 ACK（配合快重传） |

**重点：** 四种算法的触发条件与 cwnd 变化规律；超时 → cwnd=1 重新慢开始；3 个重复 ACK → 快重传 + 快恢复。

**难点：** "乘法减小、加法增大（AIMD）"原则——拥塞时窗口减半（乘法减小），正常时每轮 +1（加法增大）；区分"超时"（重来）与"重复 ACK"（减半）两种拥塞信号的不同处理。

### 拥塞窗口 cwnd 变化示例
<!-- kp: ch5-s5-kp3 emphasis: difficult -->

**1. 典型计算例题**
【例】设 TCP 连接初始 ssthresh = 16（单位 MSS），传输过程中出现拥塞，试描述 cwnd 的变化。

设 cwnd 初始为 1，一个传输轮次为 RTT：

- 传输轮次 1：cwnd = 1（发送 1 个 MSS）；
- 轮次 2：cwnd = 2（慢开始，翻倍）；
- 轮次 3：cwnd = 4；
- 轮次 4：cwnd = 8；
- 轮次 5：cwnd = 16，达到 ssthresh，进入拥塞避免；
- 轮次 6：cwnd = 17（+1 线性增长）；
- 轮次 7：cwnd = 18；
- 假设轮次 7 出现**超时**（严重拥塞）：ssthresh = 18/2 = 9，cwnd 重置为 1，重新慢开始；
- 之后：1→2→4→8，达到新门限 9，进入拥塞避免 10、11、...。

**2. 快恢复场景示例**
假设发送 1 号报文段后连续收到对 1 号报文段的 **3 个重复 ACK**（说明 2 号丢失）：
- 立即执行**快重传**：重传 2 号报文段；
- 同时执行**快恢复**：ssthresh = cwnd/2，cwnd = ssthresh，进入拥塞避免；
- 之后 cwnd 线性增长。

对比：若采用超时重传，cwnd 会降为 1 重新慢开始，吞吐量大幅下降。快恢复在"可能只是个别报文段丢失"的情况下避免过度降低发送速率。

**3. 考点归纳**
- 慢开始阶段每个轮次 cwnd 翻倍；拥塞避免阶段每个轮次 +1；
- 超时：ssthresh = cwnd/2，cwnd = 1，慢开始；
- 3 个重复 ACK：ssthresh = cwnd/2，cwnd = ssthresh，快恢复；
- 计算题通常给初始 ssthresh 和拥塞发生轮次，要求画出 cwnd 随传输轮次的变化并计算每个轮次发送的报文段数。

**4. 注意陷阱**
- 达到 ssthresh 的"当轮"即可进入拥塞避免（cwnd 从 ssthresh 起每轮 +1）；
- 快恢复后 ssthresh 已更新为原来的一半，后续慢开始的门限按新值判断；
- cwnd 的单位是 MSS，发送窗口还需取 min(cwnd, rwnd)。

**重点：** 会画/会填 cwnd 随传输轮次变化的表；超时与 3 重复 ACK 两种拥塞信号的处理差异。

**难点：** 一轮传输 = 一个 RTT，且"收到该轮全部确认后 cwnd 才增长一次"；慢开始阶段一轮发送的报文段数 = 当前 cwnd，全部确认后 cwnd 翻倍，因此累计发送量呈指数增长。
`;export{e as default};