var e=`---
id: ch2
title: 进程管理
order: 2
---

## 进程与线程
<!-- sec: ch2-s1 -->

### 进程的概念、组成与PCB
<!-- kp: ch2-s1-kp1 emphasis: key -->

在多道程序环境下，程序这个概念不足以描述并发执行带来的动态特征，因此引入进程。进程（Process）被定义为：进程是程序在一个数据集合上的一次运行过程，是系统进行资源分配和调度的独立单位（传统操作系统中）。进程具有动态性（有生有灭、有状态转换）、并发性（多个进程可同时存在于内存）、独立性和异步性。进程与程序的联系：进程是程序执行的过程，程序是进程运行的静态文本。

进程的组成：进程 = 程序段 + 数据段 + 进程控制块（PCB）。程序段存放代码，数据段存放运行所需的数据与工作区，PCB是进程存在的唯一标志。操作系统通过PCB来感知和管理进程，PCB由系统创建进程时建立、撤销进程时回收。PCB中主要包含：进程标识符（PID）、处理机状态信息（通用寄存器、程序计数器PC、程序状态字PSW、栈指针等）、进程调度信息（进程状态、优先级、等待原因等）以及进程控制信息（程序和数据段的地址、同步与通信机制、资源清单等）。PCB的组织方式有链接方式（就绪队列、阻塞队列）与索引方式。

**重点：**PCB是进程存在的唯一标志；进程与程序的区别（动态与静态、能否并发、是否可共享）。**难点：**PCB中处理机状态信息的作用——进程被切换时其现场信息保存在PCB中，再次被调度运行时恢复，这正是进程能够"走走停停"且最终结果正确的基础。

### 进程的状态与转换
<!-- kp: ch2-s1-kp2 emphasis: key -->

进程的基本状态：运行态（正在CPU上执行）、就绪态（已具备运行条件、等待分配CPU）、阻塞态（等待某事件发生，如等待I/O完成）、创建态和终止态。五种状态之间的转换：创建→就绪（创建完成）；就绪→运行（被调度程序选中）；运行→就绪（时间片用完或被更高优先级进程抢占）；运行→阻塞（主动请求资源或等待事件）；阻塞→就绪（所等待的事件发生）；运行→终止（正常结束或被撤销）。

注意状态转换的合法性与非法性：就绪→阻塞是非法转换（就绪进程不等待任何事件）；阻塞→运行是非法转换（必须先经过就绪态）。引入了挂起（suspend）的七状态模型还会增加"就绪挂起"与"阻塞挂起"两个状态，挂起通常是把进程换出到外存，以提高内存利用率。

进程控制通过原语实现：创建原语（如fork）、撤销原语（如exit）、阻塞原语（block）、唤醒原语（wakeup）。阻塞是进程自己主动执行的，唤醒是由其他进程或中断处理程序被动执行的。**重点：**五状态模型及合法的状态转换关系。**难点：**运行→阻塞是主动行为，阻塞→就绪是被动行为；原语必须一气呵成（不可被中断），通常通过关中断来保证原子性。

### 线程与多线程模型
<!-- kp: ch2-s1-kp3 emphasis: key -->

线程（Thread）是进程内的一个执行流，是处理机调度的基本单位，又称轻量级进程。引入线程后，进程成为资源分配的基本单位，线程成为调度与运行的基本单位。同一进程的多个线程共享进程的地址空间和资源（代码段、数据段、打开的文件等），但每个线程拥有自己独立的线程控制块TCB、程序计数器PC、寄存器集合和栈。

线程的优点：创建、撤销、切换的开销比进程小得多；同一进程内线程间通信无需内核介入（直接访问共享内存）；并发性能好，能充分利用多核处理器。线程的实现方式有两种：用户级线程（ULT，由用户空间的线程库管理，内核感知不到，切换快，但一个线程阻塞则整个进程阻塞，且不能利用多核并行）和内核级线程（KLT，由内核管理，调度以线程为单位，能充分利用多核，但切换开销大）。

多线程模型（用户级线程与内核级线程的组合方式）：多对一模型（多个ULT映射到一个KLT，管理开销小但无法利用多核、一个线程阻塞则进程全阻塞）、一对一模型（一个ULT对应一个KLT，并发度高、可多核并行，但线程开销大、数量受限）、多对多模型（n个ULT映射到m个KLT，n大于m，兼顾二者优点）。**重点：**进程与线程的区别；三种多线程模型及其特点。**难点：**线程切换的代价与是否进入内核相关——用户级线程的切换在用户态完成，内核级线程的切换需要陷入内核。

### 进程通信（IPC）
<!-- kp: ch2-s1-kp4 emphasis: key -->

进程通信（IPC）指进程之间交换数据与信息的机制，主要有三类方式：**共享存储、消息传递、管道通信**（进程之间不能直接访问对方的地址空间，必须通过操作系统提供的机制）。

- **共享存储**：在内存中划出一块共享区域，多个进程通过映射访问同一物理内存直接读写数据。互斥与同步由进程自己负责（P、V操作）。**低级通信方式**，速度快但同步责任在用户。
- **消息传递**：进程以"发送消息/接收消息"原语交换数据，消息含消息头（类型、长度、发送/接收者）与消息体。分为**直接通信**（指名发送给某进程）与**间接通信**（经中间实体——信箱/邮箱转发，如邮件系统）。操作系统负责收发，**高级通信方式**，同步问题由内核处理。
- **管道通信**：连接两个进程的一个共享文件（管道），一个进程写、另一个进程读，**单向**（半双工），先进先出。写满则写进程阻塞，读空则读进程阻塞——天然实现了互斥与同步。Unix中 \`|\` 命令即管道。注意与文件共享的区别：管道文件有读写同步机制，共享文件没有。

**重点：**三类通信方式及其特点（共享存储需自己同步；消息传递分直接/间接通信；管道单向、半双工、自带阻塞同步）；管道是"共享文件+同步机制"的结合。

## 处理机调度
<!-- sec: ch2-s2 -->

### 调度的层次与调度准则
<!-- kp: ch2-s2-kp1 -->

处理机调度按层次分为三级：高级调度（作业调度、长程调度）——从外存后备队列中按作业调度算法选择作业调入内存、创建进程，发生在作业到进程的转换中，频率最低；中级调度（内存调度、中程调度）——将进程从内存换出到外存（挂起）或换入，用于提高内存利用率；低级调度（进程调度、短程调度）——从就绪队列中选择一个进程分配CPU，频率最高，是操作系统中必不可少的调度。

调度算法的评价准则：CPU利用率、系统吞吐量（单位时间内完成的作业数）、周转时间（作业完成时刻减去提交时刻，包含等待、执行与I/O时间）、带权周转时间（周转时间/服务时间，越接近1越好）、等待时间、响应时间。其中周转时间与带权周转时间是考研计算的常考点。

**重点：**三级调度各自的任务与执行频率；周转时间与带权周转时间的定义。**难点：**不同场景适用的调度准则不同——批处理系统重吞吐量与周转时间，分时系统重响应时间，实时系统重截止时间的保证。

### 典型调度算法（FCFS/SJF/优先级/时间片轮转）
<!-- kp: ch2-s2-kp2 emphasis: key -->

先来先服务（FCFS）：按作业/进程到达的先后顺序调度，非抢占式。实现简单、算法公平，但有利于长作业而不利于短作业（短作业可能等待长作业很久），平均周转时间可能较长。

短作业优先（SJF/SPF）：选择服务时间最短的作业/进程先运行。分为非抢占式SJF与抢占式（最短剩余时间优先SRTN，新到进程剩余时间更短时抢占）。SJF能使平均等待时间和平均周转时间最短，从平均意义上是最优算法，但对长作业可能造成饥饿（可用老化技术缓解），且需要预知服务时间，实际中难以实现。优先级调度：按优先级高者优先调度，分抢占与非抢占两种；优先级既可由系统静态确定，也可动态调整；低优先级进程可能产生饥饿。

时间片轮转（RR）：就绪队列按FCFS排队，每个进程每次运行一个时间片q，时间片用完未完成则移到队尾重新排队，主要适合分时系统。时间片大小的选择至关重要：q过小则进程切换过于频繁、系统开销大；q过大则退化为FCFS，交互性变差。一般取q略大于一次典型交互所需的时间。**重点：**各算法的基本思想、是否抢占、优缺点与适用场景。**难点：**SJF平均周转时间最短的数学结论与饥饿问题；RR中时间片大小对系统性能的双向影响。

### 多级反馈队列调度与调度计算实例
<!-- kp: ch2-s2-kp3 emphasis: difficult -->

多级反馈队列（MLFQ）调度算法：设置多个就绪队列，各队列优先级从高到低、时间片从短到长；新到达的进程进入最高优先级队列；调度时按队列优先级由高到低选择，同一队列内按FCFS；进程用完本队列分配的时间片仍未完成，则降入下一级队列；仅当所有更高优先级队列为空时才调度本队列。该算法综合了多种算法的优点，既照顾短进程（可快速完成），又兼顾长进程（逐步降级仍能获得CPU），被许多实际操作系统采用。

计算实例（同一时刻到达）：设A、B、C、D四个作业同时到达（到达时间均为0），服务时间分别为4、3、2、1。
- FCFS（按A、B、C、D顺序）：完成时刻分别为4、7、9、10；周转时间为4、7、9、10，平均周转时间7.5；带权周转时间为1、2.33、4.5、10，平均4.46。
- SJF（按D、C、B、A顺序）：完成时刻分别为1、3、6、10；周转时间为1、3、6、10，平均周转时间5.0；带权周转时间为1、1.5、2、2.5，平均1.75，明显优于FCFS，印证了SJF平均周转时间最短的结论。
- RR（时间片q=1）：执行顺序为A B C D A B C A B A，完成时刻依次为D=4、C=7、B=9、A=10，周转时间为10、9、7、4，平均周转时间7.5。

再给一个到达时间不同的例子：作业J1~J4的到达时刻为0、1、2、3，服务时间为5、3、2、1，采用非抢占SJF。时刻0只有J1到达，运行J1至时刻5；此时就绪队列中有J2（3）、J3（2）、J4（1），按短作业优先依次运行J4（时刻5—6）、J3（时刻6—8）、J2（时刻8—11）。各作业周转时间：J1=5、J4=3、J3=6、J2=10，平均周转时间6。**难点：**计算时注意区分SJF的非抢占与抢占（SRTN）形式、时间片轮转中新进程到达时刻对排队顺序的动态影响，以及带权周转时间的定义（周转时间÷服务时间）。

## 进程同步与互斥
<!-- sec: ch2-s3 -->

### 临界区与进程同步互斥
<!-- kp: ch2-s3-kp1 -->

临界资源：一次仅允许一个进程使用的资源，如打印机、共享变量等。临界区（critical section）：访问临界资源的代码段。为保证互斥访问，需要协调各进程进入临界区。实现临界区互斥应遵循四条原则：空闲让进（临界区空闲时允许一个进程进入）、忙则等待（已有进程在临界区时其他进程必须等待）、有限等待（不能让进程无限期等待进入临界区）、让权等待（进程不能进入临界区时应立即释放处理机，防止忙等）。

软件方法：单标志法（设置turn变量严格轮流进入，违背空闲让进原则）、双标志先检查法（先检查对方标志再置自己的标志，可能同时进入临界区，违背忙则等待）、双标志后检查法（先置标志再检查对方，可能双方都进不去，违背空闲让进与有限等待）、Peterson算法（综合flag数组与turn变量，满足前三条原则，但不满足让权等待）。

硬件方法：关中断（进入临界区前关中断，实现简单但关中断时间不宜过长、不适用于多处理机）、TestAndSet指令（TS，原子地读取并置为true，可实现互斥但等待进程忙等）、Swap指令（原子交换两个变量的值）。硬件方法的优点：适用于任意数量的进程、实现简单；缺点：等待进入临界区的进程会忙等浪费CPU、可能产生饥饿。**重点：**临界区互斥的四条原则；各软件、硬件方法的原理与不足。**难点：**Peterson算法的正确性分析；用TestAndSet或Swap实现互斥的伪代码理解。

### 信号量与PV操作
<!-- kp: ch2-s3-kp2 emphasis: key -->

信号量（Semaphore）是由Dijkstra提出的进程同步与互斥机制。记录型信号量由一个整型值value和一个阻塞队列组成：value>0表示资源的剩余可用数量；value<0表示有-value个进程在等待该资源。对信号量只能执行P操作（wait，申请资源）和V操作（signal，释放资源），二者均为原语（不可中断）。

P、V操作的C语言描述如下：

\`\`\`c
typedef struct {
  int value;             // 资源数目
  struct process *list;  // 等待队列
} semaphore;

// P操作（wait，申请资源）
void wait(semaphore *S) {
  S->value--;
  if (S->value < 0) {
    // 将当前进程插入S->list，并调用block原语阻塞之
    block(S->list);
  }
}

// V操作（signal，释放资源）
void signal(semaphore *S) {
  S->value++;
  if (S->value <= 0) {
    // 从S->list中移出一个进程，调用wakeup原语唤醒之
    wakeup(S->list);
  }
}
\`\`\`

用PV操作实现互斥：设置互斥信号量mutex初值为1，进程进入临界区前执行P(mutex)，退出临界区后执行V(mutex)。用PV操作实现同步（前驱关系）：若进程B的某操作必须在进程A的某操作之后执行，设置同步信号量s初值为0，A执行V(s)，B执行P(s)。**重点：**P、V操作的执行流程（value的修改与阻塞/唤醒）；用P、V实现互斥与同步的基本模型（互斥信号量初值1、同步信号量初值0）。**难点：**P、V操作必须原子执行；当value<0时P操作阻塞、V操作唤醒的含义；对信号量的访问只能通过P、V操作进行，不能直接读写value。

### 经典同步问题（生产者-消费者、读者-写者、哲学家进餐）
<!-- kp: ch2-s3-kp3 emphasis: difficult -->

生产者-消费者问题（有界缓冲区）：多个生产者向容量为n的缓冲区放产品，多个消费者从中取产品；任何时候仅允许一个进程访问缓冲区；缓冲区空时消费者等待，缓冲区满时生产者等待。设置互斥信号量mutex=1（保护缓冲区），同步信号量empty=n（空缓冲个数）、full=0（满缓冲个数）：

\`\`\`c
// 生产者进程
while (1) {
  生产一个产品;
  P(empty);        // 申请一个空缓冲
  P(mutex);        // 进入临界区
  将产品放入缓冲区;
  V(mutex);        // 退出临界区
  V(full);         // 满缓冲数加1
}

// 消费者进程
while (1) {
  P(full);         // 申请一个满缓冲
  P(mutex);        // 进入临界区
  从缓冲区取出产品;
  V(mutex);        // 退出临界区
  V(empty);        // 空缓冲数加1
  消费该产品;
}
\`\`\`

注意P操作的顺序不能互换：若先P(mutex)再P(empty)，当缓冲区已空且生产者在临界区中等待empty时，消费者无法进入临界区取产品释放empty，将造成死锁。所以同步信号量的P操作必须位于互斥信号量的P操作之前（"先同步后互斥"原则）。

读者-写者问题：允许多个读者同时读，写者与其他读者或写者互斥。读者优先解法：设置读计数器count初值0、互斥信号量rw=1（读写互斥）、mutex=1（保护count）：

\`\`\`c
// 写者进程
P(rw);
写操作;
V(rw);

// 读者进程
P(mutex);
if (count == 0) P(rw);  // 第一个读者加"读锁"
count++;
V(mutex);
读操作;
P(mutex);
count--;
if (count == 0) V(rw);  // 最后一个读者解"读锁"
V(mutex);
\`\`\`

该解法中读者可能不断到来导致写者饥饿，若需写者优先或读写公平，可再增加一个同步信号量w，让新读者先执行P(w)，实现写者优先。

哲学家进餐问题：5位哲学家围圆桌而坐，桌上5根筷子，每人左右各一根，哲学家需同时拿到左右两根筷子才能进餐。若每个人都先拿左手筷子，可能5人各持一根而互相等待，形成死锁。常用解法：①限制至多允许4人同时去拿筷子（设置信号量room=4）；②要求必须一次同时拿两只筷子（AND型信号量，或用"先拿一只，拿不到第二只则全放下"的策略）；③奇数号哲学家先拿左手、偶数号先拿右手。此外还有吸烟者问题（三种原料组合的生产-消费模型）。**难点：**理解"先做同步信号量的P操作、后做互斥信号量的P操作"的顺序原则，以及每个经典问题中信号量的设计与初值选取。

## 管程
<!-- sec: ch2-s4 -->

### 管程的定义与基本特征
<!-- kp: ch2-s4-kp1 emphasis: difficult -->

管程（Monitor）是一种高级同步机制，其思想是把分散在各进程中的临界区集中起来统一管理。定义：管程是一个由关于共享资源的数据结构（局部数据）及其上的一组操作（过程/方法）组成的软件模块。进程要使用该资源只能调用管程中的过程，进程不能直接访问管程内的数据。管程的名称来源于它"管理共享资源的程序模块"这一角色。

管程的基本特征：①局部于管程的数据只能被管程内的过程访问；②进程想进入管程必须通过调用管程内的过程，且每次仅允许一个进程在管程内执行（管程的互斥性由编译器自动实现，无需程序员设置mutex）；③可在管程内设置条件变量（condition）以及wait/signal操作来实现同步——进程在管程内因条件不满足可调用cwait(c)把自己阻塞到条件变量c对应的队列上，另一进程可通过csignal(c)唤醒一个等待者。

管程与信号量相比：管程把互斥机制封装在模块内部，同步通过条件变量实现，结构清晰、不易出错；但条件变量不保存计数值（csignal在无等待进程时直接丢弃，无"记忆"效果），这与信号量V操作"总能加1"不同。**重点：**管程的互斥特性与条件变量机制。**难点：**条件变量的wait/signal与信号量P/V的区别——前者无累积计数值，signal在无等待进程时丢失；后者会保存计数值。

### 用管程解决经典同步问题
<!-- kp: ch2-s4-kp2 -->

用管程解决生产者-消费者问题：定义管程ProducerConsumer，其中包含缓冲区数组buf[n]、两个条件变量full（缓冲满时生产者等待）和empty（缓冲空时消费者等待），以及put()与get()两个过程。伪代码如下：

\`\`\`c
monitor ProducerConsumer {
  int buf[N];
  int in = 0, out = 0, count = 0;
  condition full, empty;          // 条件变量

  void put(int item) {
    if (count == N) wait(empty);  // 缓冲满则等待
    buf[in] = item;
    in = (in + 1) % N;
    count++;
    signal(full);                 // 唤醒可能等待的消费者
  }

  int get() {
    if (count == 0) wait(full);   // 缓冲空则等待
    int item = buf[out];
    out = (out + 1) % N;
    count--;
    signal(empty);                // 唤醒可能等待的生产者
    return item;
  }
}

// 生产者进程：ProducerConsumer.put(生产的产品);
// 消费者进程：产品 = ProducerConsumer.get();
\`\`\`

用管程解决哲学家进餐问题：在管程内用枚举数组state[5]记录每位哲学家的状态（思考、饥饿、进餐），条件变量self[i]表示第i位哲学家能否拿筷子，提供pickup(i)与putdown(i)过程。关键在test(i)中同时检查左右邻居是否都未进餐，只有两者均空闲才允许拿筷子，从根本上避免了死锁：

\`\`\`c
monitor DiningPhilosopher {
  enum { THINKING, HUNGRY, EATING } state[5];
  condition self[5];

  void pickup(int i) {
    state[i] = HUNGRY;
    test(i);                        // 试着同时拿左右筷子
    if (state[i] != EATING) wait(self[i]);
  }

  void putdown(int i) {
    state[i] = THINKING;
    test((i + 4) % 5);              // 检查左邻居
    test((i + 1) % 5);              // 检查右邻居
  }

  void test(int i) {
    if (state[i] == HUNGRY &&
        state[(i + 4) % 5] != EATING &&
        state[(i + 1) % 5] != EATING) {
      state[i] = EATING;
      signal(self[i]);
    }
  }
}
\`\`\`

**难点：**管程方案中"一次性检查左右邻居是否同时空闲"的机制天然避免了死锁；注意条件变量的signal只在有等待者时才有唤醒作用，否则丢弃。

## 死锁
<!-- sec: ch2-s5 -->

### 死锁的概念与产生的必要条件
<!-- kp: ch2-s5-kp1 emphasis: key -->

死锁（Deadlock）：多个进程因竞争资源而互相等待，若无外力作用，这些进程将都无法向前推进。死锁主要发生在多道程序共享资源、并发执行的环境下。死锁与饥饿不同：饥饿是进程长期得不到所需资源但将来仍有可能获得；死锁是进程互相等待形成循环，永远无法推进。

死锁产生的四个必要条件：①互斥条件——资源为临界资源，一次仅允许一个进程使用；②不可剥夺条件——进程已获得的资源在未使用完之前不能被其他进程强行剥夺；③请求并保持条件——进程已保持至少一个资源，又提出新的资源请求，而该资源已被其他进程占用，此时该进程阻塞但并不释放已占有的资源；④循环等待条件——存在一个进程资源的循环等待链，链中每个进程等待的资源被链中的下一个进程占有。四个条件必须同时满足才会发生死锁。

处理死锁的基本方法：①死锁预防（Prevention）——破坏四个必要条件之一，静态、实现简单但资源利用率低；②死锁避免（Avoidance）——在资源分配之前判断本次分配是否会使系统进入不安全状态，如银行家算法，属于动态策略；③死锁检测（Detection）与解除（Recovery）——允许死锁发生，检测到死锁后再解除。**重点：**死锁的四个必要条件及其与处理死锁策略的对应关系。**难点：**循环等待是死锁的必要条件而非充分条件；资源分配图中每种资源只有单个实例时，出现环则必然死锁（死锁定理）。

### 死锁的预防与避免（银行家算法）
<!-- kp: ch2-s5-kp2 emphasis: difficult -->

死锁预防（破坏四个必要条件）：破坏互斥条件不可行（资源本身互斥）；破坏不可剥夺条件（进程申请不到新资源时须释放已占资源，可能造成前功尽弃、实现复杂）；破坏请求并保持条件（采用一次性申请全部资源的静态分配法，资源利用率低、易导致饥饿）；破坏循环等待条件（资源有序分配法，各进程按资源编号递增的顺序申请，合理设计编号可避免循环等待）。

死锁避免采用银行家算法：允许进程动态申请资源，但在分配前先判断系统是否处于安全状态，只有分配后系统仍安全才真正分配。数据结构包括：可利用资源向量Available、最大需求矩阵Max、分配矩阵Allocation、需求矩阵Need（满足 Need = Max - Allocation）。银行家算法（分配判断）的流程：

\`\`\`c
// 进程Pi发出资源请求向量 Requesti
1. 若 Requesti > Needi，则出错（请求超过最大需求）;
2. 若 Requesti > Available，则Pi需等待（当前可用资源不足）;
3. 试探性分配：
   Available -= Requesti;
   Allocationi += Requesti;
   Needi -= Requesti;
4. 执行安全性算法判断系统是否安全：
   - 安全：正式分配;
   - 不安全：回退试探性分配，Pi等待。
\`\`\`

安全性算法（判断系统是否处于安全状态）：令Work=Available，Finish[i]=false；不断寻找满足 Finish[i]=false 且 Needi<=Work 的进程，若找到则令 Work+=Allocation[i]、Finish[i]=true，重复直至不存在这样的进程；若所有进程的Finish均为true，则系统安全，进程被依次找到的顺序即安全序列，否则系统不安全。

计算实例：系统中有5个进程P0~P4、3类资源A、B、C，Available=(3,3,2)，各进程的Max与Allocation如下（Need=Max-Allocation）：

| 进程 | Max | Allocation | Need |
| ---- | ---- | ---- | ---- |
| P0 | (7,5,3) | (0,1,0) | (7,4,3) |
| P1 | (3,2,2) | (2,0,0) | (1,2,2) |
| P2 | (9,0,2) | (3,0,2) | (6,0,0) |
| P3 | (2,2,2) | (2,1,1) | (0,1,1) |
| P4 | (4,3,3) | (0,0,2) | (4,3,1) |

安全性检查：P1（Need(1,2,2)<=Work(3,3,2)）可满足，Work变为(5,3,2)；P3（Need(0,1,1)<=Work）可满足，Work变为(7,4,3)；P4（Need(4,3,1)<=Work）可满足，Work变为(7,4,5)；P2（Need(6,0,0)<=Work）可满足，Work变为(10,4,7)；P0（Need(7,4,3)<=Work）可满足。安全序列为P1、P3、P4、P2、P0，系统安全。**难点：**银行家算法计算题——先求Need矩阵，再手工模拟安全性算法寻找安全序列；特别注意试探性分配失败后要回退，以及"有安全序列则安全、无则不安全"的结论。

### 死锁的检测与解除
<!-- kp: ch2-s5-kp3 -->

死锁检测：允许死锁发生，通过资源分配图来分析。资源分配图中用圆圈表示进程、方框表示资源、方框内的小圆点表示资源实例；进程→资源边表示请求，资源→进程边表示分配。死锁定理：系统状态S为死锁状态的充分必要条件是S状态下的资源分配图是不可完全简化的（即无法通过逐步消除边的方法化简为孤立点）。当每种资源只有一个实例时，资源分配图中有环则必然死锁；而当资源有多个实例时，有环不必然死锁。

死锁检测算法与安全性算法类似：令Work=Available、Finish=false，反复寻找满足Finish[i]=false且Allocationi<=Work的进程，令其完成并释放资源（Work+=Allocation[i]、Finish[i]=true）；若最后仍存在Finish[i]=false的进程，则说明存在死锁。检测算法通常周期性运行，或在资源利用率很低、系统怀疑可能死锁时运行。

死锁解除的方法：①资源剥夺法——挂起部分死锁进程，抢占其资源分给其他进程；②撤销进程法——强制撤销部分甚至全部死锁进程，撤销时按代价最小原则选择，如优先级最低、运行时间最短、占用资源最多的进程优先撤销；③进程回退法——让进程回退到不发生死锁的检查点重新执行。**重点：**资源分配图的化简与死锁定理；检测与解除的几种方法。**难点：**死锁避免与死锁检测的区别——避免在资源分配前判断安全性以预防不安全状态，检测则允许死锁发生、事后发现并解除。
`;export{e as default};