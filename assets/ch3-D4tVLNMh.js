var e=`---
id: ch3
title: 内存管理
order: 3
---

## 内存管理概述
<!-- sec: ch3-s1 -->

### 内存管理的功能与内存保护
<!-- kp: ch3-s1-kp1 -->

内存管理是操作系统对内存的分配、回收、扩充与保护。其核心功能包括：内存空间的分配与回收（记录哪些内存已被占用、哪些空闲，按需分配和回收）、地址转换（将逻辑地址转换为物理地址）、内存扩充（通过虚拟内存技术使物理内存有限的系统能运行更大的程序）以及内存保护（保证各进程只能访问自己的内存区域，互不干扰）。

内存保护通常采用两种方式：上下限寄存器（存放进程在内存中的上下界地址，访问越界即产生越界中断）和重定位寄存器加界地址寄存器（重定位寄存器存放进程起始物理地址，界地址寄存器存放逻辑地址上限，检查"逻辑地址<界地址"且保证物理地址落在进程区域内）。内存保护必须在指令执行阶段动态进行，由硬件完成校验。

**重点：**内存管理的四大功能；内存保护的两种硬件方式。**难点：**理解重定位寄存器与界地址寄存器配合工作的机制——每次访问内存前由硬件检查逻辑地址是否越界，并与重定位寄存器相加得到物理地址。

### 地址绑定与逻辑/物理地址
<!-- kp: ch3-s1-kp2 emphasis: key -->

逻辑地址（相对地址）是程序产生的与物理内存无关的地址，由CPU直接生成；物理地址（绝对地址）是内存单元的实地址，是地址总线上的地址。逻辑地址空间与物理地址空间是两个不同的概念，编译、链接后的程序往往使用从0开始的逻辑地址空间。将逻辑地址转换为物理地址的过程称为地址变换或重定位。

地址绑定（重定位）可以发生在三个时机：编译时（编译阶段就生成绝对地址，只适用于单一程序环境，程序装入后不能移动）、装入时（装入程序时一次性将逻辑地址转换为物理地址，即静态重定位，程序运行期间不能移动）、运行时（程序执行过程中动态进行地址转换，即动态重定位，需硬件支持，程序可在内存中移动、可实现虚拟存储）。对应的三种装入方式为绝对装入、可重定位装入（静态重定位装入）与动态运行时装入（动态重定位装入）。

程序的链接方式：静态链接（程序运行前将各目标模块链接成一个完整可执行文件）、装入时动态链接（装入内存时边装入边链接）、运行时动态链接（程序运行中需要某模块时才链接装入，如共享库）。**重点：**逻辑地址与物理地址的区别；三种地址绑定时机与装入方式的对应关系。**难点：**动态重定位是虚拟内存的基础——只有运行时绑定才能在程序执行过程中调整其物理位置并支持部分装入。

## 连续分配管理方式
<!-- sec: ch3-s2 -->

### 单一连续分配与固定分区分配
<!-- kp: ch3-s2-kp1 -->

单一连续分配：内存分为系统区（常驻操作系统）和用户区（仅一道用户程序使用）。实现简单、无外部碎片，但内存利用率极低，且不支持多道程序，只适用于单用户单任务系统（如早期的MS-DOS）。

固定分区分配：将内存用户区预先划分为若干个固定大小的分区，各分区大小可以相同也可以不同，每个分区中只能装入一道作业。系统维护一个分区说明表（记录每个分区的起始地址、大小、状态），作业装入时按大小查找合适分区。固定分区分配的优点是支持多道程序、实现简单；缺点是分区大小与作业大小往往不匹配，产生内部碎片（分区内未被利用的空间），且大作业无法装入小分区。

**重点：**单一连续分配与固定分区分配的特点；内部碎片的成因。**难点：**区分内部碎片与外部碎片——内部碎片是已分配给进程但未被使用的分区内空间（固定分区产生），外部碎片是空闲区太小无法利用的零散空间（动态分区产生）。

### 动态分区分配与紧凑
<!-- kp: ch3-s2-kp2 emphasis: key -->

动态分区分配：不预先划分分区，在作业装入时按需动态地从内存中划分出大小正好合适的连续区域，其余空间继续留作空闲区。系统用空闲分区表或空闲分区链记录空闲区。常用的动态分区分配算法有四种：
- 首次适应（First Fit，FF）：从低地址开始按序查找，找到第一个能满足要求的空闲分区。优点是优先利用低地址空间、查找效率高（首次找到即分配），缺点是低地址部分被不断切割、高地址大空闲区保留；
- 最佳适应（Best Fit，BF）：选择大小最接近作业要求的空闲分区（从小到大排序）。优点是尽可能留下大的空闲区，缺点是产生大量难以利用的细小碎片；
- 最坏适应（Worst Fit，WF）：选择最大的空闲分区（从大到小排序）。优点是可容纳较大作业、减少小碎片，缺点是最大分区被切碎后大作业难以装入；
- 邻近适应（Next Fit，NF）：从上次分配位置开始继续向后查找，改进了FF的查找效率，但会使高地址空闲区被过早切碎。

| 算法 | 分区排列 | 分配策略 | 优点 | 缺点 |
| ---- | ---- | ---- | ---- | ---- |
| 首次适应FF | 按地址递增 | 第一个满足者 | 优先用低地址、查找快 | 低地址碎片多 |
| 最佳适应BF | 按容量递增 | 最接近者 | 保留大空闲区 | 产生很多小碎片 |
| 最坏适应WF | 按容量递减 | 最大者 | 大作业容易装入 | 最大分区易被切碎 |
| 邻近适应NF | 循环查找 | 从上次位置起第一个满足者 | 分配均匀 | 高地址区被切碎 |

动态分区分配会产生外部碎片——内存中存在很多很小的、无法利用的空闲区。解决外部碎片的方法是紧凑（压缩）：通过移动已分配进程的位置，把所有空闲区合并成一个连续的大空闲区。紧凑的代价是需要动态重定位支持（运行时绑定），且需要耗费处理机时间。**重点：**四种动态分区分配算法的思想与优缺点；外部碎片与紧凑。**难点：**比较FF、BF、WF、NF在同一内存状态下的分配结果，注意"排序方式"与"查找方向"的差别。

### 交换技术（Swap）
<!-- kp: ch3-s2-kp3 -->

**交换（Swapping）**技术指把处于等待状态（或暂不运行）的进程从内存**换出到外存**（备份区），再把即将运行的进程从外存**换入内存**，以缓解内存不足。交换在**进程级**进行（整个进程的地址空间），与虚拟内存的"部分调入"不同。换出多选阻塞时间长、优先级低的进程；换入与换出时间（进程大小/交换区带宽）之和称为**交换时间**，交换过频会使系统性能下降。交换与"中级调度（挂起）"、进程挂起状态配合使用。**低频考点**，理解"交换=进程整体换入换出、不同于请求分页的部分调入"即可。

**重点：**交换以进程为单位整体换入换出；换出优先选阻塞/低优先级的进程；交换技术是内存扩充的早期手段。

## 分页存储管理
<!-- sec: ch3-s3 -->

### 分页存储管理的基本概念
<!-- kp: ch3-s3-kp1 emphasis: key -->

分页存储管理将进程的逻辑地址空间划分为若干大小相等的部分，称为页或页面（page）；将物理内存划分为同样大小的若干块，称为页框或物理块（frame）。页面大小一般取2的整数次幂，如4KB、8KB，以便于用移位运算快速换算地址。页与页框一一映射，任一页可装入任一页框，从而消除了外部碎片，只可能产生不超过一页的内部碎片。

系统为每个进程建立一张页表（page table），页表记录进程每一页对应的页框号。页表项通常包括页框号以及访问权限、有效位等控制信息。页表本身存放在内存中，页表基址寄存器（PTBR）指向页表在内存中的起始地址。逻辑地址被硬件自动拆分为两部分：页号P与页内偏移量W。若页面大小为2^k，则逻辑地址的低k位为页内偏移，其余高位为页号；页号 = 逻辑地址 / 页面大小，页内偏移 = 逻辑地址 % 页面大小。

**重点：**页面与页框的概念、逻辑地址的拆分、页表的作用。**难点：**页面大小的选择——过小则页表占用空间大、内存碎片多但内碎片小，过大则内碎片大；页面大小是2的整数次幂时可用位运算高效完成地址拆分。

### 基本地址变换机构
<!-- kp: ch3-s3-kp2 emphasis: key -->

基本地址变换机构的功能是将逻辑地址转换为物理地址。以页面大小4KB为例，逻辑地址为（页号P, 页内偏移W）。变换过程：①由逻辑地址计算页号P与页内偏移W；②将页表基址寄存器中的页表起始地址与页号P相加，得到对应页表项在内存中的地址；③读取该页表项，取出页框号F；④物理地址 = 页框号F × 页面大小 + 页内偏移W。

示例：设页面大小4KB，逻辑地址为 4097（十六进制0x1001）。页面大小4KB=2^12，所以低12位为页内偏移，即W=1，页号P=4097/4096=1。若页表项1中页框号F=3，则物理地址 = 3×4096+1 = 12289（0x3001）。

基本地址变换的缺点是每次访问数据都需要访问内存两次：第一次查页表取页框号，第二次真正访问数据，即存取一个数据要访问两次内存，降低了系统效率（若采用两级页表则需访问更多次）。**重点：**逻辑地址到物理地址的换算方法；分页存储使每次存取需访问两次内存。**难点：**熟练计算页号与页内偏移（除法与取模），注意逻辑地址的位数、页表项长度与页表占用空间的计算。

### 快表TLB与有效访问时间、多级页表
<!-- kp: ch3-s3-kp3 emphasis: difficult -->

快表（TLB，Translation Lookaside Buffer）又称联想寄存器，是一个高速、容量很小的相联存储器，存放最近访问过的部分页表项（页号→页框号的映射）。引入快表后，地址变换时先并行地在快表中查找页号：命中（通常命中率可达90%以上）则直接得到页框号，只需访问一次内存；未命中则查内存中的页表，并将该页表项装入快表，然后访问内存取数据。

有效访问时间（EAT）的计算：设访问一次内存的时间为t，快表查找时间为ε（通常远小于t），快表命中率为α，则 EAT = ε + α×t + (1-α)×(ε+t) + t。其中最后一项t是实际访问数据的时间。例如t=100ns、ε=20ns、α=80%，则 EAT = 20+0.8×100+0.2×(20+100)+100 = 244ns；若α=98%，则 EAT = 20+0.98×100+0.02×120+100 = 222.4ns，可见快表命中率对系统性能影响很大。

多级页表：当逻辑地址空间很大时，页表本身可能占用大量连续内存。解决方案是采用两级甚至多级页表——将页表再分页，用外层页表记录内层页表的位置，实现页表离散存放。例如逻辑地址被划分为"一级页号+二级页号+页内偏移"。段页式、多级页表的地址变换会访问多次内存，因此需要快表配合。**重点：**快表的作用与命中过程；有效访问时间EAT的计算公式。**难点：**EAT计算中的各项含义（快表查找、取页表项、取数据、未命中时访问内存的次数）；多级页表地址划分与访问内存次数分析。

## 分段存储管理
<!-- sec: ch3-s4 -->

### 分段存储管理的基本概念与地址变换
<!-- kp: ch3-s4-kp1 emphasis: key -->

分段存储管理按照程序的逻辑结构将地址空间划分为若干段，如主程序段、子程序段、数据段、栈段等，每段有独立的逻辑含义，段内地址连续、段间地址不要求连续。逻辑地址由（段号S, 段内偏移W）两部分组成。系统为每个进程建立一张段表，段表项记录每段的段长和该段在内存中的起始地址（基址）。

分段地址变换过程：①由逻辑地址拆出段号S与段内偏移W；②利用段表基址寄存器找到段表，将段号S与段长比较，若S越界则产生越界中断（分段保护）；③查段表项得到该段的基址与段长，若W>=段长则产生越界中断（段内越界）；④物理地址 = 段基址 + 段内偏移W。

分段的优点：符合程序的逻辑结构，便于共享（段是信息的逻辑单位，可以按段共享）、便于保护（每个段可设置访问权限）、便于动态链接与动态增长（如数据段、栈段可增长）。缺点是段长不固定，分配内存时易产生外部碎片。**重点：**分段地址空间的划分与地址变换过程；分段便于共享与保护的原因。**难点：**分段地址变换中的两次越界检查（段号越界与段内偏移越界）；段长可变导致的外部碎片问题。

### 分段与分页的比较
<!-- kp: ch3-s4-kp2 -->

分段与分页的根本区别在于：分页是系统为了管理内存而人为划分的，页面大小固定、对用户不可见，页是物理单位；分段是按程序的逻辑结构划分的，段长可变、对用户可见，段是逻辑单位。因此分页便于内存管理（无外部碎片），分段便于程序组织（共享、保护、动态链接）。

| 比较项 | 分页 | 分段 |
| ---- | ---- | ---- |
| 划分单位 | 页（物理单位，大小固定） | 段（逻辑单位，大小可变） |
| 划分依据 | 系统按固定大小划分 | 程序逻辑结构划分 |
| 地址空间 | 一维（线性） | 二维（段号+段内偏移） |
| 碎片 | 内部碎片 | 外部碎片 |
| 共享/保护 | 按页共享困难 | 按段共享、保护方便 |
| 动态增长 | 不支持 | 支持（如数据段、栈段） |

分段共享：若一段代码（如可重入代码、公共子程序）需要被多个进程共享，只需让各进程段表中相应段指向同一物理段即可，共享段必须可重入且不允许修改。分段保护：通过段表中每段的访问控制信息（读、写、执行权限）和越界检查实现。**重点：**分页与分段的区别对照表。**难点：**理解"分页对用户透明、分段对用户可见"以及由此带来的共享与保护能力的差异。

## 段页式存储管理
<!-- sec: ch3-s5 -->

### 段页式的概念与逻辑地址结构
<!-- kp: ch3-s5-kp1 -->

段页式存储管理结合了分段与分页的优点：先按逻辑结构将程序分段（便于共享与保护），再将每个段按固定大小分成若干页（便于内存管理、消除外部碎片）。内存按页划分页框，段长必须是页大小的整数倍（不足时段内最后一页会有内碎片）。

段页式系统的逻辑地址由三部分组成：段号S + 页号P + 页内偏移W。系统需要两张表：段表（每个段对应一个段表项，记录该段页表在内存中的起始地址与页表长度）和页表（每个段有自己的页表，记录该段各页对应的页框号）。每个进程一个段表基址寄存器（STBR）指向其段表。

**重点：**段页式的逻辑地址结构（段号+页号+页内偏移）以及段表与页表的配合。**难点：**理解段页式"分段在用户角度、分页在系统角度"的双重特性——段对用户可见便于共享保护，页对系统可见便于分配管理。

### 段页式的地址变换过程
<!-- kp: ch3-s5-kp2 emphasis: difficult -->

段页式的地址变换过程：①由逻辑地址拆出段号S、页号P与页内偏移W；②用段表基址寄存器找到段表，检查段号S是否越界；③查段表项得到该段页表的起始地址；④用页号P查页表，检查页号是否越界；⑤取出页表项中的页框号F；⑥物理地址 = 页框号F × 页面大小 + 页内偏移W。

由于段表、页表都存放在内存中，若不使用快表，一次数据访问需要三次访问内存：第一次访问内存查段表（得到页表地址），第二次访问内存查页表（得到页框号），第三次访问内存真正取数据。因此在段页式系统中快表（TLB）尤为重要——命中时可将三次内存访问降为一次。

| 管理方式 | 逻辑地址结构 | 基本地址变换访问内存次数（无快表） | 碎片 |
| ---- | ---- | ---- | ---- |
| 分页 | 页号+页内偏移 | 2次 | 内部碎片 |
| 分段 | 段号+段内偏移 | 2次 | 外部碎片 |
| 段页式 | 段号+页号+页内偏移 | 3次 | 内部碎片 |

**重点：**段页式地址变换的完整流程与无快表时的三次内存访问。**难点：**对比三种管理方式在地址结构、访问内存次数、碎片类型上的差异，并能结合快表分析有效访问时间。

## 虚拟内存与页面置换
<!-- sec: ch3-s6 -->

### 虚拟内存的概念与局部性原理
<!-- kp: ch3-s6-kp1 emphasis: key -->

虚拟内存（Virtual Memory）是指具有请求调入功能和置换功能、能从逻辑上对内存容量加以扩充的一种存储系统。其逻辑容量由内存容量与外存容量之和决定，运行速度接近内存而成本接近外存。虚拟内存具有三个特征：多次性（作业无需一次性全部装入内存，可分多次调入）、对换性（作业运行时无需常驻内存，可换入换出）和虚拟性（逻辑上扩充内存容量）。

虚拟内存实现的理论基础是局部性原理：时间局部性（程序刚访问过的数据很可能在不久的将来再次被访问，如循环体）和空间局部性（程序访问过的数据附近的存储单元很可能很快被访问，如数组顺序遍历、顺序执行的代码）。由于局部性原理，程序在一段时间内只需装入部分页面就能运行，其余页面在需要时才调入。

虚拟内存的实现必须基于离散分配方式（分页/分段/段页式），因为程序需要部分装入内存、且允许在内存中移动。若使用连续分配，部分装入与换入换出几乎无法高效实现。**重点：**虚拟内存的定义与三个特征；时间局部性与空间局部性的含义。**难点：**理解"局部性原理是虚拟内存可行的基础"，以及虚拟内存与连续分配不兼容的原因。

### 请求分页机制
<!-- kp: ch3-s6-kp2 emphasis: key -->

请求分页是虚拟内存最常见的实现方式，在基本分页的基础上增加了请求调入和页面置换功能。请求分页的页表项比基本分页多了几个字段：状态位（该页是否已调入内存）、访问字段（记录最近访问情况，供置换算法使用）、修改位（该页在内存期间是否被修改过，决定换出时是否写回外存）和外存地址（该页在外存中的存放位置）。

缺页中断的过程：当进程访问的页面不在内存时（状态位为0），硬件产生缺页中断，陷入内核；操作系统检查访问是否合法（越界则终止进程），合法则在外存中查找该页，若内存有空闲页框则直接调入，否则按置换算法选择一个页面换出（被修改过的页要先写回外存）；更新页表与快表，重新执行被中断的指令。

请求分页需要处理两个相关策略：调页策略（请求调页——缺页时才调入；预调页——预测即将访问的页面提前调入）与置换策略。页面调入过程涉及磁盘I/O，速度慢，因此缺页率直接决定虚拟内存系统的性能，页面置换算法的优劣至关重要。**重点：**请求分页页表项的状态位、修改位等字段的含义；缺页中断的处理流程。**难点：**理解"缺页中断是内中断（异常）"，且处理结束后要重新执行导致缺页的那条指令。

### 页面置换算法（FIFO/LRU/CLOCK/改进CLOCK）、抖动与工作集
<!-- kp: ch3-s6-kp3 emphasis: difficult -->

页面置换算法用于在缺页且内存无空闲页框时选择换出的页面。常用算法：

- 先进先出（FIFO）：淘汰最先进入内存的页面。实现简单，但可能淘汰经常访问的页面；存在Belady异常（分配页框数增多时缺页次数反而增加）。
- 最佳置换（OPT）：淘汰以后最长时间不被访问的页面，缺页率最低，但无法预知未来，只作为衡量其他算法优劣的理论标准。
- 最近最久未使用（LRU）：淘汰最长时间没有被访问的页面。性能接近OPT，无Belady异常，但需要硬件支持（访问字段、寄存器或栈）记录访问历史，实现开销大。
- 时钟置换（CLOCK，又称NRU）：为每页设置访问位，淘汰时从指针位置开始循环扫描，访问位为0则淘汰，为1则置0后继续；一轮未淘汰则再扫描一轮。实现简单、开销小。
- 改进型CLOCK：除访问位A外增加修改位M，优先淘汰(A=0,M=0)，其次(A=0,M=1)，再次(A=1,M=0)，最后(A=1,M=1)，减少被修改页的写回开销。

计算实例：页面走向为 7,0,1,2,0,3,0,4,2,3,0,3,2,1,2,0,1,7,0,1，页框数（物理块数）为3，求缺页次数。

LRU置换过程（初始三个页框全空）：
- 访问7：缺页，装入，内存{7}；
- 访问0：缺页，装入，内存{7,0}；
- 访问1：缺页，装入，内存{7,0,1}；
- 访问2：缺页，LRU淘汰7，内存{0,1,2}；
- 访问0：命中，内存{0,1,2}；
- 访问3：缺页，LRU淘汰1，内存{0,2,3}；
- 访问0：命中；
- 访问4：缺页，LRU淘汰2，内存{0,3,4}；
- 访问2：缺页，LRU淘汰0，内存{3,4,2}；
- 访问3：命中；
- 访问0：缺页，LRU淘汰4，内存{3,2,0}；
- 访问3：命中；
- 访问2：命中；
- 访问1：缺页，LRU淘汰3，内存{2,0,1}；
- 访问2：命中；
- 访问0：命中；
- 访问1：命中；
- 访问7：缺页，LRU淘汰2，内存{0,1,7}；
- 访问0：命中；
- 访问1：命中。

共缺页12次。同样的走向在页框数为4时LRU缺页次数为10次，而FIFO在页框数从3增到4时缺页次数从15增到16（Belady异常）。**重点：**FIFO、LRU、CLOCK的置换过程与Belady异常；LRU与OPT的比较。**难点：**手工模拟页面置换过程时严格按"淘汰策略+访问历史"推进，特别注意LRU按"最久未被访问"而非"最早进入"淘汰；改进型CLOCK按(访问位,修改位)的四级优先级淘汰。

抖动（thrashing）与工作集：抖动是指页面频繁地在内存与外存之间换进换出，使得系统大部分时间用于页面置换，CPU利用率反而急剧下降的现象。产生抖动的原因是分配给进程的物理块太少，进程频繁缺页。预防抖动的策略包括：采用局部置换策略、引入工作集（工作集W(t,Δ)指进程在时间间隔Δ内实际访问的页面集合）、设置缺页率上限/下限动态调整物理块数（缺页率过高则增分配、过低则减分配）、控制多道程序度。**难点：**工作集与驻留集的区别——工作集是进程在一段时间内真正用到的页面集合，驻留集是系统实际分配给进程的页框集合；工作集模型通过保证"驻留集≥工作集"来避免抖动。
`;export{e as default};