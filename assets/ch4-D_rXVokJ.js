var e=`---
id: ch4
title: 指令系统
order: 4
---

## 指令格式
<!-- sec: s4-1 -->

### 指令的基本格式与操作码
<!-- kp: kp4-1-1 emphasis: key -->

指令是计算机执行的最小操作单位，由**操作码（OP）**与**地址码（A）**组成：操作码指明操作类型（加、传送、转移等），地址码指明操作数的存放位置。指令的一般格式为：

$$\\text{指令} = \\text{操作码} + \\text{操作数地址}_1 + \\text{操作数地址}_2 + \\cdots$$

按地址码个数，指令分为：**零地址指令**（如停机、堆栈指令，操作数隐含在栈顶）、**一地址指令**（如 $INC\\,A$，单操作数）、**二地址指令**（如 $ADD\\,A,B$，$A \\leftarrow A + B$，第一地址同时作目的地址）、**三地址指令**（如 $ADD\\,A,B,C$，$C \\leftarrow A+B$）、**四地址指令**（额外给出下一条指令地址，一般不常用）。地址码个数越多，指令一次能覆盖的操作数越多、功能越强，但指令字长增加、所需访存（取操作数）次数也增多。

**指令字长**可以是定长（所有指令等长，如RISC固定32位，利于流水线取指译码）或变长（操作码与地址码个数可变，代码密度高但控制复杂）。指令长度与机器字长的关系：单字长指令、半字长指令、双字长指令。408常考：根据指令字长与地址字段分配，判断指令条数、操作数范围与格式是否可行。

**重点：** 二地址指令 $A \\leftarrow A \\bigtriangleup B$（目的地址即第一地址，会覆盖）；地址码个数与指令功能、字长、访存次数的关系。

### 定长操作码与扩展操作码
<!-- kp: kp4-1-2 emphasis: difficult -->

**定长操作码**：所有指令的操作码位数固定，如操作码固定8位可表示 $2^8 = 256$ 条指令。优点是译码简单、指令执行时间均匀；缺点是"停机"这类简单指令也占用相同长度，**代码密度低**。**扩展操作码（变长操作码）**：操作码位数不固定，随指令中地址码的个数而变化，设计原则是"**地址码个数多时用短操作码，地址码个数少时用长操作码**"，从而在有限指令字长内容纳更多指令并提高代码密度。

计算示例：设指令字长16位、地址码字段4位。三地址指令操作码为4位，最多 $2^4 = 16$ 条；若只设计15条，则剩余一个编码（如 $1111$）用作扩展标志；二地址指令操作码为8位（4位扩展标志＋4位），最多可设计 $1 \\times 2^4 = 16$ 条，可设计15条再留一个编码扩展一地址指令；一地址指令操作码12位可设计15条……依此类推。约束条件：

$$15 + 15 + 15 + 16 = 61 \\text{ 条指令}$$

是经典的"扩展操作码设计"结果。核心思路是每扩展一级操作码，上一级就要少用一个编码（少 $2^k$ 条指令，$k$ 为地址码位数）。408常考扩展操作码的"最多指令条数"与编码分配。

**难点：** 扩展操作码每多一位，代价是上一级少一个编码；计算时逐级"扣减"，如 三地址15条 + 二地址15条 + 一地址15条 + 零地址16条 = 61条。

### 指令字长与指令格式设计
<!-- kp: kp4-1-3 emphasis: key -->

指令字长等于操作码位数加上各地址码字段位数之和，可能与机器字长、存储字长不同。设计指令格式需要确定：**指令条数**（决定操作码位数，$\\lceil \\log_2 N \\rceil$）、**寻址方式个数**（决定寻址特征字段位数，$\\lceil \\log_2 M \\rceil$）、**地址码个数与操作数位数**、是否支持操作码扩展。指令按字长分为定长指令字（RISC）与变长指令字（CISC）；指令字长通常为存储字长的整数倍，方便取指。

设计例题：机器字长16位，要求支持64条指令、8种寻址方式，设计双地址指令。操作码位数 $= \\lceil \\log_2 64 \\rceil = 6$ 位，寻址特征位数 $= \\lceil \\log_2 8 \\rceil = 3$ 位，则两个地址字段合计 $16 - 6 - 3 = 7$ 位，每个地址字段仅3.5位，无法容纳合理的地址范围——说明格式不可行，需调整（如增加指令字长、减少寻址方式或改用变长指令）。这类"格式可行性判断"是统考高频考点：**操作码＋寻址特征＋各地址字段的位数之和必须不超过指令字长**。

**重点：** 字段位数取上整 $\\lceil \\log_2 N \\rceil$；校验"操作码＋寻址特征＋地址码"是否超出指令字长；操作码位数由指令条数决定、地址字段位数决定寻址范围。

## 寻址方式
<!-- sec: s4-2 -->

### 指令寻址与数据寻址概述
<!-- kp: kp4-2-1 emphasis: key -->

寻址方式分**指令寻址**（确定下一条指令的地址）与**数据寻址**（确定操作数的地址）。**指令寻址**有**顺序寻址**——下一条指令地址 $=$ 当前指令地址 $+1$，由程序计数器PC自动"加1"实现（"1"为一条指令的长度）；**跳跃寻址**——由转移/跳转类指令把目标地址装入PC实现。二者是理解"程序自动执行"的关键。

**数据寻址**是408的重点：指令地址码字段给出的地址是**形式地址（A）**，经一定变换得到**有效地址（EA）**，再按有效地址访问操作数：

$$EA = f(A, \\cdots)$$

按有效地址的计算方式，数据寻址分为**立即、直接、间接、寄存器、寄存器间接、变址、基址、相对、堆栈**等9种。指令中通常设置**寻址特征字段**来标识采用哪种寻址方式。常见题型：给出指令内容与寻址方式，求操作数的真值（特别注意间接寻址时指令中存放的是地址而非数据）。

**重点：** 指令寻址中PC实现顺序寻址、转移指令实现跳跃寻址；数据寻址先求有效地址EA再取操作数（立即寻址例外——操作数就在指令中）。

### 立即、直接、间接、寄存器与寄存器间接寻址
<!-- kp: kp4-2-2 emphasis: key -->

**立即寻址**：地址字段A本身就是操作数（立即数），如 $ADD\\,R_1,\\#5$。取指时同时得到操作数，**无需访存**，速度最快；缺点是操作数范围受字段位数限制，且立即数是指令的一部分，不能随程序运行改变。**直接寻址**：$EA = A$，操作数在主存单元A中，需访存1次；可访问的范围受地址字段长度限制。

**间接寻址**：指令地址字段给出的是操作数地址的**存放位置**，$EA = (A)$（先读出A单元的内容作为有效地址），取操作数需**访存2次**；通过一次间接或多级间接可以扩大寻址范围（地址可放在字长长的单元中）。**寄存器寻址**：$EA = R_i$，操作数在寄存器 $R_i$ 中，**无需访存**、速度快，但寄存器数量有限。**寄存器间接寻址**：$EA = (R_i)$，操作数的**地址**在寄存器中，操作数在主存，需访存1次——兼顾了寄存器的速度和内存寻址范围的广度。各方式访存次数对比是常考选择题。

**重点：** 访存次数：立即0次、直接1次、间接2次、寄存器0次、寄存器间接1次；间接寻址用"地址的地址"扩大寻址范围；寄存器间接兼得速度与范围。

### 变址、基址、相对与堆栈寻址
<!-- kp: kp4-2-3 emphasis: key -->

三种"寄存器＋偏移"寻址公式形式相同，但用途截然不同：**变址寻址**：$EA = (R_{变址}) + A$，变址寄存器内容为偏移、形式地址A为基值，适合**访问数组元素**——循环中修改变址寄存器即可遍历数组。**基址寻址**：$EA = (R_{基址}) + A$，基址寄存器内容为基准地址、A为偏移，用于**程序重定位**（把程序中的逻辑地址转换为物理地址，支持多道程序设计），基址值由操作系统设置。**相对寻址**：$EA = (PC) + A$，以**程序计数器PC**为基准、A为相对偏移量（补码，可正可负），用于**转移指令**，使程序代码可整体搬移（浮动）而转移关系不变。

三者区别速记：变址寻址"**寄存器内容可变、A固定**"；基址寻址"**寄存器内容固定、A可变**"；相对寻址"基准是PC"。相对寻址偏移量若为8位补码，范围为 $[-128, +127]$。**堆栈寻址**：操作数来自栈顶，由**堆栈指针SP**指示，指令为**零地址**形式，进栈/出栈自动修改SP，用于过程调用与参数传递（CALL时PC压栈、RET时弹栈回PC）。

**重点：** 变址→数组遍历、基址→程序重定位、相对→转移指令（基准为PC）；三公式都是"寄存器＋偏移"，关键在基准与可变项；堆栈寻址隐含使用SP。

## CISC与RISC
<!-- sec: s4-3 -->

### CISC的特点
<!-- kp: kp4-3-1 emphasis: key -->

**CISC（Complex Instruction Set Computer，复杂指令集计算机）**的指令系统庞大而复杂，以x86为代表。其特点：① 指令数量多、功能复杂，支持大量寻址方式（8种以上）与**变长指令**；② 指令长度不固定、格式多样，译码与控制复杂；③ 允许较多指令**直接访问主存**（运算可针对内存操作数）；④ 常采用**微程序控制器**（便于实现复杂指令）；⑤ 一条复杂指令可对应多个微操作、执行周期不统一；⑥ 对编译器要求较低。CISC的设计思想是"用单条复杂指令完成较多工作"，以减少指令条数、提高代码密度。

CISC的优缺点：向后兼容性好（同一架构可运行旧程序）、指令密度高（变长指令节省存储空间）；缺点是：指令执行时间不统一、控制器实现复杂、指令格式不规整导致**难以流水线化**与指令级并行。考试常考CISC与RISC在指令条数、寻址方式、指令格式、控制器实现、访存指令、寄存器数量等方面的差异。

**重点：** CISC：指令多而复杂、变长指令、寻址方式多、微程序控制、允许访存运算、难以流水线。

### RISC的特点
<!-- kp: kp4-3-2 emphasis: key -->

**RISC（Reduced Instruction Set Computer，精简指令集计算机）**的核心思想是"精简指令、优化流水线"，以ARM、MIPS、RISC-V为代表。其特点：① 指令数量少、功能简单，只有**加载/存储（Load/Store）指令能访问主存**，其余指令只能对寄存器操作；② **指令长度固定**（如32位）、格式规整，利于译码与流水线；③ **寻址方式少**（通常仅2~3种）；④ **通用寄存器数量多**（通常32个以上），减少访存次数；⑤ 采用**硬布线控制器**，控制逻辑简单、速度快；⑥ 大部分指令能在**一个时钟周期**内完成，易于实现流水线、超标量与多发射。

RISC的优点：指令规整使硬件实现简单、时钟频率高、易于指令级并行，能获得高的性能/功耗比；缺点：指令功能弱，完成复杂功能需要更多条指令（代码密度较低），对**编译器**的要求高（需做指令调度等优化）。现代处理器中RISC与CISC技术已逐渐融合（如x86在内部把复杂指令翻译成类似RISC的微操作，ARM也扩展了复杂指令），但两者概念对比仍是408常考内容。

**重点：** RISC：指令少而简单、定长指令、Load/Store访存、寄存器多、硬布线控制、单周期完成、利于流水线；对编译器要求高。

### CISC与RISC的对比
<!-- kp: kp4-3-3 emphasis: key -->

两者的核心对比：

| 对比项 | CISC | RISC |
|---|---|---|
| 指令系统 | 复杂、庞大、指令条数多 | 精简、规整、指令条数少 |
| 指令字长 | 不固定（变长） | 固定（定长） |
| 可访存指令 | 较多指令可直接访存 | 仅Load/Store指令访存 |
| 寻址方式 | 多（8种以上） | 少（2~3种） |
| 控制器 | 以微程序控制为主 | 以硬布线控制为主 |
| 通用寄存器 | 数量较少 | 数量多 |
| 指令执行周期 | 不统一 | 大多单周期完成 |
| 流水线 | 难以实现 | 容易实现（RISC的设计目标） |
| 对编译器要求 | 较低 | 较高 |

记忆技巧："RISC的R代表Regular（规整）"——定长指令、规整格式、少寻址方式、Load/Store结构、多寄存器、单周期执行、硬布线控制、流水线友好。考试常以表格或判断题形式考查，最显著的区分点是"访存指令"与"控制器实现方式"两项。

**重点：** 最显著区别：RISC仅Load/Store访存、定长指令、多寄存器、硬布线控制；CISC变长指令、多寻址方式、微程序控制、允许访存运算。

## 指令系统综合计算
<!-- sec: s4-4 -->

### 指令格式设计计算
<!-- kp: kp4-4-1 emphasis: difficult -->

指令格式设计综合题通常给出"指令字长、操作码位数、寻址方式数、地址码个数、寄存器数量"等条件，要求计算：操作码位数 $= \\lceil \\log_2(指令条数) \\rceil$；寻址特征位数 $= \\lceil \\log_2(寻址方式数) \\rceil$；寄存器编号位数 $= \\lceil \\log_2(寄存器数) \\rceil$；各地址字段位数由剩余位数分配，并验证不等式：

$$\\text{操作码位数} + \\text{寻址特征位数} + \\sum \\text{地址字段位数} \\le \\text{指令字长}$$

典型题：指令字长16位、操作码4位、二地址指令，寻址方式4种（需2位寻址特征）。则每个地址字段 $= (16 - 4 - 2) / 2 = 5$ 位，可寻址 $2^5 = 32$ 个主存单元。若改用寄存器寻址（寄存器编号4位），则可访问 $2^4 = 16$ 个寄存器；若采用"寄存器间接"，$EA = (R_i)$，仍能访问主存。核心对应关系：**$n$ 位地址字段最多区分 $2^n$ 个不同对象**（主存单元、寄存器或寻址目标）。此类题还常与"某指令格式是否可行"结合——字段位数之和超过字长则不可行，需调整设计。

**难点：** 操作码位数对应"指令条数"、地址字段位数对应"寻址空间"；做可行性判断时先算各位数之和再与字长比较；注意寻址特征位是否单独设置。

### 寻址范围与指令系统综合
<!-- kp: kp4-4-2 emphasis: key -->

不同寻址方式的**可寻址范围**：直接寻址 $2^n$ 个字（$n$ 位地址字段）；间接寻址的范围取决于存储字长（地址存放在主存单元中，可达整个主存空间）；寄存器寻址 $2^k$ 个寄存器；相对寻址以PC为基准，偏移量8位补码时范围为 $[-128, +127]$。**指令寻址**中PC自动"加1"的1指一条指令的长度（按字编址时加1个字，按字节编址时加指令字节数）。

综合题示例：机器字长与存储字长均为16位、按字编址，指令字长16位，转移指令采用相对寻址，偏移量8位补码。设转移指令地址为 $100_H$，则取指完成后 $PC = 100_H + 1 = 101_H$；若偏移量为 $-5$（补码 $0\\text{x}FB$），则转移目标地址 $= 101_H + 0\\text{x}FB = 9C_H$。**易错点：相对寻址的基准是"取指完成后的PC值"，而不是转移指令本身的地址**，这在按字编址与按字节编址两种情况下都要正确计算PC的增量。

**难点：** 相对寻址以取指后的PC为基准（$PC_{目标} = PC_{取指后} + 偏移$）；偏移量为补码（可负）；按字节编址时PC增量 = 指令字节数，按字编址时PC增量 = 指令字数。
`;export{e as default};