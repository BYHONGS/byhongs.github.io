var e=`---
id: ch7
title: 查找
order: 7
---

## 查找的基本概念
<!-- sec: ch7-s1 -->

### 查找与平均查找长度 ASL
<!-- kp: ch7-s1-k1 emphasis: key -->

## 基本概念

**查找（Search）**是在数据集合（查找表）中找出满足条件的数据元素的过程。查找表分为**静态查找表**（只查找，不改变表）与**动态查找表**（查找过程中同时插入、删除）。

**平均查找长度（ASL, Average Search Length）**是衡量查找算法效率的指标，定义为所有查找过程进行关键字比较次数的**期望值**：

$$ASL = \\sum_{i=1}^{n} P_i \\cdot C_i$$

其中 n 为查找表中元素个数，$P_i$ 是查找第 i 个元素的概率（通常假定**等概率**，$P_i = 1/n$），$C_i$ 是找到第 i 个元素所需比较的次数。查找不成功时的比较次数也要计入（即查找失败时的 ASL）。

## 分析时要注意

- 在等概率假设下，$ASL_{成功}$ 反映查找成功时的平均比较次数，$ASL_{失败}$ 反映查找失败时的平均比较次数，两者要分开讨论；
- ASL 越小，查找效率越高；不同存储结构下同一算法效率可能不同；
- 顺序表、有序顺序表、二叉排序树等结构在不同查找算法下的 ASL 分析是 408 计算题的主要来源。

**重点：**ASL 公式中"比较次数"是核心——每个算法都要先搞清"比较了什么、几次"。查找失败 ASL 在选择题与解答题中都频繁出现，勿只算成功情况。

### 顺序查找
<!-- kp: ch7-s1-k2 emphasis: key -->

## 顺序查找

**顺序查找**适用于顺序表与链表，从头到尾依次扫描比较。

\`\`\`c
typedef struct {
    ElemType *elem;   // 0 号单元留作"哨兵"
    int TableLen;     // 表长
} SSTable;

int Search_Seq(SSTable ST, ElemType key) {
    ST.elem[0] = key;               // 哨兵，减少越界判断
    for (int i = ST.TableLen; ST.elem[i] != key; --i)
        ;                           // 从后往前查找
    return i;                       // 返回 0 表示查找失败
}
\`\`\`

哨兵技巧：把关键字放在 0 号位置，循环不再需要判断"i 是否越界"，减少了约一半的比较次数（仅在 n 很大时才有意义）。

## ASL 分析

- **成功时**：查找第 i 个元素需比较 n-i+1 次（从表尾 i=n 起算），等概率下：

$$ASL_{成功} = \\frac{1}{n}\\sum_{i=1}^{n}(n-i+1) = \\frac{n+1}{2}$$

- **失败时**：需比较 **n+1** 次（含与哨兵的最后一次比较）；
- 时间复杂度为 **O(n)**。若表中元素按查找概率降序排列（概率大的在前），则 ASL 最小，即"最优顺序查找"应把高频元素放前面。

**重点：**顺序查找的 $ASL_{成功}=(n+1)/2$、$ASL_{失败}=n+1$ 是必背结论；对**有序表**做顺序查找失败时只需比较 n 次（不越界即可确定），这是与无序表的区别。

## 折半查找与分块查找
<!-- sec: ch7-s2 -->

### 折半查找
<!-- kp: ch7-s2-k1 emphasis: key -->

## 折半查找

**折半查找（二分查找）**只适用于**有序的顺序表**。每次取中间位置元素与关键字比较，缩小一半查找范围。

\`\`\`c
int Binary_Search(SSTable L, ElemType key) {
    int low = 0, high = L.TableLen - 1, mid;
    while (low <= high) {
        mid = (low + high) / 2;         // 取下取整
        if (L.elem[mid] == key) return mid;
        else if (L.elem[mid] > key) high = mid - 1;
        else low = mid + 1;
    }
    return -1;                          // 查找失败
}
\`\`\`

**注意**：循环条件是 low <= high；当 low > high 时查找失败。折半查找要求**顺序存储**（需要随机存取），链表上无法高效实现。

## 折半查找判定树与 ASL

折半查找的过程可以用一棵**二叉判定树**描述，其中每个结点代表一个 mid 位置。判定树满足：右子树结点数要么等于左子树，要么比左子树多 0 或 1（即中序有序）；判定树的深度为 $\\lfloor \\log_2 n \\rfloor + 1$。

$$ASL_{成功} = \\frac{1}{n}\\sum_{i=1}^{n} l_i \\approx \\log_2(n+1) - 1$$

其中 $l_i$ 为第 i 个元素在判定树中的深度。时间复杂度 **O(log n)**，且**查找失败**时比较次数不超过 $\\lceil \\log_2(n+1) \\rceil$ 次。

## 二分查找与顺序查找比较

- 顺序查找：O(n)，不要求有序，适合小规模或链表；
- 折半查找：O(log n)，要求有序顺序表，适合静态、频繁查找的大表；
- 折半查找的插入删除代价高（有序表移动元素），若需要频繁增删则不适合。

**重点：**折半查找的"中间位置取整规则"（下取整）、判定树的形态与结点深度，以及 $ASL \\approx \\log_2(n+1)-1$ 都是常考点。

### 分块查找
<!-- kp: ch7-s2-k2 -->

## 分块查找的思想

**分块查找（索引顺序查找）**综合顺序查找与折半查找：将表分成若干块，块内元素**无序**，但**块间有序**（前一块的最大关键字小于后一块的最小关键字）。建立索引表，索引表每一项记录每块的**最大关键字**与块的起始位置。

查找分两步：先在**索引表**中确定关键字所在块（索引表有序，可用折半查找），再在**块内**用顺序查找。

## ASL 分析

设表长为 n，均匀分成 b 块，每块含 s 个元素（$n = b \\times s$）。索引查找 ASL 为 $ASL_{索引}$，块内查找 ASL 为 $ASL_{块内}$，则：

$$ASL = ASL_{索引} + ASL_{块内} = \\frac{b+1}{2} + \\frac{s+1}{2}$$

若索引表用折半查找则 $ASL \\approx \\log_2(b+1) + \\frac{s+1}{2}$。当 **s 取 $\\sqrt{n}$**（即 b 也约等于 $\\sqrt{n}$）时，ASL 取得最小值，约为 $\\sqrt{n} + 1$。

## 分块查找的适用性

分块查找的特点：块内可无序、块间有序；适合"表本身不断变化但需要较快查找"的场景（动态）。相比折半查找，它允许快速插入（插入到块内末尾），比有序表的整体移动代价低；相比顺序查找，查找效率更高。

**重点：**"索引表有序可折半、块内无序用顺序"是分块查找的本质；最优分块大小为 $\\sqrt{n}$（此时 ASL ≈ $\\sqrt{n}+1$）是最优性结论，常作为计算题考点。

## 二叉排序树 BST
<!-- sec: ch7-s3 -->

### 二叉排序树的定义与查找插入
<!-- kp: ch7-s3-k1 emphasis: key -->

## 定义

**二叉排序树（BST, Binary Search Tree）**又称二叉查找树，是满足以下性质的二叉树（递归定义）：

- 若左子树非空，则左子树上所有结点的值均**小于**根结点的值；
- 若右子树非空，则右子树上所有结点的值均**大于**根结点的值；
- 左、右子树也分别是二叉排序树。

**重要推论**：对 BST 做**中序遍历**，得到的是**递增有序序列**。

## 查找与插入

查找：从根出发，key 小于当前结点则向左，大于则向右，等于则找到；到空结点则查找失败。插入：查找失败的位置即为插入位置（插入新结点必为**叶子**）。

\`\`\`c
BSTNode* BST_Search(BiTree T, ElemType key) {
    while (T != NULL && key != T->data) {
        if (key < T->data) T = T->lchild;
        else T = T->rchild;
    }
    return T;   // 找到返回结点，否则返回 NULL
}

bool BST_Insert(BiTree &T, ElemType key) {
    if (T == NULL) {                      // 找到插入位置
        T = (BiTree)malloc(sizeof(BSTNode));
        T->data = key; T->lchild = T->rchild = NULL;
        return true;
    } else if (key == T->data) {
        return false;                     // 关键字已存在，插入失败
    } else if (key < T->data) {
        return BST_Insert(T->lchild, key);
    } else {
        return BST_Insert(T->rchild, key);
    }
}
\`\`\`

**建立**：依次插入各关键字即可。不同插入次序会得到**不同形态**的 BST（同一关键字集合）。查找/插入的时间复杂度取决于树的高度：平均情况 O(log n)，最坏情况（退化成单支树，如按有序序列插入）为 **O(n)**。

**重点：**BST 中序有序是判断"一棵二叉树是否为 BST"的手段；查找的最坏情况 O(n) 说明普通 BST 不保证平衡，这正是 AVL 引入的原因。

### 二叉排序树的删除与查找效率
<!-- kp: ch7-s3-k2 emphasis: key -->

## 删除操作

删除 BST 中结点 z 分三种情况：

1. **z 是叶子结点**：直接删除；
2. **z 只有一棵子树**：删除 z，用其子树顶替 z 的位置；
3. **z 有两棵子树**：用 z 的**中序直接前驱**（z 左子树中最右下的结点）或**中序直接后继**（z 右子树中最左下的结点）替换 z 的值，然后删除该前驱/后继结点（它至多有一棵子树，退化为情况 1 或 2）。

**要点**：被替换结点必为叶子或只有单子树，故总能转化为前两种情况；删除后 BST 中序仍然有序。

## 查找效率与 ASL

查找成功时的 ASL 等于**各结点深度之和除以结点数**（等概率）。树越矮，ASL 越小：

- 理想情况（平衡形态）：$ASL = O(\\log_2 n)$；
- 最坏情况（退化为单支树）：$ASL = \\frac{n+1}{2} = O(n)$。

**例**：关键字 1,2,3,4,5 按顺序插入得到右单支树，查找第 5 个元素需比较 5 次，$ASL_{成功} = \\frac{1+2+3+4+5}{5} = 3$；若按 3,1,2,4,5（较平衡）插入，树高小、ASL 小。**结论**：关键字集合相同但插入顺序不同，BST 形态与查找效率差别很大。

## 与折半查找比较

相同关键字集合的 BST 与折半查找判定树：若 BST 形态恰好是"平衡"的，两者查找过程类似；但折半查找的判定树是唯一确定的（表固定），而 BST 形态取决于插入顺序，因此 BST 的查找效率**不稳定**。

**重点：**两子树的删除（前驱替换法）是重点题型；"中序遍历有序"与"树高决定效率"是 BST 两条主线。

## 平衡二叉树 AVL
<!-- sec: ch7-s4 -->

### 平衡二叉树的定义
<!-- kp: ch7-s4-k1 emphasis: key -->

## 平衡二叉树的概念

**平衡二叉树（AVL 树）**是特殊的二叉排序树：任意结点的**平衡因子（平衡度）**的绝对值不超过 1。**平衡因子**定义为：

$$\\text{平衡因子} = \\text{左子树高度} - \\text{右子树高度}$$

AVL 树中每个结点的平衡因子只能取 **-1、0、1**。结点插入后若某结点平衡因子绝对值大于 1，则需要**旋转调整**使其重新平衡。

## 平衡二叉树的性质

- AVL 树是"高度平衡"的 BST，其查找、插入、删除的时间复杂度均为 **O(log n)**；
- 高度为 h 的 AVL 树最少结点数满足递推：$N_h = N_{h-1} + N_{h-2} + 1$（$N_0 = 0, N_1 = 1$），类似于斐波那契数列。由此可得：含 n 个结点的 AVL 树的最大高度约为 $\\log_{\\phi}(\\sqrt{5} n)$，其中 $\\phi = \\frac{1+\\sqrt{5}}{2}$；
- 平衡调整只可能发生在插入/删除路径上的结点。

## 最小不平衡子树

当插入新结点导致失衡时，从**新插入结点向上找**第一个平衡因子绝对值大于 1 的结点 A，以 A 为根的子树称为**最小不平衡子树**。只需调整这棵子树即可使整棵树恢复平衡，因为调整后子树高度与插入前相同，不会向上传播失衡。

**重点：**"平衡因子 = 左高 - 右高"的符号约定必须记清；找最小不平衡子树是调整的第一步，树高公式 $N_h = N_{h-1}+N_{h-2}+1$ 用于计算最少结点数。

### 四种旋转调整
<!-- kp: ch7-s4-k2 emphasis: difficult -->

## LL、RR、LR、RL 四种失衡

设新插入结点为 x，最小不平衡子树的根为 A：

1. **LL（左左）**：x 插入在 **A 的左孩子的左子树**，A 的平衡因子变为 2。需**右单旋转**：以 A 的左孩子 B 为轴，A 右旋为 B 的右子树，B 的原右子树变为 A 的左子树；
2. **RR（右右）**：x 插入在 **A 的右孩子的右子树**，A 的平衡因子变为 -2。需**左单旋转**：以 A 的右孩子 B 为轴，A 左旋为 B 的左子树，B 的原左子树变为 A 的右子树；
3. **LR（左右）**：x 插入在 **A 的左孩子的右子树**。需**先左旋后右旋**：先对 A 的左孩子做左旋（变成 LL），再对 A 做右旋；
4. **RL（右左）**：x 插入在 **A 的右孩子的左子树**。需**先右旋后左旋**：先对 A 的右孩子做右旋（变成 RR），再对 A 做左旋。

## LL 右单旋转示例

\`\`\`c
// 对结点 A 做右单旋转，返回新子树根 B
AVLNode* RotateRight(AVLNode *A) {
    AVLNode *B = A->lchild;
    A->lchild = B->rchild;   // B 的右子树过继给 A 作左子树
    B->rchild = A;           // A 成为 B 的右孩子
    // 更新 A、B 的平衡因子
    return B;
}
\`\`\`

## 判断与口诀

判断步骤：①画出插入后失衡的树；②找最小不平衡子树根 A；③判断新结点相对 A 的位置（左左/右右/左右/右左）；④选相应旋转。口诀："**LL 右旋、RR 左旋、LR 先左后右、RL 先右后左**"。

**重要性质**：AVL 树插入后只需一次（或两次）旋转即可恢复平衡；但**删除**结点后可能需多次旋转（失衡沿路径向上传播）。

**难点：**旋转后必须**重新计算相关结点的平衡因子**，且旋转时子树的"过继"（如 LL 中 B 的右子树给 A 作左子树）容易画错；建议多画几棵树练习。

## 红黑树
<!-- sec: ch7-s5 -->

### 红黑树的定义与性质
<!-- kp: ch7-s5-k1 emphasis: key -->

## 红黑树的概念

**红黑树**是一种"近似平衡"的二叉排序树，每个结点有红、黑两种颜色，通过颜色约束保证树高不超过 $2\\log_2(n+1)$，从而保证查找、插入、删除的最坏时间复杂度均为 O(log n)。

红黑树的五条性质（满足全部才是红黑树）：

1. 每个结点要么是红色，要么是黑色；
2. **根结点是黑色**；
3. **叶子结点（外部结点，即 NULL 结点）是黑色**；
4. 红色结点的**两个子结点都是黑色**（即"红结点的孩子不能是红色"，不能出现连续的红色结点）；
5. **从任一结点到其每个叶子（外部结点）的所有路径上，黑色结点的数目相同**（称为黑高相等）。

## 黑高

**黑高（bh）**：从某结点出发（不含该结点）到达任一叶子结点的路径上黑色结点的个数。红黑树性质保证：任意结点的左右子树高度差不超过 2 倍（黑高相等 + 红结点不连续）。

**推论**：含 n 个内部结点的红黑树，其高度 $h \\le 2\\log_2(n+1)$；根的黑高至少为 $\\lceil h/2 \\rceil$。

## 与 AVL 的比较

| 对比项 | AVL 树 | 红黑树 |
| --- | --- | --- |
| 平衡标准 | 平衡因子 | 黑高约束 |
| 树高 | 更矮（严格平衡） | 略高（≈2log n） |
| 查找效率 | 略优 | 略逊 |
| 插入/删除 | 可能多次旋转 | 至多几次旋转+变色 |
| 适用 | 查找多、增删少 | 增删频繁（如 Map/Set 实现） |

**重点：**五条性质中"红结点孩子必黑"与"黑高相等"是判断一棵 BST 是否为红黑树的关键；红黑树与 AVL 的对比是选择题常见题材。

### 红黑树的插入与删除
<!-- kp: ch7-s5-k2 -->

## 插入

插入新结点默认为**红色**（若为黑色会破坏性质5的黑高相等，难以调整）。插入后可能破坏性质 4（红结点孩子为红）或性质 2（根为黑）。调整手段有两种：**变色**与**旋转**。

插入流程简述：先按 BST 规则插入红结点；若其双亲是黑色则无需调整；若双亲是红色，则看其**叔父结点**的颜色——

- **叔父为红**：双亲与叔父变黑、祖父变红，将祖父作为新"当前结点"继续向上调整（可能上溢到根，根最后置黑）；
- **叔父为黑**：根据新结点相对祖父的位置进行旋转（类似 AVL 的 LL/RR/LR/RL），旋转后局部变"红黑-黑"结构。

由于黑色结点"压"在红结点两侧，红黑树的插入调整次数有限，总体 O(log n)。

## 删除

删除结点后可能破坏性质 4 或 5。删除的是**红色叶子**最简单（直接删，不影响黑高）；删除**黑色**结点会破坏黑高相等，需通过"双黑"（该路径上少了一个黑色）机制借位调整：把双黑结点当作一个虚拟结点，通过旋转与变色向兄弟"借黑"，直到平衡。红黑树删除比 AVL 删除的旋转次数更少、更稳定。

## 408 的考查方式

408 对红黑树的考查集中在**选择题**：判断给定树是否满足红黑树性质、黑高的计算、红黑树与其他树结构的对比等，一般**不要求**手写插入删除的完整旋转过程，但需要掌握插入后的基本调整思路与复杂度结论。

**重点：**红黑树与 AVL、BST 的复杂度对比，以及"红黑树高度上界 $2\\log_2(n+1)$"是最常考的定量结论。

## B树与B+树
<!-- sec: ch7-s6 -->

### B树的定义与性质
<!-- kp: ch7-s6-k1 emphasis: difficult -->

## B 树的定义

**B 树（B-tree）**又称多路平衡查找树，适合**磁盘等外存**的查找。一棵 **m 阶 B 树**满足（m 为最大子树棵数）：

1. 每个结点最多有 **m 棵子树**（即最多 m-1 个关键字）；
2. 若根结点不是叶子，则至少有两棵子树；
3. 除根外所有非叶结点至少有 **$\\lceil m/2 \\rceil$ 棵子树**（即至少 $\\lceil m/2 \\rceil - 1$ 个关键字）；
4. 所有叶子结点（外部结点，查找失败结点）出现在**同一层次**上，且不带信息（可看作外部结点）；
5. 所有非叶结点包含关键字 $k_1 < k_2 < \\cdots < k_n$（升序）及 n+1 个指针 $p_0, p_1, \\ldots, p_n$，其中 $p_i$ 指向的子树中所有关键字落在 $k_i$ 与 $k_{i+1}$ 之间。

## 核心结论

- 含 n 个关键字的 m 阶 B 树，树高满足 $h \\le \\log_{\\lceil m/2 \\rceil}\\frac{n+1}{2} + 1$，且一般 $h \\le \\lceil \\log_m(n+1) \\rceil + 1$；
- B 树每个结点存放多个关键字，**降低树高**，从而减少磁盘 I/O 次数；
- 非叶结点中的关键字起**划分区间**的作用，实际数据记录在叶子结点？——不，B 树中**所有结点（含非叶结点）都存放关键字**（查找可以在任意层结束），这与 B+ 树不同；
- 关键字的个数范围（非根非叶）：$$\\lceil m/2 \\rceil - 1 \\le n \\le m - 1$$。

**难点：**m 阶的"阶"指最大子树棵数，而非最大关键字数；"叶子结点不带信息、都在同一层"是 B 树均衡性的关键保证。

### B树的插入删除与B+树
<!-- kp: ch7-s6-k2 emphasis: key -->

## B 树的插入

插入关键字的规则：先在叶子层定位插入位置，若插入后结点关键字个数**超过 m-1**（即等于 m），则发生**分裂（上溢）**：取中间位置 $\\lceil m/2 \\rceil$ 的关键字上升插入到双亲结点，左右两部分成为两个新结点；若双亲也超限则继续分裂上溢，直到根结点分裂（树高加 1）。

**例**（5 阶 B 树，结点最多 4 个关键字）：向结点中插入第 5 个关键字时分裂，中间关键字上升到双亲。

## B 树的删除

删除分两种：

- 删除的是**叶子层**关键字：直接删除，若关键字数少于 $\\lceil m/2 \\rceil - 1$ 则需"**借**"（向兄弟借关键字，通过双亲周转）或"**合并**"（与兄弟合并，中间关键字下移）；
- 删除的是**非叶层**关键字：用其**直接前驱或直接后继**（其前驱子树中最大或后继子树中最小的关键字）替代，转化为删除叶子层关键字。

删除导致结点关键字不足时，向下合并可能引起连锁反应直至根。

## B+ 树

B+ 树是 B 树的变体，用于数据库索引：

1. 非叶结点只起**索引作用**（仅存关键字与指针，不存数据记录）；
2. **所有数据记录（或指向记录的指针）都保存在叶子结点**，且叶子结点通过指针**链接成一个有序链表**；
3. 每个叶子结点存放的关键字数等于子树个数（$\\lceil m/2 \\rceil \\le n \\le m$）；
4. 支持**顺序查找**（遍历叶子链表）与**随机查找**（从根沿索引下钻）两种方式。

## B 树与 B+ 树对比

| 对比项 | B 树 | B+ 树 |
| --- | --- | --- |
| 关键字存放 | 所有结点都存 | 仅叶子结点存记录 |
| 非叶结点 | 起索引+存储作用 | 只做索引 |
| 叶子结点 | 不带信息 | 含全部记录且链成有序表 |
| 查找 | 可能在任何层结束 | 必须到叶子层 |
| 支持顺序遍历 | 不支持 | 支持 |

**重点：**B/B+ 树的插入分裂与删除合并机制、关键字数量上下界是计算题核心；"B+ 树适合数据库索引、B 树适合文件索引"是最常考的结论。

## 散列表
<!-- sec: ch7-s7 -->

### 散列函数与冲突处理
<!-- kp: ch7-s7-k1 emphasis: key -->

## 散列表与散列函数

**散列表（Hash Table）**根据关键字**直接计算**存储地址：$$addr = H(key)$$，其中 H 为**散列函数（哈希函数）**。理想情况下查找时间与表长无关，为 O(1)。

常见的散列函数：

1. **除留余数法**：$$H(key) = key \\bmod p$$，p 通常取**不大于表长 m 的最大质数**；
2. **直接定址法**：$$H(key) = a \\cdot key + b$$，适合关键字分布均匀；
3. **数字分析法**：取关键字中分布较均匀的若干位；
4. **平方取中法**：关键字平方后取中间几位。

## 冲突与处理

**冲突**是指两个不同的关键字经散列函数得到**相同地址**。冲突不可避免（除非散列函数为完全映射），只能尽量减少。处理冲突的方法：

**① 开放定址法**（所有地址对任何关键字开放）：

- **线性探测法**：$$H_i = (H(key) + d_i) \\bmod m$$，$d_i = 0, 1, 2, \\ldots$。易产生**堆积（聚集）**现象——同义词连续占据地址，降低效率；
- **平方探测法（二次探测）**：$d_i = 0^2, 1^2, -1^2, 2^2, -2^2, \\ldots$，可避免堆积，但只能探测表长一半以内的位置；
- **再散列法（双散列）**：$d_i = i \\times H_2(key)$；
- **伪随机序列法**：$d_i$ 取伪随机数。

**② 拉链法（链地址法）**：把散列地址相同的记录存放在同一个单链表中。查找时先定位链表头再顺序扫描该链表，**不会堆积**，删除方便。

## 装填因子

$$alpha = \\frac{\\text{表中记录数 } n}{\\text{散列表长度 } m}$$

装填因子反映散列表的**填装程度**。α 越大，冲突概率越高、查找效率越低；线性探测下查找效率与 α 密切相关（$ASL$ 随 α 增大而增大），拉链法下平均链长 = α。

**重点：**除留余数法的 p 取质数、线性探测的"堆积"现象、拉链法不堆积，是散列相关的核心考点。

### 散列表的查找与 ASL 计算
<!-- kp: ch7-s7-k2 emphasis: difficult -->

## 查找过程与 ASL

散列表查找仍遵循"比较关键字"的统计口径：**成功时的比较次数 = 探测位置次数**。计算 ASL 时必须区分成功与失败：

- **ASL 成功**：对每个关键字，统计从散列地址出发（含冲突探测）直到找到该关键字所需的比较次数，求和后除以关键字个数；
- **ASL 失败**：对每个可能的散列地址（0 ~ m-1），统计从该地址出发直到"遇到空位"（探测停止）所需的比较次数，求和后除以散列表长度 m。

## 计算示例

设散列表长 m=13，除留余数法 $H(key) = key \\bmod 13$，线性探测。依次插入关键字 26, 41, 25, 05, 07, 15, 12, 49, 51, 31。

- H(26)=0，H(41)=2，H(25)=12，H(5)=5，H(7)=7，H(15)=2（冲突，探测 3），H(12)=12（冲突，探测 0），H(49)=10，H(51)=12（冲突，依次探测 0,1 处已满，2? 2 已占，再探测 3 已占，4 空 → 比较 5 次），H(31)=5（冲突，探测 6 空 → 比较 2 次）。

逐个统计各关键字成功比较次数，求和后除以 10 即 $ASL_{成功}$。失败时需对地址 0~12 每个位置模拟"探测到空位"的次数再除以 13。

## 影响 ASL 的因素

1. **散列函数**：分布越均匀，冲突越少，ASL 越低；
2. **处理冲突的方法**：相同 α 下，拉链法的 ASL 通常低于线性探测法（无堆积）；
3. **装填因子 α**：α 越小，ASL 越低，但空间浪费越大。理想情况下散列表的查找时间复杂度为 **O(1)**。

**难点：**ASL 失败的计算要"从每个地址出发探测直到空位"，对线性探测尤其容易数错次数；务必区分成功（除以 n）与失败（除以 m）两种统计口径。

### 散列查找性能的定量分析（ASL公式）
<!-- kp: ch7-s7-k3 emphasis: key -->

当散列函数均匀分布时，ASL只与**装填因子 $\\alpha$**（$\\alpha = \\frac{\\text{表中记录数 } n}{\\text{表长 } m}$）有关。**必须记住的近似公式**（选择题直接套用）：

| 冲突处理方法 | $ASL_{成功}$ | $ASL_{失败}$ |
|---|---|---|
| 线性探测 | $\\frac{1}{2}\\left(1 + \\frac{1}{1-\\alpha}\\right)$ | $\\frac{1}{2}\\left(1 + \\frac{1}{(1-\\alpha)^2}\\right)$ |
| 拉链法 | $1 + \\frac{\\alpha}{2}$ | $\\alpha + e^{-\\alpha}$ |

（拉链法失败时还要加上一次"指针为空"的比较，若题目按比较次数统计需加1。）从公式可看出：$\\alpha$ 越大 ASL 越大；同样的 $\\alpha$ 下拉链法优于线性探测（无堆积现象）。对比记忆：**线性探测**适合 $\\alpha<0.5$ 左右；**拉链法**可容忍 $\\alpha$ 接近1，且删除结点方便（只需删链上结点）。

**难点：**公式前提是散列函数均匀；用公式前先算 $\\alpha = n/m$；考试可能同时要求"手工模拟+公式验证"，模拟结果应近似等于公式值。
`;export{e as default};