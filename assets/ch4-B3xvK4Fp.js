var e=`---
id: ch4
title: 串
order: 4
---

## 串的定义与基本操作
<!-- sec: ch4-s1 -->

### 串的定义与相关术语
<!-- kp: ch4-s1-k1 emphasis: key -->

## 串的定义

**串（String）**是由零个或多个字符组成的有限序列，一般记为：

$$S = 'a_1 a_2 \\cdots a_n' \\quad (n \\ge 0)$$

其中 S 是串名，单引号括起来的字符序列是串的值，n 称为串的**长度**。n=0 时称为**空串**。串与线性表的联系在于：串是数据元素为单个字符的特殊线性表，但串的操作对象往往是"串整体"或"子串"而不是单个字符，因此串有自己独立的基本操作集合。

## 相关术语

- **子串**：串中任意个**连续**字符组成的子序列称为该串的**子串**，包含子串的串称为**主串**。注意子串必须是"连续"的，这是串与线性表"子序列"的重要区别；
- **位置**：某个字符在串中的序号称为该字符在串中的**位置**（从 1 开始计数），子串在主串中**首次出现**时其第一个字符的位置称为子串在**主串中的位置**；
- **空串与空格串**：空串是长度为 0 的串，不含任何字符；空格串是只含空格字符（' '）的串，长度为空格个数，两者完全不同；
- **子序列与子串**：子串必须连续，而子序列允许跳过部分字符，如 "ac" 是串 "abc" 的子序列但不是子串。

**重点：**"子串必须连续""位置从 1 开始计数""空串 ≠ 空格串"是串的概念题三大高频辨析点。

### 串的基本操作
<!-- kp: ch4-s1-k2 -->

## 基本操作集合

\`\`\`c
StrAssign(&T, chars)   // 赋值：把 chars 赋给串 T
StrCopy(&T, S)         // 复制：由 S 复制得 T
StrEmpty(S)            // 判空
StrCompare(S, T)       // 比较：S>T 返回正值，S<T 返回负值，相等返回 0
StrLength(S)           // 求串长
SubString(&Sub, S, pos, len)  // 求子串：从 S 第 pos 个字符起长度为 len
Index(S, T)            // 定位：返回 T 在 S 中首次出现的位置，无则 0
Concat(&T, S1, S2)     // 连接
\`\`\`

## 操作要点

- **串比较按字典序**：先比较第一个字符，若相等再比较第二个字符，依次类推；两个串完全相等当且仅当长度相等且每个对应字符都相同；
- 以 "&" 引用参数传参的操作（赋值、复制、求子串、连接、定位）会修改或产生新串；
- **StrCompare 与 StrLength 是基础**：判断串相等需要先比较长度，长度不同则直接不等；"求子串"和"定位"两个操作组合起来可以求"子串在主串中的位置"，这一组合正是模式匹配的雏形；
- **Concat 与 SubString** 均要求长度参数合法，越界时出错。

**重点：**串的基本操作是理解"模式匹配（Index）"的铺垫；408 中常要求根据基本操作组合实现更复杂的功能，如判断子串关系、统计字符出现次数等。

## 串的存储结构
<!-- sec: ch4-s2 -->

### 定长顺序存储与堆分配存储
<!-- kp: ch4-s2-k1 -->

## 定长顺序存储

**定长顺序存储**用一组**地址连续**的存储单元存储串的字符序列，即把串当作数组存储。串长可以有两种表示方式：①设置独立的长度变量 length；②在串尾用特殊字符（如 C 语言的 "\\0"）结束标记，此时串长隐式确定。

定长顺序存储的**优点**：支持**随机存取**，存取任意位置的字符均为 O(1)，且存储密度高；**缺点**：串长度固定，一旦预先分配的长度不够，插入、连接等操作就可能发生**截断（溢出）**。因此定长顺序存储适合串长基本确定的场景。

## 堆分配存储

**堆分配存储（动态分配存储）**用动态分配的一维字符数组存储串（如 C 语言的 malloc/realloc），串长可以在运行期动态增长，突破定长顺序存储的长度限制。

\`\`\`c
typedef struct {
    char *ch;      // 按串长分配存储区
    int length;    // 串长
} HString;
\`\`\`

堆分配存储的**优点**：灵活，不存在截断问题；**缺点**：需要动态管理内存（分配与释放），且若频繁改变串长会带来 realloc 复制的开销。

## 字符集编码

串中字符的编码方式（ASCII、Unicode、GBK 等）影响串的比较规则与存储大小。408 中一般默认采用 ASCII 编码，一个字符占一个字节，比较时按字符的码值进行。

**重点：**"定长顺序存储可能截断"与"堆分配存储动态增长"的对比是选择题常考点；两种方式都属于顺序存储，支持随机存取。

### 链式存储与存储密度
<!-- kp: ch4-s2-k2 -->

## 链式存储

**链式存储**用链表存储串：每个结点可存 1 个或多个字符，结点包含数据域与指向下一结点的指针域。与线性表的链式存储一致，链式串**不支持随机存取**，访问第 i 个字符需从头遍历（O(n)）。

按每个结点存储的字符个数不同，链式串分为两类：

- **结点大小为 1**：每个结点只存 1 个字符。指针域开销大，存储密度低，且结点数多、管理复杂；
- **结点大小大于 1**：每个结点存多个字符。存储密度提高、结点数减少，但插入、删除、求子串等操作因要跨结点处理而变复杂。

## 存储密度

**存储密度**定义为：

$$\\text{存储密度} = \\frac{\\text{串值所占存储单元}}{\\text{实际分配的存储单元}}$$

结点大小越大，存储密度越高，但操作复杂度上升；结点大小越小，存储密度越低，但操作更灵活。链式串的**最大缺点**是随机存取困难，无法像顺序存储那样在 O(1) 时间内取到任意位置的字符，因此链式串在实际应用中较少使用。

**重点：**存储密度公式与"结点越大密度越高、操作越复杂"的权衡关系是常考点；链式串因随机存取困难而很少用于需要频繁按位取字符的场景。

## 朴素模式匹配
<!-- sec: ch4-s3 -->

### 朴素模式匹配算法
<!-- kp: ch4-s3-k1 emphasis: key -->

## 算法思想

**朴素模式匹配（Brute-Force）**从主串 S 的第 1 个字符起，与模式串 T 的第一个字符比较：若相等则继续比较后续字符；若中途某字符不相等，则将主串指针回溯到本次比较起点**后移一位**的位置，模式串指针回到开头，重新开始比较。该过程重复直到主串结束或找到匹配。

\`\`\`c
int Index(SString S, SString T) {
    // 约定 S[0] 与 T[0] 存放串长
    int i = 1, j = 1;
    while (i <= S[0] && j <= T[0]) {
        if (S[i] == T[j]) { i++; j++; }       // 继续比较后继字符
        else { i = i - j + 2; j = 1; }        // 主串指针回退后移一位，模式串从头
    }
    if (j > T[0]) return i - T[0];            // 匹配成功，返回首字符位置
    else return 0;                            // 匹配失败
}
\`\`\`

## 算法分析

**最坏情况**：主串形如 $\\underbrace{aaa\\cdots a}_{n}$，模式串形如 $aaa\\cdots ab$（前 m-1 个字符匹配、最后一个不匹配），则每轮比较 m 次、共比较 n-m+1 轮，总比较次数为：

$$(n-m+1)\\times m = O(n \\times m)$$

**最好情况**：第一轮第一个字符就失配，每轮只比较 1 次，时间复杂度为 O(n)（n 为主串长度）。

**重点：**朴素匹配的时间复杂度在最坏情况下为 O(nm)，最好情况为 O(n)，平均约为 O(n+m)（实际略大）。主串指针"回溯"是朴素算法的根本缺陷，正是 KMP 算法要解决的问题。

### 朴素匹配的改进思路
<!-- kp: ch4-s3-k2 -->

## 回溯带来的浪费

朴素匹配在失配时主串指针 i 回溯到本轮起点 +1，模式串指针 j 回到 1，这意味着**大量已经比较过的字符会被重复比较**。观察一个事实：若在匹配过程中已经知道模式串前缀 T[1..j-1] 与主串某段 S[i-j+1..i-1] 完全相等，则当 S[i] 与 T[j] 失配时，模式串完全可以**向右滑动**到一个合适的位置，而不必回溯主串指针。

## 改进方向

设失配发生在模式串第 j 个字符，此时已知 $$T[1..j-1] = S[i-j+1..i-1]$$。若模式串存在某个真前缀 $$T[1..k]$$ 与真后缀 $$T[j-k..j-1]$$ 相等（$$k<j$$），则可以直接把模式串滑到第 k+1 个字符继续与 S[i] 比较，主串指针 i **不需要回溯**。即只需找到模式串自身的**最长相等前后缀**，就能确定滑动量。

这正是 KMP 算法的核心思想：**失配时利用模式串自身的结构信息决定滑动距离，主串指针永不回溯**。滑动距离仅与模式串有关，可以预处理成 next 数组。KMP 算法将匹配过程的时间复杂度从 O(nm) 降到 O(n+m)。

**重点：**理解"主串指针不回溯"是掌握 KMP 的前提。下节将给出 next 数组的推导公式。

## KMP算法
<!-- sec: ch4-s4 -->

### next数组的推导
<!-- kp: ch4-s4-k1 emphasis: key -->

## next 数组的定义

设模式串 $$T = t_1 t_2 \\cdots t_m$$，对每个位置 j 定义 next[j]：当 $$t_j$$ 与主串失配时，模式串应滑动到使 $$t_{next[j]}$$ 与主串当前字符继续比较。定义如下：

$$next[j] = \\begin{cases} 0, & j = 1 \\\\ \\max\\{k \\mid 1 \\le k < j \\text{ 且 } t_1t_2\\cdots t_{k-1} = t_{j-k+1}\\cdots t_{j-1}\\}, & \\text{存在这样的 } k \\\\ 1, & \\text{其他情况} \\end{cases}$$

即 next[j] 等于 **T[1..j-1] 的最长相等前后缀长度加 1**；若无相等前后缀则 next[j]=1；规定 next[1]=0。

## 推导实例

模式串 T = "ababa"（下标从 1 开始）：

- next[1] = 0（规定）；
- next[2]：子串 "a" 无相等前后缀，next[2] = 1；
- next[3]：子串 "ab" 的最长相等前后缀长度为 0，next[3] = 1；
- next[4]：子串 "aba"，最长相等前后缀为 "a"（长 1），next[4] = 1 + 1 = 2；
- next[5]：子串 "abab"，最长相等前后缀为 "ab"（长 2），next[5] = 2 + 1 = 3。

故 $$next = [0, 1, 1, 2, 3]$$。当第 5 个字符失配时，模式串滑到使第 3 个字符与主串当前字符比较。

## 手工递推法

求 next[j+1] 时，只需考察 next[j] 是否为候选：若 $$t_j = t_{next[j]}$$，则 $$next[j+1] = next[j] + 1$$；否则继续考察 next[next[j]]，以此类推，直到找到相等或到达 next[1]=0。

**难点：**next 数组求的是"子串 T[1..j-1]"的自身信息，与主串无关；求"最长相等前后缀"时必须取**真前缀与真后缀**（不能是整个子串本身）。

### KMP匹配过程与复杂度
<!-- kp: ch4-s4-k2 emphasis: key -->

## KMP 匹配算法

主串指针 i 与模式串指针 j 从 1 开始。若 S[i] == T[j] 则 i、j 同时后移；若失配且 j>1，则 **j = next[j]**，i 不动，继续比较；若失配且 j=1（next[1]=0），则 i 后移一位，j 保持 1。

\`\`\`c
int KMP(SString S, SString T, int next[]) {
    int i = 1, j = 1;
    while (i <= S[0] && j <= T[0]) {
        if (j == 0 || S[i] == T[j]) {   // j=0 表示第一个字符就失配
            i++; j++;
        } else {
            j = next[j];                // 模式串右滑，主串指针不回溯
        }
    }
    if (j > T[0]) return i - T[0];      // 匹配成功
    else return 0;
}
\`\`\`

## 求 next 数组的代码

\`\`\`c
void get_next(SString T, int next[]) {
    int i = 1, j = 0;
    next[1] = 0;
    while (i < T[0]) {
        if (j == 0 || T[i] == T[j]) {
            i++; j++;
            next[i] = j;
        } else {
            j = next[j];
        }
    }
}
\`\`\`

## 复杂度分析

KMP 匹配过程中主串指针 i **从不回溯**，每个字符至多被比较一次，故匹配部分时间复杂度为 O(n)；求 next 数组过程中 j 每次要么加 1 要么通过 next 回退，总体为 O(m)。因此：

$$\\text{KMP 算法总时间复杂度} = O(n + m)$$

空间复杂度 O(m)（next 数组）。

**重点：**KMP 的匹配时间复杂度是 O(n+m)（线性），远优于朴素匹配的 O(nm)；主串指针永不回溯是 KMP 的核心特征。

### nextval数组的改进
<!-- kp: ch4-s4-k3 emphasis: key -->

## 为什么需要 nextval

当 $$t_{next[j]} = t_j$$ 时，用 next[j] 滑动后仍然会与主串中的同一个字符失配，造成一次无效比较。例如模式串 "aaaaab"，若第 5 个 'a' 失配，next[5]=4，滑动后第 4 个 'a' 仍会与同一主串字符失配，白白多比较一次。改进方法：若 $$t_j = t_{next[j]}$$，则令 nextval[j] = nextval[next[j]]，跳过这一层重复。

## nextval 的计算公式

$$nextval[j] = \\begin{cases} next[j], & t_j \\ne t_{next[j]} \\\\ nextval[next[j]], & t_j = t_{next[j]} \\end{cases} \\quad (j > 1)$$

且约定 nextval[1] = 0。

## 推导实例

模式串 T = "ababa"，已知 next = [0,1,1,2,3]：

- nextval[1] = 0；
- j=2，t_2='b'，next[2]=1，t_1='a'，t_2≠t_1，故 nextval[2] = next[2] = 1；
- j=3，t_3='a'，next[3]=1，t_3 = t_1，故 nextval[3] = nextval[1] = 0；
- j=4，t_4='b'，next[4]=2，t_4 = t_2，故 nextval[4] = nextval[2] = 1；
- j=5，t_5='a'，next[5]=3，t_5 = t_3，故 nextval[5] = nextval[3] = 0。

故 $$nextval = [0, 1, 0, 1, 0]$$。

**改进效果**：对于大量重复字符的模式串（如 "aaaaab"），nextval 能显著减少失配后的无效比较，最坏情况下仍为 O(n+m)。

**难点：**nextval 是"对 next 的修正"，求 nextval 时必须引用**已经算好的 nextval 值**（递归引用），而非 next 值；408 真题常要求同时手推 next 与 nextval，务必多练几组模式串。
`;export{e as default};