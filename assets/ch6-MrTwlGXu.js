var e=`---
id: ch6
title: 总线
order: 6
---

## 总线概述
<!-- sec: s6-1 -->

### 总线的概念与分类
<!-- kp: kp6-1-1 emphasis: key -->

**总线**是连接计算机各部件（CPU、主存、I/O设备）的一组共享传输线。按传输信息的功能分为三类：**数据总线**——双向，传输数据信息，宽度（位数）决定一次传输的数据量；**地址总线**——单向（由主设备CPU发出），宽度决定可直接寻址的范围（$2^{地址线数}$）；**控制总线**——传输控制、时序与状态信号（读写命令、中断请求、总线请求、总线允许等），其中有些为单向、有些为双向。总线按**层次**可分为：片内总线（CPU内部寄存器与ALU之间）、系统总线（连接CPU、主存与I/O接口，即数据/地址/控制总线）、I/O总线（连接I/O接口与外部设备，如PCI、PCIe）与通信总线（计算机与外部设备或计算机之间，如USB、网络）。

按信息传送方式，总线又分为**并行总线**（多位同时传送，如传统PCI，速度快但线多、抗干扰差、距离短）与**串行总线**（逐位传送，如USB、PCIe、网络，线少、成本低、可高频长距离传输）。总线的特性包括：物理特性（引脚数目与排列）、功能特性（各线功能定义）、电气特性（电平与负载）、时间特性（信号的时序关系）。考试常考三类系统总线的方向性与宽度决定的因素。

**重点：** 地址总线单向（CPU发出）、数据总线双向；寻址空间 $= 2^{地址线数}$；并行总线速度快但线多、串行总线线少但需高频。

### 总线性能指标与计算
<!-- kp: kp6-1-2 emphasis: key -->

总线的主要性能指标：**总线宽度**（数据线的位数，如32位/64位）、**总线频率**（工作时钟频率）、**数据传输率（带宽）**。带宽的计算公式为：

$$\\text{带宽} = \\frac{\\text{总线宽度（位）}}{8} \\times f \\quad (B/s)$$

示例：64位总线、频率200MHz，带宽 $= 64/8 \\times 200 \\times 10^6 = 1600$ MB/s = 1.6GB/s。相关概念：**总线周期**（一次完整总线操作的时间，可能是若干个时钟周期）、**总线复用**（如地址与数据分时复用同一组线，减少引脚数）、**猝发（突发）传送**（一次地址之后连续传送多个数据，减少地址阶段开销，提高有效带宽）。

常见计算题型：① 给定宽度与频率求带宽；② 猝发方式下传输一批数据的时间 = 地址阶段时间 + 数据阶段时间；③ 一个总线周期包含 $k$ 个时钟周期时，等效带宽 $= \\frac{\\text{每周期数据量}}{k \\times T_{时钟}}$。注意单位换算（位与字节、MHz与Hz、MB与GB）。

**重点：** 带宽 $= \\frac{\\text{宽度}}{8} \\times f$；猝发传送只传一次地址、连续传多个数据，吞吐率更高；地址/数据复用可减少引脚。

## 总线仲裁
<!-- sec: s6-2 -->

### 集中式仲裁：链式/计数器/独立请求
<!-- kp: kp6-2-1 emphasis: key -->

当多个主设备（CPU、DMA控制器等）同时请求使用总线时，需要**总线仲裁**确定使用权。**集中式仲裁**由一个中央仲裁器统一管理，有三种方式：

1. **链式查询（菊花链）**：所有设备共享**总线请求BR**与**总线忙BS**信号，"总线允许BG"信号从仲裁器串行经过各设备，BG传到哪个设备、该设备即获得总线。特点：结构简单、易于扩充（设备可串联在链上）；但**优先级固定**（靠近仲裁器的设备优先级高）、链路中某设备故障会影响其后所有设备。

2. **计数器定时查询**：仲裁器用计数器提供设备号，轮询各设备是否请求总线（请求信号共享，设备通过设备号应答）。优先级可通过设置计数器**初值**改变：计数器从0开始则设备号小者优先；计数到某设备停止则其优先级最高。特点：优先级**可变**、对链路故障不敏感，但轮询需要时间、速度较慢。

3. **独立请求方式**：每个设备有独立的**请求线BRi**与**允许线BGi**，仲裁器用排队电路并行判断优先级。特点：响应最快（各请求并行判断）、优先级可编程设置；缺点是控制线数量随设备数线性增长。

**重点：** 链式查询优先级固定且依赖链路；计数器查询优先级可通过初值改变；独立请求响应最快、控制线最多（$2n+1$ 根）。

### 分布式仲裁
<!-- kp: kp6-2-2 -->

**分布式仲裁**没有中央仲裁器，总线使用权由各设备**自行竞争**决定。典型实现：每个主设备拥有唯一的优先级编码，通过共享的"总线忙"线与仲裁线（每位代表一个优先级，采用**线与**逻辑）竞争——设备把自己的优先级编码驱动到仲裁线上并与线上的值比较，若自己的编码更高则胜出，最终编码最高的设备获得总线。常见的有"自举分布式仲裁"（按优先级编码竞争）与"冲突检测"方式（CSMA/CD，如以太网：先听后发、发生冲突后随机退避重试）。

分布式仲裁的优缺点：可靠性高（无单点故障）、扩展性好（优先级通过编码位数灵活扩展）、无需集中仲裁器；缺点：竞争/检测逻辑较复杂，重负载下效率可能下降。考试常要求对比集中式与分布式仲裁，理解"优先级编码竞争"与"总线忙"信号的机制，以及以太网CSMA/CD作为分布式总线访问方式的要点。

**重点：** 分布式仲裁无中央仲裁器，各设备自行竞争（编码比较）；以太网CSMA/CD采用"先听后发、冲突退避"的分布式介质访问控制。

## 总线定时
<!-- sec: s6-3 -->

### 同步定时方式
<!-- kp: kp6-3-1 emphasis: key -->

**同步定时（同步通信）**：所有总线操作由统一的**公共时钟**同步，主从设备在一个（或几个）时钟周期内完成规定的信息交换。读操作时序：在时钟上升沿，主设备发出地址与读命令，地址与命令保持稳定一段时间（满足从设备的建立时间），在约定的时钟沿由从设备把数据放到总线上，主设备采样。所有设备按同一时钟工作，因此控制简单、实现容易、时序关系明确。

同步通信的特点：① 每个总线周期传送时间固定，适合各设备速度接近的场合；② 时钟周期必须按**最慢设备**设计（要满足最慢设备的响应时间），导致高速设备被拖慢；③ 没有"就绪/等待"信号，从设备必须在一个时钟周期内完成响应，否则出错。改进方法：允许插入**等待周期（Tw）**来延长传输时间。计算示例：总线周期 = 2个时钟周期、时钟10MHz、8位数据线，则带宽 $= \\frac{8}{2 \\times 10^{-7}} = 5$ MB/s（每两个时钟周期传1字节）。

**重点：** 同步定时有统一时钟、按最慢设备设计周期、无就绪信号；控制简单但效率受最慢设备限制；可插入等待周期应对慢速设备。

### 异步定时方式
<!-- kp: kp6-3-2 emphasis: key -->

**异步定时（异步通信）**：不使用统一时钟，通过**握手信号（请求—应答）**协调主从设备，按"请求—应答"方式逐次传输。握手有三种形式：**不互锁**——主设备发出请求后**不等应答**，经过固定时间撤除请求，可靠性差；**半互锁**——主设备发出请求后**等待从设备应答**才撤除请求，但从设备应答后不再等待主设备撤除请求，可靠性居中；**全互锁**——主设备等待从设备应答才撤请求，从设备等待主设备撤请求后才撤应答，双方完全互锁，**最可靠**。

异步通信的优点：可以连接**速度相差很大的设备**，无需全局时钟，按设备实际速度工作、效率高；缺点：握手协议复杂、需要额外的握手信号线、控制较复杂。典型应用：CPU与慢速外设之间、异速系统之间的通信。考试常考三种互锁方式的区别：不互锁最不可靠、半互锁居中、全互锁最可靠。

**重点：** 异步定时靠"请求/应答"握手信号协调；全互锁最可靠、不互锁不可靠；异步适合速度差异大的设备、不需要全局时钟。

### 半同步与分离式定时
<!-- kp: kp6-3-3 emphasis: difficult -->

**半同步定时**是同步与异步的结合：既有统一时钟（基本按同步方式工作），又增加一条**等待（WAIT/READY）**信号线。当从设备来不及响应时，把WAIT信号拉低（无效）；主设备检测到WAIT无效则插入**等待周期（Tw）**，直到WAIT有效（就绪）才继续。这样既保持了同步通信控制简单的优点，又能适应速度较慢的设备。典型时序：$T_1$ 发地址与读命令，$T_2$ 读数据（若WAIT无效则插入若干Tw）。PCI总线即采用类似"同步＋可插入等待周期"的方式。

**分离式（分裂）事务通信**：把一次总线事务拆成两个独立的子阶段——**请求阶段**与**应答阶段**。主设备发出地址与请求后**立即释放总线**（不等待数据），其他设备可以继续使用总线；当从设备准备好数据后，再申请总线把数据传送给主设备。优点：总线在等待期间不被占用，**总线利用率高**，特别适合多处理器共享总线的场合；缺点：控制复杂，需要识别"请求—应答"的对应关系（每个请求需带事务标记）。分离式事务广泛用于高性能总线（如PCIe的虚拟通道机制）。

**难点：** 半同步 = 同步 + WAIT等待线（兼顾速度差异）；分离式事务把"请求"与"数据"分两次占用总线，消除主设备等待期间的浪费，提升利用率。

## 总线标准
<!-- sec: s6-4 -->

### PCI总线
<!-- kp: kp6-4-1 emphasis: key -->

**PCI（Peripheral Component Interconnect，外设互连）总线**是Intel提出的一种**并行、同步**的局部总线，用于连接高速外设（显卡、网卡、声卡）与CPU/主存。主要特点：① 支持32位/64位数据总线，工作频率33MHz/66MHz，传输率可达132MB/s（32位×33MHz）或528MB/s（64位×66MHz）；② **支持猝发（突发）传送**——一次地址后连续传送多个数据；③ 支持**即插即用（PnP）**——通过配置寄存器自动分配资源；④ 采用集中式总线仲裁（独立请求方式）；⑤ 支持**总线主控**（总线主设备，如DMA）；⑥ 支持中断共享；⑦ 采用同步定时并可插入等待周期。PCI的**地址与数据线分时复用**（AD线），以减少引脚数。

PCI通过"桥"（北桥/南桥）与CPU连接：北桥连接高速设备（主存、显卡），南桥连接低速设备（硬盘、USB、PCI插槽）。注意区分"PCI局部总线"与"PCIe"：PCI是并行共享总线、需要仲裁、半双工；PCIe是串行点对点链路。考试常考PCI的特点（并行、同步、猝发、即插即用、总线主控）与带宽计算。

**重点：** PCI带宽 = 宽度 × 频率（32位33MHz → 132MB/s）；支持猝发传送与即插即用；经"桥"连接CPU；地址与数据线复用。

### PCIe与USB总线
<!-- kp: kp6-4-2 emphasis: key -->

**PCIe（PCI Express）**是PCI的**串行**替代标准，采用**点对点高速串行链路**、差分信号传输。每条lane（通道）为1位的双向串行链路，带宽可按 $x1, x2, x4, x8, x16$ 扩展（多条lane并行捆绑）。例如PCIe 3.0 $x1$ 单向约8Gbps（有效约1GB/s），$x16$ 可达约16GB/s。PCIe以**数据包**为单位传输（分组交换），采用**分离式事务**与虚拟通道机制，支持热插拔与即插即用。与PCI（并行共享总线）相比，PCIe（串行点对点）无需仲裁、支持全双工、可扩展性好。

**USB（通用串行总线）**是连接外设（键盘、鼠标、U盘、打印机）的**串行**总线，采用**主从结构**（一个主机控制器管理多个设备），**主机轮询**设备（无总线仲裁需求）。USB支持热插拔、即插即用，可通过集线器（Hub）**级联**扩展，分层星形拓扑，最多可连接127个设备。USB版本：USB 2.0（480Mbps）、USB 3.0（5Gbps）、USB 3.1/3.2（10/20Gbps）、USB4（40Gbps），并支持为设备供电。USB数据传输采用"令牌包＋数据包＋握手包"的帧协议。

**重点：** PCIe为串行点对点、lane捆绑扩展、全双工、分组传输；USB为主从结构、主机轮询、星形拓扑、热插拔、即插即用；二者均非并行共享总线。
`;export{e as default};