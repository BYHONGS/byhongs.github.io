var e=`---
id: ch4
title: 文件管理
order: 4
---

## 文件系统基础
<!-- sec: ch4-s1 -->

### 文件的概念与属性
<!-- kp: ch4-s1-kp1 -->

文件是以计算机硬盘（外存）为载体、存储在计算机上的信息集合，是操作系统管理外存和向用户提供存储、检索信息的基本单位。从用户角度看，文件是信息的逻辑组织单位；从系统角度看，文件由文件控制块（FCB）和实际文件数据组成。文件系统是操作系统中负责管理文件的软件（文件管理模块）以及被管理的文件与数据结构（如目录、文件分配表）的总称。

文件的属性通常包括：文件名（用户标识）、标识符（系统内部唯一标识）、类型（扩展名）、位置（在外存中的物理地址）、大小、创建时间、修改时间、访问权限等。文件按用途可分为系统文件、用户文件、库文件；按数据形式可分为源文件、目标文件、可执行文件；按存取控制属性可分为只执行、只读、读写文件。文件的访问方式有顺序存取（按从前到后的次序逐次访问，主要用于磁带）和随机存取（直接访问任意位置，主要用于磁盘）。

**重点：**文件与文件系统的定义；文件的基本属性。**难点：**区分"文件控制块FCB"与"文件数据"——FCB是文件的管理信息（存放在目录中），文件数据是实际内容，二者共同构成一个文件在外存中的完整表示。

### 文件的逻辑结构与目录结构
<!-- kp: ch4-s1-kp2 emphasis: key -->

文件的逻辑结构是用户可见的文件组织形式，分为两大类：无结构文件（流式文件，文件内部信息按字节顺序排列，无结构单位，如Linux/UNIX中的文件）和有结构文件（记录式文件，由若干条定长或变长记录组成，如数据库文件）。有结构文件又可分为顺序文件（记录按顺序排列，定长记录可按序号随机访问，变长记录只能顺序访问）、索引文件（建立记录号到记录地址的索引表，支持快速随机访问）和索引顺序文件。

文件的目录结构用于对文件进行管理：单级目录结构（所有文件在一个目录下，简单但重名问题严重、不便于分类）、两级目录结构（主目录+用户目录，解决重名与隔离，但不支持用户内分类）、多级目录结构（树形目录，最常用，支持不同用户或子目录，路径名由各级目录名加文件名构成，用""或"/"分隔）。

目录管理的基本操作：搜索、创建、删除、显示、修改文件。文件控制块FCB存放在目录项中，目录项还可以包含文件类型、大小、权限等。当目录项只存放文件名和指向索引结点的指针时（如UNIX的inode），可将FCB的主要信息存放在索引结点中，目录项很小，从而减少目录检索时的磁盘I/O次数。**重点：**文件逻辑结构的分类；树形目录结构与路径（绝对路径、相对路径）。**难点：**理解索引结点机制的作用——把文件属性信息从目录项中分离出来，使目录项变小，加快检索并支持文件共享。

### 文件的共享与保护
<!-- kp: ch4-s1-kp3 -->

文件共享是指多个用户（或多个进程）共同使用同一个文件。硬链接：不同目录项指向同一个索引结点（inode），共享同一文件数据；任何用户对文件的修改对所有人可见，删除一个链接只是减少链接计数，仅当链接计数为0时才真正删除文件。软链接（符号链接）：目录项中存放指向目标文件路径的链接文件，类似Windows的快捷方式；若目标文件被删除，链接失效。硬链接共享文件和软链接共享文件在"主链与链接的关系""删除影响""路径解析"上均有区别，是常考点。

文件保护是指防止未授权用户访问文件。实现方式主要有：口令保护（用户访问文件需口令，简单但口令可能泄露）、加密保护（对文件加密，即使被窃取也无法读出明文，但加解密开销大）、访问控制（为每个文件设置访问控制列表ACL或存取控制矩阵，登记每个用户的访问权限——读、写、执行、删除等）。

| 保护方式 | 实现机制 | 优点 | 缺点 |
| ---- | ---- | ---- | ---- |
| 口令 | 设置/验证口令 | 实现简单、开销小 | 口令易泄露、权限粒度粗 |
| 加密 | 文件加解密 | 安全性高 | 加解密开销大 |
| 访问控制 | ACL/存取矩阵 | 权限粒度细、灵活 | 存储与管理开销大 |

**重点：**硬链接与软链接的区别；三种文件保护方式的比较。**难点：**硬链接通过共享索引结点实现、软链接通过存放路径实现，二者在"是否真正共享文件数据"上有本质区别。

## 文件系统实现
<!-- sec: ch4-s2 -->

### 文件系统的层次结构
<!-- kp: ch4-s2-kp1 -->

文件系统在逻辑上自底向上分为多个层次：①I/O控制层——驱动设备控制器完成实际的读写操作；②基本文件系统层——负责向设备驱动程序发送通用命令（如read、write），管理内存缓冲区与磁盘块；③文件组织模块层——负责文件块的定位，将逻辑块号转换为物理块号；④逻辑文件系统层——管理文件元数据（FCB）、目录结构、文件访问权限与空闲空间管理，向上提供文件操作的接口。

各层之间通过接口衔接，高层向低层发出请求，低层为高层提供服务。文件系统的实现离不开磁盘，外存的物理结构以块（盘块、簇）为单位，一个盘块通常为若干扇区大小（如4KB）。文件系统设计时要考虑：文件如何在外存中存放（物理结构）、目录如何组织、空闲空间如何管理、以及可靠性（如日志、快照）。

**重点：**文件系统四个层次各自的功能。**难点：**理解逻辑文件系统层与文件组织模块层的分工——前者管文件元数据与目录，后者管逻辑块到物理块的映射，两者共同决定文件在外存上的物理结构。

### 目录实现与文件控制块FCB
<!-- kp: ch4-s2-kp2 -->

目录的实现直接决定文件检索的效率。最简单的实现是把每个文件对应一个目录项（含FCB全部信息），目录文件由这些目录项组成。线性表方式：目录项按线性表顺序存储，查找需要顺序扫描，简单但目录大时检索慢（磁盘I/O多）。哈希表方式：根据文件名计算哈希值快速定位目录项，检索速度快，但需要处理冲突。

由于FCB中往往包含文件名、类型、大小、权限、物理地址等较多信息，直接存放在目录项中会使目录项很大。改进方案（如UNIX系统）是把文件属性等管理信息放在索引结点（inode）中，目录项只保存"文件名+inode号"。这样目录项极小，一个盘块可存放大量目录项，检索目录时的磁盘I/O次数显著减少。

文件系统的创建与挂载：创建文件系统时需要初始化超级块（记录整个文件系统的总块数、空闲块信息等）、分配inode表、建立根目录。操作系统在启动时将文件系统"挂载"（mount）到目录树的某个位置后才能使用。**重点：**目录实现的线性表与哈希表方式；索引结点机制。**难点：**比较"目录项含FCB"与"目录项仅含文件名+inode号"两种方案在目录大小与检索效率上的差别。

## 文件实现（物理结构）
<!-- sec: ch4-s3 -->

### 连续分配
<!-- kp: ch4-s3-kp1 -->

连续分配：为文件分配一组连续的磁盘块，文件在目录项中记录起始块号和文件长度（块数）。逻辑上相邻的记录存放在物理相邻的盘块中，因此支持顺序存取和随机存取（由逻辑块号直接计算物理块号：物理块号 = 起始块号 + 逻辑块号）。

连续分配的优点是实现简单、存取速度快（只需一次寻道即可连续读取多个盘块）、支持随机访问。缺点：①难以动态增长（文件增长需要移动其他文件或重新分配，即外存的外部碎片问题，需定期压缩）；②产生外部碎片；③文件长度固定，创建时必须知道文件最终大小。因此连续分配主要用于一次性写入、不再修改的文件（如光盘、只读文件）。

**重点：**连续分配的文件物理结构及其优缺点。**难点：**理解连续分配在磁盘上造成"外部碎片"的原理（与内存连续分配中的外部碎片类似），以及文件无法方便增长的限制。

### 链接分配
<!-- kp: ch4-s3-kp2 -->

链接分配：为文件分配不连续的磁盘块，通过指针将文件的所有盘块链接起来。分为两种方式：
- 隐式链接：每个盘块末尾存放指向下一盘块的指针，目录项中只记录文件的第一个盘块号和最后一个盘块号。优点是消除了外部碎片、便于文件动态增长；缺点是不能随机访问（必须沿链顺序查找），且指针占用盘块空间、可靠性差（指针损坏则文件丢失）。
- 显式链接：把各盘块的指针集中存放在一张文件分配表（FAT，File Allocation Table）中，FAT的表项存放下一盘块号，整个系统一张FAT。目录项中记录文件的首块号。由于FAT常驻内存（或部分常驻），查找块号不需访问磁盘，支持一定程度的随机访问；FAT本身占用一定内存空间（如FAT-16、FAT-32中FAT较大时开销可观）。

| 特点 | 隐式链接 | 显式链接（FAT） |
| ---- | ---- | ---- |
| 指针位置 | 盘块内部 | FAT（内存） |
| 随机访问 | 不支持 | 支持（FAT常驻内存） |
| 可靠性 | 指针易损坏 | 较好 |
| 内存开销 | 小 | FAT占用内存 |

**重点：**隐式链接与显式链接的区别。**难点：**理解FAT把"链"搬进内存后，逻辑块号→物理块号只需查FAT，从而支持随机访问并提高可靠性。

### 索引分配
<!-- kp: ch4-s3-kp3 emphasis: key -->

索引分配：为每个文件建立一个索引块（索引表），表中存放文件各逻辑块对应的物理块号；目录项中记录索引块的地址。索引分配克服了链接分配不能随机访问的缺点，同时消除了外部碎片、支持文件动态增长，是磁盘文件最常用的物理结构（如UNIX、Linux的inode）。

索引分配有三种形式：
- 单级索引：一个文件一个索引块。小文件时索引块本身浪费（需为每个文件分配一个索引块），大文件时一个索引块放不下一页号，需要链接或分级。
- 多级索引（如二级索引）：索引块中存放指向下一级索引块的指针，支持更大文件。每次访问一个逻辑块需逐级查索引，I/O次数增多。
- 混合索引（UNIX方式）：inode中直接存放若干直接地址（直接指向数据块）加一级、二级、三级间接地址指针，小文件用直接地址（快），大文件用间接索引（省空间），兼顾了单级索引与多级索引的优点。

索引分配时还要考虑索引块本身的大小：若盘块大小等于索引块大小，则一个索引块能容纳的指针数 = 盘块大小 / 指针大小。**重点：**单级、多级、混合索引的结构与适用场景。**难点：**混合索引计算题——给定盘块大小、地址长度、直接块数及间接块数，计算文件最大长度以及访问某逻辑块所需访问磁盘的次数。

## 空闲空间管理
<!-- sec: ch4-s4 -->

### 空闲表法与空闲链表法
<!-- kp: ch4-s4-kp1 -->

空闲表法：系统维护一张空闲盘块表，每个表项记录一个连续空闲区的起始块号和长度（连续块数）。分配时类似动态分区分配，可采用首次适应、最佳适应等算法从空闲表中查找合适的连续空闲区；回收时把归还的连续块合并进空闲表。空闲表法适合连续分配方式的文件系统（配合连续分配），但空闲区可能很多，表占用空间大。

空闲链表法：将空闲盘块或空闲盘区用指针链接成链表。空闲盘块链：以盘块为单位链接所有空闲块，分配时从链头摘下一个块、回收时插入链中，简单但每分配一个块都要读一次链表结点（磁盘I/O较多）；空闲盘区链：以连续空闲盘区为单位链接（链表中每个结点记录一个空闲区的起始块号和长度），分配一个连续空闲区时修改相应结点，回收时合并相邻空闲区。

**重点：**空闲表法（空闲盘块表）与空闲链表法（空闲盘块链、空闲盘区链）的基本思想。**难点：**区分盘块链与盘区链——前者以"块"为单位灵活但分配频繁I/O，后者以"连续区"为单位适合连续分配与回收合并。

### 位示图法与成组链接法
<!-- kp: ch4-s4-kp2 emphasis: difficult -->

位示图法：用一串二进制位表示磁盘块的使用情况，每位对应一个物理块，0表示空闲、1表示已分配。位示图占用的空间 = 磁盘总块数 / 8 字节。分配块时在图中找值为0的位，将其置1并换算成块号；回收时将对应位置0。位示图占用空间小、检索快，且便于随机分配，被广泛使用（如Linux的位示图思路）。

换算公式：设盘块号为b，位示图的行号i、列号j，字长为n（n=16或32）。若"从0开始计数"，块号与行列的关系为 b = i×n + j；若从1开始计数，则 b = (i-1)×n + j。计算时注意题目给定的编号起始值。

成组链接法（UNIX System V使用）：把空闲块分组管理，第一组的空闲块号记录在专用块（超级块）中，组内第一个空闲块（或组内末块）存放下一个空闲组的空闲块号与块数，从而形成链式分组结构。优点：空闲块号不必全部常驻内存，只需在内存中保存当前组的信息，既节省内存又支持大磁盘，分配与回收开销小。分配时从当前组取块，组空则读取下一组；回收时先释放的块填入当前组，当前组满则形成新组。

| 方法 | 存储方式 | 优点 | 缺点 |
| ---- | ---- | ---- | ---- |
| 空闲表法 | 空闲区表 | 适合连续分配 | 表占空间随碎片增多 |
| 空闲链表法 | 盘块/盘区链表 | 简单 | 频繁分配I/O多 |
| 位示图法 | 位图数组 | 占用小、检索快 | 大磁盘时位图也较大 |
| 成组链接法 | 分组链式 | 内存占用小、适合大磁盘 | 实现较复杂 |

**难点：**位示图块号与行列的换算要看清编号起点（0还是1）；成组链接法理解"当前组"在内存、其余组在外存的动态装载过程。

## 磁盘组织与管理
<!-- sec: ch4-s5 -->

### 磁盘的结构与磁盘管理
<!-- kp: ch4-s5-kp1 -->

磁盘由多个盘片组成，盘面划分为若干同心圆磁道，磁道再划分为若干扇区（扇区是磁盘读写的最小单位，通常512字节或4KB）。磁盘容量 = 磁道数 × 每磁道扇区数 × 每扇区字节数 × 盘面数。磁盘读写时间 = 寻道时间（磁头移动到目标磁道）+ 旋转延迟（磁盘旋转使目标扇区到达磁头下方，平均为转半圈的时间）+ 传输时间（读写数据的时间）。

减少磁盘读写时间的主要途径是减少寻道时间和旋转延迟，磁盘调度算法通过合理安排访问请求的磁道顺序来减少寻道时间。磁盘管理还包括磁盘格式化（低级格式化划分扇区、高级格式化建立文件系统）、磁盘分区、引导块（自举程序）、坏块管理等。

**重点：**磁盘的物理结构；磁盘读写时间的三个组成部分。**难点：**理解旋转延迟的平均值计算（转一圈时间的一半）以及寻道时间对磁盘性能的主导影响。

### 磁盘调度算法（FCFS/SSTF/SCAN/C-SCAN）
<!-- kp: ch4-s5-kp2 emphasis: key -->

磁盘调度算法的目标是使磁头的寻道时间尽量小、保证公平、避免"磁臂黏着"（个别请求使磁头长时间停留）。常用算法：

- 先来先服务（FCFS）：按请求到达的顺序服务，公平、实现简单，但寻道距离可能很大、性能较差。
- 最短寻道时间优先（SSTF）：优先服务离当前磁头位置最近的请求。平均寻道时间短、性能好，但可能使远离磁头的请求长期得不到服务（饥饿）。
- 扫描算法（SCAN，电梯算法）：磁头沿一个方向移动，依次服务途中遇到的请求，到达最内或最外侧后再反向移动。克服了SSTF的饥饿问题，寻道距离适中，但两侧磁道等待时间不均匀（刚经过的磁道要等磁头折返）。
- 循环扫描算法（C-SCAN）：SCAN的改进，磁头只沿一个方向服务，到达一端后立即快速返回起点，返回途中不服务。使各磁道请求的等待时间更均匀，消除了磁臂黏着。
- LOOK与C-LOOK：SCAN/C-SCAN的改进，磁头不一定移到最边缘，只移动到该方向上的最远请求处即折返/返回。

**重点：**四种基本调度算法（FCFS、SSTF、SCAN、C-SCAN）的思想、优缺点与适用场景。**难点：**SCAN与C-SCAN的区别——SCAN两端都服务（折返时也服务），C-SCAN只沿一个方向服务、返回时不服务。

### 磁盘调度计算实例
<!-- kp: ch4-s5-kp3 -->

计算实例：设磁盘共有200个磁道（编号0~199），当前磁头位于磁道143，正在向磁道号增大的方向移动。磁盘请求队列（按到达顺序）为 86, 147, 91, 177, 94, 150, 102, 175, 130。分别计算各算法的寻道顺序与总寻道距离。

FCFS：按请求顺序依次服务：143→86→147→91→177→94→150→102→175→130。寻道距离 = |143-86|+|86-147|+|147-91|+|91-177|+|177-94|+|94-150|+|150-102|+|102-175|+|175-130| = 57+61+56+86+83+56+48+73+45 = 565。

SSTF：离143最近的是147（距离4），之后按距当前磁头最近原则：147→150（3）→130（20）→102（28）→94（8）→91（3）→86（5）→175（89）→177（2）。总距离 = 4+3+20+28+8+3+5+89+2 = 162。

SCAN（电梯算法，向增大方向）：服务所有大于等于143的请求，再折返服务小于143的请求。顺序：143→147→150→175→177→（到达199折返）→130→102→94→91→86。若按LOOK（不到199，只到最远请求177）：顺序为 147,150,175,177,130,102,94,91,86，总距离 = (177-143)+(177-86) = 34+91 = 125。注意SCAN的完整版本要加折返到边界再回来的距离。

C-SCAN（循环扫描）：沿增大方向服务到最大请求177后快速返回起点，再继续增大方向服务。顺序：147,150,175,177,86,91,94,102,130。若按LOOK改进的C-LOOK（回到最小请求86）：总距离 = (177-143)+(177-86)+(130-86) = 34+91+44 = 169。

**难点：**解题时先判断磁头初始移动方向，SCAN要明确是否折返到边界（0或199），C-SCAN要计算返回段（不服务）的距离；题目常要求"总寻道距离"或"平均寻道长度"。

### RAID与SSD
<!-- kp: ch4-s5-kp4 -->

**RAID（磁盘冗余阵列）**把多块磁盘组织成一个逻辑盘，提升性能与可靠性：RAID 0（条带化、无冗余，最快）、RAID 1（镜像，可靠但容量减半）、RAID 5（条带+分布式校验，坏一块可恢复）、RAID 10（镜像+条带）。**低频考点**，记住各级别的冗余与容量利用率即可。

**SSD（固态硬盘）**：以闪存为介质，无寻道与旋转延迟，随机访问快；但**不能原地覆盖写**（需先擦除整块）、擦写次数有限，靠**磨损均衡**与**闪存转换层FTL**（逻辑块到物理块的映射、垃圾回收）管理。**中频考点**（2021年统考考查过SSD），重点理解"SSD为什么随机读快、写为什么需要先擦除"。

**重点：**RAID各级别的冗余思想；SSD无机械延迟、写前需擦除、磨损均衡延长寿命。
`;export{e as default};