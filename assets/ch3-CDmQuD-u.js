var e=`---
id: ch3
title: 第三章 数据链路层
order: 3
---

## 数据链路层的功能与组帧
<!-- sec: ch3-s1 -->

### 数据链路层概述与三个基本问题
<!-- kp: ch3-s1-kp1 emphasis: key -->

**1. 数据链路层的功能**
数据链路层位于物理层与网络层之间，其基本功能是为网络层提供服务（无确认、有确认无连接、有确认面向连接三种服务）、链路管理、**组帧**、**差错控制**和**流量控制**。数据链路层把物理层的"不可靠比特流"改造成网络层看来"无差错"的逻辑链路。

**2. 三个基本问题**
数据链路层要解决三个基本问题：

（1）**封装成帧**：在 IP 数据报的首部和尾部加上帧定界符（帧首部、帧尾部），形成帧。接收方根据定界符从比特流中识别出帧的边界。

（2）**透明传输**：当数据中出现了与定界符相同的比特组合时，要能正常传输而不被误认为是定界符。这是"透明"的含义——数据中任意比特组合都能正确传输。

（3）**差错检测**：在帧的尾部加上冗余码（如 CRC 校验码 FCS），接收方据此检测传输中是否出现比特差错。注意数据链路层主要做**检错**（发现错误就丢弃），可靠传输（重传）由更高层或协议（如 TCP）负责。

**3. 链路与数据链路**
- **链路（link）**：一条无源的点到点的物理线路，中间无任何交换结点；
- **数据链路（data link）**：物理线路加上实现通信协议的硬件和软件，如网络适配器（网卡）。我们常说"链路层"，实际指的是数据链路层。

**重点：** 三个基本问题（封装成帧、透明传输、差错检测）及其顺序；数据链路层是"检错丢弃"而非"检错重传"。

**难点：** 数据链路层的流量控制与传输层流量控制目的不同——链路层流量控制在相邻结点之间（局部），传输层流量控制是端到端（全局），但都基于滑动窗口原理。

### 组帧方法
<!-- kp: ch3-s1-kp2 emphasis: difficult -->

**1. 四种组帧方法**
（1）**字符计数法**：在帧首部用一个计数字段标明该帧所包含的字节数。接收方依据计数值确定帧边界。缺点：计数字段一旦出错，后续所有帧的边界全部错乱，且错误无法被检测到。

（2）**字符填充法（首尾定界符法）**：用特定控制字符 SOH（帧首）和 EOT（帧尾）作定界符。当数据中出现与定界符相同的字符时，在它前面插入一个转义字符 ESC。接收方去掉转义符恢复原数据，称为"字符填充"。适用于 ASCII 字符的链路层协议（如 PPP 的字节填充）。

（3）**零比特填充法**：采用**比特填充**，以特定比特模式 **01111110**（6 个 1 前后各加一个 0）作为帧首帧尾定界符。发送端扫描数据，只要发现 **5 个连续的 1**，就立即在其后插入一个 0（零比特填充），保证数据中不会出现 6 个连续的 1；接收端收到 5 个连续的 1 后，若下一个比特是 0，则删除该 0 还原数据，若下一个比特是 1，再结合后续比特判断是否为定界符。此法简单高效，广泛用于 HDLC、PPP。

（4）**违规编码法**：利用物理层编码的**违规信号**（如曼彻斯特编码中不出现的高-高、低-低电平组合）作为帧边界。物理层编码中有"冗余"即可用违规码定界，无需填充。

**2. 对比表格**

| 方法 | 定界方式 | 透明传输处理 | 典型协议 |
| --- | --- | --- | --- |
| 字符计数法 | 计数字段 | 无需填充（靠计数） | 早期协议 |
| 字符填充法 | SOH/EOT 字符 | 插入转义字符 ESC | PPP |
| 零比特填充法 | 01111110 | 5 个连续 1 后插 0 | HDLC、PPP |
| 违规编码法 | 违规码元 | 无填充 | 802.5（令牌环） |

**3. 零比特填充示例**
【例】数据为 0110 11111 1111 10（含有连续多个 1），零比特填充后：遇到 5 个连续 1 就插 0，结果为 0110 11111 0 1111 1 0 ...。练习时注意把 6 个连续 1 拆成"5 个 1 + 0 + 剩下的 1"。

**重点：** 零比特填充的具体规则（连续 5 个 1 后插 0）及接收端还原规则；四种方法的识别特征。

**难点：** 透明传输的本质——"数据中出现的任何比特组合都不得被误认为定界符"。字符填充与比特填充的区别在于填充的基本单位是"字符"还是"比特"。

## 差错控制
<!-- sec: ch3-s2 -->

### 差错类型与检错编码
<!-- kp: ch3-s2-kp1 emphasis: key -->

**1. 差错类型**
传输中的差错可分为两类：
- **位错**：传输中 0 变 1 或 1 变 0（比特差错），是最基本的差错，用**检错编码/纠错编码**处理；
- **帧错**：帧的丢失、重复或失序，用**可靠传输机制**（确认、重传）处理。

**2. 检错与纠错编码**
差错控制编码分为两类：
- **检错编码**：只能发现错误，如奇偶校验码、CRC 循环冗余码；
- **纠错编码**：既能发现又能纠正错误，如海明码。

编码的**冗余度**：在数据位之后附加校验位。码距（海明距离）是指两个码字之间对应位不同的个数的最小值。**检错与纠错能力取决于码距**：
- 要检测 d 个错误，码距至少为 d+1；
- 要纠正 d 个错误，码距至少为 2d+1。

**3. 奇偶校验码**
奇偶校验码是只含 1 个校验位的检错码，在数据后附加一位，使整个码字中 1 的个数为奇数（奇校验）或偶数（偶校验）。接收方统计 1 的个数即可判断是否出错。**局限性**：
- 只能检测**奇数个**比特错误，无法检测偶数个错误（如两位同时翻转，奇偶性不变）；
- 只能检错不能纠错。

因此奇偶校验只适用于低误码率的短距离传输，实际链路层普遍采用 CRC。

**4. 海明码（了解）**
海明码可以纠正一位错误。设数据位 k，校验位 r，需满足 2^r ≥ k + r + 1。校验位放在 2 的幂次位置（1、2、4、8...），通过分组偶校验定位错误比特的位置。408 曾考查海明码校验位的计算。

**重点：** 位错与帧错的区别；奇偶校验只能检奇数个错误；码距与检错/纠错能力的关系。

**难点：** "奇偶校验为什么检不出两位错误"——因为两位同时出错奇偶性不变；以及"检错"与"纠错"的编码冗余度不同。

### 循环冗余检验 CRC
<!-- kp: ch3-s2-kp2 emphasis: difficult -->

**1. 工作原理**
CRC（Cyclic Redundancy Check）在发送端把数据 D 与生成多项式 G(x) 进行**模 2 除法**，得到余数作为冗余码 FCS，追加在数据后发送；接收端用同样的 G(x) 对收到的比特序列做模 2 除法，若余数为 0 则认为无差错，否则丢弃该帧。

**2. 计算步骤**
设生成多项式为 G(x)（最高次幂为 r），数据为 M（二进制）：
1. 确定 r：G(x) 的最高次数，例如 G(x)=x^3+x^2+1 的比特表示为 1101，r=3；
2. 在 M 后补 r 个 0，得到 M×2^r；
3. 用 M×2^r 对 G(x) 的比特串做**模 2 除法**（不进位减法，即按位异或），得到 r 位余数（FCS，不足 r 位前面补 0）；
4. 发送的帧 = M + FCS。

接收方用 (M+FCS) 对 G(x) 做模 2 除法，余数为 0 则接收，否则丢弃。

**3. 完整计算例题**
【例】数据 M = 101001，生成多项式 G(x) = x^3 + x^2 + 1（比特串 1101），求 FCS 与发送帧。
- r = 3，M 后补 3 个 0：101001000；
- 模 2 除法：101001000 ÷ 1101，按位异或逐位相除，最终余数为 **001**；
- 发送帧 = 101001001。

（验算：发送帧 101001001 对 1101 做模 2 除法余数为 0。）

【例】G(x) = x^4 + x + 1（10011），M = 1010101，则 r=4，M 后补 4 个 0 得 10101010000，模 2 除 10011 得余数（此处略去手工过程），发送帧 = M + 余数。

**4. 性质与考点**
- CRC 只能**检错**，不能**纠错**（重传由可靠传输机制完成）；
- CRC 的检错能力取决于生成多项式的选择，G(x) 的选取越复杂检错能力越强，但不能保证检出所有错误；
- 模 2 除法"减"就是异或，没有借位；商的每一位与"被除数当前首位是否为 1"有关；
- 数据链路层的 FCS 字段采用 CRC 计算，但**不同的链路层协议可以使用不同的生成多项式**。

**重点：** 模 2 除法的计算（异或相除）；FCS = 余数，长度 = r；发送帧 = 原数据 + FCS；接收端余数非 0 即丢弃。

**难点：** 手工做模 2 除法容易出错——每步对齐最高位，异或后把下一位拉下来，注意余数位数不足 r 时前面补 0。

## 可靠传输机制
<!-- sec: ch3-s3 -->

### 停止-等待协议（Stop-and-Wait）
<!-- kp: ch3-s3-kp1 emphasis: key -->

**1. 基本思想**
停止-等待协议（停等协议）是最简单的可靠传输协议：发送方发送一个帧后，**停止发送**，等待接收方的确认（ACK）；收到确认后才发送下一帧；若等待超时（计时器到期）仍未收到确认，则**重传**原帧。接收方收到帧后校验，若正确则发回确认；若帧出错或丢失，则不发确认（或发否定确认 NAK），发送方超时重传。

**2. 信道利用率计算**
设数据帧长度为 L（bit），发送速率为 C（bit/s），单向传播时延为 R（s），RTT = 2R（忽略确认帧发送时间与处理时间）。发送一个帧的周期为 T_D + RTT，其中发送时延 T_D = L/C。

$$\\text{信道利用率} = \\frac{T_D}{T_D + RTT} = \\frac{T_D}{T_D + 2R}$$

**3. 计算例题**
【例】链路带宽 10 Mbit/s，单向传播时延 270 ms（模拟卫星链路），帧长 1000 bit，求停等协议的信道利用率。
T_D = 1000/(10×10^6) = 0.1 ms；
利用率 = 0.1 / (0.1 + 2×270) = 0.1 / 540.1 ≈ 0.0185%，几乎等于 0。

可见在长距离链路上停等协议效率极低——发送方绝大多数时间在等待。这正是滑动窗口协议（GBN/SR）存在的意义。

**4. 考点归纳**
- 停等协议中发送窗口和接收窗口大小均为 1；
- 帧编号只需 1 位（0/1）即可区分新旧帧，防止"迟到的重传帧"被误认为新帧；
- 超时重传是可靠传输的基础——即使 ACK 丢失，发送方也会因超时重发，接收方依靠帧序号丢弃重复帧。

**重点：** 停等协议的工作流程；信道利用率公式及计算；帧编号 1 位即可。

**难点：** 停等协议下信道利用率的数量级判断——链路越"长"（传播时延大）、帧越短，利用率越低，因此卫星链路必须用大发送窗口。

### 后退 N 帧协议 GBN（Go-Back-N）
<!-- kp: ch3-s3-kp2 emphasis: key -->

**1. 工作原理**
GBN（后退 N 帧/回退 N 步）协议中，发送方可以连续发送**多个**已编号的帧，而不必等待每帧的确认，但要求：
- 发送方维护发送窗口，只要窗口未满就可发送；
- 接收方采用**累积确认**：接收方按序接收，只对"按序收到的最后一个正确帧"发回确认（ACKn 表示 n 及以前的所有帧都已正确收到）；
- 若某个帧出错或丢失，接收方丢弃后续所有不按序到达的帧，并重复发送对最后一个正确帧的确认；
- 发送方**超时**时，重传**从出错帧开始的后面所有帧**（"后退 N 帧"由此得名）。

**2. 窗口大小约束**
设帧序号用 n 位二进制表示，序号空间大小为 2^n。GBN 中：
$$\\text{发送窗口大小}\\leq 2^n - 1$$

原因：若发送窗口 = 2^n，当所有确认全部丢失、发送方重传窗口内的全部帧时，接收方无法区分"新帧"与"重传的旧帧"（序号重叠）。因此必须留出一个序号。

**3. 计算例题**
【例】采用 3 位序号（n=3），GBN 发送窗口最大为 2^3 - 1 = 7。若发送序号为 0~6 的 7 个帧，其中第 2 号帧出错，则发送方需重传 2、3、4、5、6 五个帧，接收方丢弃已收到的 3、4、5、6。

**4. 特点总结**
- 接收窗口恒为 1，接收方只能按序接收，不缓存乱序帧；
- 累积确认大大减少了确认帧的数量；
- 缺点：一旦出错，重传大量帧，链路利用率下降（尤其在传播时延大时）；
- 复杂度比停等协议高，比 SR 协议低。

**重点：** 发送窗口 ≤ 2^n - 1；累积确认的含义；超时重传"出错帧及其之后全部帧"。

**难点：** 通过"接收方只按序接收、不缓存"理解为什么需要重传后续所有帧；窗口上限公式的推导（避免新旧帧序号混淆）。

### 选择重传协议 SR（Selective Repeat）
<!-- kp: ch3-s3-kp3 emphasis: key -->

**1. 工作原理**
选择重传（SR）协议改进了 GBN 的缺点：接收方对**不按序到达但正确的帧进行缓存**，并逐帧（**逐个确认**，非累积确认）确认每个正确收到的帧；发送方超时后**只重传出错的那一个帧**，而不是重传后续所有帧。最终接收方把缓存中按序的帧交给上层。

**2. 窗口大小约束**
SR 中序号空间仍需足够大，约束条件为：

$$\\text{发送窗口大小} + \\text{接收窗口大小} \\leq 2^n$$

由于 SR 的发送窗口与接收窗口可以相等，故发送窗口最大为：

$$W_{\\max} = 2^{n-1}$$

【例】n=3 时 SR 发送窗口最大为 2^(3-1) = 4（而 GBN 为 7）。

**3. 三种协议对比**

| 协议 | 发送窗口 | 接收窗口 | 确认方式 | 超时重传范围 | 接收方缓存 |
| --- | --- | --- | --- | --- | --- |
| 停等协议 | 1 | 1 | 逐个确认 | 当前帧 | 无 |
| GBN | ≤ 2^n - 1 | 1 | 累积确认 | 出错帧及之后全部 | 无 |
| SR | ≤ 2^(n-1) | ≤ 2^(n-1) | 逐个确认 | 仅出错帧 | 有（缓存乱序帧） |

**4. 考点归纳**
- SR 接收窗口 > 1 是它与 GBN 的本质区别（缓存乱序帧，逐个确认）；
- 序号位数 n 一定时，GBN 比 SR 允许更大的发送窗口；
- 无论是 GBN 还是 SR，都是**数据链路层实现可靠传输**的手段，而 TCP 中的可靠传输是基于字节流的滑动窗口机制（相似但对象不同）。

**重点：** 发送窗口上限 2^(n-1)；逐个确认 + 缓存乱序帧；只重传出错帧。

**难点：** SR 中接收窗口与发送窗口的约束关系 W_s + W_r ≤ 2^n 的推导——为了防止新旧帧序号冲突，发送窗口与接收窗口必须"对半分"序号空间。

## 介质访问控制
<!-- sec: ch3-s4 -->

### 信道划分介质访问控制
<!-- kp: ch3-s4-kp1 -->

**1. 介质访问控制的分类**
介质访问控制（MAC）解决"多结点共享信道时，如何决定谁可以发送"的问题，分三类：
- **信道划分（静态划分）介质访问控制**：频分复用 FDM、时分复用 TDM、波分复用 WDM、码分复用 CDMA；
- **随机访问介质访问控制**：ALOHA、CSMA、CSMA/CD、CSMA/CA；
- **轮询访问介质访问控制**：令牌传递（令牌环、令牌总线）。

**2. 四种信道划分技术**
（1）**频分复用 FDM**：把信道频带划分为若干子频带，各用户占用不同频段同时通信。优点：各用户独占频带、无冲突；缺点：频谱利用率不高、需要频率分割保护带。

（2）**时分复用 TDM**：把时间划分为等长的时隙，各用户轮流占用同一频带的不同时间片。同步时分复用中，即使某用户无数据，时隙也被占用（浪费）；**统计时分复用（异步 TDM）**按需分配时隙，提高利用率。

（3）**波分复用 WDM**：是频分复用应用于光纤的变种，利用不同波长（颜色）的光同时传输多路信号，密集波分复用 DWDM 可在一根光纤中传输数十上百路信号。

（4）**码分复用 CDMA**：各用户使用**相互正交**的码片序列（chip），同时用同一频带发送。每个用户被分配一个 m 位的码片序列，发送 1 时发码片本身，发送 0 时发码片反码；接收端用码片序列与收到的混合信号做**规格化内积**，由于码片正交，可以分离出本用户信号。CDMA 抗干扰性强、频谱利用率高，用于移动通信（3G 等）。

**3. CDMA 计算要点**
设用户 A 的码片序列为 S（如 00011011 记为 -1-1-1+1+1-1+1+1），B 的码片序列为 T，正交条件：S·T = 0，S·S = m（m 为码片长度）。接收端收到混合信号后，计算 S·(混合信号)/m：结果为 +1 表示 A 发 1，为 -1 表示 A 发 0，为 0 表示 A 没有发送。

**重点：** FDM 分频、TDM 分时、WDM 分波长、CDMA 分码片；CDMA 码片正交与内积计算。

**难点：** CDMA 计算题——把"1 为 +1、0 为 -1"的码片向量点乘，用结果正负判断所发比特，408 曾以选择题形式考查。

### CSMA/CD 与二进制指数退避算法
<!-- kp: ch3-s4-kp2 emphasis: difficult -->

**1. 基本思想**
CSMA/CD（载波监听多路访问/碰撞检测）是**总线型以太网**采用的随机访问介质访问控制协议。工作原则："**先听后发，边发边听，冲突停发，随机重发**"。
- **载波监听**：发送前先监听信道是否空闲，空闲则发送，忙则等待；
- **碰撞检测**：边发送边监听，若检测到冲突（电压突变），立即停止发送，并发送**强化冲突信号**（干扰信号，通知所有站点冲突发生）；
- 冲突后按**二进制指数退避算法**随机退避后重发。

**2. 二进制指数退避算法**
冲突发生后，站点随机选择退避时间：

$$T_{wait} = R \\times 2\\tau,\\quad R = \\text{随机取}[0,\\ 2^{k}-1]$$

其中 τ 为端到端单程传播时延（争用期取 2τ），k = min(重传次数, 10)。若重传 16 次仍冲突，则放弃发送并报告上层错误。
- 第 1 次冲突：k=1，R∈[0,1]，退避时间取 0 或 2τ；
- 第 2 次冲突：k=2，R∈[0,3]；
- 第 3 次冲突：k=3，R∈[0,7]，以此类推。

该算法保证冲突概率随冲突次数指数下降，避免站点"永远冲突"。

**3. 争用期与最短帧长**
以太网规定**争用期（碰撞窗口）为 2τ**（端到端往返传播时延）。为保证发送方在发送完最短帧之前能检测到冲突，要求：

$$\\text{最短帧长} = 2\\tau \\times \\text{数据率} = \\frac{2 \\times \\text{电缆长度} \\times \\text{数据率}}{\\text{电磁波传播速率}}$$

【例】以太网最大电缆长度约 1 km（往返传播 2×5 μs），数据率 10 Mbit/s，最短帧长 = 10 μs × 10 Mbit/s = 100 bit，加上前导码共 64 字节（规定最短帧长 64 字节，数据部分最短 46 字节）。若帧长小于最短帧长，则说明发生了冲突，接收方丢弃。

**4. 考点归纳**
- 以太网标准最短帧长 64 字节（数据最少 46 字节），最大帧长 1518 字节（数据最多 1500 字节）；
- 帧长与距离有关：距离越远（传播时延越大），争用期越长，最短帧长越大；
- 10 Mbit/s 以太网争用期 51.2 μs。

**重点：** "先听后发、边发边听、冲突停发、随机重发"十六字原则；二进制指数退避公式；最短帧长计算。

**难点：** 为什么帧太短无法检测冲突——若帧发送时间小于 2τ，发送方发完帧后冲突信号还未返回，就不知道发生了冲突；因此必须满足"帧发送时间 ≥ 2τ"。

### CSMA/CA（无线局域网）
<!-- kp: ch3-s4-kp3 emphasis: key -->

**1. 为什么无线不能用 CSMA/CD**
无线局域网（IEEE 802.11）不能采用 CSMA/CD，原因：
- **无法边发边听**：无线收发无法同时进行（半双工），发送时收不到信号，无法检测冲突；
- **隐蔽站问题（hidden terminal）**：A 与 C 都能与 B 通信但彼此听不到对方。A 发送前监听认为信道空闲，C 也监听认为空闲，两者同时发给 B 即冲突——"监听"在这种场景下失效。

因此无线采用 **CSMA/CA（载波监听多路访问/碰撞避免）**，核心是**避免**碰撞而非检测碰撞。

**2. CSMA/CA 的工作流程**
1. 发送前先监听信道；
2. 若信道忙则等待，直到信道空闲且**空闲持续 DIFS（分布协调功能帧间间隔）**时间；
3. 再执行**随机退避**（二进制退避），退避结束才发送数据；
4. 接收方正确收到数据后，等待 **SIFS（短帧间间隔，最短）**后回送 **ACK**；
5. 发送方收到 ACK 表示发送成功，未收到则重发。

此外可选用 **RTS/CTS 机制**解决隐蔽站问题：发送方先发 **RTS**（请求发送），接收方回 **CTS**（允许发送），其他站听到 CTS 后推迟发送。RTS/CTS 帧很短，代价小。

**3. 帧间间隔（DIFS > PIFS > SIFS）**
- **SIFS** 最短，用于 ACK、CTS 等高优先级应答；
- **PIFS** 居中，用于轮询；
- **DIFS** 最长，用于普通数据帧发送前的等待。

**4. CSMA/CD 与 CSMA/CA 对比**

| 比较项 | CSMA/CD | CSMA/CA |
| --- | --- | --- |
| 应用场景 | 有线以太网 | 无线局域网 802.11 |
| 冲突处理 | 检测（边发边听） | 避免（退避+RTS/CTS） |
| 是否可边发边听 | 是 | 否（半双工） |
| 帧间间隔 | 无（争用期 2τ） | DIFS/SIFS 等 |
| 确认机制 | 无（靠高层） | 有 ACK 帧 |

**重点：** CSMA/CA 用于无线的原因（无法边发边听、隐蔽站）；流程（监听→DIFS→退避→发送→SIFS→ACK）；RTS/CTS 解决隐蔽站问题。

**难点：** 隐蔽站问题与暴露站问题的区分——隐蔽站指"听不到对方但会互相冲突"（A 与 C 都在 B 的范围内），暴露站指"听得到对方但实际上不冲突"，两者相反。

## 以太网
<!-- sec: ch3-s5 -->

### 以太网的 MAC 帧格式与 MAC 地址
<!-- kp: ch3-s5-kp1 emphasis: key -->

**1. MAC 帧格式（DIX 以太网）**
以太网 MAC 帧由以下字段组成：

| 字段 | 目的地址 | 源地址 | 类型 | 数据 | FCS |
| --- | --- | --- | --- | --- | --- |
| 长度 | 6 B | 6 B | 2 B | 46~1500 B | 4 B |

- **目的地址**：接收方网卡的 MAC 地址；
- **源地址**：发送方网卡的 MAC 地址；
- **类型**：指明上层协议（0x0800 表示 IP，0x0806 表示 ARP）；
- **数据**：最少 46 B、最多 1500 B（即 MTU = 1500 B），不足 46 B 时填充；
- **FCS**：4 字节 CRC 校验码，检验目的地址到数据字段。

MAC 帧在物理层传输时，前面还有 **8 字节前导码**（7 字节前同步码 + 1 字节帧开始定界符 SFD），前导码不计入帧长。因此帧的最小长度 = 6+6+2+46+4 = 64 字节，最大 = 6+6+2+1500+4 = 1518 字节（不含前导码）。

**2. MAC 地址**
- MAC 地址又称物理地址、硬件地址，是数据链路层的地址，长 **48 位（6 字节）**，用十六进制表示，如 00-1A-2B-3C-4D-5E；
- 前 24 位为厂商代码（OUI），后 24 位为厂商自行分配的序号，全球唯一；
- 48 位中最高字节的最低位为 **I/G 位**：0 表示单播地址，1 表示组播地址；全 1（FF-FF-FF-FF-FF-FF）为**广播地址**；
- MAC 地址固化在网卡的 ROM 中，但可用软件修改（实际使用中以网卡当前配置为准）。

**3. 考点辨析**
- IP 地址是网络层地址（逻辑地址），随网络位置变化；MAC 地址是链路层地址（物理地址），与地理位置无关；
- 以太网帧中的"类型"字段与 802.11 等帧中的"长度"字段容易混淆；
- 网卡（适配器）实现数据链路层和物理层的功能，既组帧又做信号收发。

**重点：** MAC 帧字段及各字段长度；帧长范围 64~1518 字节；MAC 地址 48 位。

**难点：** "帧的最小长度必须 ≥ 64 字节"与 CSMA/CD 最短帧长（100 bit 约 12.5 字节 + 填充）的联系——以太网取 64 字节是为了满足 10 Mbit/s 下 2τ 约束并留有余量。

### 交换机的工作原理与自学习
<!-- kp: ch3-s5-kp2 emphasis: difficult -->

**1. 以太网交换机的地位**
交换机（switch）本质是**多端口网桥**，工作在数据链路层。它是**即插即用**设备，不需要人工配置，依靠**自学习**建立交换表（MAC 地址表），并按交换表转发帧。

**2. 自学习过程**
交换机学习的是"源地址—端口"的映射关系。收到帧后：
1. 记录帧的**源 MAC 地址**与该帧**到达的端口**的对应关系，写入交换表（表项带时间戳，超时自动删除，如默认 300 s）；
2. 在交换表中查找**目的 MAC 地址**：
   - 若命中（找到对应端口）且不是入端口：向该端口**定向转发**；
   - 若命中但就是入端口：**丢弃**（目的主机在同一网段，帧无需转发）；
   - 若未命中：向**除入端口外的所有端口****洪泛转发**（广播式扩散）。

**3. 帧的转发三种方式**
- **存储转发**：先完整接收帧并校验，再查表转发。优点：可检错、支持不同速率端口间转发；缺点：时延大；
- **直通交换**：只检查目的地址字段（前 6 字节）就立即转发。优点：时延小；缺点：不能检错、无法连接不同速率的端口；
- **无碎片转发**：先接收 64 字节（帧头）再转发，折中方案。

**4. 典型例题**
【例】交换机 1 号端口收到帧，源 MAC 为 S，目的 MAC 为 D。交换表无 S 记录则学习 S→端口1；若查 D 无记录则向 2、3、4 端口洪泛；若 D 在交换表中对应端口 3，则仅向端口 3 转发。若 D 对应端口 1（与入端口相同）则丢弃。

**重点：** 自学习的三步（学习源地址、查目的地址、命中定向转发/未命中洪泛/同端口丢弃）；交换表超时老化。

**难点：** "洪泛"不等于"广播帧"——洪泛是交换机的转发行为（转发给除入端口外的所有端口），广播帧（目的地址全 1）则必然向所有端口转发；两者概念不同。

### 冲突域与广播域
<!-- kp: ch3-s5-kp3 emphasis: key -->

**1. 概念定义**
- **冲突域**：一组"同一时刻只能有一个结点发送数据"的设备集合，两个结点若同时发送就会产生冲突，则它们属于同一个冲突域；
- **广播域**：广播帧能到达的所有结点构成的集合，同一广播域内所有设备都能收到广播帧。

**2. 各设备的隔离能力**

| 设备 | 工作层次 | 隔离冲突域 | 隔离广播域 |
| --- | --- | --- | --- |
| 中继器 | 物理层 | 否 | 否 |
| 集线器 | 物理层 | 否 | 否 |
| 网桥 | 数据链路层 | 是（每端口） | 否 |
| 交换机 | 数据链路层 | 是（每端口） | 否 |
| 路由器 | 网络层 | 是 | 是 |

**3. 典型结论**
- 集线器：所有端口属于**同一个冲突域、同一个广播域**；
- 交换机：**每个端口是一个独立的冲突域**（端口独享带宽、可并行转发），但所有端口仍在**同一个广播域**；
- 路由器：既隔离冲突域也隔离广播域，每个接口连接的网络是独立的冲突域和广播域；
- 二层交换机互连仍不能减少广播域，广播风暴需要通过路由器（三层）或 VLAN 划分来隔离。

**4. 计算例题**
【例】用 1 个 8 口交换机连接 8 台主机（每端口 1 台），冲突域个数 = 8（每端口一个，与主机间点对点），广播域个数 = 1。若把交换机换成集线器，冲突域 = 1，广播域 = 1。

**重点：** 冲突域/广播域的定义；"交换机逐端口隔离冲突域、不隔离广播域；路由器两者都隔离"的结论。

**难点：** VLAN（虚拟局域网）可以在二层通过标记隔离广播域，这是"交换机不隔离广播域"结论的例外——常规二层交换机不隔离，VLAN 交换机按 VLAN 划分广播域。

### VLAN 虚拟局域网（802.1Q）
<!-- kp: ch3-s5-kp4 emphasis: key -->

**VLAN（虚拟局域网）**在**数据链路层**通过给帧打**标记（tag）**，把一个物理局域网在逻辑上划分为多个相互隔离的广播域——广播帧只在同一VLAN内传播，实现了"交换机不隔离广播域"的例外。VLAN标准为**IEEE 802.1Q**：在以太网帧的源地址字段与类型字段之间插入4字节VLAN标记（2字节TPID=0x8100 + 2字节TCI，其中12位**VID**标识VLAN编号，取值范围1~4094），最大帧长由1518增至1522字节。

VLAN的划分方式：**基于端口**（最常用，管理员把交换机的某些端口指定到某VLAN）、基于MAC地址、基于IP子网、基于协议等。同一VLAN的主机即使连接在不同交换机上（经Trunk干道链路传递带标记的帧）也可通信；不同VLAN的主机必须经**路由器（三层设备/三层交换机）**才能通信。

**重点：** VLAN=二层按标记隔离广播域；802.1Q在帧中插入4字节（含12位VID）；跨交换机用Trunk干道传带标记帧；不同VLAN通信需路由。

## 数据链路层设备
<!-- sec: ch3-s6 -->

### 网桥
<!-- kp: ch3-s6-kp1 -->

**1. 功能**
网桥（bridge）工作在**数据链路层**，用于连接两个（或多个）局域网网段，其基本功能：
- **过滤与转发**：根据 MAC 地址决定帧是转发到另一网段还是丢弃，减少不必要的跨网段流量；
- **隔离冲突域**：连接在网桥两个端口上的网段各自是独立的冲突域，两端可同时传输；
- 不隔离广播域（广播帧会跨越网桥传播）。

**2. 透明网桥与源路由网桥**
- **透明网桥**（IEEE 802.1d 生成树算法 STP）：对终端系统透明，站点感觉不到网桥的存在。它通过自学习建立转发表，用**生成树协议**消除环路（阻塞冗余端口，防止广播风暴）；
- **源路由网桥**（IEEE 802.5）：由源站决定帧的传送路径，帧中携带路由信息。

**3. 网桥的优缺点**
优点：隔离冲突域、扩展物理范围、提高可靠性；缺点：增加帧转发时延、不隔离广播域（广播风暴仍可能发生）。

**4. 考点归纳**
- 网桥只在必要时转发帧（目的地址在另一侧时），转发表未命中时洪泛；
- 网桥的"过滤"功能以 MAC 地址为基础，工作在数据链路层；
- 通过生成树算法删除冗余链路，防止**环路**引起的帧在环路中无限循环和广播风暴。

**重点：** 网桥隔离冲突域不隔离广播域；透明网桥的自学习与生成树算法。

**难点：** 网桥与交换机的区别——交换机是"多端口网桥"的现代形态，功能更强（全双工、并行转发、无冲突），但原理（自学习、过滤、洪泛）完全一致。

### 以太网交换机
<!-- kp: ch3-s6-kp2 -->

**1. 交换机 vs 集线器**
交换机是数据链路层的多端口网桥，与物理层的集线器有本质区别：
- 交换机按 MAC 地址**定向转发**，集线器向所有端口**广播**；
- 交换机每个端口是独立冲突域、**独享带宽**，集线器所有端口共享带宽；
- 交换机支持**全双工**通信，集线器只能半双工。

**2. 交换机的交换方式（复习）**

| 方式 | 工作特点 | 时延 | 检错 |
| --- | --- | --- | --- |
| 直通交换 | 收到目的地址即转发 | 小 | 无 |
| 存储转发 | 收完整帧并校验后转发 | 大 | 有 |
| 无碎片转发 | 收到 64 字节后转发 | 中 | 部分 |

**3. 交换式以太网的特点**
- 每个端口独享带宽，N 个端口的交换机总吞吐量可达 N 倍端口速率（如 8 个 100 Mbit/s 端口，理论总吞吐量 800 Mbit/s）；
- 端口间可**并行转发**（同时多对通信）；
- 由于每端口独立冲突域，**CSMA/CD 协议不再需要**（发送前无需监听），但帧格式不变；
- 交换机可连接不同速率、不同类型的以太网。

**4. 考点归纳**
- 交换机是**即插即用**、自学习、不需人工配置；
- 交换机工作在**数据链路层**，但**三层交换机**可识别 IP 地址、实现路由功能（等价于"交换+路由"）；
- 交换机不能连接两个速率不匹配且采用直通交换的端口（需存储转发方式）。

**重点：** 交换机独享带宽、并行转发、每端口隔离冲突域、全双工；存储转发与直通交换的取舍。

**难点：** 交换式以太网中 CSMA/CD 被废弃，但**帧的格式和 MAC 地址机制不变**——"协议废弃、格式保留"是高频辨析点。

## 无线局域网与广域网协议
<!-- sec: ch3-s7 -->

### IEEE 802.11 无线局域网
<!-- kp: ch3-s7-kp1 -->

**IEEE 802.11（Wi-Fi）**是无线局域网标准，采用**星型拓扑**：**接入点（AP，无线接入点）**与若干**站点（STA）**组成基本服务集（BSS），站点通过AP接入有线网络；多个BSS经**分布式系统（DS）**互联扩展覆盖。802.11的MAC层使用**CSMA/CA**（见前文），帧间间隔、RTS/CTS、隐蔽站问题都是其特色。802.11帧与以太网帧不同：前三字段为帧控制、持续时间和地址（最多4个地址字段，因涉及收发双方与AP），且用"长度"字段而非"类型"字段。

802.11与以太网对比：无线用CSMA/CA（**避免**冲突），有线用CSMA/CD（**检测**冲突）；无线帧需ACK确认，以太网无；无线通过频段划分互不干扰（2.4GHz/5GHz多个信道），有线用线缆隔离。**中频考点**，重点掌握AP/BSS的组织方式与CSMA/CA的适用原因。

**重点：** 802.11由AP+站点组成BSS；MAC用CSMA/CA+ACK确认；帧格式与以太网不同（多地址字段）。

### HDLC 与 PPP 广域网协议（低频）
<!-- kp: ch3-s7-kp2 -->

广域网数据链路层协议主要考查 **HDLC** 与 **PPP** 两个：

**HDLC（高级数据链路控制）**：面向**比特**的链路层协议，帧格式为 标志(01111110) + 地址 + 控制 + 信息 + FCS + 标志，采用**零比特填充**保证透明传输（连续5个1后插0），支持可靠传输（帧编号、确认、重传），用于点对点或点对多点。

**PPP（点对点协议）**：面向**字节**的点到点协议，广泛应用于拨号与家庭宽带（PPPoE）。帧格式：标志(0x7E) + 地址(0xFF) + 控制(0x03) + 协议 + 信息 + FCS + 标志，透明传输用**字节填充**（0x7E→0x7D 5E）。PPP只提供**检错**（FCS），**不提供可靠传输**（无帧编号与重传），与HDLC不同。

**低频考点**：记住"HDLC面向比特、零比特填充、可靠；PPP面向字节、字节填充、不可靠只检错"以及帧标志字段即可。

**重点：** HDLC零比特填充、PPP字节填充；PPP无序号无重传（非可靠），HDLC可靠。

### 轮询访问：令牌传递（低频）
<!-- kp: ch3-s7-kp3 -->

**轮询访问介质访问控制**与随机访问（CSMA/CD）相对：由主结点**轮流询问**（轮询）各从结点是否有数据发送，或通过**令牌**在环形网络上依次传递来控制发送权。典型应用是**令牌环（IEEE 802.5）**与**令牌总线**：网络上的站点组成逻辑环，**令牌**（一个特殊的短帧）沿环单向传递，**只有持有令牌的站点才能发送数据**，发送完毕把令牌传给下一站。

特点：各站点发送机会**公平且确定**（无冲突、可计算最坏等待时间），适合对时延有要求的场合（如工业控制）；缺点是实现复杂、令牌丢失需维护（需定时器与监控站）。与CSMA/CD"冲突后重发"相比，令牌方式**根本无冲突**。**低频考点**，理解"持有令牌才能发送、令牌环形传递、无冲突、确定性时延"即可。

**重点：** 令牌传递=无冲突的确定性子访问控制；只有持令牌者可发送；令牌环/令牌总线是代表。
`;export{e as default};