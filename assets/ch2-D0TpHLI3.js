var e=`---
id: ch2
title: 数据的表示和运算
order: 2
---

## 数制与编码
<!-- sec: s2-1 -->

### 进位计数制与进制转换
<!-- kp: kp2-1-1 emphasis: key -->

在进位计数制中，任意 $R$ 进制数都可按权展开：

$$(K_n K_{n-1} \\cdots K_1 K_0 . K_{-1} \\cdots K_{-m})_R = \\sum_{i=-m}^{n} K_i \\cdot R^{i}$$

其中 $K_i$ 为第 $i$ 位的数码（$0 \\le K_i < R$），$R^i$ 为该位的权（位权）。十进制、二进制、八进制、十六进制是最常用的四种进制，B（binary）、O（octal）、D（decimal）、H（hexadecimal）分别表示它们。二进制与八进制、十六进制之间可通过**分组法**快速转换：以小数点为中心，二进制转八进制每3位一组，转十六进制每4位一组（不足补0）。

十进制转 $R$ 进制：**整数部分"除R取余、逆序排列"**，**小数部分"乘R取整、顺序排列"**。注意：十进制小数转二进制可能产生**无限循环小数**，例如 $0.1_{10}$ 的二进制表示是无限循环的 $0.0001100110011\\ldots_2$，因此机器只能近似表示——这正是浮点数精度问题的根源之一。此外还要注意十六进制与二进制的逐位对应（每位十六进制 = 4位二进制），如 $3A_{H} = 0011\\,1010_2$。

**重点：** 按权展开是各种进制转换的基础；整数除R取余逆序、小数乘R取整顺序；二进制小数未必能精确表示十进制小数。

### 真值、机器数与BCD码
<!-- kp: kp2-1-2 emphasis: key -->

**真值**是带正负号的实际数值（如 $-5$），**机器数**是符号数字化的编码表示（如用0表示正、1表示负），二者是"数与编码"的关系。对于 $n+1$ 位机器数，最高位通常作为**符号位**，其余 $n$ 位为数值位。**BCD码（Binary-Coded Decimal）**用若干位二进制表示一位十进制数码，常用的有：**8421码**（有权码，位权分别为8、4、2、1）、**余3码**（8421码加3，是一种无权偏置码）、**2421码**（有权码）与**格雷码**（无权码，相邻两个码只有一位不同，抗干扰）。

8421码是最常用的BCD码，例如十进制 $35$ 表示为 $0011\\,0101$。需要注意：8421码中的 $1010\\sim1111$ 六种组合是**非法码**；两个8421码相加时，若本位和超过9或产生进位，需加 $0110$（即6）修正。例如 $5 + 8 = 13$：$0101 + 1000 = 1101$（非法码），加 $0110$ 得 $0001\\,0011$，即十进制 $13$，正确。考试常考8421码的表示与加法修正规则，以及格雷码"相邻码仅一位不同"的特性。

**重点：** 8421码为有权码且只表示0~9；8421加法超过9需加6修正；格雷码相邻码仅一位不同；余3码是8421码加3的偏置码。

### 字符编码：ASCII与汉字编码
<!-- kp: kp2-1-3 -->

**ASCII码**用7位二进制编码128个常用字符，存储时通常占一个字节（最高位为0）。常见字符的ASCII码值必须熟记：数字'0'~'9'对应 $48\\sim57$（即 $0\\text{x}30\\sim0\\text{x}39$），大写字母'A'~'Z'对应 $65\\sim90$（$0\\text{x}41$ 起），小写字母'a'~'z'对应 $97\\sim122$（$0\\text{x}61$ 起），大小写字母相差32。考试常要求根据ASCII码确定字符、或计算一段字符串所需存储容量。

**汉字编码**包括**输入码**（外码，如拼音、五笔）、**国标码**（交换码，GB2312用两个字节）、**机内码**（内码）与**字形码**（点阵字模）。GB2312将汉字分为 $94$ 区 $\\times 94$ 位，每个汉字对应一个区位号；国标码两个字节的最高位都为0，为与ASCII码区分，**机内码把两个字节的最高位置为1**。存储量计算：一个 $16 \\times 16$ 点阵的汉字字模占 $16 \\times 16 / 8 = 32$ 字节。Unicode/UTF-8采用变长编码，并与ASCII码兼容。

**重点：** '0'=48、'A'=65、'a'=97，大小写相差32；汉字机内码 = 国标码字节最高位置1；$16 \\times 16$ 点阵汉字 = 32字节。

### 校验码：奇偶校验、海明码与CRC
<!-- kp: kp2-1-4 emphasis: key -->

**校验码**是在数据位之外附加冗余位（校验位），用于检测/纠正数据传输或存储中的错误。相关术语：**码距**（海明距离）是任意两个合法码字之间二进制位不同的最少位数。码距决定了检错纠错能力：**要检测 $d$ 位错，码距 $\\ge d+1$；要纠正 $d$ 位错，码距 $\\ge 2d+1$**。

- **奇偶校验码**：仅1位校验位，使整个码字中1的个数为奇数（奇校验）或偶数（偶校验）。只能检测**奇数个**位错，不能纠错，且检不出偶数个错误，适用于低误码率场景。
- **海明码**：能**纠正一位错、检测两位错**。设数据位 $k$、校验位 $r$，必须满足 $2^r \\ge k + r + 1$（如数据4位需校验3位：$2^3=8 \\ge 4+3+1$）。校验位放在 $2^{i-1}$ 位（第1、2、4、8…位），分组偶校验可定位出错位。2011年统考考过海明码大题（校验位个数、出错位定位）。
- **CRC循环冗余校验**：用生成多项式 $G(x)$ 对数据做模2除法取余得FCS，接收端再除一次，余数为0则无错。只能检错不能纠错，是链路层标准检错方式（详见计算机网络部分）。

**重点：**码距与检错/纠错能力的关系；海明码校验位个数 $2^r \\ge k+r+1$；奇偶校验只检奇数位错；CRC只检错不纠错。

### 数据存储与大小端：字节序与边界对齐
<!-- kp: kp2-1-5 emphasis: key -->

**大小端字节序**描述多字节数据（如int）在内存中的字节存放顺序：**大端方式**把数据最高有效字节（MSB）存放在低地址；**小端方式**把最低有效字节（LSB）存放在低地址。例如32位数 $0\\text{x}12345678$ 从地址0开始存放：大端为 $12\\ 34\\ 56\\ 78$，小端为 $78\\ 56\\ 34\\ 12$（x86是典型小端，网络字节序是规定的大端）。

**边界对齐**：按数据的自然边界存放——32位机器上int的起始地址应为4的倍数、short为2的倍数。对齐便于硬件一次取数（否则可能需两次访存拼接），代价是可能产生**填充字节（空隙）**。408常考：给定结构体成员顺序，计算大小时考虑对齐与填充；或判断多字节数据在某地址如何存放。

**重点：**大端MSB在低地址、小端LSB在低地址；x86小端、网络字节序大端；边界对齐产生填充字节，影响结构体大小计算。

## 定点数表示
<!-- sec: s2-2 -->

### 原码与反码
<!-- kp: kp2-2-1 emphasis: key -->

**原码**用最高位作符号位（0正1负），其余位为真值的绝对值。设机器字长为 $n+1$ 位（1位符号位 + $n$ 位数值位），定点整数原码的表示范围为 $[-(2^n - 1),\\ 2^n - 1]$。原码的优点是与真值对应直观、乘除运算实现简单；缺点是加减运算需先判断符号、比较绝对值，运算器实现复杂，而且**0有两种表示**（$+0 = 00\\cdots 0$，$-0 = 10\\cdots 0$），浪费了一个编码。8位原码示例：$+7 = 0000\\,0111$，$-7 = 1000\\,0111$。

**反码**的定义：正数的反码等于原码；**负数的反码是符号位不变、其余各位按位取反**。反码中0同样有二义性：$+0 = 0000\\,0000$，$-0 = 1111\\,1111$。反码的主要价值在于作为求补码的过渡编码。由于0的双重表示与"循环进位"等问题，实际机器几乎不直接用反码存储和运算数据，但它与补码、移码的转换关系是考试常考点（$[x]_{反}$ 末位加1即得 $[x]_{补}$）。

**重点：** 原码/反码的0都有二义性；负数反码 = 符号位不变其余取反；$n+1$ 位原码整数范围 $[-(2^n-1),\\ 2^n-1]$。

### 补码与移码
<!-- kp: kp2-2-2 emphasis: key -->

**补码**是为"减法变加法"而引入的编码。正数的补码等于其原码；**负数的补码是其反码末位加1**（即"取反加1"）。快速求法：从右向左找到第一个"1"，该1及其右边的各位保持不变，其左边（不含该1）各位取反，符号位保持为1。例如 $-5$ 的8位补码：原码 $1000\\,0101$，取反加1得 $1111\\,1011$。补码的重要性质：$[x+y]_{补} = [x]_{补} + [y]_{补} \\pmod{2^{n+1}}$，即**符号位一起参与运算**，运算器可统一处理加减法。补码中0只有唯一表示（全0），因此 $n+1$ 位补码范围 $[-2^n,\\ 2^n-1]$，比原码多表示一个最负数 $-2^n$（8位为 $-128$，即 $1000\\,0000$）。

**移码**的定义是：$[x]_{移} = x + 2^n$（$n+1$ 位时），等价于"补码的符号位取反"。移码常用于表示浮点数的**阶码**，因为它可以无符号地比较大小——移码数值大者真值大。$n+1$ 位移码与补码有相同的数值范围 $[-2^n,\\ 2^n-1]$，且0的移码唯一。补码与真值互转：$[x]_{补}$ 符号位为0时直接按权展开；符号位为1时，$x = [x]_{补} - 2^{n+1}$（按无符号数解释后减去 $2^{n+1}$），例如8位补码 $1000\\,0000$ → $128 - 256 = -128$。

**重点：** 负数补码"取反加1"；$n+1$ 位补码范围 $[-2^n,\\ 2^n-1]$，能多表示 $-2^n$；补码符号位参与运算；移码 = 补码符号位取反，用于阶码。

### 定点数表示范围与常见陷阱
<!-- kp: kp2-2-3 emphasis: difficult -->

设机器字长为 $n+1$ 位（含1位符号位），各编码的定点整数范围汇总如下：

| 编码 | 定点整数范围 |
|---|---|
| 原码 | $[-(2^n-1),\\ 2^n-1]$ |
| 反码 | $[-(2^n-1),\\ 2^n-1]$ |
| 补码 | $[-2^n,\\ 2^n-1]$ |
| 移码 | $[-2^n,\\ 2^n-1]$ |

对于**定点小数**（小数点固定在符号位之后，表示纯小数），原码/反码的范围为 $[-(1-2^{-n}),\\ 1-2^{-n}]$，补码的范围为 $[-1,\\ 1-2^{-n}]$，即补码能表示 $-1$（8位补码 $1000\\,0000$ 作为定点小数即 $-1$，作为定点整数则是 $-128$）。

常见陷阱：① 同一编码 $1000\\,0000$ 在"定点整数"与"定点小数"语境下的含义不同；② 无符号整数8位范围 $0\\sim255$，而补码8位范围 $-128\\sim127$，两者直接换算要小心；③ 判断溢出不能只看结果的符号位——两个正数相加结果为负必溢出；④ 负数求真值的快捷方法"按位取反加1"只对负数成立，正数补码就是原码；⑤ 补码 $-2^n$（如 $-128$）的表示：$0\\cdots0$ 取反得 $1\\cdots1$，加1得 $1000\\cdots0$，是在模 $2^{n+1}$ 意义下合法的编码。

**难点：** 区分定点整数与定点小数的范围；区分"进位"与"溢出"；补码 $-2^n$ 的编码只有符号位为1、数值位全0这一种形式。

## 定点数运算
<!-- sec: s2-3 -->

### 补码加减运算
<!-- kp: kp2-3-1 emphasis: key -->

补码加法的规则是"**连同符号位一起相加，按模 $2^{n+1}$ 取结果**"，即 $[x+y]_{补} = [x]_{补} + [y]_{补} \\pmod{2^{n+1}}$。减法可以转换为加法：$[x-y]_{补} = [x]_{补} + [-y]_{补}$，其中 $[-y]_{补}$ 等于 $[y]_{补}$ **连同符号位一起"取反加1"**。因此在硬件上，减法器只需在加法器的基础上对减数取反并将最低位的进位输入置为1即可实现。

示例：用8位补码计算 $100 - 50$。$[100]_{补} = 0110\\,0100$，$[50]_{补} = 0011\\,0010$，$[-50]_{补} = 1100\\,1110$。求和：$0110\\,0100 + 1100\\,1110 = 1\\,0011\\,0010$，丢弃最高位进位（模256），得 $0011\\,0010 = 50$，结果正确。注意：结果的符号位也由运算自然得到，无需额外处理；只要结果在表示范围内，得到的补码本身就对应正确的真值。若结果超出范围则产生**溢出**（见下一个知识点）。

**重点：** $[x-y]_{补} = [x]_{补} + [-y]_{补}$；取反加1要连同符号位；进位丢弃遵循模 $2^{n+1}$ 规则。

### 溢出判断
<!-- kp: kp2-3-2 emphasis: difficult -->

**溢出**指运算结果超出机器数所能表示的范围。只有**同号数相加**或**异号数相减**才可能溢出。常用的判断方法有三种：

1. **进位法**：最高有效位（数值位最高位）产生的进位 $C_{n-1}$ 与符号位产生的进位 $C_n$ **不同**时溢出，即溢出标志 $OF = C_n \\oplus C_{n-1}$。
2. **双符号位法（变形补码）**：用两个符号位（如 $00$ 正、$11$ 负）参与运算，结果双符号位为 $01$ 表示正溢出、$10$ 表示负溢出，为 $00$ 或 $11$ 表示无溢出（两个符号位相异即溢出，结果仍取高位符号位）。
3. **单一符号位法**：两操作数同号时，若结果符号与操作数符号不同则溢出。

示例：8位补码计算 $120 + 20 = 140$，超过最大值 $+127$：$0111\\,1000 + 0001\\,0100 = 1000\\,1100$，结果符号位为1而两个正数相加，正溢出。用变形补码表示：$00111\\,1000 + 00001\\,0100 = 01000\\,1100$，双符号位为 $01$，正溢出。

**难点：** 必须区分"进位"与"溢出"：最高位进位不一定溢出（如两个负数相加时最高位进位是正常现象）；溢出与否取决于符号位参与运算后的状态，硬件的判断依据是 $OF = C_n \\oplus C_{n-1}$。

### 移位运算：算术/逻辑/循环移位
<!-- kp: kp2-3-3 emphasis: key -->

移位运算分为**算术移位、逻辑移位和循环移位**三类。**算术移位**保持符号位不变，用于带符号数：右移时低位丢弃、高位（数值最高位）补**符号位**；左移时最低位补0，若最高数值位与符号位不同则左移可能溢出。算术左移一位相当于乘以2（未溢出时），算术右移一位相当于除以2（负数按"向下取整"方向进行，如 $-5 >> 1 = -3$）。**逻辑移位**不区分符号位，所有位整体移动，左移、右移都补0，用于无符号数（逻辑左移相当于乘2，逻辑右移相当于无符号除以2）。

**循环移位**分为**带进位循环移位**（连同进位标志CF一起循环，常用于多字长移位）与**不带进位循环移位**（在本字内部循环），多用于字节/位操作与密码算法。注意补码移位的特点：正数算术左移丢1可能溢出；负数补码算术右移一位相当于除以2（向负无穷取整）。示例：8位补码 $-4$（$1111\\,1100$）算术右移一位得 $1111\\,1110$，即 $-2$，符合除以2的语义。

**重点：** 算术右移补符号位、逻辑右移补0、循环移位保持位数不变；算术左移可能溢出；补码右移1位近似除以2（负数向负无穷取整）。

### 定点数乘除运算
<!-- kp: kp2-3-4 -->

乘除运算在统考中属**低频考点**（偶以选择题出现），了解原理与结论即可：

- **原码一位乘法**：符号位单独处理（异或得积的符号），绝对值逐位相乘——根据乘数最低位决定是否加被乘数，每步右移一位，重复 $n$ 次。
- **补码一位乘法（Booth算法）**：符号位参与运算，用乘数相邻两位之差（$y_{i+1}-y_i$）决定加/减被乘数，右移 $n+1$ 次，适合补码机器实现。
- **原码除法**：**恢复余数法**（余数为负时加回除数再移位）与**加减交替法**（不恢复，直接根据余数符号决定下一步加或减除数，速度快、实现简单）。
- **阵列乘法器/除法器**：用组合逻辑阵列并行完成乘法，速度快但硬件量大。

结论速记：乘法核心是"移位+加"，除法核心是"移位+减（比较）"；Booth算法符号位参与；加减交替除法优于恢复余数法。

**重点：**了解各算法的基本思想与"乘=移位加、除=移位减"的统一本质即可，低频考点不必深究电路细节。

## 浮点数表示
<!-- sec: s2-4 -->

### 浮点数的一般表示与规格化
<!-- kp: kp2-4-1 emphasis: key -->

浮点数（类似科学计数法）把一个数表示为：

$$N = M \\times r^{E}$$

其中 $M$ 为**尾数**（决定精度），$E$ 为**阶码**（决定表示范围），$r$ 为**基数**（通常为2）。机器中的浮点数格式一般分为三部分：**符号位 $S$**、**阶码**（带符号，常用移码/补码表示）与**尾数**（带符号，常用原码或补码表示）：

$$N = (-1)^S \\times M \\times 2^{E}$$

**规格化**是指尾数的最高数值位为1（对二进制），使尾数绝对值落在 $[1/2,\\ 1)$ 或 $[1,\\ 2)$ 之间。对补码表示的尾数，规格化的判断为：尾数为正时首位为1（$M \\in [1/2,1)$），尾数为负时首位为0（$M \\in [-1,-1/2)$）。规格化的意义在于：每个数有唯一的表示形式，且能最大限度利用尾数位数提高精度。若尾数不规格化，需要通过**左规**（尾数左移1位、阶码减1，用于尾数绝对值小于规格化范围）或**右规**（尾数右移1位、阶码加1，用于尾数溢出）来调整。

浮点数的**表示范围由阶码位数决定**（阶码越大可表示的数越大），**精度由尾数位数决定**（尾数位越多有效数字越多）。阶码常用**移码**表示以便于比较大小；0的浮点表示是阶码与尾数均为0（或特殊编码）。考试常考规格化条件与左规/右规的方向。

**重点：** $N = M \\times r^E$；规格化要求二进制尾数最高位为1；左规时尾数左移、阶码减1，右规时尾数右移、阶码加1；阶码定范围、尾数定精度。

### IEEE 754单精度浮点数
<!-- kp: kp2-4-2 emphasis: key -->

IEEE 754是目前最通用的浮点数标准。**单精度（float）**共32位：符号位 $S$ 占1位，阶码（exponent）字段占8位（采用偏置/移码表示，**偏置值 $bias = 127$**），尾数（fraction）字段占23位（采用**隐藏位**技术，规格化数省略整数位的1）。其数值为：

$$\\text{值} = (-1)^S \\times 1.f \\times 2^{E - 127}$$

其中 $E$ 是8位无符号阶码字段，真实指数为 $E - 127$。阶码字段 $E=0$ 表示**非规格化数**（真实指数为 $-126$，数值为 $0.f \\times 2^{-126}$，含0）；$E=255$ 表示**无穷大**（$f=0$）或**NaN**（$f \\ne 0$）；规格化数对应 $E = 1 \\sim 254$，真实指数范围 $-126 \\sim 127$。

十进制转单精度示例：$25.75$。$25.75 = 11001.11_2 = 1.100111_2 \\times 2^4$，符号位 $S=0$，真实指数 $=4$，阶码字段 $E = 4 + 127 = 131 = 1000\\,0011_2$，尾数 $f = 10011100000000000000000_2$。故机器表示为 \`0 10000011 10011100000000000000000\`，即 $0\\text{x}41CE0000$。反向转换（从编码恢复真值）同样高频：如给定 $0\\text{x}40490FDB$，可还原出近似 $\\pi$。

**重点：** 单精度格式 $(-1)^S \\times 1.f \\times 2^{E-127}$；阶码字段 $=$ 真实指数 $+127$；规格化尾数整数位1被隐含；$E$ 全0为非规格化（含0）、全1为无穷/NaN。

### IEEE 754双精度与表示范围
<!-- kp: kp2-4-3 emphasis: difficult -->

**双精度（double）**共64位：符号位1位，阶码字段11位（**偏置值 $bias = 1023$**），尾数字段52位（隐藏1位）。其数值为：

$$\\text{值} = (-1)^S \\times 1.f \\times 2^{E - 1023}$$

规格化数对应 $E = 1 \\sim 2046$，真实指数范围 $-1022 \\sim 1023$；$E=0$ 为非规格化，$E=2047$ 为无穷/NaN。相比单精度，双精度的指数范围更宽、尾数精度更高（约15~17位十进制有效数字，单精度约6~7位）。

IEEE 754单精度可表示的**范围**：最大规格化数约为 $1.11\\ldots1 \\times 2^{127} \\approx 3.4 \\times 10^{38}$；最小规格化数为 $1.0 \\times 2^{-126} \\approx 1.18 \\times 10^{-38}$；非规格化数可以表示比最小规格化数更小的正数，最小非规格化数为 $2^{-149}$。**上溢**指指数超出最大值（结果为无穷大），**下溢**指结果小于最小可表示数（趋向0或变为非规格化数，精度丢失）。设计选择：表示范围不够（易上溢）时应增加**阶码位数**；精度不够（有效数字不足）时应增加**尾数位数**。

**难点：** 隐藏位使规格化数的最小尾数为1；非规格化数用于填补0与最小规格化数之间的间隙；$E$ 字段全0与全1是保留状态，计算范围时要排除；区分上溢（指数超界）与下溢（精度丢失/趋向0）。

## 浮点数运算
<!-- sec: s2-5 -->

### 浮点数加减运算步骤
<!-- kp: kp2-5-1 emphasis: difficult -->

浮点数加减运算遵循"**对阶→尾数运算→规格化→舍入→溢出判断**"的流程（设尾数用补码、阶码用移码/补码表示）：

1. **对阶**：使两数阶码相同——**小阶向大阶看齐**，阶码小的尾数右移，右移位数等于阶码差 $\\Delta E$（尾数右移 $\\Delta E$ 位，阶码加 $\\Delta E$）。对阶只能右移小数阶的尾数，因为左移会破坏大数的高位造成溢出。
2. **尾数求和/差**：对阶后的尾数按定点补码规则做加（减）法。
3. **规格化**：若尾数溢出（如双符号位为 $01$ 或 $10$），**右规**一次（尾数右移1位、阶码加1）；若最高数值位不为1，则**左规**（尾数左移、阶码减1），直到满足规格化条件。
4. **舍入**：对规格化（右移）过程中被丢弃的低位按舍入规则处理。
5. **溢出判断**：规格化后若阶码超出表示范围，则发生溢出。

示例：$A = 0.1101 \\times 2^{3}$，$B = 0.1011 \\times 2^{2}$，求 $A+B$。对阶：$B$ 的尾数右移1位得 $0.01011 \\times 2^{3}$；尾数求和：$0.1101 + 0.01011 = 1.00101$，尾数溢出，右规：$0.100101 \\times 2^{4}$；最终结果 $0.100101 \\times 2^{4}$。整个过程体现了"小阶向大阶看齐"与"尾数运算后规格化"的规则。

**难点：** 对阶时"小阶向大阶看齐"的原因——大阶尾数左移会丢失高位而溢出；对阶与右规都会引入舍入误差；最后仍需检查阶码是否溢出。

### 舍入方法与浮点运算精度
<!-- kp: kp2-5-2 emphasis: key -->

规格化与对阶过程中尾数右移会丢弃低位，需要用舍入规则处理。常用方法有：**0舍1入法**：被丢弃的最高位为1则末位加1，为0则直接舍去；**就近舍入到偶数**（IEEE 754默认）：向最近的偶数舍入，平均误差最小；**恒置1法（截断/朝零舍入）**：无论丢弃位如何，尾数末位恒置1，实现简单但存在系统误差；IEEE 754还支持"向 $+\\infty$ 舍入"与"向 $-\\infty$ 舍入"（用于区间运算）。

浮点运算误差的来源有三个：**对阶丢位**、**右规丢位**与**舍入本身**。典型现象：$0.1 + 0.2 \\ne 0.3$——因为 $0.1$ 与 $0.2$ 的二进制表示都是无限循环小数，机器只能近似存储，累加与舍入后产生误差。因此**判断两个浮点数是否相等时不能直接用"=="，而应比较其差是否小于容差**。阶码运算的溢出判断与定点数不同：阶码常以移码/补码表示，用双符号位法判断溢出。

**重点：** "0舍1入"（丢弃最高位为1则末位+1）是最常考的舍入规则；就近舍入到偶数是IEEE 754默认；浮点相等的正确比较方法是容差比较。

## ALU与加法器
<!-- sec: s2-6 -->

### 串行进位加法器
<!-- kp: kp2-6-1 emphasis: key -->

一位**全加器（FA）**的输入为本位两个操作数 $A_i$、$B_i$ 与来自低位的进位 $C_{i-1}$，输出为本位和 $S_i$ 与进位 $C_i$，逻辑表达式为：

$$S_i = A_i \\oplus B_i \\oplus C_{i-1}, \\qquad C_i = A_i B_i + (A_i \\oplus B_i) C_{i-1}$$

**串行进位加法器**（行波进位加法器）由 $n$ 个一位全加器**串行连接**而成：第 $i$ 位的进位输出 $C_i$ 作为第 $i+1$ 位的进位输入 $C_{i+1}$。其优点是结构简单、所用元件少；缺点是**进位必须逐级传递**，高位的运算要等待所有低位的进位稳定之后才能完成，因此**速度慢**，关键路径（进位链）延迟与字长 $n$ 成正比，即 $O(n)$。对于32位或64位运算器，串行进位无法满足高速要求。考试常考：全加器逻辑表达式、串行进位的延迟分析与适用场景。

**重点：** 全加器进位公式 $C_i = A_i B_i + (A_i \\oplus B_i) C_{i-1}$；串行进位的关键路径是"从最低位到最高位的进位链"，速度慢。

### 并行进位加法器（超前进位）
<!-- kp: kp2-6-2 emphasis: difficult -->

**并行进位**（超前进位，Carry Look-ahead）的思想是让各高位进位**并行产生**，不再依赖相邻低位的进位。定义两个辅助信号：**生成函数** $G_i = A_i B_i$（本位直接产生进位）与**传递函数** $P_i = A_i \\oplus B_i$（本位传递低位的进位），则本位进位满足：

$$C_i = G_i + P_i \\cdot C_{i-1}$$

逐层展开（对4位超前进位）：

$$C_1 = G_1 + P_1 C_0$$
$$C_2 = G_2 + P_2 G_1 + P_2 P_1 C_0$$
$$C_3 = G_3 + P_3 G_2 + P_3 P_2 G_1 + P_3 P_2 P_1 C_0$$
$$C_4 = G_4 + P_4 G_3 + P_4 P_3 G_2 + P_4 P_3 P_2 G_1 + P_4 P_3 P_2 P_1 C_0$$

即每位进位只由操作数各位与最低进位 $C_0$ 直接决定，消除了逐级延迟，进位产生时间与字长基本无关。但位数增多时，进位表达式的与项数按指数增长、电路复杂度急剧上升，因此实际中采用**分组方案**：如把32/64位分成若干4位一组，**组内超前进位、组间串行**，或组间也采用超前进位（二级CLA），在速度与复杂度之间折中。

**难点：** 掌握 $C_i = G_i + P_i C_{i-1}$ 及4位CLA进位公式；理解"以硬件复杂度换取速度"的思想；组内并行、组间串行/并行的两种分组方案。

### ALU的功能与组成
<!-- kp: kp2-6-3 -->

**ALU（Arithmetic Logic Unit，算术逻辑单元）**是运算器的核心部件，完成**算术运算**（加、减、加1、减1、乘除辅助等）与**逻辑运算**（与、或、非、异或、移位等）。ALU的输入通常是一个操作数、另一个操作数以及由控制器（操作码译码结果）产生的**功能选择控制信号**，输出为运算结果与状态标志：**ZF**（零标志，结果为0）、**SF**（符号标志，最高位）、**OF**（溢出标志，$C_n \\oplus C_{n-1}$）、**CF**（进位/借位标志，最高位进位）。ALU内部主要由加法器（串行或超前进位）、移位器及多路选择逻辑构成。

通过功能选择信号可选择不同的运算，例如4位功能选择信号可组合出16种操作。标志位在**条件转移**指令、溢出判断与状态检测中起关键作用。运算器除ALU外还包括通用寄存器、累加器、乘商寄存器、暂存寄存器与**状态寄存器（PSW）**等。408考试中ALU部分常与加法器进位方式、标志位含义结合出选择题，注意区分：CF来自最高位进位，OF来自符号位与次高位的进位异或。

**重点：** ALU基于加法器实现算术与逻辑运算；四大标志ZF/SF/OF/CF的定义（$OF = C_n \\oplus C_{n-1}$，$CF = C_n$）。
`;export{e as default};