var e=`---
id: ch7
title: 输入/输出系统
order: 7
---

## I/O接口
<!-- sec: s7-1 -->

### I/O接口的功能与组成
<!-- kp: kp7-1-1 emphasis: key -->

I/O接口（I/O接口电路）是主机与I/O设备之间的桥梁，其主要功能：① **地址译码与设备选择**：根据地址总线上的设备号选中本接口；② **数据缓冲**：用数据缓冲寄存器暂存数据，匹配CPU与设备之间的速度差异；③ **命令传递与状态报告**：CPU向控制寄存器写入控制命令，接口通过状态寄存器向CPU报告设备状态（忙/就绪、出错等）；④ **信号转换**：电平转换、串并转换、码制转换（把设备信号转换为CPU能识别的形式）；⑤ **中断与DMA控制**：向CPU发出中断请求、配合DMA控制器完成传送；⑥ **定时协调**：与时序配合实现CPU与设备的同步。

接口的组成：设备选择电路（地址译码器）、数据缓冲寄存器（DBR/数据端口）、状态寄存器、控制寄存器、中断控制逻辑、时序控制逻辑等。按数据传输方式分为并行接口与串行接口；按功能分为通用接口与专用接口；按数据传送的控制方式分为程序查询接口、中断接口与DMA接口。考试常考接口五大功能的选择题与各寄存器的作用。

**重点：** 接口核心功能：数据缓冲（速度匹配）、设备选择（地址译码）、状态与命令传递、中断/DMA控制、信号转换；接口中通常含数据/状态/控制三个端口。

### 端口编址方式：统一编址与独立编址
<!-- kp: kp7-1-2 emphasis: key -->

CPU通过访问接口中的**端口（寄存器）**来访问I/O设备。**端口编址**有两种方式：

1. **统一编址（存储器映射I/O）**：把I/O端口当作主存单元的一部分，划出部分主存地址空间给I/O。CPU用**访存指令**访问I/O端口，无需专门的I/O指令。优点：指令丰富、可直接对端口做运算、无需额外指令；缺点：**占用主存地址空间**、译码较复杂。

2. **独立编址（I/O映射I/O）**：I/O端口与主存地址空间**独立编址**，需要专门的**I/O指令**（如x86的IN/OUT）访问。优点：**不占用主存地址空间**、译码简单；缺点：需要专用I/O指令、指令功能有限（一般不能直接对端口做算术逻辑运算）。

考试要点：统一编址用访存指令访问端口、独立编址用专用I/O指令访问端口；统一编址下任何访存指令均可访问I/O端口，独立编址下只有I/O指令能访问端口。接口中的三类端口：**数据端口**（数据缓冲寄存器）、**状态端口**（状态寄存器）、**控制端口**（控制寄存器）。

**重点：** 统一编址（存储器映射）：端口占主存空间、用访存指令；独立编址：端口独立空间、用专用I/O指令；二者不可混用访问方式。

## I/O控制方式
<!-- sec: s7-2 -->

### I/O方式的分类与程序查询方式
<!-- kp: kp7-2-1 emphasis: key -->

CPU与I/O设备之间数据传送的基本方式有四类：**程序查询方式、程序中断方式、DMA方式、通道方式**，控制复杂度和效率依次提高。**程序查询方式**：CPU不断用程序"测试—传送"——先读状态端口判断设备是否就绪，就绪则执行I/O指令传送一个数据，否则继续等待循环，直到传送完成。其本质是**忙等待（轮询）**：传送期间CPU一直被占用，不能执行其他程序，**CPU与I/O设备串行工作**，CPU利用率极低；但控制简单、无需额外硬件，适用于慢速简单设备（如拨码开关、简单外设）。

查询方式的效率分析：设CPU频率 $f$、每查询一次需 $k$ 个时钟周期、设备每 $t$ 秒产生一个数据，则CPU用于查询的时间占比（忙率）可计算。408计算题常考：给定数据产生速率与每次查询/传送耗时，求CPU忙占比或查询方式能达到的**最大数据传输率**——最大传输率 $= \\frac{1}{\\text{每传送一个数据的总耗时}}$。

**重点：** 程序查询：CPU忙等待轮询、串行低效、实现简单；适合低速设备；最大数据率受"查询＋传送"总耗时限制。

### 程序中断方式
<!-- kp: kp7-2-2 emphasis: key -->

**程序中断方式**：当I/O设备准备好数据（或发生故障）时，向CPU发出**中断请求**；CPU在执行完**当前指令**之后响应中断，转入中断服务程序处理数据传输，处理完毕返回断点继续执行原程序。这样，在设备准备期间CPU可以执行其他程序，实现**CPU与I/O设备的并行工作**，显著提高了CPU利用率。中断响应的时机是"当前指令执行结束后"，且满足响应条件（开中断、无更高优先级请求）。

中断方式的处理流程：设备就绪 → 发中断请求 → CPU在当前指令结束后响应 → （多个请求时）判优 → **关中断** → **保存断点**（PC压栈）→ 识别中断源（软件查询或中断向量）→ 转中断服务程序 → 保护现场 → 执行服务（传送数据）→ 恢复现场 → **开中断** → 返回断点继续执行。与程序查询相比，中断方式不需要CPU忙等待，但每次传送仍需CPU执行中断服务程序（保护/恢复现场、取数、送数），开销比DMA大。适用于中速设备（键盘、打印机、终端）。

**重点：** 中断方式使CPU与设备并行（设备准备期间CPU执行其他程序）；响应中断在"当前指令执行完毕后"；传送数据仍需CPU执行中断服务程序。

### DMA方式与通道方式
<!-- kp: kp7-2-3 emphasis: key -->

**DMA（直接存储器存取）方式**：由**DMA控制器**接管主存与I/O设备之间的数据传送，数据直接在设备与主存之间传送（不需要CPU逐字执行指令），CPU仅在**传送开始**（预处理，初始化DMA控制器）与**传送结束**（后处理中断）时介入。传送期间CPU可以继续执行其他指令（仅在访问主存的总线周期发生冲突时让出总线），因此效率很高，适用于高速设备（磁盘、网卡）的大块数据传送。**通道方式**：通道是专门管理I/O的**I/O处理机**，能执行**通道程序**、管理多台设备的I/O操作，CPU只需一条"启动I/O"指令，通道便独立完成数据传送与设备管理，比DMA更"智能"。

四种I/O方式对CPU的占用程度（效率由低到高、控制由简到繁）：**程序查询 < 程序中断 < DMA < 通道**。考试常考四种方式的对比：程序查询与中断由CPU逐字传送；DMA由DMA控制器硬件传送；通道能执行通道程序。DMA控制器的工作细节见后续知识点。

**重点：** DMA由DMA控制器直接完成主存与设备间的数据传送，CPU不逐字参与；通道能执行通道程序、管理多台设备；效率排序：查询<中断<DMA<通道。

## 中断系统
<!-- sec: s7-3 -->

### 中断的处理过程
<!-- kp: kp7-3-1 emphasis: key -->

中断处理的全过程分为五个阶段：**① 中断请求**——设备就绪/故障时向CPU发出中断请求（INTR）；**② 中断判优**——多个请求同时到达时按优先级确定响应顺序；**③ 中断响应**——CPU在当前指令结束后，由硬件自动完成"关中断、保存断点、识别中断源、转中断服务程序入口"；**④ 中断处理**——执行中断服务程序（保护现场→处理→恢复现场）；**⑤ 中断返回**——恢复PC（断点）、开中断、返回原程序继续执行。

其中**中断响应由硬件自动完成**，这一步对应一组微操作，称为**中断隐指令**（它不是真正的机器指令）：① **关中断**（禁止再响应新的中断，保护现场与断点不被破坏）；② **保存断点**（把PC内容压入堆栈或存入固定单元）；③ **形成中断服务程序的入口地址**（查询法或向量法）。而**中断处理（服务程序主体）由软件完成**：保护现场（保存通用寄存器等内容）、执行具体服务、恢复现场。考试常考：响应中断的时机（当前指令执行结束后）、隐指令完成的三个动作、保护/恢复现场由软件实现。

**重点：** 中断隐指令（硬件）完成：关中断、保存断点、形成中断服务程序入口；响应时机是"当前指令执行完毕之后"；保护与恢复现场由中断服务程序（软件）完成。

### 中断向量与中断向量表
<!-- kp: kp7-3-2 emphasis: key -->

识别中断源（确定是哪个设备请求、其服务程序入口在哪）有两种方式：**查询方式**——CPU依次查询各设备的状态寄存器，软件逐项比较，速度慢；**向量方式**——硬件为每个中断源分配一个**中断类型号（中断向量号）**，CPU根据类型号查**中断向量表**得到该中断服务程序的**入口地址**。

**中断向量**是中断服务程序的入口地址；**中断向量表**存放在主存固定区域，每一项（表项）存放一个中断服务程序的入口地址；**中断类型号**是中断向量表的**索引**。中断响应时，硬件由类型号取出向量表项，把入口地址装入PC，从而转入服务程序。示例（x86）：中断类型号N对应的向量表项地址 $=$ 向量表基址 $+ N \\times 4$（每项4字节）。向量方式由硬件完成中断源识别，速度比查询方式快。考试常考：中断向量与中断类型号的区别、向量表项的内容、入口地址的获取过程。

**重点：** 中断向量 = 中断服务程序入口地址；类型号 → 查向量表 → 入口地址；向量方式由硬件识别中断源，查询方式由软件识别。

### 多重中断与中断屏蔽
<!-- kp: kp7-3-3 emphasis: difficult -->

**多重中断（中断嵌套）**指CPU正在处理某个中断时又响应更高级别的中断请求，允许"中断之中断"。实现条件：① 中断服务程序中要**开中断**（重新置IF=1）才能再次响应；② 有**优先级**机制，仅当新请求的优先级**高于**当前正在处理的中断时才响应；③ 有足够的断点保存能力（多层嵌套时断点与现场用**堆栈**保存）。**单重中断**不允许嵌套：响应中断后立即关中断，直到服务结束才开中断返回，期间其他请求只能排队等待。

**中断屏蔽**与优先级设置：用**中断屏蔽字**控制哪些中断暂时被禁止。CPU中有**中断屏蔽寄存器（IMR）**，某位为1表示屏蔽对应的中断请求。各中断源的**优先级**由屏蔽字编码决定：处理某级中断时，屏蔽字中比它**高或相等**的优先级对应的位为0（不屏蔽），**低优先级**对应的位为1（屏蔽），这样高优先级可打断当前中断（嵌套），低优先级不能。判优可用硬件排队器实现。注意区分**响应优先级**（硬件判优决定谁先被响应）与**处理优先级**（屏蔽字设置决定嵌套顺序）——两者可以不同。

**难点：** 多重中断需要"保护现场后开中断"；屏蔽字编码规则（屏蔽本级及更低级、不屏蔽更高级）；响应优先级与处理优先级的区别；嵌套层数受堆栈深度限制。

### 中断的分类：内中断与外中断
<!-- kp: kp7-3-4 emphasis: key -->

按中断来源，中断分为两大类：**内中断（异常/例外，Exception）**——由CPU内部执行指令引起，与当前指令同步发生，如除零、溢出、缺页、非法指令、系统调用（陷入）；**外中断（Interrupt）**——由CPU外部设备信号引起，与当前指令异步，如I/O完成、时钟、键盘输入。注意"中断"一词广义含两者，狭义仅指外中断。

外中断又按能否被屏蔽分为**可屏蔽中断**（通过IF标志或屏蔽字禁止响应，如常规I/O中断）与**不可屏蔽中断**（NMI，必须立即响应，如电源故障、内存校验错）。内中断一般不可屏蔽。异常内部还细分为故障（Fault）、陷阱（Trap）与终止（Abort）三类，该细节在操作系统课程中展开；此处只需掌握"内/外、可屏蔽/不可屏蔽"两对分类与典型例子。

**重点：**内中断=指令引起的同步事件（除零、缺页、trap），外中断=设备引起的异步事件（时钟、I/O）；可屏蔽与否取决于CPU的屏蔽机制（IF标志/屏蔽字）；NMI典型例子：电源故障。

## DMA方式
<!-- sec: s7-4 -->

### DMA的工作原理与传送过程
<!-- kp: kp7-4-1 emphasis: key -->

DMA控制器的主要寄存器：**主存地址寄存器（MAR/AR）**（存放主存起始地址）、**传送长度计数器（WC）**（待传送的字数/块数）、**设备地址寄存器（DAR）**、**数据缓冲寄存器（DBR）**与**控制/状态寄存器**。DMA传送分三个阶段：

1. **预处理（初始化）**：CPU向DMA控制器写入主存起始地址、设备地址、传送长度与方向，并启动设备；
2. **数据传送**：DMA控制器接管总线，在设备与主存之间按块（或逐个字）直接传送数据，**不需要CPU逐字干预**；
3. **后处理**：长度计数器减到0时，DMA控制器向CPU发出**中断请求**，CPU执行中断服务程序完成善后处理（校验、处理结束状态等）。

DMA传送期间CPU与主存总线的使用权有三种分配方式：**停止CPU访存**（DMA独占主存，传送期间CPU暂停访存，效率低）；**周期挪用（周期窃取）**（DMA在CPU不访存的周期内窃取一个周期进行传送，对CPU影响最小，最常用）；**交替访存**（CPU与DMA分时轮流使用主存，适合主存周期很短的情况）。注意：DMA只占用**总线**，不占用CPU——传送期间CPU可以继续执行指令，只是访存被暂时延迟。

**重点：** DMA三阶段（预处理/数据传送/后处理）；周期挪用最常用、对CPU影响最小；DMA占总线不占CPU；启动与结束仍需CPU参与。

### DMA与中断方式的区别
<!-- kp: kp7-4-2 emphasis: key -->

DMA与程序中断方式的本质区别：

| 比较项 | 程序中断 | DMA |
|---|---|---|
| 数据传送 | CPU执行中断服务程序逐字传送 | DMA控制器（硬件）直接传送 |
| 传送单位 | 一个数据（一个字/字节） | 数据块（批量） |
| 响应时机 | 当前指令执行结束后 | 任一时刻（周期挪用/总线空闲） |
| CPU干预程度 | 每次传送都要CPU中断服务 | 仅预处理与后处理需要CPU |
| 并行性 | 设备准备期间CPU可执行其他程序 | CPU与设备高度并行（仅总线冲突） |
| 适用设备 | 中低速设备（键盘、打印机） | 高速设备（磁盘、网卡） |
| 其他功能 | 可处理各类事件（含故障） | 主要用于批量数据传送（故障处理仍走中断） |

**中断请求在DMA中的作用**：DMA传送完成时向CPU发"传送完成"中断，CPU做后处理——因此DMA中也包含中断机制，但**数据传送本身不经过中断服务程序**。考试常考：哪些工作由DMA控制器完成、哪些仍需CPU完成；DMA请求与中断请求的触发条件（DMA请求频率高、用于整块传送，中断请求每传一个数据/事件触发一次）。

**重点：** DMA传送单位是"块"、中断传送单位是"字"；DMA仅开始与结束时需要CPU，中断每次传送都需要CPU；DMA由硬件完成数据传送。

### 通道方式
<!-- kp: kp7-4-3 -->

**通道（Channel）**是专门管理I/O的部件，本质上是一台能执行**通道程序**的小型处理机。CPU用一条"**启动I/O**"指令把通道程序的首地址交给通道，之后通道独立地按通道程序控制各台设备的传送，完成后向CPU发出中断。通道与CPU**并行工作**，使CPU完全从I/O管理中解放出来。与DMA相比，DMA控制器比较"笨"（只能做连续传送，每传送一段就需CPU重新初始化），而通道能执行复杂通道程序（选择设备、变换地址、条件控制等），可管理多台设备。

通道的类型：**字节多路通道**——分时共享连接多台低速设备，按字节轮流传送；**选择通道**——每次只服务一台高速设备，独占直到整块数据传完；**数组多路通道**——以数据块为单位分时服务多台高速设备。通道方式常见于大型机与高速多设备环境，现代微机多采用DMA与中断的组合。408对该知识点以概念与分类考查为主。

**重点：** 通道 = 可执行通道程序的I/O处理器，比DMA更智能；字节多路通道（多台低速、按字节分时）、选择通道（单台高速、独占）、数组多路通道（多台高速、按块分时）。

## 外存储器
<!-- sec: s7-5 -->

### 磁盘存储器
<!-- kp: kp7-5-1 emphasis: key -->

磁盘是磁表面存储器，由盘片、磁头、主轴等组成。盘面上划分**磁道**（同心圆），磁道又划分为**扇区**（最小读写单位，常512B/4KB）；同一垂直位置的所有磁道构成**柱面**。常用指标与公式：

- 磁盘容量 $= \\text{盘面数} \\times \\text{每面磁道数} \\times \\text{每道扇区数} \\times \\text{每扇区字节数}$；
- **平均存取时间 = 寻道时间 + 旋转延迟 + 传输时间**，其中平均旋转延迟 $=$ 转半圈的时间 $= 60/(2r)$（$r$ 为转速 r/min）。例：7200转/分磁盘的平均旋转延迟 $= 60/(2 \\times 7200) \\approx 4.17$ ms；
- **数据传输率**（持续传输率）$=$ 每道字节数 $\\times$ 转速/60（或 每扇区字节数 $\\times$ 每圈扇区数 $\\times$ 转速）。

磁盘按柱面/磁道/扇区编址，访问以扇区为单位；顺序读写（连续磁道）效率远高于随机访问。磁盘调度（FCFS/SSTF/SCAN等）详见操作系统课程。408常考：容量与平均存取时间的计算、扇区地址换算。

**重点：**容量公式与平均存取时间三部分；平均旋转延迟=半圈时间；计算题注意转速单位换算（r/min与ms）。

### RAID与SSD
<!-- kp: kp7-5-2 -->

**RAID（廉价磁盘冗余阵列）**把多块磁盘组成一个逻辑磁盘，提高容量、速度与可靠性。常用级别：**RAID 0**（条带化，无冗余，速度最快但坏一块全丢）；**RAID 1**（镜像，两块互为备份，可靠但容量减半）；**RAID 5**（条带+分布式奇偶校验，坏一块可恢复，容量损失1/n）；**RAID 10**（先镜像再条带）。**中低频考点**，重点记各级别的冗余方式与容量利用率。

**SSD（固态硬盘）**以闪存（Flash）为存储介质，无机械部件，随机访问快、无寻道与旋转延迟，但存在**写寿命限制**（块擦写次数有限），通过**磨损均衡**（均衡各块的擦写次数）与**闪存转换层FTL**（地址映射、垃圾回收）管理。统考2021年首次涉及SSD，属**中频考点**，理解"SSD为什么随机读快、为什么不能直接覆盖写"即可。

**重点：**RAID 0/1/5/10的冗余思想；SSD无机械延迟、写寿命有限、靠磨损均衡延长寿命。
`;export{e as default};