var e=`---
id: ch1
title: 绪论
order: 1
---

## 数据结构的基本概念
<!-- sec: ch1-s1 -->

### 数据、数据元素、数据项与数据对象
<!-- kp: ch1-s1-k1 emphasis: key -->

## 基本术语

**数据（Data）**是信息的载体，是描述客观事物属性的数值、字符以及所有能输入到计算机中并被计算机程序识别和处理的符号的集合。**数据元素（Data Element）**是数据的基本单位，通常作为一个整体进行考虑和处理，也称为**元素、记录或结点**。例如学生信息表中每一个学生的记录就是一个数据元素。**数据项（Data Item）**是构成数据元素的不可分割的最小单位，一个数据元素可由若干个数据项组成，如学生的"学号""姓名"等。**数据对象（Data Object）**是具有相同性质的数据元素的集合，是数据的一个子集。

## 相互关系

四者之间的关系是包含关系：**数据 > 数据对象 > 数据元素 > 数据项**。以一张学生成绩表为例：整张表是数据；表中学号、姓名、各科成绩均相同的记录集合构成数据对象；表中每一行是一个数据元素；每一行中的"姓名"列是一个数据项。**注意**：一个数据元素可以由多个数据项组成，数据项又分为"初等项"和"组合项"，例如"学生信息"可组合"学号""姓名""年龄"等数据项。

**重点：**区分数据元素（基本单位）与数据项（最小单位）是408单选题的常考点，需结合具体例子理解四者的包含层次。

### 逻辑结构与存储结构
<!-- kp: ch1-s1-k2 emphasis: key -->

## 逻辑结构

**逻辑结构**是指数据元素之间的逻辑关系，与数据在计算机中的存储位置无关。逻辑结构分为两大类：**线性结构**（如线性表、栈、队列、串）与**非线性结构**（如树、图、集合）。具体而言包括四种基本类型：

1. **集合**：数据元素之间除"同属于一个集合"外无其他关系；
2. **线性结构**：数据元素之间存在一对一的关系，如线性表、栈、队列；
3. **树形结构**：存在一对多的层次关系，如树、二叉树；
4. **图状结构（网状结构）**：存在多对多的关系，如图。

## 存储结构

**存储结构（物理结构）**是指数据结构在计算机中的表示（映像），包括**顺序存储、链式存储、索引存储、散列存储**四种。顺序存储把逻辑上相邻的元素存储在物理位置上也相邻的存储单元中；链式存储不要求逻辑上相邻的元素物理上也相邻，借助指示元素存储地址的指针表示元素之间的逻辑关系；索引存储在存储元素信息的同时建立附加的索引表；散列存储根据元素的关键字直接计算出该元素的存储地址。

**重点：**逻辑结构独立于计算机、存储结构依赖于计算机；同一逻辑结构可有多种存储结构。例如线性表既可用顺序表存储，也可用单链表存储。判断题目中给出的"逻辑结构"还是"存储结构"是408高频考点。

### 数据运算与抽象数据类型
<!-- kp: ch1-s1-k3 -->

## 数据运算

**数据运算**包括运算的定义和实现两部分。运算的定义是针对逻辑结构的，指出运算的功能（如线性表的"在第i个位置插入元素x"）；运算的实现是针对存储结构的，指出运算的具体操作步骤。定义与实现相分离是数据结构课程的重要思想：逻辑结构、存储结构、数据运算三者合称为**数据结构的三要素**。

## 抽象数据类型

**抽象数据类型（ADT, Abstract Data Type）**是指一个数学模型以及定义在该模型上的一组操作，用三元组表示为：

$$ADT = (D, S, P)$$

其中 D 是数据对象，S 是 D 上的关系集，P 是 D 上的基本操作集。抽象数据类型定义了数据的逻辑结构和运算而不涉及具体实现，体现了**封装和信息隐蔽**的思想。例如"栈"这一 ADT，只关心其"后进先出"的特性及入栈、出栈等操作，不关心底层用数组还是链表实现。

**难点：**区分"运算的定义"与"运算的实现"，前者面向逻辑结构、后者面向存储结构。408中常以选择题形式考查三要素与 ADT 的特征。

## 算法与算法的评价
<!-- sec: ch1-s2 -->

### 算法的定义与五大特性
<!-- kp: ch1-s2-k1 emphasis: key -->

## 算法的定义

**算法（Algorithm）**是对特定问题求解步骤的一种描述，它是指令的有限序列。一个算法必须满足以下**五大特性**：

1. **有穷性**：一个算法必须总是在执行有穷步之后结束，且每一步都可在有穷时间内完成；
2. **确定性**：算法中每条指令必须有确切的含义，对于相同的输入只能得出相同的输出，不存在二义性；
3. **可行性**：算法中描述的操作都可以通过已经实现的基本运算执行有限次来实现；
4. **输入**：一个算法有零个或多个输入，这些输入取自某个特定的对象的集合；
5. **输出**：一个算法有一个或多个输出，这些输出是同输入有某种特定关系的量。

## 算法与程序的区别

有穷性是算法与程序最重要的区别：**程序不一定满足有穷性**，例如操作系统只要不关机就一直运行，它属于程序而不是算法。另外，算法可以用自然语言、流程图、伪代码或程序语言描述，其"确定性"要求每条指令含义唯一、无歧义。

**重点：**五特性中"有穷性"与"确定性"是考查最多的两项，常以反例命题，如"一个永不终止的循环程序是不是算法？"答案是否定的。

### 算法好坏的评判标准
<!-- kp: ch1-s2-k2 -->

## 评价标准

评价一个算法优劣通常从以下四个方面衡量：

1. **正确性**：算法应能正确解决求解问题，满足问题对输入输出的要求；
2. **可读性**：算法应具有良好的可读性，便于理解和修改（算法设计首要考虑的是正确性与可读性）；
3. **健壮性**：算法对非法输入能做出相应的处理，而不是产生异常或莫名崩溃；
4. **高效率与低存储量需求**：即运行时间短（时间复杂度低）、占用内存少（空间复杂度小）。

## 效率度量方法

算法的效率可以通过**事后统计**和**事前分析**两种方法度量。事后统计依赖具体运行的机器、编译器与输入数据，结果不具可比性；事前分析主要依据**时间复杂度**和**空间复杂度**进行估算，不依赖机器环境，是408考核的重点。算法效率的度量只关心**数量级**（渐近复杂度），通常忽略低阶项和常系数，例如 $$T(n)=3n^2+2n+1$$ 记作 $$O(n^2)$$。

**重点：**408中"分析时间复杂度/空间复杂度"题目均采用事前分析，考查算法的渐近复杂度，无需实际运行程序。

## 时间复杂度
<!-- sec: ch1-s3 -->

### 时间复杂度的概念与渐近记号
<!-- kp: ch1-s3-k1 emphasis: key -->

## 基本概念

**时间复杂度**是指算法中基本运算执行次数的数量级。设算法中基本运算的频度为 $$f(n)$$（n 为问题规模），则算法的时间复杂度记为 $$T(n)=O(f(n))$$。基本运算是指最深层循环内的语句（如赋值、比较、自增等），其执行次数决定了算法的运行时间。计算时间复杂度时只保留最高阶项，并去掉其系数，例如 $$f(n)=5n^3+3n^2$$ 时 $$T(n)=O(n^3)$$。

## 三种渐近记号

- **大 O 记号（渐进上界）**：$$T(n)=O(g(n))$$ 表示存在正常数 c 和 $$n_0$$，使得当 $$n \\ge n_0$$ 时 $$T(n) \\le c \\cdot g(n)$$；
- **大 Ω 记号（渐进下界）**：$$T(n)=\\Omega(g(n))$$ 表示当 n 充分大时 $$T(n) \\ge c \\cdot g(n)$$；
- **大 Θ 记号（渐进紧确界）**：$$T(n)=\\Theta(g(n))$$ 表示同时满足上界与下界，即 $$c_1g(n) \\le T(n) \\le c_2g(n)$$。

**重点：**408中绝大多数题目要求的是大 O 记号的渐进上界。最坏情况、平均情况与最好情况中，通常关注**最坏时间复杂度**，部分题目会考查平均时间复杂度。

### 常见复杂度量级与分析方法
<!-- kp: ch1-s3-k2 emphasis: key -->

## 常见量级排序

常见的时间复杂度按数量级递增排列为：

$$O(1) < O(\\log_2 n) < O(n) < O(n\\log_2 n) < O(n^2) < O(n^3) < O(2^n) < O(n!)$$

其中 $$O(1)$$ 为常数阶，$$O(\\log_2 n)$$ 为对数阶，$$O(n)$$ 为线性阶，$$O(n\\log_2 n)$$ 为线性对数阶，$$O(n^2)$$ 为平方阶，$$O(2^n)$$ 与 $$O(n!)$$ 为指数/阶乘阶，属于不可接受的复杂度。

## 分析方法

1. **单层循环**：设循环变量从 1 加到 n，每轮执行常数次操作，则 $$T(n)=O(n)$$；
2. **多层嵌套循环**：相乘，如双重循环内层全遍历时 $$T(n)=O(n^2)$$；
3. **循环变量呈倍增减**：循环变量每次乘以 2，执行次数为 $$O(\\log_2 n)$$；
4. **递归**：写出递推关系求解，如斐波那契递归 $$T(n)=T(n-1)+T(n-2)+O(1)$$，其时间复杂度约为 $$O(2^n)$$。

典型例题：

\`\`\`c
int count = 0;
for (int i = 1; i <= n; i *= 2)
    for (int j = 1; j <= i; j++)
        count++;
\`\`\`

内层循环次数随 i 变化，总次数为 $$1+2+4+\\cdots+2^k$$，其中 $$2^k \\le n$$，故总次数约为 $$2^{k+1}-1=O(n)$$。

**难点：**对于"循环次数不固定"的嵌套循环，不能简单地相乘，必须按每轮实际执行次数求和；"i*=2"或"i/=2"形式的循环应联想到对数阶。

## 空间复杂度
<!-- sec: ch1-s4 -->

### 空间复杂度的概念
<!-- kp: ch1-s4-k1 emphasis: key -->

## 基本概念

**空间复杂度（Space Complexity）**是指算法在运行过程中**除输入数据所占空间之外**所需的辅助存储空间的大小，记作 $$S(n)=O(f(n))$$。算法运行所需要的空间包括：存储程序本身的空间、存储输入数据的空间、以及算法执行过程中所需的辅助空间。前两者与算法无关，因此空间复杂度主要考察**额外开辟的辅助空间**。

若算法执行时所需辅助空间相对于问题规模 n 而言是常数，则称该算法为**原地工作（In-place）**，其空间复杂度为 $$O(1)$$。例如交换两个变量的值：

\`\`\`c
void swap(int &a, int &b) {
    int temp = a;  // 只使用常数个辅助单元
    a = b;
    b = temp;
}
\`\`\`

该操作只使用了一个辅助变量 temp，空间复杂度为 $$O(1)$$。

**重点：**判断"是否原地工作"只需看算法是否额外开辟了与 n 同量级的存储空间。例如归并排序需要额外 O(n) 的辅助数组，故其空间复杂度为 $$O(n)$$，不是原地算法。

### 递归程序的空间复杂度
<!-- kp: ch1-s4-k2 -->

## 递归调用栈

递归程序每次递归调用都会在系统调用栈中分配一块栈帧，用于保存局部变量、参数和返回地址。因此递归算法的空间复杂度等于**递归调用的最大深度**乘以每层栈帧所需空间。例如计算阶乘的递归程序：

\`\`\`c
int fact(int n) {
    if (n <= 1) return 1;
    return n * fact(n - 1);
}
\`\`\`

递归深度为 n，每层栈帧空间为常数，故 $$S(n)=O(n)$$。

## 典型递归复杂度

- 二叉树的前/中/后序遍历（递归版）：递归深度为树高 h，最坏情况下（单支树）h=n，空间复杂度为 $$O(n)$$；
- 折半查找的递归实现：深度为 $$O(\\log_2 n)$$，空间复杂度为 $$O(\\log_2 n)$$；
- 斐波那契数列的朴素递归：虽然时间复杂度高达 $$O(2^n)$$，但递归深度不超过 n，空间复杂度为 $$O(n)$$。

**难点：**空间复杂度看"最大递归深度"而非"调用总次数"。区分时间复杂度（看基本运算执行次数）与空间复杂度（看辅助空间大小）是408选择题的经典陷阱。

## 复杂度分析综合例题
<!-- sec: ch1-s5 -->

### 循环结构的经典复杂度例题
<!-- kp: ch1-s5-k1 -->

## 例题一：i 随 i*=2 增长

\`\`\`c
int i = 1;
while (i <= n) {
    i = i * 2;
}
\`\`\`

设执行次数为 k，则 $$2^k \\le n$$，解得 $$k \\le \\log_2 n$$，故时间复杂度为 $$O(\\log_2 n)$$。若循环体改为 \`i = i * 3\`，则同理为 $$O(\\log_3 n) = O(\\log_2 n)$$，底数不影响数量级。

## 例题二：双重循环取最大值

\`\`\`c
int m = 0;
for (int i = 0; i < n; i++)
    for (int j = i; j < n; j++)
        m++;
\`\`\`

内层循环执行次数为 $$\\sum_{i=0}^{n-1}(n-i) = n+(n-1)+\\cdots+1 = \\frac{n(n+1)}{2}$$，故 $$T(n)=O(n^2)$$。

## 例题三：循环条件与结果相关

\`\`\`c
int sum = 0;
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= n; j += i)
        sum++;
\`\`\`

内层循环次数为 $$\\lfloor n/i \\rfloor$$，总次数为 $$\\sum_{i=1}^{n}\\frac{n}{i} = n\\cdot H_n \\approx n\\ln n$$，故 $$T(n)=O(n\\log n)$$。这一类"调和级数"形式的复杂度在408中偶有出现。

**重点：**分析复杂度时先确定"基本运算"，再看基本运算随 n 的增长规律，最后化为数量级。

### 递归程序的时间复杂度例题
<!-- kp: ch1-s5-k2 -->

## 递推法分析

**例1**：计算二分查找递归版本的时间复杂度。递推关系为：

$$T(n) = T(n/2) + O(1), \\quad T(1) = O(1)$$

由主定理或迭代展开：$$T(n) = T(n/2) + c = T(n/4) + 2c = \\cdots = T(1) + c\\log_2 n = O(\\log_2 n)$$。

**例2**：Fibonacci 数列的朴素递归：

\`\`\`c
int fib(int n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);
}
\`\`\`

递推关系为 $$T(n) = T(n-1) + T(n-2) + O(1)$$，解为 $$T(n) = O(2^n)$$（斐波那契数通项约等于 $$\\frac{1}{\\sqrt{5}}(\\frac{1+\\sqrt{5}}{2})^n$$）。

**例3**：树形递归，如归并排序：

$$T(n) = 2T(n/2) + O(n)$$

由主定理得 $$T(n) = O(n\\log n)$$。该形式是分治算法的典型递推关系，408中快速排序、归并排序的平均时间复杂度均由它导出。

**难点：**递归复杂度分析的两种工具——递推方程展开法与主定理（$$T(n)=aT(n/b)+f(n)$$），需熟练区分 $$O(n\\log n)$$（分治）与 $$O(2^n)$$（指数爆炸）两类结果。
`;export{e as default};