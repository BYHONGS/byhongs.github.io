var e=`---
id: ch5
title: 中央处理器
order: 5
---

## CPU功能与结构
<!-- sec: s5-1 -->

### CPU的功能
<!-- kp: kp5-1-1 emphasis: key -->

CPU（中央处理器）是计算机的核心部件，主要功能有：① **指令控制**：保证指令按程序规定的顺序执行（取指、分析、执行的控制）；② **操作控制**：产生并发送各种控制信号，控制各部件协调工作；③ **时间控制**：对各种操作实施时间上的定时（时钟节拍与时序）；④ **数据加工**：对数据进行算术与逻辑运算；⑤ **中断处理**：响应和处理各种中断请求，这也是I/O系统的基础。CPU的工作过程可概括为"取指—分析—执行"的循环。

从功能上看，CPU中有一条由**取指、译码、执行、访存、写回**等阶段构成的指令处理流水线：程序计数器PC提供指令地址，指令寄存器IR暂存当前指令，控制器根据IR中的操作码与寻址方式产生控制信号，运算器完成数据加工。CPU性能由三个因素共同决定：$T_{CPU} = IC \\times CPI \\times T$（指令条数、每条指令平均时钟周期数、时钟周期），改变任何一个都会影响性能。408中CPU部分常考功能表述、寄存器作用与指令流程的对应关系。

**重点：** CPU四大功能：指令控制、操作控制、时间控制、数据加工（另加中断处理）；CPU性能由IC、CPI、主频三因素决定。

### CPU的基本结构：运算器、控制器与寄存器
<!-- kp: kp5-1-2 emphasis: key -->

CPU由**运算器、控制器**以及**寄存器组**和内部总线组成。**运算器**完成算术与逻辑运算，包含ALU、累加器（ACC）、暂存寄存器、乘商寄存器、状态寄存器（PSW）等。**控制器**产生控制信号，包含程序计数器（PC）、指令寄存器（IR）、指令译码器（ID）、时序产生器、操作控制器（硬布线逻辑或微程序控制单元）。**寄存器组**中的通用寄存器供程序员使用，专用寄存器对程序员透明。

各类寄存器的功能与可见性（408高频考点）：

| 寄存器 | 功能 | 程序员是否可见 |
|---|---|---|
| PC | 存放下一条要执行指令的地址，取指后自动+1 | 否（透明） |
| IR | 存放当前正在执行的指令 | 否（透明） |
| MAR | 存放欲访问的存储单元地址 | 否（透明） |
| MDR | 存放与主存交换的数据 | 否（透明） |
| ACC | 累加器，存放运算中间结果 | 是 |
| 通用寄存器R0~Rn | 存放操作数与结果 | 是 |
| PSW/状态寄存器 | 存放ZF/SF/OF/CF等标志 | 部分可见 |

**重点：** PC存"下一条指令地址"且取指后自动+1；IR存"当前指令"；PC、IR、MAR、MDR对程序员透明；通用寄存器可见、专用寄存器大多透明。

## 指令执行过程
<!-- sec: s5-2 -->

### 指令周期与四个子周期
<!-- kp: kp5-2-1 emphasis: key -->

**指令周期**指CPU从取出一条指令到执行完该指令所需的全部时间。一个指令周期通常包含几个**机器周期（子周期）**：**取指周期（IF）**、**间址周期（IND）**（仅间接寻址的指令才有）、**执行周期（EX）**、**中断周期（INT）**（有中断请求被响应时才出现）。每个机器周期由若干**时钟周期（节拍T）**组成。任何指令都**必须有取指周期与执行周期**，间址与中断周期则视指令类型与系统状态而定。

取指周期的基本微操作序列：$PC \\to MAR$（指令地址送地址寄存器），$CU$ 发出读命令，主存读出 $M(MAR) \\to MDR$，$MDR \\to IR$（指令送指令寄存器），同时 $PC \\leftarrow PC + 1$（修改PC，指向下一条指令）。间址周期：把IR中的地址码送MAR、读主存，取出的内容（真正的操作数地址）送回IR或地址寄存器。执行周期按操作码完成具体操作。中断周期：保存断点（把PC压入堆栈）、转入中断处理程序入口。考试常考各周期的存在条件与取指周期的微操作流程。

**重点：** 指令周期 $\\supseteq$ 取指/间址/执行/中断周期；所有指令必有取指与执行周期；取指周期中完成 $PC \\to MAR \\to 主存 \\to MDR \\to IR$ 与 $PC+1$。

### 指令执行的微操作序列
<!-- kp: kp5-2-2 emphasis: difficult -->

每个机器周期由一系列**微操作**组成。以取指周期为例，按时钟节拍可写为：

$$T_0: PC \\to MAR; \\qquad T_1: \\text{读主存}，M(MAR) \\to MDR; \\qquad T_2: MDR \\to IR,\\ PC \\to PC+1$$

执行周期示例（加法指令 $ADD\\,X$，X为直接地址）：$T_0: X \\to MAR$；$T_1$: 读主存，$M(MAR) \\to MDR$；$T_2: MDR \\to Y$（送入ALU暂存器Y），随后 $ACC + Y \\to ACC$。转移指令执行周期：$T_0$：目标地址 $\\to PC$（实现跳跃）。中断周期：$SP-1 \\to SP$、$PC \\to MDR$、写主存（断点入栈）、中断向量 $\\to PC$。

书写微操作序列的要点：① 数据通路要正确（如"$PC \\to MAR \\to 主存 \\to MDR \\to IR$"）；② 遵守"**同一节拍内不能有两个部件争用同一条总线**"的约束；③ 注意哪些微操作可以并行（如"$MDR \\to IR$"与"$PC+1$"互不冲突，可安排在同一节拍）。408常考"取指周期微操作序列"或"某指令执行周期的微操作序列"，以及判断两个微操作能否并行及其原因。

**难点：** 微操作序列中"同一节拍不冲突"原则（单总线结构下尤其重要）；间址周期取的是"地址"、执行周期取的是"操作数"，二者微操作内容不同。

## 数据通路
<!-- sec: s5-3 -->

### 数据通路的基本概念与单总线结构
<!-- kp: kp5-3-1 emphasis: key -->

**数据通路**指数据在CPU内部传输的路径与部件组织（ALU、寄存器、总线、多路选择器），它决定了指令执行时数据的流向。**单总线结构**：CPU内部所有部件（通用寄存器、ALU输入、IR、MAR、MDR）通过**一条**内部总线相连。其优点是结构简单、成本低；缺点是同一时刻只允许两个部件之间传送数据（总线是共享互斥资源），多个传送必须**分时**进行，因此指令周期较长、CPI较大。例如执行 $ADD\\,R_1,R_2$ 需分多个节拍完成"$R_1 \\to$ ALU输入、$R_2 \\to$ ALU输入、ALU结果 $\\to$ 寄存器"等传送。

单总线结构的关键限制：ALU需要两个操作数同时输入，而总线上同一时刻只能传送一个数据，因此ALU的两个输入必须借助**暂存寄存器（Y）**——先把一个操作数锁存进Y，再从总线送入另一个操作数。访存路径：地址先送MAR（$PC \\to MAR$），数据经MDR与主存交换。**多总线结构**（双总线、三总线）：双总线把ALU输入总线与输出总线分开，三总线提供两条输入总线与一条输出总线（或增加专用主存总线），使多个传送可并行、减少节拍数、提高速度，代价是硬件复杂、成本高。

**重点：** 单总线：同一时刻只能一对部件通信，需暂存器Y配合ALU；多总线增加并行度、降低CPI，但硬件更复杂。

### 多总线结构与数据通路设计
<!-- kp: kp5-3-2 emphasis: difficult -->

现代CPU内部普遍采用多总线结构。三总线结构示例：一条总线连接通用寄存器组，两条总线分别供ALU的两个输入使用，另一条输出总线把ALU结果写回寄存器。在"取指"节拍 $PC \\to MAR$ 的同时，可并行完成 $R_i \\to$ ALU输入，从而减少节拍数、降低CPI。数据通路设计的核心问题是：部件之间的连通性（哪些部件连到哪些总线）、总线冲突的避免、ALU输入暂存、访存路径的安排。

数据通路设计题的解题步骤（408简答/综合题）：① 把每条指令的微操作分解为一系列"源 $\\to$ 目的"的传送；② 判断哪些传送可使用不同总线或不同部件而**并行执行**；③ 检查并行方案是否存在总线争用（同一总线同一时刻只能有一个数据）。例如三总线结构中，取指周期可安排：$T_0: PC \\to MAR$ 且 $R_1 \\to BUS_1$（ALU输入）；$T_1$: 读主存 $MDR \\leftarrow M(MAR)$，同时ALU计算；$T_2: \\text{ALU结果} \\to R_2$。通过合理调度，指令周期数可明显减少。

**难点：** 并行性的判断依据是"总线资源互斥"；简答题要求写出能并行的微操作并说明理由（使用了不同总线/不同部件）；注意ALU两输入端在单总线时必须用暂存器错开。

## 控制器
<!-- sec: s5-4 -->

### 硬布线控制器
<!-- kp: kp5-4-1 emphasis: key -->

**硬布线控制器**（组合逻辑控制器）用**组合逻辑电路**直接产生全部控制信号：把"指令操作码、节拍、状态条件（如标志位）"作为组合逻辑的输入，输出即为各微操作控制信号。其结构包括指令译码器、节拍发生器、状态条件输入以及由门电路和触发器构成的逻辑网络（也可用PLA或ROM实现）。特点：① **速度快**——控制信号并行直接产生，无需额外存取延迟；② 控制逻辑**专用**——指令系统变化时必须修改电路，不灵活；③ 指令增多或变复杂时电路规模爆炸，因此难以支持很复杂的指令；④ 主要应用于RISC处理器与流水线CPU（指令规整、利于硬布线实现）。

硬布线控制器与微程序控制器的本质区别在于"控制信号的产生方式"：硬布线用组合逻辑，微程序用存储逻辑。考试要点：硬布线控制器靠组合逻辑电路产生控制信号、速度快、灵活性差；RISC常配硬布线控制，CISC早期多用微程序控制。

**重点：** 硬布线：组合逻辑生成控制信号、速度快、不灵活、修改指令需改电路、适合RISC与规整指令。

### 微程序控制器
<!-- kp: kp5-4-2 emphasis: key -->

**微程序控制器**把一条机器指令的执行分解为若干**微操作**，把微操作按节拍组织成**微指令**，微指令序列构成**微程序**，存放在**控制存储器（CM）**中。执行过程：指令的**操作码**经译码作为微程序的**入口地址**，控制器逐条取出微指令，微指令中的**控制字段**（微命令）直接或经译码产生控制信号，**地址字段**指明下一条微指令的地址。相关概念必须严格区分：**微操作**是CPU最基本、不可再分的操作（如"$PC \\to MAR$"）；**微命令**是控制微操作执行的信号；**微指令**是若干微命令的组合（存放在CM的一个单元中）；**微程序**是微指令组成的序列（对应一条机器指令）；**控制存储器CM**是存放微程序的高速只读存储器。

微程序控制器的优点：规整、灵活（修改微程序即可改变指令功能或增加新指令）、易于实现复杂指令（适合CISC）；缺点：每条指令的执行都要多次访问控制存储器，**速度比硬布线慢**。微指令的编码方式：**直接控制**（每一位微命令对应一根控制线，无需译码、速度快但存储量大）、**字段译码**（把互斥的微命令分在同一字段、经译码后输出，节省存储空间）、混合编码。考试常考四个"微"概念的关系与CM的作用。

**重点：** 微程序（对机器指令）由微指令组成、微指令含微命令、微命令产生微操作；微程序控制器靠访问控制存储器CM取微指令执行，速度慢于硬布线。

### 两种控制器的比较
<!-- kp: kp5-4-3 -->

硬布线控制器与微程序控制器的对比：

| 比较项 | 硬布线控制器 | 微程序控制器 |
|---|---|---|
| 控制信号的产生 | 组合逻辑电路直接产生 | 微指令（存于CM）产生 |
| 速度 | 快 | 慢（需访问控制存储器） |
| 灵活性 | 差（改功能需改电路） | 好（改微程序即可） |
| 实现复杂指令 | 电路复杂、困难 | 容易 |
| 通用性/扩展性 | 差 | 好（同一控制器可适配多指令集） |
| 主要应用 | RISC、流水线CPU | CISC（早期） |

总结：微程序控制器用"存储逻辑"代替"组合逻辑"，以时间（访问CM）换取灵活性与实现复杂指令的能力；硬布线控制器以硬件复杂度换取速度。统考常以选择题或简答题对比两者，特别要注意"微程序控制器的每次访问控制存储器是其时间开销的主要来源"。

**重点：** "硬布线快而不灵活、微程序灵活而慢"是核心结论；控制存储器（CM）只属于微程序控制器；二者均可实现RISC/CISC，但典型搭配是RISC-硬布线、CISC-微程序。

### 微指令格式：水平型与垂直型
<!-- kp: kp5-4-4 emphasis: difficult -->

**微指令格式**按控制字段的编码组织方式分为两类（408微程序大题考查点）：

- **水平型微指令**：一条微指令的每个控制位（或每个字段）直接对应一个微命令，可**并行执行多个微操作**，执行速度快、微程序短；缺点是微指令字长很长，占用控制存储器空间大，编写复杂。其中**直接控制编码**（一位一命令，无需译码）速度最快、字长最长；**字段直接/间接译码**把互斥的微命令分到同一字段共用一个译码器，节省存储空间。
- **垂直型微指令**：类似机器指令格式（操作码+操作数），每条微指令只完成一个（或少数）微操作，微指令字长短、编写直观；缺点是一个微操作周期只能做一个微操作，执行速度慢、微程序长。

**下一条微指令地址（下地址）的形成**有三种方式：①**下址字段（断定方式）**：微指令中专门设置下址字段直接指明下一条微指令地址，最常用；②**由微程序入口地址（操作码译码）确定**：第一条微指令地址由机器指令操作码经译码映射得到；③**转移方式**：根据测试条件（如标志位）决定顺序执行或转移。编写微程序时先排定各微指令地址，再用下址字段连接。

**难点：**水平型=并行微操作、字长；垂直型=顺序微操作、字短；"互斥微命令同字段、相容微命令异字段"是字段译码的分组原则；下地址常用断定方式（下址字段）。

## 指令流水线
<!-- sec: s5-5 -->

### 流水线的基本概念与性能指标
<!-- kp: kp5-5-1 emphasis: key -->

**指令流水线**把指令的执行过程划分为若干阶段（如取指IF、译码ID、执行EX、访存MEM、写回WB），各阶段由独立硬件并行工作，使多条指令同时处于不同的执行阶段。流水线利用**指令级并行**提高**吞吐率**（单位时间完成的指令数），但**单条指令的延迟并不缩短**（还可能因流水寄存器增加微小开销）。理想情况下，$k$ 段流水线执行 $n$ 条指令共需 $k + (n-1)$ 个时钟周期——前 $k$ 个周期流水线充满（建立时间），之后每周期完成一条指令：

$$T_{流水} = (k + n - 1) \\times \\tau$$

其中 $\\tau$ 为时钟周期（$\\ge$ 最长流水段的时间）。流水线性能指标：**吞吐率** $TP = \\frac{n}{k+n-1}$（$n$ 很大时趋近1条/周期）；**加速比** $S_p = \\frac{\\text{串行时间}}{\\text{流水时间}} = \\frac{nk}{k+n-1} \\to k$（理想上限为段数 $k$）；**效率**（设备利用率）$E = \\frac{nk}{k(k+n-1)}$。

例题：4段流水线执行100条指令（无冒险），总时间 $= 4 + 99 = 103$ 个时钟周期，加速比 $= 400/103 \\approx 3.88$。考试常考：给定段数、指令数与时钟周期求执行时间与加速比，注意每条指令的执行时间不因流水线而缩短。

**重点：** 流水线总时间 $T = (k + n - 1) \\times \\tau$；加速比上限为段数 $k$；时钟周期由最长流水段决定；吞吐率 $\\approx 1$ 条/周期（理想时）。

### 结构冒险与处理
<!-- kp: kp5-5-2 emphasis: difficult -->

**结构冒险**（资源冲突）指多条指令在同一时钟周期争用**同一个硬件资源**。典型例子：在单端口存储器的冯·诺依曼结构中，"取指"与"访存"指令（Load/Store）同时需要访问存储器，从而发生冲突；或取指与写寄存器冲突。处理办法有两种：① **停顿（Stall，插入气泡）**：让冲突的指令延迟一个（或多个）周期再执行，代价是降低吞吐率；② **资源重复**：把指令存储器与数据存储器分开（**哈佛结构**，分离的指令Cache与数据Cache）、采用多端口寄存器堆等，使取指与访存并行，从根源上消除多数结构冲突。

经典五段RISC流水线中，如果采用"分离的指令Cache与数据Cache"，则取指（IF）与访存（MEM）可并行，几乎不产生结构冒险；若使用统一Cache/单端口主存，则"Load后紧跟取指"必然停顿。考试常考：判断某结构下流水线是否发生结构冒险、在指令序列中标出停顿位置、计算停顿造成的额外周期数。结构冒险的本质是"同一部件同一时刻只能被一条指令使用"。

**难点：** 区分三类冒险：资源争用→结构冒险；数据依赖→数据冒险；控制流变化→控制冒险；停顿以"气泡"形式体现，会拉长流水线总时间。

### 数据冒险与处理（前推/阻塞）
<!-- kp: kp5-5-3 emphasis: difficult -->

**数据冒险**（数据相关）指后一条指令需要用到前一条指令尚未写回的数据。经典例子：

\`\`\`c
I1: ADD R1, R2, R3     ; R1 ← R2+R3
I2: SUB R4, R1, R5     ; 需要R1（I1的结果在EX段末才产生）
\`\`\`

在经典五段流水线中，I2在ID段读寄存器时，I1的结果要到WB段才写回寄存器堆，若不加处理，I2读到的是旧值。处理办法：**① 数据前推（转发/旁路，Forwarding）**：在结果产生的时刻就通过专用通路把数据直接送到需要它的执行段（如"EX/MEM寄存器→EX段输入"），无需等待写回。前推能消除"ALU→ALU"的立即依赖。**② 停顿（阻塞）**：对于**Load-Use冒险**（需要的数据来自Load指令，前推也来不及），必须插入气泡等待数据可用。**③ 编译器调度**：调整指令顺序，把无关指令插到相关指令之间。

Load-Use冒险示例：\`LW R1, 0(R2)\` 后紧跟 \`ADD R3, R1, R4\`——LW在MEM段末才有数据，即使前推，ADD也需在EX前插入**1个停顿周期**。408常考：识别冒险类型、判断前推能否消除、计算Load-Use需要停顿的周期数。按序流水线中不会出现WAR与WAW冒险。

**难点：** 前推消除"EX/MEM→EX"类依赖；Load-Use冒险必须停顿（典型1个周期）；只有"写后读"（RAW）依赖会发生在按序流水线中。

### 控制冒险与处理
<!-- kp: kp5-5-4 emphasis: difficult -->

**控制冒险**（分支冒险）由转移、跳转等改变PC的指令引起：分支指令的结果（跳转与否、目标地址）尚未确定时，流水线已经预取了其后的指令；若最终跳转，则已进入流水线的后续指令全部作废，造成浪费（分支惩罚）。处理办法：① **分支预测**——**静态预测**（如"总是预测不跳转"，实现简单但命中率有限）与**动态预测**（根据历史分支行为预测，如一位/二位饱和计数器、**分支目标缓冲器BTB**）；② **延迟分支（延迟槽）**——把与分支无关的指令放入延迟槽，无论分支是否跳转都执行（RISC常用，如MIPS每条分支后一个延迟槽），减少作废指令；③ **提前判断分支条件**——把比较与转移提前（如把分支条件的判断提前到ID段），缩短分支解决时间。

性能计算：若分支概率为 $p$、每次分支作废 $m$ 条指令，则每条指令平均额外开销 $p \\times m$ 个周期。示例：每5条指令有1条转移、每次转移作废2条指令，则平均每条指令额外 $0.2 \\times 2 = 0.4$ 个周期，吞吐率明显下降。考试常考：给定分支比例与惩罚周期，计算流水线的实际吞吐率或加速比。

**难点：** 三类冒险的影响计算：结构冒险（资源）、数据冒险（数据依赖）、控制冒险（分支）；分支惩罚周期数 = 分支判定位置之后被作废的指令数；延迟槽指令必须与分支结果无关。
`;export{e as default};