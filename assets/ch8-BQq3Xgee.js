var e=`---
id: ch8
title: 排序
order: 8
---

## 插入排序
<!-- sec: ch8-s1 -->

### 直接插入排序
<!-- kp: ch8-s1-k1 emphasis: key -->

## 基本思想

**直接插入排序**：把待排序记录看作两部分，前部分为**有序区**，后部分为**无序区**。依次将无序区第一个元素插入到有序区的正确位置（类似整理扑克牌）。从 i=1 开始，每次将 L[i] 与有序区中元素从后往前比较，找到插入位置。

\`\`\`c
void InsertSort(ElemType A[], int n) {
    int i, j;
    for (i = 2; i <= n; i++) {        // A[0] 为哨兵
        A[0] = A[i];                  // 哨兵暂存待插元素
        j = i - 1;
        while (A[0] < A[j]) {         // 从后往前找插入位置
            A[j + 1] = A[j];
            j--;
        }
        A[j + 1] = A[0];              // 插入
    }
}
\`\`\`

代码中 A[0] 充当**哨兵**：既暂存待插元素，又避免了循环中 j 越界的判断。

## 性能分析

- **空间复杂度**：O(1)（原地排序，仅用哨兵）；
- **最好情况**（初始有序）：每趟只需比较 1 次、移动 0 次，总比较次数 n-1，时间复杂度 **O(n)**；
- **最坏情况**（初始逆序）：第 i 趟需比较 i 次、移动 i+1 次，总比较与移动次数均约为 $\\frac{n^2}{2}$，时间复杂度 **O(n²)**；
- **平均情况**：O(n²)；
- **稳定性**：**稳定**（相等元素不会交换相对次序）。

**特点**：直接插入排序对"基本有序"的数据表现优异，规模小或近乎有序时很实用。

**重点：**最好 O(n)、最坏与平均 O(n²)、稳定、空间 O(1) 是直接插入排序的四大结论；"从后往前比较并后移"是算法的核心操作。

### 折半插入排序与希尔排序
<!-- kp: ch8-s1-k2 emphasis: key -->

## 折半插入排序

在直接插入排序的基础上，由于有序区是有序的，可以用**折半查找**确定插入位置，从而**减少比较次数**（移动次数不变）。

\`\`\`c
void BinaryInsertSort(ElemType A[], int n) {
    for (int i = 2; i <= n; i++) {
        A[0] = A[i];
        int low = 1, high = i - 1;
        while (low <= high) {                    // 折半查找插入位置
            int mid = (low + high) / 2;
            if (A[mid] > A[0]) high = mid - 1;
            else low = mid + 1;                  // 相等时插到其右侧，保持稳定
        }
        for (int j = i - 1; j >= low; j--) A[j + 1] = A[j];
        A[low] = A[0];
    }
}
\`\`\`

- 比较次数降为 O(n·log n)，但移动次数仍为 O(n²)，故**总时间复杂度仍为 O(n²)**；
- **稳定**，空间 O(1)。当 A[mid] == A[0] 时继续在右半边查找（low = mid+1），保证相等元素插入在右侧，维持稳定性。

## 希尔排序（缩小增量排序）

**希尔排序**是对直接插入排序的改进：将待排序列按增量 d 分成若干子序列，分别做直接插入排序；逐步缩小 d（如 $d = \\lfloor d/2 \\rfloor$），最后 d=1 时对整个序列做一次直接插入排序。前几趟使序列**基本有序**，最后一趟插入排序代价很小。

\`\`\`c
void ShellSort(ElemType A[], int n) {
    for (int dk = n / 2; dk >= 1; dk = dk / 2)   // 增量序列
        for (int i = dk + 1; i <= n; i++)
            if (A[i] < A[i - dk]) {              // 组内直接插入
                A[0] = A[i];
                int j;
                for (j = i - dk; j > 0 && A[0] < A[j]; j -= dk)
                    A[j + dk] = A[j];
                A[j + dk] = A[0];
            }
}
\`\`\`

- 时间复杂度与增量序列选取有关，约为 **O(n^1.3)** ~ O(n²)（无法精确分析，取 $n^{1.3}$ 左右的量级）；
- 空间 O(1)；**不稳定**（组间交换可能破坏相等元素的相对次序）。

**重点：**希尔排序是"不稳定"的，且时间复杂度**不收敛于固定阶**；折半插入排序的比较次数减少但移动次数不变，总复杂度仍 O(n²)。

## 交换排序
<!-- sec: ch8-s2 -->

### 冒泡排序
<!-- kp: ch8-s2-k1 emphasis: key -->

## 基本思想

**冒泡排序**：从后往前（或从前往后）两两比较相邻元素，若为逆序则交换，每趟将**最大（或最小）元素"冒"到末尾（或开头）**。进行 n-1 趟，或某趟无任何交换（已有序）时提前结束。

\`\`\`c
void BubbleSort(ElemType A[], int n) {
    for (int i = 0; i < n - 1; i++) {
        bool flag = false;                    // 本趟是否有交换
        for (int j = n - 1; j > i; j--)       // 从后往前冒泡
            if (A[j - 1] > A[j]) {
                swap(A[j - 1], A[j]);
                flag = true;
            }
        if (!flag) return;                    // 本趟无交换，已有序
    }
}
\`\`\`

## 性能分析

- **空间复杂度**：O(1)；
- **最好情况**（初始有序）：一趟扫描发现无交换即结束，比较 n-1 次，**O(n)**；
- **最坏情况**（初始逆序）：需 n-1 趟，比较与交换次数均约为 $\\frac{n(n-1)}{2}$，**O(n²)**；
- **平均情况**：O(n²)；
- **稳定性**：**稳定**（仅交换逆序的相邻元素）。

## 特点与题目陷阱

- 每趟确定一个元素的最终位置（最大元素沉底）；
- 冒泡排序的**趟数**：最坏 n-1 趟，若某趟无交换可提前结束；
- 对于已部分有序的序列，冒泡排序的 flag 优化效果明显；
- 冒泡排序每趟比较次数依次为 n-1, n-2, …, 1。

**重点：**冒泡排序的最好情况 O(n)（得益于 flag 提前终止）、最坏/平均 O(n²)、稳定、空间 O(1)。它是"交换排序"中稳定的一支，与快速排序形成对比。

### 快速排序
<!-- kp: ch8-s2-k2 emphasis: difficult -->

## 基本思想

**快速排序**采用**分治**策略：选取一个**枢轴（基准）元素**，一趟划分后把序列分为两部分——枢轴左边都 ≤ 枢轴，右边都 ≥ 枢轴，枢轴到达其**最终位置**；然后递归对左右两部分继续划分。划分算法：

\`\`\`c
int Partition(ElemType A[], int low, int high) {
    ElemType pivot = A[low];          // 取第一个元素为枢轴
    while (low < high) {
        while (low < high && A[high] >= pivot) high--;
        A[low] = A[high];             // 比枢轴小的移到左边
        while (low < high && A[low] <= pivot) low++;
        A[high] = A[low];             // 比枢轴大的移到右边
    }
    A[low] = pivot;                   // 枢轴归位
    return low;                       // 返回枢轴最终位置
}
void QuickSort(ElemType A[], int low, int high) {
    if (low < high) {
        int pivotpos = Partition(A, low, high);
        QuickSort(A, low, pivotpos - 1);
        QuickSort(A, pivotpos + 1, high);
    }
}
\`\`\`

## 性能分析

- **空间复杂度**：递归栈深度，平均 O(log n)，最坏 O(n)；
- **最好与平均情况**：划分均衡时递推 $T(n) = 2T(n/2) + O(n)$，**O(n·log n)**；
- **最坏情况**：序列**基本有序**或**逆序**时（枢轴总是最小值/最大值），划分极不均衡，退化为 **O(n²)**；
- **稳定性**：**不稳定**（划分时元素跨越式交换，破坏相对次序）；
- **提高效率**：随机选取枢轴或"三数取中"可避免最坏情况。

## 关键结论

- 快速排序是**平均性能最优**的排序算法（O(n·log n)），但不是所有情况都最优；
- **一趟划分**后，枢轴元素处于其最终位置；每趟划分至少确定一个元素的最终位置；
- 快速排序不适合基本有序的序列（会退化为 O(n²)），插入排序反而适合。

**难点：**Partition 的"挖坑填数"过程与枢轴的移动轨迹需亲手模拟；"已经有序的序列用快排最慢"这一反直觉结论是 408 高频考点。

## 选择排序
<!-- sec: ch8-s3 -->

### 简单选择排序
<!-- kp: ch8-s3-k1 -->

## 基本思想

**简单选择排序**：每趟在剩余元素中选出**最小元素**，与无序区第一个元素交换，有序区逐步扩大。共进行 n-1 趟。

\`\`\`c
void SelectSort(ElemType A[], int n) {
    for (int i = 0; i < n - 1; i++) {
        int min = i;
        for (int j = i + 1; j < n; j++)
            if (A[j] < A[min]) min = j;      // 找最小元素
        if (min != i) swap(A[i], A[min]);    // 交换到有序区末尾
    }
}
\`\`\`

## 性能分析

- **比较次数与初始序列无关**：无论初始状态如何，第 i 趟都需比较 n-i 次，总比较次数恒为：

$$\\sum_{i=1}^{n-1}(n-i) = \\frac{n(n-1)}{2}$$

- **移动次数**：最好（初始有序）0 次，最坏（每趟都需交换）不超过 3(n-1) 次；
- 时间复杂度：最好、最坏、平均均为 **O(n²)**（比较次数占主导，与初始状态无关）；
- **空间复杂度**：O(1)；
- **稳定性**：**不稳定**——例如序列 [2, 2, 1]，第一趟选出 1 与第一个 2 交换，两个 2 的相对次序被破坏。

**重点：**简单选择排序的"比较次数恒为 n(n-1)/2、与初始状态无关"与"不稳定"是最常考的两点；移动次数很少是它的优点。

### 堆排序
<!-- kp: ch8-s3-k2 emphasis: difficult -->

## 堆的定义

**堆**是一棵**完全二叉树**，满足：大根堆（大顶堆）中每个结点的值**大于等于**其左右孩子；小根堆（小顶堆）中每个结点**小于等于**其左右孩子。堆通常用**顺序存储**（数组）表示，结点 i 的孩子为 2i 与 2i+1。

$$\\text{大根堆：} A[i] \\ge A[2i] \\text{ 且 } A[i] \\ge A[2i+1]$$

## 建堆与调整

**建堆**：对 n/2 个分支结点从下往上依次执行"向下调整"（Sift Down）：若结点小于其较大孩子（大根堆），则与较大孩子交换，继续向下调整直至满足堆序。建堆时间复杂度为 **O(n)**。

**向下调整**：

\`\`\`c
void HeadAdjust(ElemType A[], int k, int len) {
    A[0] = A[k];                              // 暂存待调整结点
    for (int i = 2 * k; i <= len; i *= 2) {
        if (i < len && A[i] < A[i + 1]) i++;  // 取较大孩子
        if (A[0] >= A[i]) break;
        else { A[k] = A[i]; k = i; }
    }
    A[k] = A[0];
}
\`\`\`

## 堆排序过程

1. **建初始大根堆**（O(n)）；
2. 将堆顶（最大值）与最后一个元素交换，堆规模减 1；
3. 对新的堆顶做向下调整恢复大根堆；
4. 重复步骤 2、3 共 n-1 次，得到递增序列。

## 性能分析

- 时间复杂度：建堆 O(n)，n-1 次"交换 + 调整"每次 O(log n)，总 **O(n·log n)**，且最好最坏平均都是 O(n·log n)；
- 空间复杂度：O(1)（原地排序）；
- **稳定性**：**不稳定**；
- 堆排序适合从 n 个元素中选取**前 k 大/小**元素（k << n）等问题，复杂度 O(k·log n) + 建堆 O(n)。

**难点：**建堆从"最后一个非叶结点（下标 n/2）"开始向下调整；"初始堆的构建结果""每次调整后的堆形态"是 408 画图题常考内容，务必按步骤手推。

### 建堆与调整的完整示例
<!-- kp: ch8-s3-k3 emphasis: key -->

**408画图/填空高频题**：给定关键字序列，画出初始大根堆，或写出每趟"交换+调整"后的堆形态。以序列 \`53, 17, 78, 9, 45, 65, 87, 23\` 为例建**大根堆**（n=8，从 i=n/2=4 开始向下调整）：

- **i=4**：结点9，与孩子23比较，9<23 交换 → \`53, 17, 78, 23, 45, 65, 87, 9\`；
- **i=3**：结点78，孩子65、87中较大为87，78<87 交换 → \`53, 17, 87, 23, 45, 65, 78, 9\`；
- **i=2**：结点17，孩子23、45中较大为45，17<45 交换（17已无子树）→ \`53, 45, 87, 23, 17, 65, 78, 9\`；
- **i=1**：结点53，孩子45、87中较大为87，交换 → \`87, 45, 53, 23, 17, 65, 78, 9\`；53继续下移与65比较再交换 → **\`87, 45, 65, 23, 17, 53, 78, 9\`**，初始大根堆建成。

**排序第一趟**：堆顶87与末尾9交换 → \`9, 45, 65, 23, 17, 53, 78 | 87\`，对堆顶9向下调整：与45、65中较大者65交换，再与53比较交换 → \`65, 45, 53, 23, 17, 9, 78 | 87\`。此后每趟重复"交换末尾+向下调整"，n-1趟后得到递增序列。

**难点：**建堆必须从**最后一个非叶结点（n/2）**开始**从下往上**调整；每次交换后新堆顶要继续向下调整到合适位置；答题时写清每趟调整后的完整序列，注意"已排好的末尾元素不再参与调整"。

## 归并排序
<!-- sec: ch8-s4 -->

### 二路归并排序
<!-- kp: ch8-s4-k1 emphasis: key -->

## 基本思想

**二路归并排序**采用分治：把长度为 n 的序列分为两个长度约 n/2 的子序列，递归地各自排序，然后**归并**两个有序子序列为一个有序序列。归并操作（Merge）是把两个有序序列按序合并且不改变相对顺序，因此归并排序是**稳定**的。

\`\`\`c
ElemType *B = (ElemType*)malloc((n + 1) * sizeof(ElemType));

void Merge(ElemType A[], int low, int mid, int high) {
    // 两段有序序列 A[low..mid] 与 A[mid+1..high] 归并
    for (int k = low; k <= high; k++) B[k] = A[k];
    int i = low, j = mid + 1, k = low;
    while (i <= mid && j <= high) {
        if (B[i] <= B[j]) A[k++] = B[i++];   // <= 保证稳定
        else A[k++] = B[j++];
    }
    while (i <= mid) A[k++] = B[i++];        // 左段剩余
    while (j <= high) A[k++] = B[j++];       // 右段剩余
}

void MergeSort(ElemType A[], int low, int high) {
    if (low < high) {
        int mid = (low + high) / 2;
        MergeSort(A, low, mid);
        MergeSort(A, mid + 1, high);
        Merge(A, low, mid, high);            // 归并
    }
}
\`\`\`

## 性能分析

- 递推式：$$T(n) = 2T(n/2) + O(n)$$，解得时间复杂度在最好、最坏、平均情况下均为 **O(n·log n)**；
- **空间复杂度**：O(n)（需要与待排序列等长的辅助数组 B）——这是归并排序与堆/快排（O(1)/O(log n)）的主要区别；
- **稳定性**：**稳定**（归并时相等元素取左段的先）；
- **适用**：对链表等顺序结构也可高效归并（无随机存取需求），是外部排序的基础。

**重点：**归并排序"三态同阶"（最好=最坏=平均 O(n·log n)）、空间 O(n)、稳定，与快速排序（空间 O(log n)、不稳定）、堆排序（空间 O(1)、不稳定）形成对比。

### 归并排序的趟数与性质
<!-- kp: ch8-s4-k2 -->

## 归并趟数

对含 n 个元素的序列进行二路归并排序，共需 $\\lceil \\log_2 n \\rceil$ 趟归并。第 i 趟后，每个归并段长度为 $2^i$（最后一趟可能不足）。例如 n=8 需 3 趟；n=9 需 $\\lceil \\log_2 9 \\rceil = 4$ 趟。

## 常见考查方式

1. **给出一趟归并后的序列**：将序列按当前归并段长度分组，段内有序即可判断正确性；
2. **求归并趟数**：$\\lceil \\log_2 n \\rceil$；
3. **逆推某趟前的序列**：分段还原；
4. **k 路归并**：m 个归并段做 k 路归并需 $\\lceil \\log_k m \\rceil$ 趟（外部排序中常用）。

**例**：对序列 49, 38, 65, 97, 76, 13, 27 做二路归并排序。第一趟后：38,49 | 65,97 | 13,76 | 27（两两有序）；第二趟后：38,49,65,97 | 13,27,76；第三趟后：13,27,38,49,65,76,97（全部有序）。共 $\\lceil \\log_2 7 \\rceil = 3$ 趟。

**要点**：归并排序每趟产生的归并段**长度翻倍**（2, 4, 8, …）；归并过程不依赖于初始数据的"有序程度"，故最好最坏均为 O(n·log n)。

**重点：**归并趟数 $\\lceil \\log_2 n \\rceil$ 与"段长翻倍"规律是快速判定归并过程正误的工具。

## 基数排序
<!-- sec: ch8-s5 -->

### 基数排序的原理
<!-- kp: ch8-s5-k1 emphasis: key -->

## 基本思想

**基数排序**不基于关键字比较，而基于**分配与收集**。以"位数"为关键字，从**最低位（LSD）**到最高位逐趟处理，每趟按当前位的取值分配到若干"桶"（队列）中，再按桶序收集回原序列。经过 d 趟（d 为最大关键字位数），序列有序。

以三位十进制数（个、十、百位）为例：

1. **按个位分配**：0~9 号桶，按原顺序收集；
2. **按十位分配**：同上；
3. **按百位分配**：同上，最终序列有序。

\`\`\`c
void RadixSort(int A[], int n, int d) {
    // queue[10] 为 10 个队列；从低位到高位处理 d 趟
    for (int i = 1; i <= d; i++) {
        // 1. 分配：按第 i 位数值入对应队列
        // 2. 收集：0..9 依次出队放回数组 A
    }
}
\`\`\`

**稳定性的要求**：分配-收集必须保证"先入队先出队"（先进先出），即每趟用**队列**收集，才能保持上一位的相对次序，最终结果才有序。

## 适用场景

基数排序适用于关键字**可分割成多位**且位数不太多的情况，如整数、字符串（按字符）、日期（年/月/日）。不适用于浮点数等难以按位划分的关键字。

**重点：**基数排序的时间与"比较"无关，这是它与所有基于比较的排序（插入、交换、选择、归并）的本质区别。

### 基数排序的性能分析
<!-- kp: ch8-s5-k2 emphasis: key -->

## 复杂度分析

设待排序关键字有 d 位（或由 d 个分量组成），每位可能的取值数为 r（基数，如十进制 r=10），序列长度为 n。

- 每趟分配：把 n 个元素分发到 r 个桶，时间 **O(n)**；
- 每趟收集：把 r 个桶的元素依次收拢，时间 **O(r)**；
- 共 d 趟，总时间复杂度：

$$O(d \\times (n + r))$$

- **空间复杂度**：O(r)（r 个队列的头尾指针等辅助空间）；
- **稳定性**：**稳定**。

## 与比较排序对比

基数排序的时间复杂度与 n 的关系取决于 d 与 r。当 d、r 均为常数时（如三位十进制整数，d=3, r=10），时间复杂度为 **O(n)**，优于基于比较排序的理论下界 O(n·log n)。但若 d 随 n 增长（如关键字位数为 log n），则退化为 O(n·log n)。

## 与比较排序下界

**比较排序的下界**：任何基于"关键字比较"的排序算法，其最坏时间复杂度下界为：

$$\\Omega(n\\log n)$$

（由比较树的高度推导）。基数排序不通过比较而是通过"分配-收集"排序，可以突破该下界。

**重点：**基数排序的时间 O(d(n+r))、空间 O(r)、稳定，以及"突破比较排序下界"是它的三大标签；每趟必须用"先进先出的队列"收集以保证稳定性。

## 排序总结与外部排序
<!-- sec: ch8-s6 -->

### 各排序算法复杂度与稳定性对比
<!-- kp: ch8-s6-k1 emphasis: key -->

## 内部排序算法对比总表

| 算法 | 最好 | 平均 | 最坏 | 空间 | 稳定性 |
| --- | --- | --- | --- | --- | --- |
| 直接插入排序 | O(n) | O(n²) | O(n²) | O(1) | 稳定 |
| 折半插入排序 | O(n·log n) | O(n²) | O(n²) | O(1) | 稳定 |
| 希尔排序 | — | ≈O(n^1.3) | O(n²) | O(1) | 不稳定 |
| 冒泡排序 | O(n) | O(n²) | O(n²) | O(1) | 稳定 |
| 快速排序 | O(n·log n) | O(n·log n) | O(n²) | O(log n) | 不稳定 |
| 简单选择排序 | O(n²) | O(n²) | O(n²) | O(1) | 不稳定 |
| 堆排序 | O(n·log n) | O(n·log n) | O(n·log n) | O(1) | 不稳定 |
| 二路归并排序 | O(n·log n) | O(n·log n) | O(n·log n) | O(n) | 稳定 |
| 基数排序 | O(d(n+r)) | O(d(n+r)) | O(d(n+r)) | O(r) | 稳定 |

## 记忆要点

- **稳定**的算法：直接插入、冒泡、归并、基数（"插冒归基"——插、冒、归、基）；
- **不稳定**的算法：希尔、快排、简单选择、堆排序（"选堆快希尔"）；
- **每趟能确定一个元素最终位置**的算法：冒泡（最大沉底）、简单选择（最小归位）、快速排序（枢轴归位）、堆排序（堆顶归位）；
- **时间复杂度与初始序列有关**的算法：插入、冒泡、快排、希尔；**与初始序列无关**的：简单选择（比较次数恒定）、堆、归并（最好最坏相同）；
- **n 较小时**用直接插入；**基本有序**时用直接插入（最好 O(n)）；**n 很大**时用快排/堆/归并。

**重点：**对比表是 408 选择题的"送分+陷阱"来源，务必做到"默写表"的程度；关注"稳定与初始状态的关系"这类衍生判断。

### 外部排序与多路归并
<!-- kp: ch8-s6-k2 emphasis: key -->

## 外部排序的概念

当数据量太大无法全部装入内存时，需要借助**外存（磁盘）**进行排序，称为**外部排序**。外部排序的典型方法是**归并排序**：

1. **生成初始归并段（顺串）**：把外存中的文件分段读入内存，用内部排序（如快速排序或败者树）排好，写回外存，生成若干初始归并段；
2. **多路归并**：将各归并段不断两两（或多路）归并，最终得到一个有序文件。

## 外部排序的开销

外部排序的总时间 = 内部排序时间 + **磁盘 I/O 时间** + 归并时 CPU 比较时间。由于磁盘读写远慢于内存运算，**I/O 次数是外部排序的主要开销**，应尽量减少。

设初始归并段个数为 m，采用 **k 路归并**，则归并趟数为：

$$\\lceil \\log_k m \\rceil$$

每趟归并都要把所有数据读写一遍磁盘，故 **k 越大，趟数越少，I/O 次数越少**。增大 k 可减少 I/O，但 k 增大后一次归并需同时从 k 个段取最小值，可用**败者树**优化选择过程，将每趟归并的比较次数从 O(n·k) 降为 O(n·log k)。

## 外部排序相关概念

- **置换-选择排序**：利用"败者树 + 缓冲区"生成更长的初始归并段，减少归并段个数 m，从而减少趟数；
- **最佳归并树**：各初始归并段长度不同时，用**哈夫曼树**思想构造 k 叉归并树，使总归并代价（I/O）最小。若初始段数不足以构成严格 k 叉树，需添加**虚段**（权值为 0）补足：要求 $(m-1) \\bmod (k-1) = 0$；
- 外部排序的最优方法将**归并段个数、归并路数、败者树、最佳归并树**综合使用。

**重点：**归并趟数 $\\lceil \\log_k m \\rceil$、增加 k 减少 I/O、败者树优化比较、最佳归并树与虚段补足是外部排序的四大考点，常以填空或选择题形式出现。
`;export{e as default};