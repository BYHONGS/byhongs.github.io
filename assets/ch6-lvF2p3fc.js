var e=`---
id: ch6
title: 第六章 应用层
order: 6
---

## DNS 域名系统
<!-- sec: ch6-s1 -->

### 域名空间与域名服务器
<!-- kp: ch6-s1-kp1 emphasis: key -->

**1. 域名系统的作用**
DNS（域名系统，Domain Name System）实现**域名到 IP 地址**的解析。用户记忆域名（如 www.baidu.com）方便，但网络层通信需要 IP 地址，DNS 负责把两者对应起来。DNS 采用**分布式数据库**和**客户-服务器**方式，DNS 使用 **UDP** 传输（端口 53），因为查询报文很短，UDP 效率高。

**2. 层次域名空间**
域名采用**层次树状**命名结构，由若干标号（label）组成，标号之间用点分隔，从右到左级别递增：

\`www.baidu.com\`

最右端为**顶级域名**，依次为二级域名、三级域名等。顶级域名分为：国家顶级域名（如 .cn、.us）、通用顶级域名（.com、.net、.org、.edu、.gov 等）、反向域名。域名不区分大小写，标号总长不超过 63 个字符，完整域名不超过 255 个字符。

**3. 域名服务器**
按层次划分，域名服务器有四种：
- **根域名服务器**：最高层，全世界 13 个根服务器（名字），知道所有顶级域名服务器的地址，是解析的"起点"；
- **顶级域名服务器**：管理注册在该顶级域名下的所有二级域名；
- **权限域名服务器**：负责一个区（zone）的域名解析，一个区可以是一台主机管理的范围；
- **本地域名服务器**：主机所在网络配置的默认 DNS 服务器（如 8.8.8.8 或运营商 DNS），主机查询请求首先发给它。

**4. 考点归纳**
- 域名结构自右向左从高到低；顶级域名分类；
- 四类域名服务器的职责层次；
- DNS 使用 UDP 53 端口（区域传送使用 TCP）。

**重点：** 域名的层次结构与顶级域名分类；四类域名服务器的职责。

**难点：** 域名与 IP 是"多对多"关系（一个域名对应多个 IP，如 CDN；一个 IP 上可有多个域名，如虚拟主机）；域名解析的起点是本地域名服务器而非根服务器。

### 域名解析：递归查询与迭代查询
<!-- kp: ch6-s1-kp2 emphasis: key -->

**1. 解析过程（递归 + 迭代混合）**
典型查询过程（主机 → 本地域名服务器采用**递归查询**，本地 → 其他各级服务器采用**迭代查询**）：

1. 主机把待解析域名发给**本地域名服务器**（递归查询：本地服务器必须给出最终结果）；
2. 本地域名服务器若缓存命中，直接返回结果；否则先查**根域名服务器**（迭代查询）；
3. 根服务器返回**顶级域名服务器的地址**（不给最终结果）；
4. 本地域名服务器再查**顶级域名服务器**，得到**权限域名服务器**的地址；
5. 本地域名服务器再查**权限域名服务器**，得到该域名对应的 **IP 地址**；
6. 本地域名服务器把结果返回主机，并**存入缓存**。

**2. 两种查询方式对比**

| 查询方式 | 行为 | 负担 |
| --- | --- | --- |
| 递归查询 | 服务器"代为查询到底"，把最终结果返回给请求者 | 对承担递归的服务器负担大 |
| 迭代查询 | 服务器只返回"下一步该问谁"，由请求者自己继续查 | 请求者（本地服务器）负担大 |

实际中：主机对本地域名服务器是递归查询；本地域名服务器对根/顶级/权限域名服务器通常是迭代查询。

**3. DNS 缓存与 TTL**
- 每个域名服务器（包括主机）都缓存最近查询的结果；
- 缓存表项设置**生存时间 TTL**，超时删除，保证域名与 IP 对应关系的及时更新；
- 缓存可以显著减少 DNS 查询流量。

**4. 考点归纳**
- 递归查询与迭代查询的区别（谁干活）；
- 缓存 + TTL；
- 一个完整解析可能要查询多个服务器，但用户感知的只是一次 DNS 请求。

**重点：** 递归查询与迭代查询的含义及典型组合（本地递归、逐级迭代）；DNS 缓存。

**难点：** 若本地域名服务器也采用递归查询，则根/顶级/权限服务器将逐级向上承担大量递归任务，负担过重——所以设计成"本地递归、全局迭代"的折中。

## 文件传送协议 FTP
<!-- sec: ch6-s2 -->

### FTP 的工作原理
<!-- kp: ch6-s2-kp1 emphasis: key -->

**1. 功能**
FTP（文件传送协议，File Transfer Protocol）用于在互联网上**可靠地传送文件**（上传与下载），采用 **TCP** 连接，保证传输的可靠性。FTP 采用**客户-服务器**模式，服务器端有主进程和若干从属进程（控制进程、数据传送进程）。

**2. 两条 TCP 连接**
FTP 使用**两条独立的 TCP 连接**，这是 FTP 最核心的特征：

（1）**控制连接**：用于传输控制信息（命令与应答），服务器端**熟知端口 21**，在整个会话期间保持连接；
（2）**数据连接**：用于传输文件内容（数据），服务器端**熟知端口 20**，每次传输文件时**临时建立**，文件传完后关闭。

**3. 工作流程**
1. 客户向服务器的 21 端口发起连接请求，建立**控制连接**（随时保持）；
2. 客户通过控制连接发送命令（如 LIST、RETR、STOR）；
3. 服务器在端口 20 建立**数据连接**传送数据；
4. 数据传完后数据连接关闭；会话结束控制连接关闭。

**4. 考点归纳**
- 控制连接 21 端口、数据连接 20 端口；
- 控制连接全程保持，数据连接按需建立、传完即关；
- 两个连接都使用 TCP，可靠传输；
- "控制连接带外传输（out-of-band）"：控制信息与数据分开在不同连接上传送。

**重点：** 两条连接（21 控制、20 数据）的建立时机与生命周期。

**难点：** 为什么文件传完数据连接要关闭？——因为每传一个文件就新建数据连接，可以避免长时间占用端口，且连接数可动态管理。

### FTP 的两种工作模式
<!-- kp: ch6-s2-kp2 -->

**1. 主动模式（PORT 模式）**
主动模式下，**服务器主动**向客户发起数据连接：
1. 客户在控制连接（21）上发送 PORT 命令，告知服务器"我开放了某个端口 X 等你连"；
2. 服务器从它的 **20 端口**主动连接到客户的 X 端口建立数据连接；
3. 数据传完后服务器关闭数据连接。

问题：客户端常在 NAT/防火墙之后，服务器主动连接客户端口可能被防火墙拦截。

**2. 被动模式（PASV 模式）**
被动模式下，**客户主动**发起数据连接：
1. 客户发送 PASV 命令；
2. 服务器开放一个**临时端口**，通过控制连接告知客户；
3. **客户**主动连接服务器的这个临时端口建立数据连接。

被动模式解决了防火墙问题（连接全部由客户端发起），现代 FTP 客户端默认使用被动模式。

**3. 两种模式对比**

| 比较项 | 主动模式 PORT | 被动模式 PASV |
| --- | --- | --- |
| 数据连接发起方 | 服务器 | 客户端 |
| 服务器数据端口 | 固定 20 | 临时端口 |
| 防火墙兼容性 | 差 | 好 |
| 适用场景 | 服务器与客户端均可直连 | 客户端在 NAT/防火墙后 |

**4. 其他 FTP 知识**
- **匿名 FTP**：用户以 anonymous 登录，密码任意（通常为邮件地址），可下载公开文件；
- FTP 传输的文件类型：ASCII 文件与二进制文件；
- 文件传送三要素：文件类型、传输模式（流模式、块模式）、数据结构。

**重点：** 主动/被动模式下数据连接的方向；服务器端口 20（主动）vs 临时端口（被动）。

**难点：** 无论主动还是被动，**控制连接始终由客户端发起**（连 21 端口）；变化的只是数据连接的发起方向与服务器端口。

## 电子邮件
<!-- sec: ch6-s3 -->

### 电子邮件系统的组成与协议
<!-- kp: ch6-s3-kp1 emphasis: key -->

**1. 邮件系统组成**
电子邮件系统由**用户代理（UA）**、**邮件服务器**和**邮件协议**三部分组成：
- **用户代理**：用户与邮件系统的接口（如 Outlook、Foxmail、网页邮箱），负责撰写、显示、处理邮件；
- **邮件服务器**：运行在邮件服务端，**发送/接收邮件并维护用户邮箱**，功能类似"邮局"；
- **邮件协议**：SMTP（发送）、POP3/IMAP（接收）。

**2. 发送与接收协议**
- **SMTP**（简单邮件传送协议）：用于**发送方把邮件推送到邮件服务器，以及邮件服务器之间的转发**，使用 TCP，端口 **25**（提交用 587）。SMTP 只支持 7 位 ASCII 文本内容，因此发送中文/二进制需用 MIME 编码（见下节）；
- **POP3**（邮局协议第 3 版）：用户**从邮件服务器读取**邮件，使用 TCP，端口 **110**。POP3 是"**下载并删除**"模式（也可保留），无状态；
- **IMAP**（网际报文存取协议）：功能更强的收信协议，端口 **143**，允许用户在服务器上管理邮件（查看邮件头、在服务器端建立文件夹等），支持"下载但服务器保留"。

**3. 邮件发送流程**
1. 用户代理 →（SMTP 25）→ 发送方邮件服务器；
2. 发送方邮件服务器 →（SMTP）→ 接收方邮件服务器（若不同服务器）；
3. 用户代理 →（POP3 110 / IMAP 143）→ 接收方邮件服务器，读取邮件。

注意：**发送用 SMTP，接收用 POP3/IMAP**，这两个方向使用不同协议，这是常考辨析点。

**4. 对比表格**

| 协议 | 作用 | 端口 | 传输层 |
| --- | --- | --- | --- |
| SMTP | 发送邮件 | 25 | TCP |
| POP3 | 接收邮件（下载） | 110 | TCP |
| IMAP | 接收邮件（在线管理） | 143 | TCP |

**重点：** SMTP 管发送、POP3/IMAP 管接收；各协议端口号；邮件传送是"推（push）"与"拉（pull）"并存。

**难点：** SMTP 是"推"协议（主动推给服务器），POP3/IMAP 是"拉"协议（用户主动去取）；用户从"发送服务器"发信与从"接收服务器"收信是两个独立过程。

### MIME 多用途互联网邮件扩展
<!-- kp: ch6-s3-kp2 -->

**1. 为什么要 MIME**
SMTP 只能传送 **7 位 ASCII 码**的文本，无法直接传送中文、二进制文件（图片、视频）、多媒体等。**MIME（Multipurpose Internet Mail Extensions，多用途互联网邮件扩展）** 作为 SMTP 的补充，把非 ASCII 数据**编码成 ASCII** 后再交给 SMTP 传送，接收端再还原。MIME 不改变 SMTP 本身，只是在 SMTP 之上增加邮件正文格式与编码规定。

**2. MIME 的主要内容**
- **邮件首部增加字段**：MIME-Version、Content-Type（内容类型）、Content-Transfer-Encoding（传输编码）等；
- **正文格式定义**：支持多部分（multipart）邮件，允许一封邮件包含文本、图片、附件等多个部分（boundary 分隔）；
- **编码方式**：
  - **Base64 编码**：把每 3 字节（24 位）的二进制数据分成 4 组 6 位，映射为 64 个 ASCII 字符，编码后数据长度约为原长的 4/3；
  - **Quoted-Printable 编码**：适合大部分是 ASCII 的内容，只对特殊字符编码。

**3. 邮件报文格式**
一封标准邮件由**信封、首部、正文**三部分组成：首部含 To（收件人）、From（发件人）、Subject（主题）、Date（日期）等字段，首部之后是正文。MIME 扩展字段也位于首部中。

**4. 考点归纳**
- MIME 的作用是让 SMTP 能传非 ASCII 内容，是"扩展"而非替代；
- Base64 的编码规则与膨胀率（4/3）；
- MIME 新增首部字段：Content-Type、Content-Transfer-Encoding。

**重点：** MIME 是 SMTP 的扩展（解决 7 位 ASCII 限制）；Base64 编码思想。

**难点：** MIME 编码在"应用层用户代理"完成（先编码再交 SMTP），SMTP 协议本身并不感知 MIME——理解层次关系：MIME 属于用户代理层面，SMTP 属于邮件传送层面。

## HTTP 协议
<!-- sec: ch6-s4 -->

### HTTP 报文格式与请求方法
<!-- kp: ch6-s4-kp1 emphasis: key -->

**1. HTTP 的特点**
HTTP（超文本传送协议，HyperText Transfer Protocol）是万维网 WWW 的核心协议，基于 **TCP**（默认端口 **80**），采用**请求-响应**模式：客户（浏览器）发送请求报文，服务器返回响应报文。HTTP 是无状态协议（服务器不记忆过去的请求，用 Cookie 弥补）。

**2. 请求报文格式**
请求报文由**请求行、首部行、空行、实体主体**四部分组成：

- **请求行**：方法 + 空格 + URL + 空格 + 版本，如 GET /index.html HTTP/1.1；
- **首部行**：若干"字段名: 值"的行，如 Host、User-Agent、Connection、Cookie 等；
- **空行**：标志首部结束（一个 CRLF）；
- **实体主体**：请求数据（GET 一般没有，POST 有）。

**3. 请求方法**

| 方法 | 含义 |
| --- | --- |
| GET | 请求读取 URL 标识的资源（最常用） |
| POST | 向服务器提交数据（如表单） |
| HEAD | 只请求响应报文的首部（不返回主体），用于检查资源 |
| PUT | 上传/替换指定资源 |
| DELETE | 删除指定资源 |
| OPTIONS | 查询服务器支持的请求方法 |
| TRACE | 回显请求，用于诊断 |

**4. 响应报文格式**
响应报文由**状态行、首部行、空行、实体主体**四部分组成。状态行如 HTTP/1.1 200 OK，包含版本、状态码、短语。

**5. 考点归纳**
- 请求报文四部分结构与响应报文四部分结构；
- 常用方法 GET/POST/HEAD 的区别；
- HTTP 基于 TCP 80 端口，无状态。

**重点：** 请求报文/响应报文的组成；GET 与 POST 的区别；首部行与空行（空行 CRLF 标识首部结束）。

**难点：** 报文中"空行"不可或缺——它是首部与主体的分界；Content-Length 与传输结束的判定。

### HTTP 状态码
<!-- kp: ch6-s4-kp2 emphasis: key -->

**1. 状态码分类**
HTTP 状态码由 3 位数字组成，第一位数字表示类别：

| 类别 | 含义 | 典型例子 |
| --- | --- | --- |
| 1xx | 信息性状态码 | 100 Continue（继续） |
| 2xx | 成功 | 200 OK（成功）、204 No Content |
| 3xx | 重定向 | 301 Moved Permanently、302 Found、304 Not Modified |
| 4xx | 客户端错误 | 400 Bad Request、403 Forbidden、404 Not Found |
| 5xx | 服务器错误 | 500 Internal Server Error、502 Bad Gateway、503 Service Unavailable |

**2. 常见状态码详解**
- **200 OK**：请求成功，返回请求的资源；
- **301 Moved Permanently**：请求的资源已**永久**移到新 URL，响应中 Location 字段给出新地址，客户端应用新地址再次请求；
- **302 Found**：临时重定向，资源临时移到别处；
- **304 Not Modified**：条件请求（If-Modified-Since）中资源未修改，服务器不返回实体，客户端使用本地缓存——HTTP 缓存机制的核心状态码；
- **400 Bad Request**：请求报文语法错误；
- **403 Forbidden**：服务器拒绝请求（无权限）;
- **404 Not Found**：请求的资源不存在（最常考）；
- **500 Internal Server Error**：服务器内部错误；
- **503 Service Unavailable**：服务器暂时过载或维护中。

**3. 考点归纳**
- 第一位数字代表响应类别（1 信息、2 成功、3 重定向、4 客户端错、5 服务器错）；
- 404、301/302、304 是常考具体状态码；
- 状态行格式：HTTP/版本 状态码 短语。

**重点：** 状态码分类规则；常见状态码（200/301/302/304/403/404/500）的含义。

**难点：** 301（永久）与 302（临时）的区别、304（未修改，用缓存）与 200（返回内容）的区别——涉及 HTTP 缓存判断逻辑。

### 持久连接与非持久连接、Cookie
<!-- kp: ch6-s4-kp3 emphasis: difficult -->

**1. 非持久连接**
**非持久连接**：每一个请求-响应对都要**建立一条独立的 TCP 连接**。服务器发送完响应后即关闭连接，下次请求再重新建立连接。

【例】请求一个包含 10 个图片对象的网页（共 11 个对象），采用非持久连接需要建立 **11 次 TCP 连接**（每个对象一次）。设建立连接耗时 RTT（三次握手约 1.5 RTT），则总时间 ≈ 11 × (1.5 RTT + 请求传输 + 响应传输) ≈ 11×2×RTT + ...。非持久连接效率低，HTTP/1.0 采用。

**2. 持久连接（HTTP/1.1 默认）**
**持久连接**：一次 TCP 连接上可以传送**多个**请求/响应对象，服务器发送响应后**不关闭**连接。分为两种：
- **流水线方式**：客户在收到响应之前，**连续发送多个请求**（并行流水），服务器按序响应，效率最高；
- **非流水线方式**：客户收到上一个响应后才发下一个请求，效率次之。

HTTP/1.1 默认采用**持久连接（流水线方式）**，通过在报文首部设置 Connection: keep-alive。

**3. 三种连接方式对比**

| 方式 | TCP 连接次数 | 效率 | 协议版本 |
| --- | --- | --- | --- |
| 非持久连接 | 每对象 1 次 | 低 | HTTP/1.0 |
| 持久非流水线 | 1 次（串行请求） | 中 | HTTP/1.1 |
| 持久流水线 | 1 次（并行请求） | 高 | HTTP/1.1 |

**4. Cookie 机制**
HTTP 是无状态协议，服务器不记忆之前的交互。**Cookie** 在客户端（浏览器）保存一小段文本，用于**识别用户状态**（如登录状态、购物车）：
1. 服务器首次收到请求后，在响应报文首部加 Set-Cookie 字段；
2. 浏览器保存 Cookie，之后每次请求在首部携带 Cookie 字段；
3. 服务器据此识别用户，提供个性化/会话状态服务。

Cookie 保存在**用户端**（浏览器端）；与之相对，**会话跟踪**也可用服务器端保存的 Session（结合 Cookie 中的会话 ID）。

**5. 考点归纳**
- 非持久连接每对象一次 TCP；持久连接复用一个 TCP；
- HTTP/1.0 非持久、HTTP/1.1 默认持久流水线；
- Cookie 解决 HTTP 无状态问题，保存在客户端。

**重点：** 非持久与持久连接的对象数/TCP 连接数关系；HTTP/1.1 默认持久连接；Cookie 在客户端。

**难点：** 计算题——给定网页对象数、RTT，比较非持久/持久流水线/非流水线三种方式的总响应时间，需注意 TCP 建立连接约需 1.5 RTT（TCP 三次握手 + 请求）。

## HTTPS
<!-- sec: ch6-s5 -->

### HTTPS 概述
<!-- kp: ch6-s5-kp1 emphasis: key -->

**1. HTTPS 的本质**
HTTPS（HTTP over SSL/TLS）不是新的应用层协议，而是 **HTTP 加上 SSL/TLS 安全层**。在 TCP 与 HTTP 之间插入 SSL/TLS 子层，对 HTTP 报文进行加密、完整性校验和身份认证。HTTPS 使用 **TCP 端口 443**（HTTP 为 80）。

$$HTTPS = HTTP + SSL/TLS$$

**2. HTTPS 提供的安全服务**
- **机密性（加密）**：数据加密传输，防止窃听。应用数据用**对称加密**（如 AES）加密，密钥通过**非对称加密**（RSA/ECC）安全协商；
- **完整性**：报文用**消息认证码（MAC）/哈希**校验，防止篡改；
- **身份认证**：服务器（和可选客户端）通过**数字证书**证明身份，防止冒充（中间人攻击）。

**3. 数字证书与 CA**
- **CA（证书颁发机构）**为服务器签发**数字证书**，证书包含：服务器公钥、证书持有者信息、CA 签名、有效期等；
- 浏览器内置受信任的 CA 根证书，验证服务器证书的签名，确认"这个公钥确实是该服务器的"；
- 若证书无效（过期、域名不符、自签名），浏览器警告用户。

**4. HTTPS 与 HTTP 对比**

| 比较项 | HTTP | HTTPS |
| --- | --- | --- |
| 端口 | 80 | 443 |
| 加密 | 明文 | SSL/TLS 加密 |
| 身份认证 | 无 | 有（证书） |
| 数据完整性 | 无 | 有（MAC） |
| 性能 | 快 | 较慢（握手+加密开销） |

**重点：** HTTPS = HTTP + SSL/TLS；443 端口；三大安全目标（机密性、完整性、身份认证）。

**难点：** 加密是"混合加密"——用非对称加密协商对称密钥，用对称加密传输数据：非对称慢但安全，对称快但密钥分发难，两者结合兼顾安全与性能。

### SSL/TLS 握手过程
<!-- kp: ch6-s5-kp2 emphasis: difficult -->

**1. 握手目标**
SSL/TLS 握手在数据传输前完成三件事：**协商加密算法**、**认证服务器身份（证书）**、**生成会话密钥（对称密钥）**。以最常用的 TLS 1.2 完整握手为例：

**2. TLS 1.2 握手步骤（简化的双向密钥交换）**
1. **ClientHello**：客户端 → 服务器，发送支持的 TLS 版本、密码套件列表、客户端随机数（random_c）；
2. **ServerHello**：服务器 → 客户端，选定协议版本与密码套件，发送服务器随机数（random_s）；
3. **Certificate**：服务器 → 客户端，发送自己的**数字证书**（含服务器公钥）；
4. **ServerHelloDone**：服务器通知客户端"我的信息发送完毕"；
5. 客户端**验证证书**（信任链检查、有效期、域名），验证通过后生成**预主密钥（pre-master secret）**；
6. **ClientKeyExchange**：客户端 → 服务器，用**服务器公钥加密**预主密钥发送给服务器（非对称加密）；
7. 双方各自用 random_c、random_s 与预主密钥计算**主密钥**，再派生会话密钥（对称密钥）；
8. **ChangeCipherSpec**：双方告知对方"后续报文开始加密"；
9. **Finished**：双方发送握手完成消息（用会话密钥加密的哈希），互相验证后握手结束，开始加密传输应用数据。

**3. 关键点**
- 对称密钥（会话密钥）的协商通过**非对称加密**完成：只有拥有服务器私钥的一方才能解开 ClientKeyExchange 得到预主密钥；
- 预主密钥本身是随机生成的，即使被中间人截获也无法解密（无服务器私钥）；
- 每次会话的随机数不同，因此每次握手生成的会话密钥不同，防止重放攻击；
- **TLS 1.3** 简化握手：把 ServerHello 后的证书交换与密钥协商合并（0-RTT 支持），提高性能。

**4. 考点归纳**
- 握手的两阶段：认证（证书）→ 密钥交换（非对称）→ 加密通信（对称）；
- 证书的作用是"把服务器公钥安全地交给客户端"；
- 对称加密用于数据，非对称加密用于密钥交换与签名。

**重点：** 握手过程的三要素（协商算法、证书认证、密钥交换）；"非对称换钥、对称加密"的混合加密结构。

**难点：** 公钥加密预主密钥的安全性——公钥人人可得，但只有私钥能解密；数字证书解决了"公钥是谁的"这个信任问题（防中间人攻击）。
`;export{e as default};