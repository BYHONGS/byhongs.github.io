var e=`---
id: ch2
title: 线性表
order: 2
---

## 线性表的定义与基本操作
<!-- sec: ch2-s1 -->

### 线性表的定义与逻辑特征
<!-- kp: ch2-s1-k1 emphasis: key -->

## 线性表的定义

**线性表（Linear List）**是具有**相同数据类型**的 n（$$n \\ge 0$$）个数据元素的有限序列，记为 $$L=(a_1, a_2, \\ldots, a_i, a_{i+1}, \\ldots, a_n)$$。其中 n 称为线性表的**长度**，当 n=0 时称为**空表**。除第一个元素外，每个元素有且仅有一个直接前驱；除最后一个元素外，每个元素有且仅有一个直接后继。第一个元素 a_1 是唯一的表头元素（无前驱），最后一个元素 a_n 是唯一的表尾元素（无后继）。

## 逻辑特征

线性表的逻辑特征总结为**四个"一"**：

1. **同一性**：表中元素具有相同的数据类型；
2. **有穷性**：表中元素个数是有限的；
3. **有序性**：表中元素之间有严格的先后次序，a_i 是 a_{i+1} 的直接前驱；
4. **单个性**：除首尾元素外，每个元素有且只有一个直接前驱和一个直接后继。

线性表强调的是**元素之间的逻辑关系是线性的（一对一）**。栈、队列、串等都可以看作线性表的特殊情形。**注意**：线性表中的"元素有序"指的是逻辑次序，与元素值的大小无关。

**重点：**线性表区别于集合——线性表元素之间有前后相继的关系，而集合中的元素无序且互异。

### 线性表的基本操作
<!-- kp: ch2-s1-k2 -->

## 基本操作集合

线性表的抽象数据类型定义了一组基本操作（运算），常见的如下：

\`\`\`c
InitList(&L)          // 初始化线性表，构造一个空的线性表 L
Length(L)             // 求表长，返回线性表 L 中元素个数
LocateElem(L, e)      // 按值查找，返回值为 e 的元素在 L 中的位置
GetElem(L, i)         // 按位查找，返回 L 中第 i 个位置的元素值
ListInsert(&L, i, e)  // 插入，在 L 的第 i 个位置插入元素 e
ListDelete(&L, i, &e) // 删除，删除 L 中第 i 个位置的元素，并用 e 返回
PrintList(L)          // 输出线性表
Empty(L)              // 判空
DestroyList(&L)       // 销毁线性表
\`\`\`

## 操作要点

- 以 **"&"引用参数**传参的操作表示会对原表进行修改，如插入、删除、初始化；只读操作如求长度、查找则不必引用；
- 基本操作**只描述逻辑功能**，不涉及具体实现；实现方式取决于存储结构（顺序表或链表）；
- 题目中常把两个或多个基本操作组合起来（如"先查找再删除"）构成复杂操作，复杂度需按组合分析。

**重点：**区分按位查找（按序号 i，从 1 开始计数）与按值查找（按元素 e）；注意位序从 1 开始，而数组下标从 0 开始，这是很多算法实现与题目解答中的易错点。

## 顺序表
<!-- sec: ch2-s2 -->

### 顺序表的存储结构
<!-- kp: ch2-s2-k1 emphasis: key -->

## 顺序表的定义

**顺序表**是指用一组**地址连续**的存储单元依次存储线性表中各个元素的存储结构，即线性表的顺序存储。设顺序表起始位置为 LOC(A)，每个元素占 L 个存储单元，则第 i 个元素的存储位置为：

$$LOC(a_i) = LOC(A) + (i-1) \\times L$$

由于每个元素存储位置可由起始地址直接计算，顺序表具有**随机存取**的特性，即只要给出序号 i 就能在 $$O(1)$$ 时间内得到元素。

## 静态分配与动态分配

\`\`\`c
#define MaxSize 50
typedef struct {
    ElemType data[MaxSize];  // 静态分配，容量固定
    int length;              // 当前长度
} SqList;

typedef struct {
    ElemType *data;          // 动态分配
    int MaxSize, length;     // 最大容量与当前长度
} SeqList;
\`\`\`

静态分配一旦数组满便无法插入新元素；动态分配通过 \`realloc\` 可扩展容量，但扩充时需复制原有数据，代价为 O(n)。顺序表最重要的两个特征是：**逻辑上相邻的元素物理位置上也相邻**、**可随机存取**。

**重点：**顺序表要求连续的存储空间且随机存取 O(1)；下标从 0 开始，位序从 1 开始，下标 = 位序 - 1。

### 顺序表的插入与删除
<!-- kp: ch2-s2-k2 emphasis: key -->

## 插入操作

在第 i（$$1 \\le i \\le n+1$$）个位置插入元素 e：首先检查 i 是否合法且表是否已满，然后从最后一个元素起依次向后移动一个位置，空出第 i 个位置后放入 e，表长加 1。

\`\`\`c
bool ListInsert(SqList &L, int i, ElemType e) {
    if (i < 1 || i > L.length + 1) return false; // 位置非法
    if (L.length >= MaxSize) return false;       // 表满
    for (int j = L.length; j >= i; j--)          // 元素后移
        L.data[j] = L.data[j - 1];
    L.data[i - 1] = e;
    L.length++;
    return true;
}
\`\`\`

在第 i 个位置插入元素，需移动的元素个数为 $$n-i+1$$。插入到表头（i=1）需移动 n 个元素，插入到表尾（i=n+1）移动 0 个元素。若插入位置等概率，则平均移动次数为：

$$E_{insert} = \\sum_{i=1}^{n+1}\\frac{1}{n+1}(n-i+1) = \\frac{n}{2}$$

故插入操作的平均时间复杂度为 **O(n)**。

## 删除操作

删除第 i 个位置元素（$$1 \\le i \\le n$$）：将被删元素赋值给 e，然后从 i+1 起依次前移，表长减 1。

\`\`\`c
bool ListDelete(SqList &L, int i, ElemType &e) {
    if (i < 1 || i > L.length) return false;
    e = L.data[i - 1];
    for (int j = i; j < L.length; j++)
        L.data[j - 1] = L.data[j];
    L.length--;
    return true;
}
\`\`\`

删除第 i 个元素需移动 $$n-i$$ 个元素，平均移动次数为 $$\\frac{n-1}{2}$$，平均时间复杂度为 **O(n)**。

**重点：**插入/删除的平均移动次数分别约为 n/2 与 (n-1)/2，最坏为 O(n)，最好为 O(1)。

### 顺序表的查找
<!-- kp: ch2-s2-k3 -->

## 按位查找

按位查找返回第 i 个位置的元素值。由于顺序表随机存取，直接返回 $$L.data[i-1]$$ 即可，时间复杂度为 **O(1)**。

\`\`\`c
ElemType GetElem(SqList L, int i) {
    return L.data[i - 1];  // 随机存取，O(1)
}
\`\`\`

## 按值查找

按值查找在表中查找第一个值与给定值 e 相等的元素，返回其位序；查找失败返回 0。

\`\`\`c
int LocateElem(SqList L, ElemType e) {
    for (int i = 0; i < L.length; i++)
        if (L.data[i] == e)
            return i + 1;   // 返回位序
    return 0;
}
\`\`\`

按值查找需要从头依次比较。若查找成功概率均等，平均比较次数为 $$\\frac{1}{n}\\sum_{i=1}^{n}i = \\frac{n+1}{2}$$，时间复杂度为 **O(n)**。

## 插入、删除、查找复杂度汇总

| 操作 | 最好 | 最坏 | 平均 | 说明 |
| --- | --- | --- | --- | --- |
| 按位查找 | O(1) | O(1) | O(1) | 随机存取 |
| 按值查找 | O(1) | O(n) | O(n) | 需逐个比较 |
| 插入 | O(1) | O(n) | O(n) | 平均移动 n/2 |
| 删除 | O(1) | O(n) | O(n) | 平均移动 (n-1)/2 |

**重点：**顺序表的核心优势是随机存取（O(1) 按位查找），核心劣势是插入删除需大量移动元素。

## 单链表
<!-- sec: ch2-s3 -->

### 单链表的存储结构与头结点
<!-- kp: ch2-s3-k1 emphasis: key -->

## 存储结构

**单链表**通过一组任意的存储单元存储线性表元素，每个结点包含**数据域（data）**与**指针域（next）**两部分，指针域存放后继结点的地址。结点定义如下：

\`\`\`c
typedef struct LNode {
    ElemType data;
    struct LNode *next;
} LNode, *LinkList;
\`\`\`

由于每个结点存储位置与逻辑次序无关，链表中逻辑相邻的元素物理上不一定相邻，因此**不能随机存取**，只能从表头指针出发沿指针链**顺序存取**。查找第 i 个元素需要从头遍历 i-1 个结点，时间复杂度为 O(n)。

## 头结点与头指针

- **头指针**：指向链表中第一个结点的指针，是链表的"身份证"，只要头指针存在，链表就存在；
- **头结点**：在第一个元素结点之前附加的一个结点，其数据域可以为空或存表长等附加信息。

引入头结点有三大优点：①在链表**第一个位置插入/删除**元素时，无需特殊处理头指针，插入删除逻辑统一；②无论链表是否为空，**头指针都非空**，空表与非空表的处理统一；③可以方便地在头结点数据域存放表长等信息。**注意**：头结点的 next 为 NULL 表示空表，但带头结点时空表头指针仍然指向头结点而非 NULL。

**重点：**区分"头指针"（标志链表的指针，必不为空）与"头结点"（可选附加结点）。408 中大量链表算法题要求熟练处理带头结点的情况。

### 单链表的建立：头插法与尾插法
<!-- kp: ch2-s3-k2 emphasis: key -->

## 头插法

头插法每次将新结点插入到表头（头结点之后），读入数据的**顺序与生成的链表顺序相反**，即实现了"逆序建表"。时间复杂度为 O(n)。

\`\`\`c
LinkList HeadInsert(LinkList &L) {
    LNode *s; int x;
    L = (LinkList)malloc(sizeof(LNode));  // 创建头结点
    L->next = NULL;
    scanf("%d", &x);
    while (x != 9999) {
        s = (LNode*)malloc(sizeof(LNode));
        s->data = x;
        s->next = L->next;   // 关键：新结点指向原首元结点
        L->next = s;         // 头结点指向新结点
        scanf("%d", &x);
    }
    return L;
}
\`\`\`

## 尾插法

尾插法借助一个尾指针 r 始终指向当前最后一个结点，新结点接到链表尾部，读入顺序与生成顺序一致。时间复杂度为 O(n)。

\`\`\`c
LinkList TailInsert(LinkList &L) {
    LNode *s, *r; int x;
    L = (LinkList)malloc(sizeof(LNode));
    L->next = NULL;
    r = L;                     // r 始终指向尾结点
    scanf("%d", &x);
    while (x != 9999) {
        s = (LNode*)malloc(sizeof(LNode));
        s->data = x;
        r->next = s;
        r = s;                 // 更新尾指针
        scanf("%d", &x);
    }
    r->next = NULL;            // 置空表尾
    return L;
}
\`\`\`

**重点：**头插法适用于"就地逆置"单链表的操作；尾插法建表必须维护尾指针。两者时间复杂度均为 O(n)，但头插法实现简单、无需尾指针。

### 单链表的插入与删除
<!-- kp: ch2-s3-k3 emphasis: key -->

## 按位插入

在第 i 个位置插入结点：先找到第 i-1 个结点（前驱），再执行"新结点先连后继、前驱再连新结点"两步。

\`\`\`c
bool ListInsert(LinkList &L, int i, ElemType e) {
    if (i < 1) return false;
    LNode *p = L; int j = 0;
    while (p != NULL && j < i - 1) { p = p->next; j++; } // 找第 i-1 个结点
    if (p == NULL) return false;
    LNode *s = (LNode*)malloc(sizeof(LNode));
    s->data = e;
    s->next = p->next;   // 1. 新结点指针指向后继
    p->next = s;         // 2. 前驱指向新结点
    return true;
}
\`\`\`

找到前驱结点耗时 O(n)，插入本身 O(1)。**前插法技巧**：已知某结点 p 时，可在 p 之后插入新结点 s（O(1)），再交换 p 与 s 的数据域，即可实现"在 p 之前插入"，时间仍为 O(1)。

## 删除结点

删除第 i 个结点：找到第 i-1 个结点 p，令 p->next 指向被删结点的后继，然后释放被删结点。

\`\`\`c
bool ListDelete(LinkList &L, int i, ElemType &e) {
    if (i < 1) return false;
    LNode *p = L; int j = 0;
    while (p->next != NULL && j < i - 1) { p = p->next; j++; }
    if (p->next == NULL) return false;  // 第 i 个结点不存在
    LNode *q = p->next;
    e = q->data;
    p->next = q->next;   // 跳过 q
    free(q);
    return true;
}
\`\`\`

**技巧**：删除结点 p 且没有前驱指针时，可用"偷梁换柱"法——把 p 的后继数据复制到 p，再删除后继结点，时间复杂度 O(1)。该方法在"删除单链表中值为 x 的结点"等题型中经常使用。

**重点：**链表插入删除的优点是"改指针即可、无需移动元素"，但必须先找到前驱，导致整体为 O(n)；只有已知前驱或操作首元结点时才能 O(1) 完成。

## 双链表与循环链表
<!-- sec: ch2-s4 -->

### 双链表的插入与删除
<!-- kp: ch2-s4-k1 emphasis: key -->

## 双链表的存储结构

双链表每个结点有**两个指针域**：prior 指向直接前驱，next 指向直接后继。相比单链表，双链表可以**从任意结点出发向前或向后遍历**，查找前驱的时间从 O(n) 降为 O(1)。

\`\`\`c
typedef struct DNode {
    ElemType data;
    struct DNode *prior, *next;
} DNode, *DLinkList;
\`\`\`

## 双链表的插入

在结点 p 之后插入新结点 s：

\`\`\`c
s->next = p->next;        // 1. s 连后继
if (p->next != NULL)      // 2. 后继回连 s（p 不是尾结点时）
    p->next->prior = s;
s->prior = p;             // 3. s 连前驱
p->next = s;              // 4. p 连 s
\`\`\`

四步顺序不能乱：**"先连后断"**——先把 s 的两侧连接好，最后再断开/修改 p 与后继的原链接，避免链表断裂丢失。插入时间 O(1)（已知结点 p）。

## 双链表的删除

删除结点 p 的后继 q：

\`\`\`c
p->next = q->next;
if (q->next != NULL)
    q->next->prior = p;
free(q);
\`\`\`

删除结点 p 本身：\`p->prior->next = p->next\`，\`p->next->prior = p->prior\`，再 free(p)，时间复杂度 O(1)。

**重点：**双链表操作比单链表多了一步"回连 prior"；特别注意边界情况——在尾结点后插入或删除尾结点时，\`->next\` 可能为 NULL，须判空。

### 循环链表与静态链表
<!-- kp: ch2-s4-k2 -->

## 循环单链表

**循环单链表**的最后一个结点的 next 指针指向**头结点**（或首元结点，取决于是否带头结点），形成一个环。判断空表条件由"L->next == NULL"变为"L->next == L"。循环单链表可以从任意结点出发遍历整个链表。

若用**尾指针 r 代替头指针**，则访问表头元素（r->next）与表尾元素（r）均为 O(1)，非常适用于"将两个循环单链表合并为一个"的操作。合并两个循环链表时，只需把 A 的表尾接到 B 的表头、B 的表尾接到 A 的表头，并修改尾指针即可，时间为 O(1)。

## 循环双链表

**循环双链表**的头结点的 prior 指向表尾结点，表尾结点的 next 指向头结点。判空条件为"L->next == L 且 L->prior == L"。

## 静态链表

**静态链表**借助数组来模拟链表，每个数组元素含 data 与 cur 两个域，cur 存放后继元素在数组中的下标。静态链表以"游标"代替指针，**优点**：插入删除只需修改游标、无需移动元素；**缺点**：容量固定不可动态扩充，且不能随机存取。静态链表适用于不支持指针的早期语言（如部分嵌入式环境）。

**重点：**循环链表的核心是"表尾指向头"形成的环，判空条件变化是高频考点；静态链表用数组下标（游标）模拟指针，理解"游标 -1/0 表示表尾"这类约定即可。

## 顺序表与链表的比较及综合应用
<!-- sec: ch2-s5 -->

### 顺序表与链表的性能对比
<!-- kp: ch2-s5-k1 emphasis: key -->

## 对比表

| 比较项目 | 顺序表 | 链表 |
| --- | --- | --- |
| 存储方式 | 连续存储 | 链式存储（任意位置） |
| 随机存取 | 支持，O(1) | 不支持，需遍历 O(n) |
| 按值查找 | O(n)（有序时可二分 O(log n)） | O(n) |
| 插入/删除 | 需移动大量元素，平均 O(n) | 只需改指针，已知位置 O(1) |
| 空间分配 | 静态分配容量固定 / 动态分配需移动 | 按需分配，灵活但每结点多存指针 |
| 空间利用率 | 高（无指针开销），但可能浪费连续空间 | 较低（指针域开销），无碎片问题 |
| 适用场景 | 表长基本确定、频繁按位查找 | 频繁插入删除、表长变化大 |

## 选择原则

- 若线性表**长度基本固定**且**频繁按序号访问**，应选用顺序表；
- 若**频繁插入删除**或表长**难以预计**，应选用链表；
- 若在线性表中进行的操作是**按值查找**，两者都是 O(n)；若顺序表保持有序，可采用折半查找降为 O(log n)，优于链表的 O(n)。

**重点：**408 常以"给出操作序列，判断选用哪种存储结构最优"命题。判断标准：随机存取选顺序表，频繁插入删除选链表。

### 典型综合应用题
<!-- kp: ch2-s5-k2 -->

## 常考题型

408 线性表大题为算法设计题（15 分左右），常考以下操作，均需用 C 语言实现并分析复杂度：

**1. 链表就地逆置**：利用头插法逐个摘取结点插入表头，时间复杂度 O(n)、空间 O(1)。

\`\`\`c
void Reverse(LinkList &L) {
    LNode *p = L->next, *q;
    L->next = NULL;
    while (p != NULL) {
        q = p->next;
        p->next = L->next;  // 头插
        L->next = p;
        p = q;
    }
}
\`\`\`

**2. 删除所有值为 x 的结点**：遍历链表，借助前驱指针删除；也可用尾插法把不等于 x 的结点重新链成一个新表。

**3. 有序单链表的合并**：两个递增有序链表归并为一个递增有序链表，用尾插法依次取较小者，时间 O(m+n)。

**4. 找出链表的中间结点**：快慢指针法——快指针每次走两步、慢指针每次走一步，快指针到表尾时慢指针恰好在中点，时间 O(n)。

**5. 判断链表是否有环**：同样用快慢指针，若两指针相遇则有环。

**重点：**408 算法题评分关注"算法思想 + 代码正确 + 复杂度分析"。熟练掌握头插逆置、快慢指针、尾插归并三大套路可覆盖大部分线性表大题。
`;export{e as default};