var e=`---
id: ch5
title: 树与二叉树
order: 5
---

## 树的基本概念
<!-- sec: ch5-s1 -->

### 树的定义与基本术语
<!-- kp: ch5-s1-k1 emphasis: key -->

## 树的定义

**树（Tree）**是 n（$$n \\ge 0$$）个结点的有限集合。n=0 时称为**空树**。任意非空树满足：①有且仅有一个特定的称为**根（Root）**的结点；②其余结点可分为 m（$$m \\ge 0$$）个互不相交的有限集合 $$T_1, T_2, \\ldots, T_m$$，每个集合本身又是一棵树（称为根的**子树**）。树的定义是递归的：树中每个结点都是其子树的根。

## 基本术语

- **结点度（Degree）**：结点拥有的子树个数；
- **树的度**：树内各结点度的最大值；
- **分支结点**：度大于 0 的结点（非终端结点）；**叶子结点（终端结点）**：度为 0 的结点；
- **孩子与双亲**：某结点子树的根称为该结点的孩子，反之该结点为孩子的双亲（注意术语为"双亲"而非"父"）；
- **兄弟、堂兄弟**：同一双亲的孩子互称兄弟，双亲是兄弟关系的结点互称堂兄弟；
- **祖先与子孙**：从根到某结点路径上的所有结点是该结点的祖先；某结点子树中的所有结点是其子孙；
- **层次（Level）**：根为第 1 层，其孩子为第 2 层，以此类推；
- **深度（高度）**：结点的深度是从根到该结点路径上的边数或层数；树的高度是树中结点的最大层次；
- **有序树与无序树**：子树有左右次序之分称为有序树，否则为无序树；
- **森林**：m（$$m \\ge 0$$）棵互不相交的树的集合。

**重点：**"树的高度（最大层次）"与"结点的深度"计算是选择题常考点；注意双亲-孩子、兄弟-堂兄弟、祖先-子孙三组概念的精确含义。

### 树的性质
<!-- kp: ch5-s1-k2 -->

## 树的常用性质

1. **结点数与边数**：树中结点数 n 等于所有结点的度之和加 1，即 $$n = \\sum_{v \\in V} deg(v) + 1$$，因为除根外每个结点都有一条入边；
2. **度为 m 的树**：第 i 层上至多有 $$m^{i-1}$$ 个结点；
3. **高度为 h 的 m 叉树**：至多有 $$\\frac{m^h - 1}{m - 1}$$ 个结点；
4. **n 个结点的 m 叉树**：最小高度为 $$\\lceil \\log_m(n(m-1)+1) \\rceil$$；
5. **已知叶子数**：若度为 2 的结点数为 $$n_2$$，则叶子数为 $$n_0 = n_2 + 1$$（该性质对任意树不成立，仅对二叉树成立）。

## 路径与路径长度

结点之间的**路径**是从一个结点到另一个结点所经过的**分支序列**（边序列），路径长度是路径上经过的边的数目。树中两结点之间路径唯一。从根到第 k 层结点的路径长度为 k-1。

**典型例题**：一棵度为 4 的树，n_1=3, n_2=2, n_3=1, n_4=1，求叶子数。由性质1，总边数 = 结点数-1，即 $$3+2+1+1 = 7$$ 条边，故结点总数 $$n = 7+1 = 8$$，叶子数 $$n_0 = 8 - (3+2+1+1) = 1$$。

**重点：**"度和 + 1 = 结点数"是树类题目最常用的万能公式，由它可以推导出各种度数结点之间的数量关系。

## 二叉树的概念与性质
<!-- sec: ch5-s2 -->

### 二叉树的定义与形态
<!-- kp: ch5-s2-k1 -->

## 二叉树的定义

**二叉树（Binary Tree）**是 n（$$n \\ge 0$$）个结点的有限集合，要么是空集，要么由一个根结点及两棵**互不相交**的、分别称为根结点的**左子树**和**右子树**的二叉树组成。二叉树每个结点至多有两棵子树，且子树有**左右之分**，次序不能颠倒。

## 二叉树的五种基本形态

1. 空二叉树；
2. 只有根结点；
3. 只有左子树（右子树为空）；
4. 只有右子树（左子树为空）；
5. 左右子树均非空。

## 满二叉树与完全二叉树

- **满二叉树**：高度为 h 且含有 $$2^h - 1$$ 个结点的二叉树，即每一层的结点数都达到最大值。满二叉树中只有度为 0 和度为 2 的结点；
- **完全二叉树**：高度为 h、有 n 个结点的二叉树，其结点与高度为 h 的满二叉树中编号 1~n 的结点一一对应。完全二叉树的特点是：①叶子结点只可能出现在**最后两层**；②对任一结点，若其右子树的最大层次为 l，则其左子树的最大层次必为 l 或 l+1。

**注意**：满二叉树一定是完全二叉树，完全二叉树不一定是满二叉树。判断是否为完全二叉树可用"按层编号"法：结点编号与满二叉树编号无空缺即为完全二叉树。

**重点：**二叉树与度为 2 的树不同——二叉树区分左右子树且允许度为 1 的结点只有一个孩子；而"度为 2 的树"没有左右之分，且至少有一个结点度为 2。

### 二叉树的5条性质
<!-- kp: ch5-s2-k2 emphasis: key -->

## 五条重要性质

**性质1**：非空二叉树第 i 层（$$i \\ge 1$$）上至多有 $2^{i-1}$ 个结点。

**性质2**：高度为 h 的二叉树至多有 $2^h - 1$ 个结点。

**性质3**：对任意二叉树，设度为 0、1、2 的结点数分别为 $n_0, n_1, n_2$，则：

$$n_0 = n_2 + 1$$

**推导**：结点总数 $n = n_0 + n_1 + n_2$；总边数 $= n - 1 = n_1 + 2n_2$；两式相消得 $n_0 = n_2 + 1$。

**性质4**：具有 n 个结点的完全二叉树的高度为：

$$h = \\lceil \\log_2(n+1) \\rceil \\quad \\text{或} \\quad h = \\lfloor \\log_2 n \\rfloor + 1$$

**性质5**：对 n 个结点的完全二叉树按层编号（从 1 开始，自上而下、自左至右），则对任一结点 i（$$1 \\le i \\le n$$）：

- 若 i=1，则 i 为根结点；若 i>1，则其**双亲**为 $\\lfloor i/2 \\rfloor$；
- 若 $2i \\le n$，则 i 的**左孩子**为 $2i$，否则 i 无左孩子（i 为叶子）；
- 若 $2i+1 \\le n$，则 i 的**右孩子**为 $2i+1$，否则 i 无右孩子。

**典型应用**：已知完全二叉树有 768 个结点，则叶子数 $n_0 = n - n_1 - n_2$，且 $n_1$ 只可能是 0 或 1；由 $n_0 = n_2 + 1$ 得 $n = 2n_0 - 1 + n_1$，故 $n_0 = \\frac{n + 1 - n_1}{2}$，当 $n=768$ 时 $n_1=1$（因为 $768-2n_0=-1$ 为奇数），得 $n_0 = 384$。

**难点：**性质5（编号规律）是顺序存储完全二叉树的基础，也是判断双亲/孩子下标的依据，须烂熟于心。

### 完全二叉树的性质与应用
<!-- kp: ch5-s2-k3 emphasis: key -->

## 完全二叉树的性质

- 叶子结点只可能出现在最后两层；若最后一层不满，则其结点集中在**最左边**；
- 对完全二叉树，若 i 为分支结点，则 $\\lfloor n/2 \\rfloor$ 是最后一个分支结点的编号（即编号 $> \\lfloor n/2 \\rfloor$ 的结点均为叶子）；
- 若 n 为奇数，则每个分支结点都有两个孩子（$n_1 = 0$）；若 n 为偶数，则编号 $n/2$ 的结点只有左孩子（$n_1 = 1$）；
- 完全二叉树的高度 $h = \\lceil \\log_2(n+1) \\rceil$。

## 典型应用

**例**：一棵完全二叉树有 1001 个结点，求叶子结点数。由 $n = 2n_0 - 1 + n_1$，因 n 为奇数，$n_1 = 0$，故 $n_0 = (1001+1)/2 = 501$。

**例**：完全二叉树第 6 层有 8 个叶子，求最多/最少结点数。最多：第 6 层除 8 个叶子外其余均为分支结点且第 7 层填满，$n_{max} = (2^6-1) + (64-8) \\times 2 = 63 + 112 = 175$；最少：第 6 层全部是叶子，$n_{min} = 2^6 - 1 = 63$（第 7 层没有结点）。

**重点：**$n_1 \\in \\{0, 1\\}$ 的性质使完全二叉树的结点计数问题成为 408 高频计算题，务必熟练掌握 $n = 2n_0 - 1 + n_1$ 这一变形公式。

## 二叉树的存储与遍历
<!-- sec: ch5-s3 -->

### 二叉树的顺序存储与链式存储
<!-- kp: ch5-s3-k1 emphasis: key -->

## 顺序存储

用一组连续存储单元按**完全二叉树的编号次序**存储结点。结点 i 存放在下标 i 处，其双亲为 $\\lfloor i/2 \\rfloor$、左孩子为 $2i$、右孩子为 $2i+1$。这种存储方式**只适合完全二叉树**：普通二叉树存在空结点时，必须用"0"或特殊值占位，会造成大量空间浪费。例如一棵单支深度为 h 的二叉树，顺序存储需要 $2^h-1$ 个单元，而实际只有 h 个结点，空间利用率极低。

## 链式存储

**二叉链表**每个结点含数据域 data、左孩子指针 lchild、右孩子指针 rchild。这是最常用的存储方式。

\`\`\`c
typedef struct BiTNode {
    ElemType data;
    struct BiTNode *lchild, *rchild;
} BiTNode, *BiTree;
\`\`\`

在含有 n 个结点的二叉链表中，有 n-1 条边对应 n-1 个非空指针（每个结点除根外都有一条入边），共有 $$2n$$ 个指针域，因此**空指针域个数为**：

$$2n - (n-1) = n + 1$$

## 三叉链表

需要频繁访问双亲时可采用三叉链表，增加 parent 指针指向双亲结点，空间开销更大。

**重点：**二叉链表空指针域个数为 n+1 是选择题常考结论，它与线索二叉树的建立密切相关。

### 先序、中序、后序遍历的递归实现
<!-- kp: ch5-s3-k2 emphasis: key -->

## 遍历的定义

**遍历**是指按某条搜索路径访问树中每个结点，使每个结点被访问一次且仅被访问一次。若规定先左后右，则二叉树有三种经典遍历：

- **先序遍历（PreOrder）**：根 → 左 → 右，即"根左右"；
- **中序遍历（InOrder）**：左 → 根 → 右，即"左根右"；
- **后序遍历（PostOrder）**：左 → 右 → 根，即"左右根"。

## 递归实现

\`\`\`c
void PreOrder(BiTree T) {           // 先序遍历
    if (T != NULL) {
        visit(T);                   // 访问根
        PreOrder(T->lchild);        // 遍历左子树
        PreOrder(T->rchild);        // 遍历右子树
    }
}
void InOrder(BiTree T) {            // 中序遍历
    if (T != NULL) {
        InOrder(T->lchild);
        visit(T);
        InOrder(T->rchild);
    }
}
void PostOrder(BiTree T) {          // 后序遍历
    if (T != NULL) {
        PostOrder(T->lchild);
        PostOrder(T->rchild);
        visit(T);
    }
}
\`\`\`

三种递归遍历的时间复杂度均为 O(n)（每个结点访问一次），空间复杂度为 O(h)（h 为树高，递归栈深度）。单支树最坏时 h=n，空间复杂度退化为 O(n)。

## 遍历次序的特征

- **先序**的第一个访问结点是根；
- **后序**的最后一个访问结点是根；
- **中序**中根将左右子树分开，可据此划分左右子树序列。

**重点：**写出给定二叉树的三种遍历序列是基本功；反过来，已知"先序+中序"或"后序+中序"可以唯一确定二叉树。

### 非递归遍历与层次遍历
<!-- kp: ch5-s3-k3 emphasis: key -->

## 中序非递归遍历（用栈）

借助栈模拟递归过程：从根出发，沿左孩子不断入栈；左路走到头则出栈访问，再转向右子树重复。先序与后序非递归遍历类似，后序需额外处理"右子树已访问"的标志（或用两个栈/逆序法）。

\`\`\`c
void InOrder2(BiTree T) {
    SqStack S; InitStack(S);
    BiTNode *p = T;
    while (p || !StackEmpty(S)) {
        if (p) {                // 一路向左
            Push(S, p);
            p = p->lchild;
        } else {                // 出栈访问，转右
            Pop(S, p);
            visit(p);
            p = p->rchild;
        }
    }
}
\`\`\`

## 层次遍历（用队列）

层次遍历按"自上而下、自左至右"逐层访问，借助队列：根结点入队，循环中出队一个结点并访问，若其左/右孩子非空则依次入队。

\`\`\`c
void LevelOrder(BiTree T) {
    SqQueue Q; InitQueue(Q);
    if (T == NULL) return;
    EnQueue(Q, T);
    while (!QueueEmpty(Q)) {
        BiTNode *p; DeQueue(Q, p);
        visit(p);
        if (p->lchild) EnQueue(Q, p->lchild);
        if (p->rchild) EnQueue(Q, p->rchild);
    }
}
\`\`\`

层次遍历的时间复杂度 O(n)，空间复杂度 O(n)（队列最大长度接近最后一层结点数）。

## 由遍历序列构造二叉树

- **先序 + 中序**：先序第一个为根，在中序中定位根即可划分左右子树，递归构造；
- **后序 + 中序**：后序最后一个为根，其余同法；
- **先序 + 后序**：不能唯一确定二叉树（无法区分左右子树）。

**重点：**"先序+中序""后序+中序"确定唯一二叉树是 408 必考题型；层次遍历同时是图 BFS 的基础。

## 线索二叉树
<!-- sec: ch5-s4 -->

### 线索二叉树的概念与结构
<!-- kp: ch5-s4-k1 emphasis: key -->

## 线索化的动机

n 个结点的二叉链表有 n+1 个空指针域（前文已证），这些空指针被白白浪费。**线索二叉树**利用这些空指针存放结点在某种遍历次序下的**前驱**与**后继**信息：若结点的左指针为空，则改为指向其**前驱**；若右指针为空，则改为指向其**后继**。

## 存储结构

为区分指针到底指向孩子还是前驱/后继，每个结点增加两个标志位 ltag 与 rtag：

\`\`\`c
typedef struct ThreadNode {
    ElemType data;
    struct ThreadNode *lchild, *rchild;
    int ltag, rtag;   // 0 表示孩子指针，1 表示线索
} ThreadNode, *ThreadTree;
\`\`\`

规定：$$ltag = \\begin{cases} 0, & lchild \\text{ 指向左孩子} \\\\ 1, & lchild \\text{ 指向前驱} \\end{cases}$$，rtag 同理。按遍历次序的不同，线索二叉树分为**先序线索二叉树、中序线索二叉树、后序线索二叉树**三种。

## 线索二叉树的建立

线索化的本质是**遍历过程中记录前驱结点 pre**：访问当前结点 p 时，若 p->lchild 为空，则令其指向前驱 pre 并置 ltag=1；若 pre->rchild 为空，则令其指向 p 并置 rtag=1。遍历结束后最后一个结点的后继线索置为 NULL。

**重点：**线索是针对**某一遍历次序**而言的，同一棵二叉树的中序线索与前序线索完全不同。408 常考"某结点的前驱/后继是谁"以及线索指针的指向。

### 线索二叉树的查找与遍历
<!-- kp: ch5-s4-k2 emphasis: difficult -->

## 中序线索二叉树的查找

在中序线索二叉树中：

- **找中序后继**：若 rtag=1，则 rchild 直接指向后继；若 rtag=0，则后继是**其右子树中最左下（第一个中序访问）的结点**；
- **找中序前驱**：若 ltag=1，则 lchild 直接指向前驱；若 ltag=0，则前驱是**其左子树中最右下（最后一个中序访问）的结点**。

\`\`\`c
// 找以 p 为根的子树中序第一个结点（最左下）
ThreadNode* FirstNode(ThreadNode *p) {
    while (p->ltag == 0) p = p->lchild;
    return p;
}
// 找 p 的中序后继
ThreadNode* NextNode(ThreadNode *p) {
    if (p->rtag == 1) return p->rchild;
    return FirstNode(p->rchild);   // 右子树最左下
}
// 中序遍历（无需递归栈）
void InOrder(ThreadTree T) {
    for (ThreadNode *p = FirstNode(T); p != NULL; p = NextNode(p))
        visit(p);
}
\`\`\`

## 前序与后序线索的注意点

- **先序线索二叉树找前驱**：若 ltag=1 直接用线索；若 ltag=0（有左孩子），其前驱需分情况讨论（双亲或双亲的左兄弟等），不能像中序那样简单找到；
- **后序线索二叉树找后继**：同理需要分情况（双亲或双亲右孩子），无法统一用右子树判断。

**难点：**线索化时"空指针指向前驱/后继"的判定（rtag/ltag）以及"非空线索下的前驱后继定位"需要结合遍历次序仔细推理；后序线索找后继是难点中的难点。

### 线索二叉树的建立（中序线索化）
<!-- kp: ch5-s4-k3 -->

**线索化**就是把二叉树中的空指针改为指向前驱/后继的过程：中序遍历二叉树，用指针 **pre 记录刚访问过的结点**，当当前结点 p 的 lchild 为空时令其指向 pre（并置 ltag=1），当 pre 的 rchild 为空时令其指向 p（并置 rtag=1）。中序线索化的标准写法（王道版）：

\`\`\`c
void InThread(ThreadTree &p, ThreadNode *&pre) {
    if (p == NULL) return;
    InThread(p->lchild, pre);              // 线索化左子树
    if (p->lchild == NULL) {               // 左子树为空，建立前驱线索
        p->lchild = pre;  p->ltag = 1;
    }
    if (pre != NULL && pre->rchild == NULL) {  // 前驱右子树为空，建立后继线索
        pre->rchild = p;  pre->rtag = 1;
    }
    pre = p;                                // 更新前驱
    InThread(p->rchild, pre);              // 线索化右子树
}
void CreateInThread(ThreadTree T) {
    ThreadNode *pre = NULL;
    if (T != NULL) { InThread(T, pre); pre->rchild = NULL; pre->rtag = 1; }
}
\`\`\`

先序/后序线索化同理（把访问位置移到相应次序），但**先序线索化可能对左子树递归造成环路**（若左孩子已改为线索指向pre，需先判断 ltag==0 再递归左子树）；后序线索化只需在左右子树处理完之后访问。408常考：补全线索化代码、判断某结点在某种线索二叉树中的前驱/后继。

**难点：**"pre指针随遍历推进"是线索化的核心；先序线索化递归左子树前必须判断 ltag==0 防止死循环；线索化结束后还要处理最后一个结点的空右链。

## 树与森林
<!-- sec: ch5-s5 -->

### 树与森林的存储结构
<!-- kp: ch5-s5-k1 -->

## 双亲表示法

用一组连续空间存储结点，每个结点含数据域 data 与双亲下标域 parent。**优点**：查找某结点的双亲非常方便（O(1)）；**缺点**：找孩子需要遍历整个表，效率低。适用于"并查集"等只关心双亲的应用。

\`\`\`c
typedef struct {          // 树的结点定义
    ElemType data;
    int parent;           // 双亲在数组中的下标
} PTNode;
typedef struct {          // 树的类型定义
    PTNode nodes[MaxSize];
    int n;                // 结点数
} PTree;
\`\`\`

## 孩子表示法

每个结点除 data 外，用一个链表存放其所有孩子结点的下标。**优点**：找孩子方便；**缺点**：找双亲需遍历所有链表。

## 孩子兄弟表示法（二叉树表示法）

每个结点含 data、firstchild（第一个孩子指针）、nextsibling（下一个兄弟指针）。**优点**：可以将任意一棵树方便地转换为二叉树，且便于实现树的遍历。

\`\`\`c
typedef struct CSNode {
    ElemType data;
    struct CSNode *firstchild, *nextsibling;
} CSNode, *CSTree;
\`\`\`

**重点：**孩子兄弟表示法是"树↔二叉树"转换的桥梁。森林同样可用"第一棵树的根作为根、其余树作为其右兄弟"的方式转换为二叉树。

### 树、森林与二叉树的转换
<!-- kp: ch5-s5-k2 emphasis: key -->

## 转换规则

**树 → 二叉树**（左孩子右兄弟法）：每个结点的**第一个孩子**作为其**左孩子**，**下一个兄弟**作为其**右孩子**。转换后二叉树的右子树为空当且仅当原树每个结点至多有一个孩子……不对——更准确地说，树的根结点转换后**没有右子树**（根没有兄弟）。

**森林 → 二叉树**：把每棵树的根作为二叉树的根，第一棵树的根作为整个二叉树的根，其余树的根依次作为前一棵树根的**右孩子**（即"兄终弟及"）。

**二叉树 → 树/森林**：逆过程——左孩子是第一个孩子，右孩子是下一个兄弟。

## 转换后的性质

树转换为二叉树后，遍历关系成立：

| 原结构 | 对应的二叉树遍历 |
| --- | --- |
| 树的先根遍历（根→子树依次） | 二叉树的先序遍历 |
| 树的后根遍历（子树依次→根） | 二叉树的中序遍历 |
| 森林的先序遍历 | 二叉树先序遍历 |
| 森林的中序遍历 | 二叉树中序遍历 |

因此，**树的先根遍历结果 = 转换后二叉树的先序遍历结果，树的后根遍历结果 = 转换后二叉树的中序遍历结果**。森林的两种遍历与之对应。

**重点：**"左孩子右兄弟"是唯一的转换口诀；"树的后根遍历对应二叉树中序遍历"是 408 常考对应关系。

## 哈夫曼树与哈夫曼编码
<!-- sec: ch5-s6 -->

### 哈夫曼树的定义与构造
<!-- kp: ch5-s6-k1 emphasis: key -->

## 相关概念

- **路径长度**：从一个结点到另一个结点的边数；
- **树的路径长度**：根到每个结点的路径长度之和；
- **权（Weight）**：给树的结点赋予的具有某种含义的数值；
- **带权路径长度（WPL, Weighted Path Length）**：树中所有**叶子结点**的带权路径长度之和：

$$WPL = \\sum_{i=1}^{n} w_i \\times l_i$$

其中 $w_i$ 是第 i 个叶子的权值，$l_i$ 是该叶子到根的路径长度。

## 哈夫曼树

**哈夫曼树（最优二叉树）**：在含有 n 个带权叶结点的二叉树中，**带权路径长度 WPL 最小**的二叉树。构造算法（哈夫曼算法）：

1. 将 n 个结点各自视为一棵只有根的树，构成森林 F；
2. 在 F 中选取**权值最小**的两棵树合并，生成一个新结点作为它们的双亲，新结点的权值为两者权值**之和**；
3. 从 F 中删除这两棵树，将新树加入 F；
4. 重复步骤 2、3，直到 F 中只剩一棵树。

## 哈夫曼树的性质

- 每个初始结点最终都是**叶子结点**，且权值越小离根越远；
- 哈夫曼树中**只有度为 0 和度为 2 的结点**，没有度为 1 的结点；
- 含 n 个叶结点的哈夫曼树共有 **2n-1** 个结点；
- 构造出的哈夫曼树**不唯一**（权值相同的叶子可互换位置），但 **WPL 唯一且最小**。

**重点：**构造过程必须"每次选权值最小的两棵"，并重新计算新树权值；2n-1 个结点、只有度 0 和 2 的结点是选择题高频结论。

### 哈夫曼编码与 WPL 计算
<!-- kp: ch5-s6-k2 emphasis: key -->

## 哈夫曼编码

**哈夫曼编码**是一种前缀编码，用于数据压缩。构造方法：把每个待编码字符作为叶子结点，其出现**频率作为权值**构造哈夫曼树，然后从根到叶子的路径上，**左分支标 0、右分支标 1**，从根到某叶子路径上的 0/1 序列即为该字符的编码。

**前缀编码**：任何一个字符的编码都不是另一个字符编码的前缀，从而解码时不会产生二义性。哈夫曼编码是**最优前缀编码**——用频率最高的字符编码最短，总编码长度最小。编码总长度即为对应哈夫曼树的 WPL。

## 计算示例

字符集 {A, B, C, D}，频率 {0.4, 0.3, 0.2, 0.1}。构造哈夫曼树：

- 最小两棵 0.1 与 0.2 合并为 0.3；
- 再选 0.3（新）与 0.3 合并为 0.6；
- 最后 0.4 与 0.6 合并为根 1.0。

编码：D(0.1)→"111"，C(0.2)→"110"，B(0.3)→"10"，A(0.4)→"0"（具体与左右分配有关）。WPL 计算：

$$WPL = 0.4 \\times 1 + 0.3 \\times 2 + 0.2 \\times 3 + 0.1 \\times 3 = 0.4 + 0.6 + 0.6 + 0.3 = 1.9$$

即平均码长约 1.9 位/字符，相比定长编码（2 位/字符）更节省空间。

## 前缀编码判定

**重要结论**：任一**叶子结点序列**对应哈夫曼编码必为前缀编码；反之，若无任何编码是另一编码的前缀，则任意二进制序列可唯一解码。判断一个编码集合是否为前缀编码，只需检查是否有编码是另一编码的**前缀**。

**重点：**WPL 既可通过"叶子权×路径长求和"计算，也可通过"所有非叶结点权值之和"计算（两者相等），后者在构造过程中累加新结点权值更快捷。

## 并查集
<!-- sec: ch5-s7 -->

### 并查集的存储结构与基本操作
<!-- kp: ch5-s7-k1 emphasis: key -->

## 并查集的存储

**并查集（Union-Find Set）**支持两种操作：查找元素所属集合（Find）与合并两个集合（Union）。通常用**树的双亲表示法**存储：每个集合用一棵树表示，树中每个结点存放其双亲下标，根结点的双亲指向自身（或为 -1）。数组下标对应元素编号。

\`\`\`c
#define SIZE 13
int UFSets[SIZE];   // 集合元素数组，双亲表示法

// 初始化：每个元素各自为一个集合（根结点，双亲置 -1）
void Initial(int S[]) {
    for (int i = 0; i < SIZE; i++) S[i] = -1;
}

// Find：查找 x 所在集合的根（路径压缩版本）
int Find(int S[], int x) {
    if (S[x] < 0) return x;          // 找到根
    else {
        S[x] = Find(S, S[x]);        // 路径压缩：递归沿途根挂到根下
        return S[x];
    }
}

// Union：将两个不同集合合并（小树并入大树）
void Union(int S[], int root1, int root2) {
    if (root1 == root2) return;
    if (S[root2] < S[root1]) {       // 以负数存树高，小树并入大树
        S[root1] = root2;
    } else {
        if (S[root1] == S[root2]) S[root1]--;  // 树高相同则高度 +1
        S[root2] = root1;
    }
}
\`\`\`

## 操作与复杂度

- 不优化时，Find 的时间与树高有关，最坏 O(n)（退化成单链）；Union 合并两个根为 O(1)；
- **按秩（树高/规模）合并** + **路径压缩**后，n 次操作的总时间接近 O(n·α(n))，其中 α 为反阿克曼函数，增长极其缓慢，可视为常数。

**重点：**并查集用双亲表示法存储，根的双亲值为负（表示集合规模或秩）。408 中并查集主要出现在"Kruskal 判断是否成环""网络连通性问题"等综合题中。

### 并查集的应用
<!-- kp: ch5-s7-k2 -->

## 主要应用场景

1. **Kruskal 算法判环**：将每条边的两个顶点用并查集判断是否已连通（Find），若已连通则此边会形成回路，舍弃；否则合并（Union），直到选出 n-1 条边；
2. **求连通分量**：对图中所有顶点做 Union，最后根结点的个数即连通分量数；
3. **动态连通性问题**：判断两个元素是否在同一集合、动态添加连接，如"朋友关系分组""元素同族判定"等。

## 示例

初始状态 S = [-1, -1, -1, -1, -1, -1]，执行 Union(0,1)（0 与 1 合并），再执行 Union(2,3)，再执行 Union(1,2)，则 0、1、2、3 属于同一集合，集合根为合并后确定的根。Find(3) 会沿双亲链找到根。

**优化要点**：每次 Union 前先 Find 两个根；若不按秩合并也不路径压缩，最坏情况（依次 Union 0-1, 1-2, 2-3 …）会退化为链，Find 代价为 O(n)。

**重点：**路径压缩是"递归地把沿途所有结点直接挂到根下"，使得后续 Find 接近 O(1)；408 中并查集的实现细节（数组双亲表示、-1 初值、负数存秩）偶见于大题第二问的编程实现。
`;export{e as default};