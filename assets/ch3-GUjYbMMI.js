var e=`---
id: ch3
title: 存储系统
order: 3
---

## 存储器概述
<!-- sec: s3-1 -->

### 存储器的分类
<!-- kp: kp3-1-1 emphasis: key -->

存储器按**存储介质**分为半导体存储器（SRAM、DRAM、ROM、Flash）、磁表面存储器（磁盘、磁带）与光存储器（光盘）。按**存取方式**分为：**随机存取存储器**（RAM，可按地址随机读写，如主存）、**只读存储器**（ROM）、**顺序存取存储器**（SAM，如磁带，必须顺序查找，存取时间与位置有关）与**直接存取存储器**（DAM，如磁盘，先直接定位到目标磁道/扇区再顺序读写）。按**信息的可更改性**分为可读可写存储器（RWM）与只读存储器（ROM）；按**断电后信息是否丢失**分为易失性存储器（RAM）与非易失性存储器（ROM、磁盘、Flash）。

按在计算机系统中的作用与层次分：**主存**（内存，直接与CPU交换信息，存放正在运行的程序与数据）、**外存**（辅存，容量大、速度慢、非易失，如磁盘、SSD）、**Cache**（高速缓冲存储器，位于CPU与主存之间）、**寄存器**（在CPU内部，速度最快、容量最小）。考试常考：SRAM/DRAM/ROM的易失性对比（RAM断电丢失、ROM与磁盘断电不丢失）；按存取方式RAM/SAM/DAM的分类（磁带属SAM、磁盘属DAM）；随机存取与顺序存取的区别。

**重点：** 按存取方式分为RAM/SAM/DAM/ROM；RAM易失、ROM与磁/光存储非易失；磁带=顺序存取、磁盘=直接存取、主存=随机存取。

### 存储系统层次结构
<!-- kp: kp3-1-2 emphasis: key -->

现代计算机采用"**Cache—主存—辅存**"三级存储层次，其理论基础是**程序的局部性原理**：**时间局部性**（刚访问过的数据不久后很可能再次访问，如循环体）与**空间局部性**（访问过某个数据后，其邻近的数据很快也会被访问，如顺序执行的数组）。Cache—主存层次主要由**硬件**实现（对程序员透明），用于解决CPU与主存之间的**速度匹配**问题；主存—辅存层次由**操作系统与硬件共同**实现（即虚拟存储器），用于解决**容量**问题。

层次结构的核心指标是**命中率 $H$**（访问的数据在较高层中成功找到的概率）与**平均访问时间**。设Cache访问时间为 $T_c$、主存访问时间为 $T_m$，命中率 $H$，则平均访问时间为：

$$T_a = H \\cdot T_c + (1-H) \\cdot (T_c + T_m) = T_c + (1-H) \\cdot T_m$$

命中率越高，平均访问时间越接近Cache的访问时间。对于Cache—主存—辅存三级结构，若辅存未命中（缺页），还需叠加磁盘访问时间，平均访问时间会显著增大。层次结构的目标是：访问速度接近Cache/主存，容量接近辅存，价格处于两者之间。

**重点：** 平均访问时间公式 $T_a = T_c + (1-H) T_m$；命中率 $H$ 与缺失率 $1-H$；两级/三级层次的时间计算是常考题型。

### 存储器的性能指标
<!-- kp: kp3-1-3 -->

存储器的主要性能指标有：**存储容量**（存储单元个数 $\\times$ 单元位数，如 $8M \\times 16$）、**存取时间**（从给出地址到稳定读出或写入完成所需时间，又分读出时间与写入时间）、**存取周期**（连续两次独立访问存储器之间的**最小时间间隔**，包含恢复时间，因此**存取周期 $>$ 存取时间**）、**存储器带宽**（单位时间内存取的信息量）。带宽的计算：

$$\\text{带宽} = \\frac{\\text{每个存取周期传送的数据量}}{\\text{存取周期}}$$

例如：存取周期为 $0.5\\mu s$、每个周期能传输4字节的存储器，带宽为 $\\frac{4}{0.5 \\times 10^{-6}} = 8 \\times 10^{6}$ B/s = 8MB/s。提高带宽的方法：加宽数据总线（每次传输更多位）、缩短存取周期（提高频率）、采用**多模块交叉存储器**实现并行存取、采用猝发（突发）传送。408中常以存取周期、带宽、数据宽度为条件计算存储系统的数据传输率，注意"存取时间"与"存取周期"的区别。

**重点：** 存取周期 $\\ge$ 存取时间（周期含恢复时间）；带宽 = 每周期传送量 ÷ 周期；多模块交叉可成倍提高带宽。

## 主存储器
<!-- sec: s3-2 -->

### SRAM与DRAM
<!-- kp: kp3-2-1 emphasis: key -->

**SRAM（静态RAM）**用**双稳态触发器**存储一位信息，只要不断电数据就不丢失，**无需刷新**；速度快、集成度低、功耗较大、价格昂贵，主要用于**Cache**。**DRAM（动态RAM）**用电容上的电荷存储信息，电荷会随时间泄漏，因此必须**周期性地刷新**（典型每 $2ms$ 刷新一遍）；集成度高、功耗低、价格便宜，主要用于**主存**。DRAM刷新按**行**进行，刷新方式有三种：**集中刷新**（在所有正常存取完成后集中刷新所有行，期间停止访存，存在"死区"）、**分散刷新**（每存取一次就刷新一行，无死区但每次存取被拉长）、**异步刷新**（把刷新周期分散到各存取周期之间，折中）。

DRAM采用**行、列地址分时复用**（先送行地址、再送列地址），因此地址引脚数比SRAM少一半。刷新时间计算：设DRAM有 $2^r$ 行、每行刷新时间为 $t_r$，则整个刷新周期占用时间 $= 2^r \\times t_r$；集中刷新的"死区"比例 $= \\frac{\\text{总刷新时间}}{\\text{刷新周期}}$。考试常考SRAM与DRAM的对比、DRAM刷新方式的时间计算（如集中刷新的死区比例、异步刷新的安排）。

**重点：** SRAM用触发器、不需刷新、速度快、用于Cache；DRAM用电容、需周期刷新、用于主存；刷新按行进行；三种刷新方式的死区/开销对比。

### 只读存储器ROM
<!-- kp: kp3-2-2 -->

**ROM（Read-Only Memory）**是只读存储器，**非易失**，断电后信息不丢失。常见类型：**掩膜ROM（MROM）**——出厂时用掩模工艺固化内容，不可改写，适合批量生产的固定程序；**PROM**（可编程ROM）——用熔丝实现，用户可一次性编程写入，不可改写；**EPROM**——紫外线可擦除，擦除后能重新编程，可多次使用；**EEPROM**——电可擦除，可逐字节擦写，无需紫外线；**Flash（闪存）**——电可擦除、**按块擦写**，U盘与固态硬盘（SSD）的核心存储器件。ROM与RAM的重要区别：ROM一般不能（或很难）随机写入，但可以随机读出。

ROM的典型用途：存放BIOS、引导程序、微程序、常数表等只读信息。注意：现代SSD虽然在主机看来如同磁盘（按块读写、非易失），但其内部由NAND Flash芯片构成，属于非易失性半导体存储器。考试常考ROM各类别的特性（MROM一次性固化、PROM一次性编程、EPROM紫外线擦除、EEPROM电擦除、Flash按块擦写）以及RAM/ROM的易失性对比。

**重点：** ROM非易失、RAM易失；Flash按块擦写（SSD/U盘）；掩膜ROM出厂固化、PROM一次性、EPROM紫外线、EEPROM电擦除。

### 存储芯片扩展：字扩展、位扩展与字位扩展
<!-- kp: kp3-2-3 emphasis: difficult -->

当单片存储芯片容量不足时，需要对芯片进行扩展。**位扩展**：增加字长（每个存储单元的位数），把多个芯片的地址线、片选线、读写控制线并联，数据线分别接到不同位——如用 $8K \\times 1$ 位芯片构成 $8K \\times 8$，需要8片，数据线分别接 $D_0\\sim D_7$。**字扩展**：增加存储单元个数，各芯片数据线与控制线并联，**高位地址**经译码器产生**片选信号**选择不同芯片——如用 $8K \\times 8$ 构成 $32K \\times 8$，需要4片，片内地址13位（$2^{13} = 8K$），用 $A_{15}A_{14}$（或 $A_{14}A_{13}$，取决于地址空间布局）经2-4译码器选片。

**字位扩展**：先做位扩展再做字扩展。所需芯片总数：

$$\\text{芯片数} = \\frac{\\text{总容量（位）}}{\\text{单片容量（位）}} = \\frac{M \\times N}{m \\times n}$$

连接要点：① 芯片地址线数由片内容量决定（$2^{片内地址线数} = m$）；② 低位地址作片内寻址、高位地址经译码作片选；③ 数据线按位扩展分组连接；④ 注意地址空间的连续性分配（判断各芯片占用的地址范围）。例题：用 $64K \\times 1$ 位的DRAM构成 $256K \\times 16$ 的存储器，需 $(256/64) \\times 16 = 64$ 片；片内地址线16根（$2^{16}=64K$），高位 $A_{19}A_{18}$ 作片选译码。

**难点：** 片选信号的使能条件与地址范围映射；地址字段划分（片内地址 / 片选地址）；"总容量/单片容量"求芯片数；位扩展与字扩展的数据线与地址线连接方式。

## 主存与CPU的连接
<!-- sec: s3-3 -->

### 主存与CPU的连接方式
<!-- kp: kp3-3-1 emphasis: key -->

主存通过**地址总线、数据总线、控制总线**与CPU连接。**地址总线**为单向（由CPU发出），其宽度（位数）决定CPU可直接寻址的最大空间：可寻址 $2^{地址线数}$ 个存储单元。**数据总线**为双向，其宽度决定一次数据传输的位数，应与存储单元位数/机器字长相匹配。**控制总线**包括读写控制（$\\overline{RD}$、$\\overline{WR}$）、片选（$\\overline{CS}$）、时序与中断请求等信号，其中读写控制线由CPU发出，中断请求线由设备发向CPU。主存地址空间的设计要考虑芯片的选择与地址分配：通常将ROM安排在低地址区、RAM安排在其余区域，或按系统需要分区。

与CPU相连时要注意**时序配合**：CPU的存取时间必须大于存储器的存取周期，否则需要插入**等待周期（Tw）**等待存储器就绪。数据宽度的匹配：若存储单元为8位而CPU数据总线为16位，则需按字节编址、两字节一组传输；若总线宽度不足，还要考虑分两次传输与字节使能信号。考试常见题型：给定地址总线宽度求最大寻址空间、根据存储器组织画连接图、计算片选信号的译码逻辑与地址范围。

**重点：** 地址总线单向（CPU→存储器）、数据总线双向；最大寻址空间 $= 2^{地址线数}$；读写控制线与片选信号配合；必要时插入等待周期。

### 双端口RAM与多模块存储器
<!-- kp: kp3-3-2 emphasis: key -->

**双端口RAM**有两个相互独立的读写端口（两组地址线、数据线、控制线），允许两个部件（如CPU与DMA控制器）**同时对不同单元**进行读写，从而提高并行度与吞吐率。若两个端口同时访问**同一单元**则发生冲突，需要由接口中的仲裁逻辑判定优先级（通常发出忙信号BUSY让后到的一方等待）。

**多模块交叉存储器**（低位交叉编址）把存储器分成 $m$ 个模块，采用**低位交叉编址**：相邻的地址分布在不同模块中，使得连续地址的访问可以**并行/流水**进行。当模块数 $m \\ge \\frac{\\text{存取周期}}{\\text{总线周期}}$ 时，可在 $m$ 个总线周期内完成 $m$ 次访存，有效带宽提高约 $m$ 倍。**高位交叉编址**（顺序编址，模块号在高位）便于按序扩充容量，但连续地址落在同一模块，无法并行访问。例题：存取周期 $100ns$、总线周期 $25ns$，取 $m = 4$ 个模块低位交叉，连续访问4个字的时间可由 $4 \\times 100ns = 400ns$ 降为约 $4 \\times 25ns = 100ns$（加上首访延迟）。

**重点：** 双端口RAM通过增加端口提高并行性（冲突需仲裁）；低位交叉编址使连续地址分布在不同的模块从而可并行存取；模块数 $m$ 满足 $m \\ge$ 存取周期/总线周期时带宽提高 $m$ 倍。

## Cache高速缓冲存储器
<!-- sec: s3-4 -->

### Cache的基本原理与命中率
<!-- kp: kp3-4-1 emphasis: key -->

**Cache**位于CPU与主存之间，利用程序的**局部性原理**，把主存中最近使用过的数据块复制到Cache中，使CPU的大部分访问在Cache中**命中**，从而显著提高平均访存速度。Cache与主存交换数据的最小单位是**块（行）**，主存被划分为大小相等的块，Cache行与主存块大小相同。Cache的容量一般远小于主存，但速度快得多（通常与CPU同速）。**命中率 $H$**定义为：

$$H = \\frac{\\text{Cache命中次数}}{\\text{总访问次数}}$$

设Cache访问时间为 $T_c$、主存访问时间为 $T_m$（含Cache更新/调块开销），则平均访问时间：

$$T_a = H \\cdot T_c + (1-H) \\cdot (T_c + T_m) = T_c + (1-H) \\cdot T_m$$

命中率越高，$T_a$ 越接近 $T_c$。Cache对程序员完全**透明**，其全部管理（地址映射、替换、写策略）都由硬件自动完成。Cache容量越大命中率越高，但价格升高、命中后的访问延迟也可能增大，因此存在容量与性能的权衡。考试常给命中率、Cache与主存访问时间求平均访问时间，或求使平均访问时间达到某个值所需的最低命中率。

**重点：** 局部性原理（时间+空间）是Cache的理论基础；$T_a = T_c + (1-H) T_m$；Cache行与主存块大小一致；Cache管理全部由硬件完成。

### 地址映射方式与计算
<!-- kp: kp3-4-2 emphasis: difficult -->

Cache与主存之间的映射方式有三种。**① 直接映射**：每个主存块只能装入Cache中唯一的行。主存地址分为**标记（Tag）＋行号＋块内地址**三段；行号由 $\\text{Cache行号} = \\text{主存块号} \\bmod \\text{Cache行数}$ 决定。比较标记即可判断命中，实现简单，但多个主存块竞争同一行，命中率较低。

**② 全相联映射**：每个主存块可装入Cache的任何一行。主存地址分为**标记＋块内地址**两段（无行号字段），需要把地址标记与Cache所有行的标记并行比较，命中率最高，但比较电路复杂、成本高。

**③ 组相联映射**：Cache分成若干组，每组 $q$ 行（$q$ 路组相联）。主存块映射到固定的组（$\\text{组号} = \\text{主存块号} \\bmod \\text{组数}$），组内可放任意一行。主存地址分为**标记＋组号＋块内地址**三段。它是直接映射与全相联的折中：$q=1$ 即直接映射，组数为1即全相联。

字段位数计算：设块大小为 $2^b$ 字（块内地址 $b$ 位）、Cache行数为 $2^r$（行号 $r$ 位）、组数为 $2^s$（组号 $s$ 位），则直接映射时 $r = s$，组相联时行号 = 组号 + 组内行号。例题：Cache共16行、块大小4字（按字编址）。直接映射下，主存地址低2位为块内偏移，次4位为行号，其余高位为标记；主存地址 $0\\text{x}1F$ 的块内偏移 $= 0\\text{x}1F \\bmod 4 = 3$，行号 $= (0\\text{x}1F / 4) \\bmod 16 = 7$，标记 $= 0\\text{x}1F / 64 = 0$。

**难点：** 三种映射的地址划分与命中比较的标记位数；Cache总容量 $=$ 行数 $\\times$（标记位 + 数据位 + 有效位 + 写回时的脏位）；由Cache行数、块大小、组数反推地址字段位数。

### 替换算法与写策略
<!-- kp: kp3-4-3 emphasis: key -->

当Cache已满而需要装入新块时，必须替换某个块。常用替换算法有：**随机替换（RAND）**、**先进先出（FIFO）**（替换最早装入的块，可能出现"抖动"，如循环访问导致刚被替换的块马上又要用）、**最近最少使用（LRU）**（替换最长时间未被访问的块，基于局部性原理，命中率通常最高）、**最不经常使用（LFU）**（替换访问次数最少的块）。LRU的实现用每行的"年龄计数器"（2路组相联时每组只需1位计数器），硬件开销小、性能好，是408最常考的算法。

**写策略**解决CPU写Cache时如何保持Cache与主存一致的问题：**写直达（Write Through）**——写Cache的同时也写主存，主存始终最新，但每次写操作都要访存，速度慢；**写回（Write Back）**——只写Cache并置**脏位（dirty bit）**，仅当该行被替换时才一次性写回主存，写操作快，但主存可能暂时与Cache不一致。写缺失时的处理：**写分配**（把主存块调入Cache再写）与**写不分配**（直接写主存，不调入Cache）。常用组合：写直达＋写不分配、写回＋写分配。考试常考：给定访问序列用LRU/FIFO求命中率，以及写策略对主存一致性的影响。

**重点：** LRU命中率通常最高、实现简单；FIFO可能抖动；写直达每次写主存（慢、一致性好）、写回替换时写主存（快、需脏位）；Cache行含有效位、标记位、数据，写回时还含脏位。

## 虚拟存储器
<!-- sec: s3-5 -->

### 页式虚拟存储器与地址转换
<!-- kp: kp3-5-1 emphasis: key -->

虚拟存储器由"主存—辅存"构成，使程序能够使用比物理主存大得多的逻辑（虚拟）地址空间。**页式虚存**把虚拟地址空间与物理地址空间都划分为**大小相等**的页（页面/页框，典型4KB），虚拟地址分为**虚页号＋页内偏移**，物理地址分为**页框号＋页内偏移**。地址转换依靠**页表**（存放在主存中，每个进程一张）：用虚页号索引页表得到页框号，再拼接页内偏移：

$$\\text{物理地址} = \\text{页框号} \\times \\text{页大小} + \\text{页内偏移}$$

页表项包含：**页框号、有效位**（该页是否在主存）、脏位、访问位等控制信息。若有效位为0（该页不在主存），则发生**缺页**，需从磁盘把页面调入主存。页内偏移位数由页大小决定（$2^{页内偏移位数}$ = 页大小），虚页号位数决定虚拟地址空间大小（$2^{虚页号位数} \\times$ 页大小）。页式虚存的优点是页面大小固定、无外部碎片、易于管理；缺点是存在内部碎片。地址转换由**MMU**（存储管理部件）硬件加速，页表由操作系统维护。

**重点：** 虚页号→页框号的查表转换；页大小固定、页表在主存；页表项有效位为0即缺页；物理地址 = 页框号×页大小 + 页内偏移。

### 段式与段页式虚拟存储器
<!-- kp: kp3-5-2 -->

**段式虚存**按程序的**逻辑结构**（模块、函数、数据段）分段，各段长度不相等，段内地址连续、段间独立。虚拟地址分为**段号＋段内偏移**，通过**段表**（含段起始地址、段长、装入位/有效位）进行转换。段式的优点：便于按模块组织程序、便于**共享与保护**（段可作为共享/保护单位），段长可随程序动态增长；缺点：段长不统一，主存分配产生**外部碎片**（碎片在段之间），且地址转换较复杂。页式虚存无外部碎片但页面内可能有**内部碎片**。

**段页式虚存**结合两者优点：先按逻辑分段，再把每段分为若干页，虚拟地址为"**段号＋段内页号＋页内偏移**"。地址转换需先查段表（得到该段的页表起始地址），再查页表（得到页框号），共需**两次访存**（可用TLB快表加速）。段页式兼顾了段的逻辑共享保护与页的无外部碎片，但硬件复杂、访存次数最多。考试常以选择题比较三种方式的优缺点与地址结构。

**重点：** 页式按固定大小分页（无外部碎片、有内部碎片）、段式按逻辑分段（有外部碎片、便于共享保护）、段页式需查两次表（段表+页表）。

### TLB与缺页处理
<!-- kp: kp3-5-3 emphasis: difficult -->

页表存放在主存中，若每次地址转换都查页表，则每次访存需额外访问一次主存，使有效访存时间明显增加。为此CPU内设**TLB（快表，Translation Lookaside Buffer）**，存放最近使用的页表项；位于主存中的页表称为**慢表**。访问流程：CPU先查TLB，命中则直接得到页框号（不额外访存）；TLB未命中再查页表（额外访存一次），并把该页表项装入TLB。TLB通常采用全相联或组相联组织、容量很小、速度很快。**有效访问时间（EAT）**的计算：设TLB命中率为 $H$、TLB查找时间为 $t_t$、一次主存访问时间为 $t_m$，则：

$$EAT = H \\cdot (t_t + t_m) + (1-H) \\cdot (t_t + 2t_m)$$

即TLB命中时只需1次访存，未命中时需要2次访存（查页表＋取数据）。

**缺页处理**：当访问的页不在主存时，硬件发出**缺页异常**，由操作系统的缺页中断处理程序从磁盘调入页面（若主存已满，先按替换算法换出牺牲页），更新页表与TLB，然后**重新执行被中断的指令**。缺页需要磁盘I/O，代价极大，因此应尽量提高TLB命中率、降低缺页率。408题常综合考查：TLB命中率与EAT计算、Cache与TLB同时考虑时的平均访问时间、缺页率对有效访问时间的影响。

**难点：** 区分TLB缺失与缺页：TLB缺失但页在主存时只需多访存一次；缺页（页不在主存）才触发磁盘I/O，两种开销天差地别；EAT计算要正确判断每种情况下的访存次数。
`;export{e as default};