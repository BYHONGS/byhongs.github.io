var e=`---
id: ch4
title: 第四章 网络层
order: 4
---

## 网络层的功能
<!-- sec: ch4-s1 -->

### 网络层的功能与两种服务
<!-- kp: ch4-s1-kp1 emphasis: key -->

**1. 网络层的主要功能**
网络层负责把分组从源主机**跨网络**传送到目的主机，主要功能：
- **路由选择**：根据路由算法/协议确定分组从源到目的的最佳转发路径；
- **转发**：路由器根据转发表把分组从合适的输出端口发出；
- **分组封装**：把传输层报文段封装成 IP 数据报；
- **拥塞控制**：当网络负载过大时，通过流量调节避免网络瘫痪；
- **异构网络互联**：通过网关/路由器把不同技术（不同链路层）的网络互连。

网络层为传输层提供两种服务：**虚电路服务**（面向连接）与**数据报服务**（无连接）。互联网的 IP 网络采用**数据报服务**，即"尽力而为、无连接、不可靠"。

**2. 虚电路与数据报对比**

| 比较项 | 虚电路服务 | 数据报服务 |
| --- | --- | --- |
| 连接 | 需先建立 | 无需建立 |
| 路径 | 建立时确定，分组同路 | 每个分组独立选路 |
| 地址 | 每个分组只需虚电路号 | 每个分组需完整目的地址 |
| 故障处理 | 电路中断需重建 | 故障可绕过（更健壮） |
| 有序到达 | 保证（同一路径） | 不保证（可能乱序） |
| 典型网络 | ATM、帧中继 | 互联网 IP |

**3. IP 服务的"尽力而为"**
IP 只负责"尽最大努力"交付，不保证：不保证分组不丢失、不保证按时到达、不保证顺序、不保证不重复。可靠交付由传输层 TCP 负责，网络层只提供"无连接、不可靠、尽力而为"的数据报服务。这一设计使得 IP 网络能互联各种异构网络、实现简单、健壮性好。

**重点：** 数据报服务（无连接、不可靠）是互联网网络层的基本特征；路由选择与转发是网络层的核心功能。

**难点：** "网络层不可靠，那数据怎么可靠到达？"——可靠是端到端（传输层）的事，网络层只做"转发尽力"，这正是分层思想的体现：网络层负责"找到路"，传输层负责"保证到"。

### IP 数据报的格式与分片
<!-- kp: ch4-s1-kp2 emphasis: key -->

**1. IPv4 数据报首部格式（固定 20 字节）**
IPv4 数据报由**首部**和**数据**两部分组成，首部固定部分 20 字节，加上可选字段最长 60 字节。主要字段：

| 字段 | 长度 | 含义 |
| --- | --- | --- |
| 版本 | 4 bit | IP 版本号（IPv4 为 4） |
| 首部长度 | 4 bit | 首部长度（以 4 字节为单位，最大 60 字节） |
| 区分服务 | 8 bit | 服务类型（可表示优先级、DSCP） |
| 总长度 | 16 bit | 首部 + 数据的总长度（最大 65535 字节） |
| 标识 | 16 bit | 分片标识，同一数据报的各分片标识相同 |
| 标志 | 3 bit | DF（禁止分片）、MF（还有分片） |
| 片偏移 | 13 bit | 分片在原数据报数据部分的偏移（以 8 字节为单位） |
| 生存时间 TTL | 8 bit | 每经过一个路由器减 1，减到 0 则丢弃（限制环路） |
| 协议 | 8 bit | 上层协议（TCP=6、UDP=17、ICMP=1） |
| 首部校验和 | 16 bit | 只校验首部（每跳重新计算） |
| 源地址/目的地址 | 各 4 B | IP 地址 |

**2. 数据报分片**
当数据报长度超过链路 MTU（如以太网 1500 字节）时必须分片。分片由**发送方或路由器**完成（IPv4 路由器可分片），片在目的主机重组。分片依据：**标识**（同属一个数据报）、**标志**（MF=1 表示后面还有分片；DF=1 表示禁止分片）、**片偏移**（以 8 字节为单位的偏移量）。各分片的**片偏移**按数据部分相对位置计算，首部长度 20 字节时每片最多携带 1480 字节数据（1500-20），且必须是 8 的倍数。

**3. 计算例题**
【例】数据报总长度 4000 字节（首部 20 字节 + 数据 3980 字节），MTU = 1500 字节，进行分片。
- 每片最多携带数据 1480 字节（1500-20）；
- 3980 = 1480 + 1480 + 1020，分为 3 片；
- 三片的片偏移分别为 0、185（1480/8）、370（2960/8），片长分别 1500、1500、1040 字节，MF 标志分别为 1、1、0。

**4. 考点归纳**
- TTL 每跳减 1，用于防止数据报无限循环（traceroute 正是利用 TTL 原理）；
- 首部校验和只校验首部（传输层校验和才覆盖数据）；
- 片偏移以 8 字节为单位；DF 位禁止分片，MF 位表示还有后续分片。

**重点：** 首部各字段含义（版本、TTL、协议、标识、标志、片偏移）；分片计算（每片数据须为 8 的倍数）。

**难点：** 片偏移单位是"8 字节"而非字节——1480/8=185 因此片偏移 185；分片后除首部字段变化外，各片仍是独立 IP 数据报，由目的主机重组。

## IPv4 地址
<!-- sec: ch4-s2 -->

### IP 地址与分类编址
<!-- kp: ch4-s2-kp1 emphasis: key -->

**1. IP 地址的基本概念**
IP 地址是网络层为每台主机（或路由器接口）分配的逻辑地址，IPv4 地址长 **32 位**，用**点分十进制**表示（每 8 位一组，如 192.168.1.1）。IP 地址 = 网络号 + 主机号，网络号标识主机所在的网络，主机号标识该网络内的具体主机。IP 地址是整个网络的主机之间通信的基础。

**2. 分类编址（A/B/C/D/E 类）**
最早的 IPv4 采用分类编址（classful addressing），前 1~4 位用于区分类别：

| 类别 | 前几位 | 网络号位数 | 主机号位数 | 地址范围 | 默认子网掩码 | 用途 |
| --- | --- | --- | --- | --- | --- | --- |
| A | 0 | 8 | 24 | 1.0.0.0 ~ 126.255.255.255 | 255.0.0.0 | 大型网络 |
| B | 10 | 16 | 16 | 128.0.0.0 ~ 191.255.255.255 | 255.255.0.0 | 中型网络 |
| C | 110 | 24 | 8 | 192.0.0.0 ~ 223.255.255.255 | 255.255.255.0 | 小型网络 |
| D | 1110 | — | — | 224.0.0.0 ~ 239.255.255.255 | — | 组播 |
| E | 1111 | — | — | 240.0.0.0 ~ 255.255.255.255 | — | 保留实验 |

注意 A 类网络号范围是 1~126（0 和 127 有特殊用途），即 A 类共有 2^8 - 2 = 126 个网络，每个网络可容纳 2^24 - 2 台主机（全 0 和全 1 主机号不能用）。

**3. 特殊 IP 地址**

| 地址 | 含义 |
| --- | --- |
| 0.0.0.0 | 本机（表示"本网络上的本主机"，常作源地址） |
| 127.0.0.1（127.x.x.x） | 环回地址，用于本机自测，不送网 |
| 主机号全 0 | 表示该网络的网络地址（不能分配给主机） |
| 主机号全 1 | 该网络的直接广播地址 |
| 255.255.255.255 | 有限广播地址（本网络内广播） |
| 网络号全 0 | 本网络上的特定主机（如 0.0.0.5） |

**4. 私有地址（RFC 1918）**
私有地址只能在内部网络使用，不可在公网路由：A 类 10.0.0.0/8、B 类 172.16.0.0/12、C 类 192.168.0.0/16。家庭/公司局域网普遍使用私有地址，通过 NAT 转换后访问公网。

**重点：** 各类地址的范围与默认掩码；"主机号全 0 是网络地址、全 1 是广播地址"；私有地址三段。

**难点：** 判定一个地址是否可分配给主机——必须排除网络地址（主机号全 0）、广播地址（主机号全 1）以及环回/保留段。

### 子网划分
<!-- kp: ch4-s2-kp2 emphasis: difficult -->

**1. 子网划分的思想**
分类编址浪费地址（B 类 6 万多主机地址往往用不完），子网划分通过**借用主机号的若干位作为子网号**，把一个网络分成多个子网。子网划分不改变 IP 地址总长度，只改变"网络号+主机号"的分界位置，分界位置由**子网掩码**标出。子网掩码中网络号和子网号对应位为 1，主机号对应位为 0。

**2. 关键公式**
设主机号原有 h 位，借用 n 位作子网号：

- 子网掩码向主机方向扩展 n 位；
- 可划分子网个数：**2^n**（若按 RFC 950 保留全 0/全 1 子网则为 2^n - 2，新标准允许全 0/全 1 子网，考试按题目说明）；
- 每个子网可分配主机数：**2^(h-n) - 2**（去掉网络地址与广播地址）。

**3. 完整计算例题**
【例】某单位申请到网络号 192.168.1.0/24，需划分 4 个子网。
- C 类地址主机号 h=8 位，划分 4 个子网需借用 n=2 位（2^2=4）；
- 子网掩码 = 原掩码 255.255.255.0 后加两位 1，即 255.255.255.192（/26）；
- 每个子网主机号剩 6 位，可分配主机数 = 2^6 - 2 = 62 台；
- 4 个子网地址为 192.168.1.0/26、192.168.1.64/26、192.168.1.128/26、192.168.1.192/26；
- 以第一个子网为例：子网网络地址 192.168.1.0，可分配主机范围 192.168.1.1 ~ 192.168.1.62，广播地址 192.168.1.63。

【例】某公司获得 172.16.0.0/16，要求每个子网至少能容纳 500 台主机。
- 主机号需满足 2^h - 2 ≥ 500，h = 9（2^9 - 2 = 510 ≥ 500）；
- 子网号位数 = 32 - 16 - 9 = 7，可划分子网 2^7 = 128 个；
- 子网掩码 = /23，即 255.255.254.0。

**4. 考点归纳**
- 子网掩码的换算与书写（点分十进制与斜线记法 /n 互换）；
- 主机地址范围 = [子网地址 + 1, 子网广播地址 - 1]；
- 判定两个 IP 是否同属一个子网：把两个 IP 与子网掩码做按位与，结果相同则同网段。

**重点：** 借用主机位划分子网的方法；主机数 = 2^k - 2；子网掩码与 /n 的换算；广播地址的确定。

**难点：** 借位不足时子网数不够、主机数不够的权衡——n 越大子网越多但每子网主机越少，两者此消彼长；以及"主机数 ≥ 2 且不能全借"的约束。

### CIDR 与路由聚合
<!-- kp: ch4-s2-kp3 emphasis: difficult -->

**1. CIDR 概念**
CIDR（无分类域间路由选择，Classless Inter-Domain Routing）消除了传统的 A/B/C 类地址划分，用"IP 前缀 + 前缀长度"表示地址块：

$$128.14.32.0/20$$

表示从 128.14.32.0 开始、网络前缀长度为 20 位的地址块。该地址块包含 2^(32-20) = 2^12 = 4096 个 IP 地址。CIDR 的地址块可以任意长度划分，解决了分类编址的浪费问题。CIDR 把网络前缀相同的连续 IP 地址组成"CIDR 地址块"，路由表中用"网络前缀"而非完整网络号。

**2. 最长前缀匹配**
路由器转发分组时，在转发表的所有匹配项中选择**前缀最长（最具体）**的一项作为转发依据，称为最长前缀匹配。这样可以保证精确路由。例如目的地址 128.14.35.7 同时匹配 128.14.0.0/16 与 128.14.32.0/20，应选择 /20 的转发项。

**3. 路由聚合（超网）**
路由聚合（route aggregation）把**多个前缀连续的地址块**合并成一个更大的地址块，减少路由表项数。方法是找出这些地址块的**共同前缀**。

**计算例题**
【例】把 192.168.32.0/24 和 192.168.33.0/24 聚合。
- 32 = 0010 0000，33 = 0010 0001（二进制）；
- 前 7 位相同（0010 000），第 8 位不同；
- 因此 16 位网络号 + 8 位前 7 位相同，共同前缀为 23 位；
- 聚合结果为 192.168.32.0/23，包含 512 个地址。

【例】把 192.168.32.0/24、192.168.33.0/24、192.168.34.0/24、192.168.35.0/24 四个地址块聚合。
- 32=00100000, 33=00100001, 34=00100010, 35=00100011，前 6 位相同（001000）；
- 共同前缀 16+6=22 位，聚合为 192.168.32.0/22。

**注意**：只有**连续**的地址块才能聚合。若把 192.168.32.0/24 与 192.168.34.0/24 聚合，会错误包含 192.168.33.0/24，因此 32 与 34 不能直接聚合成 /23（需按二进制判断边界）。

**4. 考点归纳**
- 地址块包含地址数 = 2^(32-前缀长度)；
- 一个 /n 地址块的起始地址满足"主机号部分全 0"，末地址"主机号部分全 1"（即广播地址）；
- 最长前缀匹配：有多个匹配时选前缀最长的。

**重点：** CIDR 地址块的计算（包含地址数、起止地址）；路由聚合找公共前缀；最长前缀匹配规则。

**难点：** 聚合时地址块必须连续且按边界对齐——检查二进制中"差异从哪一位开始"，公共前缀即差异位之前的部分；不能跨越的边界（如 /24 聚合必须成对、成 4、成 8 等）。

### NAT 网络地址转换
<!-- kp: ch4-s2-kp4 emphasis: key -->

**NAT（网络地址转换）**使内部网络使用私有地址的主机通过**一个（或一组）公有IP地址**访问互联网：NAT路由器把数据报的**源IP+源端口**（私有）替换为**公有IP+新端口**，并在**NAT转换表**中记录对应关系；应答报文到达时根据转换表反向替换，发给内部主机。**端口号参与转换**的机制称为**NAPT（网络地址端口转换）**，可让一个公网IP同时服务成千上万台内网主机（靠不同端口区分）。私有地址 + NAT 缓解了IPv4地址短缺问题。

考点：①NAT路由器在**网络层**工作，修改IP首部（源/目的IP）后，因IP与TCP/UDP校验和都覆盖源IP、目的IP，**传输层校验和也需随之更新**；②转换表项有**超时**机制；③NAT使外部无法主动访问内网主机（外部只能响应已建立的会话），这是内网主机对外提供服务困难的原因。408常考：给定内网主机访问外网的会话，写出NAT转换前后源/目的IP与端口的对应。

**重点：**NAT+端口（NAPT）的转换表机制；公网IP由内网主机共享；NAT只对穿越的数据报做IP/端口替换，内部私有地址对外不可见。

### VPN 与移动 IP（低频）
<!-- kp: ch4-s2-kp5 -->

**VPN（虚拟专用网）**利用**隧道技术**在公共互联网上构建逻辑专用网络：数据报在源端被封装进新的IP数据报（加隧道首部）穿越公网，到达目的端再解封装，使异地分支机构像在同一个私有网络中。IPsec是常用的加密隧道协议。**低频考点**，理解"隧道=整包封装、地址不变穿过公网"即可。

**移动IP**使主机在移动中保持原IP地址通信：家乡代理与外地代理协作，外地代理把发给移动主机的数据报**隧道转发**给主机当前所在位置（转交地址）。**极低频**，知道"转交地址+隧道转发"的思路即可，不必深究细节。

## ARP 协议
<!-- sec: ch4-s3 -->

### ARP 的作用与原理
<!-- kp: ch4-s3-kp1 emphasis: key -->

**1. 为什么需要 ARP**
IP 数据报在数据链路层传输时必须封装成帧，而帧的寻址用的是 **MAC 地址**（物理地址）。主机只知道目的主机的 IP 地址，不知道其 MAC 地址。**ARP（地址解析协议，Address Resolution Protocol）** 实现从 **IP 地址到 MAC 地址**的映射。它工作在数据链路层与网络层之间（直接封装在以太网帧中传输），但通常归为网络层的辅助协议。

**2. 工作原理（同网段）**
1. 主机 A 要与主机 B 通信，先查本机的 **ARP 高速缓存**表，看是否有 B 的 IP 对应的 MAC 地址；
2. 若命中，直接封装帧发送；
3. 若未命中，A 发送**广播**的 ARP 请求报文："谁是 192.168.1.2？请告诉 192.168.1.1（我的 MAC）"；
4. 网内所有主机都收到该广播，但只有 IP 地址匹配的主机 B 响应，B 发送**单播**的 ARP 应答报文，把自己的 MAC 地址告诉 A；
5. A 收到应答后把"IP→MAC"映射写入 ARP 缓存，并设置生存期（TTL，通常 2~10 分钟），然后发送数据帧。

**3. 跨网段通信**
若目的主机不在同一网段，主机 A 不能直接解析目的 IP 的 MAC。此时 A 把帧发送给**默认网关（路由器）**：A 先解析**网关的 MAC 地址**（用 ARP 查网关 IP→MAC），把帧的目的 MAC 填成网关的 MAC，由路由器再按路由表转发，路由器在自己的网络中继续用 ARP 解析下一跳的 MAC。因此**逐跳使用 ARP，ARP 只在同一网段内有效**。

**4. 考点归纳**
- ARP 请求是**广播**，ARP 应答是**单播**；
- ARP 缓存表项有生存期，避免失效；
- ARP 只解决"同网段内"的 IP→MAC 解析；
- 收到 ARP 请求的主机还会把请求者的 IP→MAC 写入自己的缓存（"免费 ARP"用于检测 IP 冲突）。

**重点：** ARP 请求广播、应答单播；ARP 缓存；跨网段时先解析网关 MAC。

**难点：** "ARP 是广播的、但只在该网段内"——路由器隔离广播域，所以 ARP 请求不会穿过路由器；跨网段通信时帧的目的 MAC 始终是下一跳（网关）的 MAC，而目的 IP 不变。

### ARP 报文格式与安全
<!-- kp: ch4-s3-kp2 -->

**1. ARP 报文格式**
ARP 报文直接封装在以太网帧中，不经过 IP 层，其字段包括：

| 字段 | 长度 | 含义 |
| --- | --- | --- |
| 硬件类型 | 2 B | 链路层类型（以太网为 1） |
| 协议类型 | 2 B | 上层协议（IP 为 0x0800） |
| 硬件地址长度 | 1 B | MAC 地址长度（6） |
| 协议地址长度 | 1 B | IP 地址长度（4） |
| 操作码 | 2 B | 1=ARP 请求，2=ARP 应答，3=RARP 请求，4=RARP 应答 |
| 发送方硬件地址 | 6 B | 发送方 MAC |
| 发送方协议地址 | 4 B | 发送方 IP |
| 目的硬件地址 | 6 B | 请求时全 0（未知），应答时填目标 MAC |
| 目的协议地址 | 4 B | 目的 IP |

**2. ARP 与 DNS 的区别（高频辨析）**
- **DNS**：把**域名解析为 IP 地址**，工作在应用层，基于 UDP 53；
- **ARP**：把 **IP 地址解析为 MAC 地址**，工作在网络层（封装在帧中），仅在同一网段内有效。
两个协议是不同层次的"地址解析"，方向完全不同，考题常把两者放在一起辨析。

**3. ARP 的安全问题**
- **ARP 欺骗/毒化**：攻击者发送伪造的 ARP 应答，把网关或目标主机的 IP 映射到攻击者 MAC，使流量被劫持到攻击者主机（中间人攻击）；
- 防御：静态 ARP 表、ARP 检测（如交换机端口安全）。

**4. 考点归纳**
- ARP 报文操作码区分请求（1）与应答（2）；
- 请求报文中目的硬件地址为全 0；
- ARP 帧的以太网类型字段为 0x0806（IP 为 0x0800）。

**重点：** ARP 请求时目的 MAC 填 0、应答时填目标 MAC；ARP（IP→MAC）与 DNS（域名→IP）的辨析。

**难点：** ARP 报文虽然承载在以太网帧中，但其作用属于网络层的地址解析范畴——不能因此误认为 ARP 是数据链路层协议。

## DHCP 与 ICMP 协议
<!-- sec: ch4-s4 -->

### DHCP 动态主机配置协议
<!-- kp: ch4-s4-kp1 emphasis: key -->

**1. 作用**
DHCP（动态主机配置协议，Dynamic Host Configuration Protocol）用于给主机**动态分配 IP 地址**及其他网络参数（子网掩码、默认网关、DNS 服务器地址等），解决了人工配置静态 IP 的繁琐与地址浪费问题。DHCP 基于 **UDP**，客户端使用端口 **68**，服务器使用端口 **67**。DHCP 服务器维护 IP 地址池，按需分配并管理租约。

**2. 工作过程（四次报文交互）**
1. **DHCP Discover（发现）**：主机以源地址 0.0.0.0、目的地址 255.255.255.255 广播"我还没有 IP，谁能给我分配"；
2. **DHCP Offer（提供）**：收到请求的 DHCP 服务器广播（或单播）"我可以提供 IP 地址 x.x.x.x 及其配置"；
3. **DHCP Request（请求）**：客户端选择某一台服务器（一般选第一个收到的 Offer），广播请求"我接受该地址"（通知其他服务器撤销预分配）；
4. **DHCP ACK（确认）**：服务器确认分配，客户端即可使用该 IP。

**3. 租约与续租**
DHCP 分配的是有租期的地址。租期过半时客户端会**单播**续租请求（DHCP Request），若服务器同意则单播 DHCP ACK 续租；若租期到 7/8 时仍未续租成功，客户端必须停止使用该地址，重新进入 Discover 流程。

**4. 考点归纳**
- DHCP 用 UDP 端口 67/68，先广播后应答；
- 客户端在拿到地址之前用 0.0.0.0 作源地址、255.255.255.255 作目的地址；
- 若网段内没有 DHCP 服务器，需配置 **DHCP 中继代理**跨网段转发请求。

**重点：** DHCP 四次交互（Discover/Offer/Request/ACK）；UDP 端口 67/68；租约与续租。

**难点：** 为什么 Offer 也可能广播——因为客户端此时还没有 IP 地址，广播是为了让未分配地址的客户端也能收到；区分"Offer 的广播/单播"在不同阶段的不同处理。

### ICMP 网际控制报文协议
<!-- kp: ch4-s4-kp2 -->

**1. 作用与分类**
ICMP（网际控制报文协议，Internet Control Message Protocol）用于在 IP 层报告差错和提供网络诊断信息，封装在 IP 数据报中传输（ICMP 报文作为 IP 数据报的数据部分）。ICMP 报文分两大类：
- **差错报告报文**：目的不可达、源点抑制（已废弃）、超时、参数问题、重定向；
- **询问报文**：回送请求/应答（echo request/reply）、时间戳请求/应答。

**2. 差错报告报文的规则**
- ICMP 差错报文必须**携带出错的 IP 数据报的首部和前 8 字节数据**，以便源点获知出错报文的相关信息；
- 对 ICMP 差错报文不再发送 ICMP 差错报文（避免无限循环）；
- 对分片后的**第一个分片**才发送差错报文；
- 对组播地址、特殊地址（如环回、广播）不发差错报文。

**3. 典型应用**
- **ping**：利用 ICMP **回送请求/应答**报文测试两台主机之间的连通性与往返时延，工作在应用层（但使用 ICMP）；
- **traceroute/tracert**：利用 IP 数据报的 TTL（每经过一个路由器减 1，减到 0 时路由器发送"超时"差错报文）逐跳探测到达目的主机的路径。

**4. 考点归纳**
- ICMP 报文封装在 IP 数据报中，属于网络层的辅助协议；
- ping 依赖 ICMP 回送请求/应答；traceroute 依赖 ICMP 超时差错报文；
- ICMP 只报告差错，不纠正差错（纠错由高层协议负责）。

**重点：** ICMP 的两类报文；ping 与 traceroute 的原理；ICMP 差错报文不再发 ICMP 差错。

**难点：** ICMP 差错报文的数据部分构成——携带出错 IP 数据报的首部 + 前 8 字节，目的是让源点能识别是哪个高层协议（TCP/UDP 端口）的数据报出错。

## 路由协议
<!-- sec: ch4-s5 -->

### RIP 路由信息协议
<!-- kp: ch4-s5-kp1 emphasis: key -->

**1. 基本思想**
RIP（路由信息协议，Routing Information Protocol）是一种**距离向量（distance-vector）**路由协议，用于自治系统（AS）内部（内部网关协议 IGP）。RIP 以**跳数（hop count）**作为度量：每经过一个路由器跳数加 1，**最多允许 15 跳**，16 跳表示"不可达"，因此 RIP 只适用于中小型网络。RIP 选择"跳数最少"的路径（并不总是最优带宽路径）。

**2. 工作原理**
- 每个路由器维护一张到**所有目的网络**的路由表（目的网络、下一跳、距离）；
- 每个路由器与**相邻**路由器交换**完整的路由表**；
- 按固定的时间间隔（**30 s**）向邻居广播自己的路由表，邻居收到后用"距离向量算法"（Bellman-Ford）更新自己的路由表：若收到"到网络 X 距离 d"，则经该邻居到达 X 的距离为 d+1，若比现有路由短则更新；
- **好消息传得快，坏消息传得慢**：某路由器失效后，其邻居需要多轮交互才能把"不可达"传播开（收敛慢），可能产生**路由环路**，常用"水平分割"等方法缓解。

**3. RIP 报文**
RIP 报文封装在 **UDP** 用户数据报中，使用端口 **520**。RIP 分为 v1（广播）与 v2（组播，支持 CIDR 与认证）。

**4. 考点归纳**
- RIP 是距离向量协议、跳数度量、最多 15 跳、周期 30 s 交换完整路由表；
- 收敛慢、不适合大型网络；
- 好消息传得快、坏消息传得慢。

**重点：** RIP 的度量（跳数）与 15/16 跳限制；周期交换完整路由表；距离向量更新算法。

**难点：** RIP 的收敛问题——坏消息（链路故障）需要逐步传播，可能经过 16 次更新才标记不可达，期间可能形成环路（计数到无穷问题）。

### OSPF 开放最短路径优先协议
<!-- kp: ch4-s5-kp2 emphasis: key -->

**1. 基本思想**
OSPF（开放最短路径优先，Open Shortest Path First）是**链路状态（link-state）**路由协议，属于内部网关协议（IGP）。它不使用跳数，而使用**费用（cost）**作为度量（费用可由带宽、延迟等定义，默认与带宽成反比）。OSPF 通过**洪泛法**向全网（AS 内所有路由器）发送**链路状态信息**，每个路由器据此构建全网拓扑图，再用 **Dijkstra 最短路径算法（SPF 算法）**计算出到每个目的网络的最短路径树。

**2. 工作特点**
- **洪泛发送链路状态**：每个路由器把与自己相邻路由器的链路状态（邻居、费用）广播给全网所有路由器，而不是只发给邻居；
- **收敛快**：链路状态变化时立即触发更新（不再周期 30 s 盲发），全网同步数据库后快速收敛，无路由环路；
- 由于所有路由器都知道全网拓扑，计算出的路由自然无环；
- 支持**区域划分**（area）：把 AS 划分为骨干区域 area 0 和若干其他区域，减少链路状态洪泛的范围，提高可扩展性；
- OSPF 报文直接封装在 **IP 数据报**中（协议号 89），使用**组播**发送（224.0.0.5、224.0.0.6）。

**3. 与 RIP 的对比**

| 比较项 | RIP | OSPF |
| --- | --- | --- |
| 类型 | 距离向量 | 链路状态 |
| 度量 | 跳数（≤15） | 费用（cost） |
| 交换内容 | 完整路由表 | 链路状态信息 |
| 交换对象 | 相邻路由器 | 全网（洪泛） |
| 交换时机 | 30 s 周期 | 链路变化时触发 |
| 收敛速度 | 慢 | 快 |
| 环路 | 可能有（计数到无穷） | 无（算法保证） |
| 适用规模 | 小型 | 大型 |
| 封装 | UDP 520 | 直接 IP（协议号 89） |

**重点：** OSPF 是链路状态协议、用 Dijkstra 算法、洪泛链路状态、收敛快；与 RIP 的对比。

**难点：** 链路状态与距离向量的本质区别——距离向量"只见邻居、交换路由表"，链路状态"全知拓扑、自己算最短路径"，因此链路状态协议天然无环。

### BGP 边界网关协议
<!-- kp: ch4-s5-kp3 emphasis: key -->

**1. 为什么需要 BGP**
互联网由大量自治系统（AS）组成。AS 之间需要交换可达性信息，但 AS 数量巨大、路径选择还要考虑**政策（如运营商之间商业关系）**，因此不能简单用 Dijkstra 或距离向量算法。**BGP（边界网关协议，Border Gateway Protocol）** 是 AS 之间的路由协议，属于**外部网关协议（EGP）**。

**2. 工作原理**
- BGP 是**路径向量（path-vector）**协议：交换的不仅是距离，而是**到达目的网络的完整路径（AS 序列）**；
- BGP 发言人（每个 AS 选取若干 BGP 边界路由器）之间建立 TCP 连接（端口 179），在 TCP 连接上交换 BGP 报文；
- BGP 不计算最短路径，而是根据**可达性 + 策略**选择路径，找到"较好"路由而非"最优"路由；
- 刚开始交换时交换**完整路由信息**，之后只在路由变化时更新变化部分。

**3. BGP 报文类型**
- **OPEN**：建立邻居关系（TCP 连接建立后）；
- **UPDATE**：通告/撤销路由；
- **KEEPALIVE**：周期保活（30 s）；
- **NOTIFICATION**：差错通知。

**4. 三种路由协议对比**

| 比较项 | RIP | OSPF | BGP |
| --- | --- | --- | --- |
| 作用范围 | AS 内（IGP） | AS 内（IGP） | AS 之间（EGP） |
| 算法类型 | 距离向量 | 链路状态 | 路径向量 |
| 度量 | 跳数 | 费用 | 策略+路径属性 |
| 传输方式 | UDP 520 | IP 直接封装 | TCP 179 |
| 目标 | 最优路径 | 最优路径 | 较好的可达路径（策略） |

**5. 考点归纳**
- BGP 支持 CIDR、支持策略路由，是当前互联网 AS 间事实标准（BGP-4）；
- BGP 建立在 TCP 之上，可靠传输由 TCP 保证；
- "找好路由而非最优路由"是 BGP 与其他协议的重要区别。

**重点：** BGP 是路径向量协议、AS 之间使用、基于 TCP 179；RIP/OSPF 属于 IGP（AS 内部），BGP 属于 EGP（AS 之间）。

**难点：** 为什么 BGP 不追求最优路径——AS 间路由要服从运营商之间的商业策略与安全约束，与 AS 内"代价最小"的目标不同。

### 路由算法分类
<!-- kp: ch4-s5-kp4 emphasis: key -->

路由算法决定路由器如何计算路由表，分类如下：

- **静态路由算法**：路由表由管理员手工配置，不随网络状态变化，简单可靠（不产生路由开销）但不能适应拓扑变化，适合小型/稳定网络；**动态路由算法**：路由器间自动交换信息、根据网络状态更新路由表，能适应故障与拓扑变化，是互联网实际使用的（RIP/OSPF/BGP）。
- **距离向量算法**：只与**相邻**路由器交换**自己的路由表**（"我到各网络的距离"），按Bellman-Ford思想逐跳更新，实现简单但收敛慢、易产生环路（RIP即代表）；**链路状态算法**：向**全网洪泛**自己的**链路状态信息**（邻居+代价），每台路由器掌握全网拓扑后用**Dijkstra**独立计算最短路径，收敛快、无环（OSPF即代表）。
- **层次路由**：互联网规模巨大，路由器不可能掌握全网信息，故把网络划分为**自治系统（AS）**：AS内部用IGP（RIP/OSPF）计算内部路由，AS之间用EGP（BGP）交换可达性信息，形成两层的层次结构，降低了路由计算的复杂度。

**重点：**静态vs动态；距离向量（见邻居、慢收敛、可能环）vs链路状态（全网拓扑、快收敛、无环）；层次路由=AS内IGP+AS间EGP。

## IPv6 与路由器
<!-- sec: ch4-s6 -->

### IPv6 协议
<!-- kp: ch4-s6-kp1 emphasis: key -->

**1. IPv6 的特点**
IPv6 是为解决 IPv4 地址耗尽而设计的新一代网际协议，主要特点：
- **地址长度 128 位**，比 IPv4 的 32 位大幅扩充，地址空间几乎无限（约 3.4×10^38 个）；
- 采用**冒号十六进制**记法：8 组 16 位十六进制数，如 2001:0DB8:0000:0000:0000:FF00:0042:8329；可省略每组**前导的 0**，可把**连续的全 0 组压缩为"::"**（只能压缩一次）；
- **IPv6 基本首部固定为 40 字节**，取消首部校验和字段（减轻转发负担），支持**流标签**（区分服务质量）；
- 不再支持分片（发送方负责分片，路由器不做分片）、不提供任何选项字段（选项用扩展首部实现）；
- **即插即用**：支持无状态地址自动配置（SLAAC），通过路由器通告自动获取地址，也可用 DHCPv6。

**2. IPv6 地址类型**
- **单播地址**：一对一通信；
- **组播地址**：一对多，把 IPv4 的广播地址并入组播（IPv6 没有广播地址）；
- **任播地址**：一对一组中最近的一个（新引入）。

**3. IPv6 与 IPv4 对比**

| 比较项 | IPv4 | IPv6 |
| --- | --- | --- |
| 地址长度 | 32 位 | 128 位 |
| 表示法 | 点分十进制 | 冒号十六进制 |
| 首部长度 | 可变（20~60 B） | 固定 40 B |
| 校验和 | 有 | 无 |
| 广播地址 | 有 | 无（用组播） |
| 分片位置 | 路由器可分片 | 只允许源站分片 |
| 自动配置 | 无（需 DHCP） | 有（SLAAC） |

**4. 过渡技术**
IPv4 与 IPv6 过渡技术：**双协议栈**（同时支持 IPv4/IPv6，最简单）、**隧道技术**（IPv6 数据报封装在 IPv4 数据报中穿越 IPv4 网络）、**NAT-PT**（协议翻译，已基本弃用）。

**重点：** IPv6 地址 128 位、冒号十六进制与 "::" 压缩规则；固定 40 字节首部、无校验和；无广播地址。

**难点：** "::" 只能出现一次（否则地址无法唯一确定）；IPv6 路由器不分片，但源站可以分片，这与 IPv4 的分片位置不同。

### 路由器的组成与转发
<!-- kp: ch4-s6-kp2 -->

**1. 路由器的功能**
路由器是网络层的核心设备，主要功能：
- **路由选择**：通过路由协议（RIP/OSPF/BGP）动态维护路由表；
- **分组转发**：根据转发表把分组从合适的输出端口转发出去；
- **异构网络互联**：连接不同技术、不同层次的网络；
- 路由器**隔离冲突域和广播域**。

**2. 路由器的结构**
路由器由**输入端口、交换结构（交换矩阵）、输出端口**和**路由选择处理器（控制部分）**组成：
- **输入/输出端口**：完成物理层接收、数据链路层拆/装帧、查表与排队；
- **交换结构**：把分组从输入端口交换到输出端口，是路由器转发速度的关键；
- **控制部分（路由选择处理机）**：运行路由协议、计算路由表、维护转发表，是"慢路径"；
- **转发部分**：按转发表逐分组转发，是"快路径"（硬件线速转发）。

**3. 路由表与转发表**
- **路由表**由路由协议计算得到，包含：目的网络前缀、下一跳地址、出口、度量；
- **转发表**由路由表优化派生，用于快速查表转发，采用**最长前缀匹配**；
- 转发时：查转发表得到下一跳 IP → 用 ARP 得到下一跳的 MAC → 重新封装帧发送。

**4. 路由器与交换机的区别**

| 比较项 | 路由器 | 交换机 |
| --- | --- | --- |
| 工作层次 | 网络层 | 数据链路层 |
| 寻址依据 | IP 地址（网络前缀） | MAC 地址 |
| 隔离广播域 | 是 | 否 |
| 冲突域 | 是（每接口独立） | 是（每端口独立） |
| 主要功能 | 路由选择与跨网转发 | 网段内帧转发 |

**重点：** 路由器的路由选择与分组转发两大功能；最长前缀匹配转发；隔离冲突域与广播域。

**难点：** 分组进入路由器要经过"解封装（去帧头）→ 查 IP 转发表 → 重新封装（换帧头）"的过程——IP 数据报内容不变，但数据链路层的 MAC 地址每跳都要更新，这正是"IP 不变、MAC 逐跳变"的转发原理。

### IP 组播与 IGMP（低频）
<!-- kp: ch4-s6-kp3 -->

**组播（多播）**实现一对多的高效传输：源主机只发送一份数据，路由器按需复制转发给组内各成员，比分别单播节省带宽。组播地址使用**D类地址（224.0.0.0~239.255.255.255）**，主机用**IGMP协议**向本网组播路由器报告"加入/离开某组"，路由器据此维护组成员关系，并用组播路由协议（如PIM）构建组播转发树。**低频考点**，掌握"D类地址=组播地址、IGMP管理组成员关系、组播是网络层一对多"即可。

**重点：** D类地址用于组播；IGMP负责组管理（加入/离开）；组播数据在网络层被路由器复制转发。
`;export{e as default};